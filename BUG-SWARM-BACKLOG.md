# Bug-backlog (konsoliderad)

**Enda aktiva bugglistan i repot.** Lokal markdown är källan till sanning — ingen Linear, ingen extern tracker. Formatkontrollen (`npm run check:bug-backlog` → `scripts/dev/check-bug-backlog.mjs`, blockerande CI-steg i `quality`) och canvas-generatorn (`scripts/canvas/build-llm-flow-canvas.mjs`) läser **`## Aktiv kö`** nedan.

Avslutade rader flyttas till [`docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-07-25.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-07-25.md) (senaste), [`backlog-arkiv-2026-07-24.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-07-24.md), [`backlog-arkiv-2026-07-22.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-07-22.md), [`backlog-arkiv-2026-07-02.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-07-02.md), [`backlog-arkiv-2026-06-27.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-06-27.md) och [`backlog-arkiv-2026-06-24.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-06-24.md) (fryst historik). Grandmaster-svärmens B01–B15 = löst historik i [`docs/plans/avklarat/bug-swarm/README.md`](docs/plans/avklarat/bug-swarm/README.md).

## Hur den hålls sann

| Regel | Vad |
| --- | --- |
| **Aktiv kö = bara öppna defekter** | Rader där systemet gör fel. Alla rader är `[ ]`. När en rad fixas: **flytta** den till arkivfilen som `[x]` med fix-referens (commit/PR). Bocka inte av på plats. |
| **En sanning per rad** | Skriv aldrig "FIXAD" i prosan på en `[ ]`-rad. Det är en motsägelse och formatkontrollen failar (`npm run check:bug-backlog`). |
| **Beslut ≠ bug** | Saker där systemet gör som tänkt men vi *kan* välja annorlunda (fail-open, default-off-flaggor, degraded-policy) hör hemma i "Beslut & policy", inte i Aktiv kö — de räknas inte som risk i canvasen. |
| **Kan ej verifieras statiskt** | Fynd som kräver repro/livekörning hör i "Behöver repro" tills någon kört dem. |
| **Inflöde** | Nya buggar: `/buggrapport` (skriver en `M#`-rad här) eller PR-review-gaten (`pr-merge-review-gate.mdc` → logga med fil-ankare). |

**Status-legend:** `[ ]` = öppen. Källa-taggar: `G#`/`U#` = gamla GPT/UI-svärmlistor, `N#` = defensiv triage, `R#` = äldre kodrapport, `B#` = grandmaster-svärm, `E#` = eval, `FEL#`/`MB#`/`BB#` = PR-review-fynd, `M#` = manuellt rapporterad.

## Aktiv kö

Verkliga öppna defekter. Detta är den enda listan att jobba ur. Canvasen visar dessa som öppna huvudrisker.

> **Senast helverifierad:** 2026-07-22 mot master `bd09e331` (gated bug-swarm-pass, PR #592). Varje kvarvarande rad kodverifierades som fortfarande öppen; 15 rader fixades, 5 var redan fixade/obsoleta/avfärdade och 2 flyttades (arkiv: [`backlog-arkiv-2026-07-22.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-07-22.md)). Fil-ankare uppdaterade efter modulsplittarna #560/#562/#563.
>
> **Bugfix-svep 2026-07-24** (mot master `626f15e6`, branch `fix/bugfix-svep-0724`): 6 kodverifierade lågrisk-rader fixade och flyttade (arkiv: [`backlog-arkiv-2026-07-24.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-07-24.md)); omtriage av rader i #593–#597-berörda områden (sendMessage-outcome, version_busy-UX, quick-edit-lease, autofix-kontext) bekräftade dem fortfarande öppna.
>
> **Efterslag:** warm-cache-SDK-raden som lades till vid #600-mergen fick sin **Clerk-del** stängd av #603 (`6820dc42`) utan att backloggen rördes. Delen arkiverades i efterhand (`backlog-arkiv-2026-07-24.md`) och resten — 15 dossier-egna paket — låg kvar som egen rad. Två lärdomar: stäng raden i samma PR som fixen, och arkivera aldrig en bred rad på en smal fix (dela den).
>
> **Åtgärdsprogrammet efter observationssessionen 2026-07-25**, levererat 2026-07-26: nio av tio defekter (F1–F9) stängda i sex spår och arkiverade i [`backlog-arkiv-2026-07-25.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-07-25.md). De kom in via planfamiljen, inte via Aktiv kö, så de har aldrig legat som `[ ]`-rader här. **F10 är inte fixad** — den är konsoliderad in i innehållsrevisions-raden nedan som punkt (d), eftersom reparationens versionsidentitet är samma saknade primitiv.
>
> **Tvåradsleverans 2026-07-25** (mot master `1cd8f547`): warm-cache-SDK-restposten och `sendMessage`-outcome-raden stängda, båda kodverifierade före fix (arkiv: [`backlog-arkiv-2026-07-25.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-07-25.md)). Warm-cache-raden avgjordes till **linje (c)** — pre-VM-passet släpper olösbara modul-diagnostiker i st.f. att gissa installationsstatus — och tog samtidigt bort #603:s per-paket-stub, som bevisat bytte TS2307 mot TS2305 på giltig Clerk-kod. Två följdrader upptäcktes under arbetet och stängdes i samma leverans (narrow-stub-regressionen och fyra dossier-deps som importskannern inte kunde pinna).
>
> **Acceptanskörningen för åtgärdsprogrammet, genomförd i prod 2026-07-27** (chat `bcd3b493`, #629): sju av åtta kontroller gröna. Kontroll 8 föll — F9:s kodvägg kom tillbaka när modellen skickade ett ojämnt antal fences — och spår 01 steg 3 föll av en **annan** orsak än planen antog: snapshot-nyckelbudgeten tappade `mutedCapabilities` innan den nådde databasen, så #623:s läs-fix aldrig hade något att läsa. Samma budget kunde tappa den durable borttagnings-tombstonen, vilket var det allvarligaste fyndet i sessionen. Båda plus tre separata fynd (thumbnail-race, Deep Brief-fallbackens schema, plan-lägets fel-toast) är stängda i [`backlog-arkiv-2026-07-25.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-07-25.md) → "Fixade 2026-07-27". Lärdomen är att en läs-fix inte är verifierad förrän skriv-sidan testats: rutans enhetstest mockade `readMutedCapabilitiesFromSnapshot` och var grönt hela tiden.
>
> **Efterhandsgranskning av nattens fyra PR:er (#641–#644), 2026-07-29:** fyra otriagerade externa bot-fynd hittades — samtliga postade **före** författarens sign-off, som ändå angav "P0/P1: 0". Lärdomen är process, inte kod: `gh pr checks` visar inte review-kommentarer, så en grön checklista sa inget om att Bugbot/Codex/Vercel Agent redan hade skrivit P1:or i diffen — se [`pr-bot-findings-sweep.mdc`](.cursor/rules/pr-bot-findings-sweep.mdc). Prod-läsning samma dag bekräftade att #642 lever skarpt (135/135 versioner har `files_revision`) men att R7 då ännu inte skrivit en enda rad.
>
> Åtgärdat i tre PR:er samma dag, alla arkiverade i [`backlog-arkiv-2026-07-25.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-07-25.md): **#646** (R7:s skrivning hann inte köra före svaret; repair-passet stämplade fel revision), **#651** (R7-raden raderades ändå av prunet; `meta`-exporten oscopad; provider-kodval; freshness-grinden) och **#652** (freshness-grindens tidsstämpel bunden till GitHub i stället för till fritext, plus tester). `providerFault`-flaggan stängdes 2026-07-30 sedan ägarbeslutet tagits ("en generationsrad beskriver utfallet, inte det värsta ögonblicket") — arkiverad i samma fil. Kvar som öppen rad nedan: den saknade Postgres-backade testen för `files_revision` (P1 — omklassad från P2 efter Codex på #653, eftersom repots egen regel gör saknade tester på DB-/runtime-kontrakt till P1).
>
> Granskningen kostade tio bot-fynd att triagera och gav en generell lärdom: varje fix i den här kedjan hittade nästa lager. Prunet upptäcktes först när `after()`-fixen fanns, tidsstämpel-hålet först när skev-toleransen fanns. Räkna med att en säkerhetsgrind behöver mer än ett varv.
>
> **Buggkollen på åtgärdsprogrammets PR (#623)** gav tre fynd: två P1 från Codex fixade i PR:en (prefix-only Node-built-ins, rollback av partiell blockerarminskning) och ett medium från Cursor-bugbot som ligger nedan som raden **"Kontraktsradernas filbevis är basversionens, inte rundans"** (referens by namn, inte position: nya rader läggs överst, så "första raden nedan" pekade fel redan efter #665). Det loggades i st.f. att fixas eftersom felet pekar åt det ofarliga hållet (understryker en leverans) och bara träffar chattens detaljkort; den riktiga fixen kräver att filbevis räknas om efter finalize.
>
> **Efterslag 2026-07-31:** livepanel-raden ("Liveheadern döljer ett terminalt fel") stängdes av **#686** utan att backloggen rördes i samma PR — samma mönster som #603-efterslaget ovan. Arkiverad i efterhand ([`backlog-arkiv-2026-07-25.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-07-25.md) → "Fixade 2026-07-31"). Raden bad om ett ägarval mellan två alternativ; fixen valde ett tredje som undviker båda nackdelarna — headern signalerar felstatus via färg, ikon och etikett utan att återge den gamla feltexten. Lärdomen på raden om #603 gäller alltjämt: stäng raden i samma PR som fixen.

| Klar | Status | Prio | Fynd | Källa | Beslut / nästa steg |
| --- | --- | --- | --- | --- | --- |
| [ ] | Öppen bug | P2 | **En oparsbar planner-plan försvinner vid omladdning:** plan-läget strömmar den råa modelltexten till klienten, men `persistAssistantSummary` (`plan-mode-turn.ts:205-214`) sparar bara en sammanfattning byggd ur `resolvePlanArtifact(...) ?? {}` (`plan-mode-stream.ts:228-234`). Går svaret inte att parsa blir artefakten `{}`, och `review.ts:83-87` skriver då en generisk rad. Användaren ser alltså planen under körningen och får "Plan skapad och redo för granskning" efter en sidladdning — `accumulatedContent` sparas aldrig. | Extern granskning 2026-08-01 (#682), kodverifierad | Persistera den råa plantexten som meddelandeinnehåll när `normalizePlanArtifact` returnerar tomt, så reload visar det modellen faktiskt skrev. Ägare: `src/lib/gen/stream/plan-mode-stream.ts` + `plan-mode-turn.ts`. |
| [ ] | Öppen bug | P2 | **Dossier-env rehydreras inte vid force-restart eller quick-edit-fallback:** `selectedDossierEnvKeys` trådas från finalize (`generation-stream-post-finalize.ts:350-364`) men skickas inte av `POST /preview-session` (`route.ts:188-201`) eller quick-edit-fallbacken (`quick-edit/service.ts:179-186`). Båda bygger om `.env.local` och nollställer den pipeline-skrivna varianten (`preview-session.ts:673-678`), så F2:s mock-seed (`env-local.ts:353-361`) uteblir och dossier-UI:t tappar sitt demo-läge efter en omstart. | Extern granskning 2026-08-01 (#675), kodverifierad | Blockerad på att urvalet inte persisteras — ingen kolumn eller JSON bär `selectedDossierEnvKeys` per version (verifierat: inga träffar i `src/lib/db`). Kräver antingen att nycklarna sparas på versionsraden eller att dossier-urvalet kan härledas om. Ägare: `src/lib/gen/stream/finalize-version/runner.ts` + versionsschemat. |
| [ ] | Öppen kvalitet-risk | P3 | **Strömmat innehåll utan sparad version rapporteras som empty-output i UI och telemetri:** servern skiljer redan på fallen i `reason` (`generation-stream.ts:941-946` väljer `stream_ended_without_version` när `accumulatedContent` finns), men progress-fasen sätts ändå till `"empty-output"` (`generation-stream.ts:351-354`), och klientens `hasRecoveredArtifact` räknar bara version/preview/plan — inte strömmad text (`stream-handlers.ts:1148-1168`). Användaren kan se kod i chatten och samtidigt få en fel-toast. | Extern granskning 2026-08-01 (#684), kodverifierad | Låt fasen följa `reason` och låt `hasRecoveredArtifact` räkna strömmat innehåll, så statusen beskriver utfallet i stället för det värsta ögonblicket. Ägare: `src/lib/providers/own-engine/generation-stream.ts` + `src/lib/hooks/chat/stream-handlers.ts`. |
| [ ] | Öppen bug | P2 | **Klargörandefrågan kastar användarens ursprungliga follow-up-prompt:** när `resolveFollowUpClarification` klassar en follow-up som `ambiguous-redesign`/`ambiguous-followup` stoppas rundan och användaren får svarsalternativ ("Förfina nuvarande design" osv.). Svaret skickas som ett **nytt** meddelande och blir hela genereringsprompten — originalprompten kombineras aldrig in. Observerat i prod chat `e8bd3ba6` (2026-08-01): en detaljerad hydration-fix-prompt (mål, filpekare, begränsningar) triggade frågan; efter svaret var `prompt_logs` enda follow-up-rad "Förfina nuvarande design" och den formatterade prompten saknade varje spår av fix-instruktionerna — LLM:en fick aldrig veta vad som skulle fixas. Originalet persisteras som user-meddelande (`persistFollowUpClarification`) men läses inte av nästa tur. | /logg-utredning 2026-08-01 (prod chat `e8bd3ba6`, `prompt_logs` + formatterad prompt, read-only) | Kombinera originalprompt + klargörandesvar när svaret kommer (t.ex. läs senaste user-meddelandet med `awaiting-input`-markering och prefixa det), i stället för att låta svaret ersätta. Ägare: `src/lib/providers/own-engine/follow-up-clarification.ts` + stream-routens svarshantering (`src/app/api/engine/chats/[chatId]/stream/route.ts`). |
| [ ] | Öppen bug | P2 | **SEO-copypasset kan inte åtgärda en nyckel som saknas helt:** `missing-title` och `missing-description` står i både `FIXABLE` (`audit.ts`) och `COPY_FINDING_IDS` (`llm-copy.ts`), så passet anropar modellen — men `writeMetadataString` (`metadata-literal.ts`) *ersätter* en befintlig toppnivå-literal och returnerar källan orörd när nyckeln inte finns. En layout som exporterar metadata utan `title`-fält får alltså ett modellanrop utan resultat. Pre-existerande: den gamla regexen krävde också ett befintligt `title:`. **Ingen false-green** — efter-granskningen är färsk och `dropUnresolvedImprovements` stryker förbättringen, så rapporten säger korrekt att bristen står kvar. Kostnaden är ett bortkastat modellanrop och en utlovad fix som inte levereras. | Bugbot på #707 2026-08-01, kodverifierad | Lär `metadata-literal` att *infoga* en saknad nyckel i metadata-objektet (position + indentering), eller ta bort `missing-title`/`missing-description` ur `COPY_FINDING_IDS` så passet slutar lova det. Ägare: `src/lib/seo/metadata-literal.ts` + `llm-copy.ts`. |
| [ ] | Öppen bug | P2 | **En dev-process som avslutar rent i en omstartsloop lämnar readiness på `starting` för alltid:** boot-vägen sätter `readinessState = "starting"` vid VARJE boot (`runtime.js`, `bootRuntimeForSession`), och `waitForReady`-proben skriver sitt utfall grindat på att `stored.versionId` är oförändrad. Startar processen om innan probens 180 s-deadline löper ut skrivs "starting" tillbaka och det failed-utfallet försvinner — hosten bootar om i evighet medan appen ser `starting`, alltså varken `preview_success=false`, fellogg eller RepairGate. **Reproducerat i prod 2026-08-01** (session `ps_6cc7ab57`): ett projekt med `.tsx` men utan `tsconfig.json`/`typescript` får Next dev att auto-installera TS, flippa beroendeuppsättningen och krascha med `TypeError: Cannot read properties of undefined (reading 'endsWith')` — `exitCode 0`, sex omstarter på åtta minuter, readiness `starting` hela vägen. Riktiga genererade projekt har tsconfig, så själva triggern är osannolik; **loopen och den tysta readiness-flaggan är det verkliga fyndet**. Pre-existerande — #701 stängde "processen startade aldrig" och "overlay/500", inte "processen dör rent om och om igen". | Post-deploy prod-smoke 2026-08-01 (integratorsagenten), hostloggar | Räkna omstarter per (session, version) och sätt `readinessState: "failed"` med sista utdatan efter N rena exits inom ett fönster, i stället för att blint sätta `starting` på varje boot. Ägare: `preview-host/src/runtime.js` (`bootRuntimeForSession` + `waitForReady`-utfallsskrivningen). |
| [ ] | Öppen kvalitet-risk | P2 | **Preview-sessionen bär ännu inte den bootade innehållsrevisionen hela vägen:** `recordPreviewRuntimeOutcomeForVersion` tar nu emot `bootedFilesRevision` explicit (`generation-telemetry.ts`, `PreviewRuntimeOutcomeOptions`), men ingen callsite fyller den ännu — kvittovägarna (`preview-status`, `preview-heartbeat`, `preview-session`, `generation-stream-post-finalize`) faller därför tillbaka på DB-läsningen av versionens NUVARANDE `files_revision`. Den är en proxy, inte sanning: versionsraden kan avancera till N+1 (repair-accept, användarredigering) medan VM:n fortfarande serverar N. **Latent så länge `SAJTMASKIN_CONTENT_REVISION_GATE` är av** (fallbacken körs bara med flaggan på) — men den måste vara stängd innan flaggan slås på. | Coach-review av #693 2026-08-01, kodverifierad | Låt `PreviewSessionEntry` bära `filesRevision` (sätts vid start/update/patch från versionens revision vid sändningstillfället, samma läsning som `getVersionFilesRevision`) och tråda den till de fyra kvittovägarna. Ägare: `src/lib/gen/preview/session-store.ts` + `preview-session.ts` + kvittorouterna. |
| [ ] | Öppen testrisk | P2 | **Tidsberoende tester failar under parallell last, oberoende av diffen.** Två mekanismer: (a) `llm-usage.test.ts` synkroniserar `AsyncLocalStorage` + `after()` med `setTimeout(5)`/`vi.waitFor` i stället för deterministiska gates — "skickar med scopets claim-nyckel…" förväntar 2 anrop men får 1, och ordningstestet förväntar tom `order` men får `["claim"]`. (b) `default-thinking.test.ts` ("maps shared reasoning effort levels…") och `preview/warm-eslint.test.ts` ("runs when cache is warm…") tar ~4 s var i isolering och slår i vitests default-`testTimeout` på 5 s när sviten kör parallellt. Alla fyra är gröna i isolering och ligger i filer som diffen inte rör. | Full `test:ci` + `npx vitest run src/lib/gen` under preview-host-diskfixen 2026-07-31; verifierat pre-existerande via importanalys + isolerad körning | (a) Byt tidsbaserad synk mot en explicit gate (samma mönster som `insertGate` längre ner i filen) så testet mäter ordning, inte klockan. (b) Sätt en uttrycklig `testTimeout` på de två långsamma testerna, eller stubba spawn-vägen. Ägare: `src/lib/observability/llm-usage.test.ts`, `src/lib/gen/default-thinking.test.ts`, `src/lib/gen/preview/warm-eslint.test.ts`. |
| [ ] | Öppen kvalitet-risk | P2 | **Ett persist-fel rapporteras som tom utdata:** finally-vägen skickar `stream_ended_without_version` genom `finishWithoutVersion` (`generation-stream.ts:941-946`), som alltid sätter progress-fasen `empty-output` och devlog-typen `site.empty_generation` när `awaitingInput` är false. Har innehåll strömmat men dubbel-finalize misslyckats säger klientens text och telemetrin att rundan saknade användbar kod — trots att assistentmeddelandet redan innehåller filerna. Kräver dubbelt persist-fel, samma sällsynta väg som P3-raden om utebliven återbetalning. | Bugbot-subagent på #684:s diff 2026-07-31 (merge-agentens pass), kodläst utan repro | Skilj "ingen utdata" från "utdata fanns men kunde inte sparas" i fas/devlog-etiketten. Ägare: `src/lib/providers/own-engine/generation-stream.ts`. |
| [ ] | Öppen kvalitet-risk | P2 | **`auth-pages` räknas som app-scaffold i retry-pivoten:** att byta den handplockade `APP_SCAFFOLD_IDS`-mängden mot `isAppScaffold` (`scaffold-aware-retry.ts:82-87`) gör att `site-shell-mismatch` nu även träffar `auth-pages`, som är `siteKind: "app"` men ett giltigt val för `website`-intent. På ett website-bygge med färre än tre route-filer och blockerande preflight kan retry därför pivotera mot `landing-page` fast auth var rätt match. | Bugbot-subagent på #684:s diff 2026-07-31 (merge-agentens pass), kodläst utan repro | Undanta `auth-pages` (eller andra `siteKind: "app"`-scaffolds som är giltiga website-val) från mismatch-pivoten. Ägare: `src/lib/gen/scaffolds/scaffold-aware-retry.ts`. |
| [ ] | Öppen kvalitet-risk | P2 | **Add-verb-vakten släpper igenom refine-prompter:** de nya `ska … jag … kunna`- och `skulle vilja testa/prova`-mönstren samt API-nyckel-öppnaren sätter `allowDetection` även när prompten innehåller refine-verb som `flytta` (`follow-up-capability-detection.ts:115-119`). En follow-up som flyttar ett befintligt kontaktformulär kan då klassas som `capability-add` och återinjicera dossier-skal på en ren layoutändring — samma felläge som den ursprungliga add-verb-vakten skrevs för att stoppa. | Bugbot-subagent på #683:s diff 2026-07-31 (merge-agentens pass), kodläst utan repro | Låt refine-verben väga tyngre än de nya add-mönstren, eller kräv att add-mönstret matchar utan refine-verb i samma mening. Ägare: `src/lib/builder/follow-up-capability-detection.ts`. |
| [ ] | Öppen bug | P2 | **Rematchad scaffold persisteras innan kontraktsgrinden har passerat:** `codegen-turn.ts:336-343` skriver `updateChatScaffoldId` innan `contractClarification`-grenen (`:426`) kan avbryta rundan, och init-vägen pinnar scaffolden redan vid `createChat(..., resolvedScaffold?.id)` (`create-chat-stream-post.ts:807-811`). Om grinden avbryter för att ställa en följdfråga ligger alltså ett matchningsbeslut kvar som fattats på den *ofullständiga* prompten — och nästa tur ser det som `persistedScaffoldId`, vilket stänger av rematchen. Fel scaffold blir därmed permanent för chatten. | 24-agentsvärm 2026-07-31, kodverifierad (M#gs2) | Flytta skrivningen till efter att grinden släppt igenom, eller rulla tillbaka vid gate-only-exit. Ordningen är inte testad idag — lägg ett test som binder "gate avbröt → ingen scaffold persisterad". Ägare: `src/lib/api/engine/chats/chat-message-stream/codegen-turn.ts`, `create-chat-stream-post.ts`. |
| [ ] | Öppen kvalitet-risk | P2 | **Scaffold-matcherns synkroniska väg avgör på ordning i stället för poäng, och ignorerar capabilities:** `matcher.ts:382-396` returnerar `auth-pages` direkt när `authScore >= 2` — innan ecommerce/SaaS ens poängsätts — så "webbshop med inloggning" landar på auth-scaffolden även när ecommerce har högre poäng. Samma funktion tar inte emot `capabilities`, så flaggor som `needsGame` bara justerar embedding-overridens styrka (`buildKeywordScores`, `:155-162`) utan att kunna påverka keyword-valet. Embedding-vägen har en egen guard (`canUseEmbeddingOverride`) och drabbas inte. | 24-agentsvärm 2026-07-31, kodverifierad (M#gs4) | Poängsätt alla familjer i ett svep och jämför, och tråda `capabilities` in i `matchScaffold`. Ändrar faktiska val → kör `npm run scaffolds:eval` före/efter (se även raden om att den evalen inte kan fela). Ägare: `src/lib/gen/scaffolds/matcher.ts`. |
| [ ] | Öppen kvalitet-risk | P2 | **Variantvalets keyword-väg hashar bort en tydlig vinnare:** `pickScaffoldVariant` tar topp-4 och väljer med seed-hash (`matcher.ts:229-236`) utan dominansmarginal — exakt det som gav `corporate-grid` 0/20 i landing-auditen och som embedding-vägen fick en fix för (`VARIANT_DOMINANT_MARGIN`, `:392-404`). Keyword-vägen är den som kör utan API-nyckel, utan embeddings-cache och vid svag cosine, så feltypen lever kvar där. | 24-agentsvärm 2026-07-31, kodverifierad (M#gs5) | Spegla dominansmarginalen i keyword-pickern: vinnaren rakt av när poänggapet är tydligt, rotation bara vid jämnt fält. Ägare: `src/lib/gen/scaffold-variants/matcher.ts`. |
| [ ] | Öppen bug | P2 | **Variantdata ljuger om färgläge:** `starter-neutral` (base-nextjs default) deklarerar `"colorMode": "either"` men dess EXACT-tokens är mörka (`background: oklch(0.15 0.004 0)` med ljus foreground), och prompten skickar båda ordagrant — så basvariantens "neutrala" default drar mot mörkt utan att någon bett om det. Samma datafel i dashboard: båda varianterna (`glass-frosted`, `dense-terminal`) är `colorMode: "dark"`, så en ljus dashboard har ingen variant att välja. Briefen rangordnas över variantdefaults i prompten, så effekten är en dragning — inte en hård låsning. | 24-agentsvärm 2026-07-31, kodverifierad mot JSON:erna (M#gs7) | Ägarval per fall: sätt `colorMode: "dark"` (ärligast mot tokens) eller byt tokens till neutrala. Dashboard behöver en ljus variant om ljust läge ska vara möjligt. Ägare: `config/scaffold-variants/base-nextjs/starter-neutral.json`, `config/scaffold-variants/dashboard/*.json`. |
| [ ] | Öppen kvalitet-risk | P3 | **Grönt stämplas innan grinden kört, och advisory-fynden tappas ur bussen:** `persistTelemetryRecord` skriver `qualityGateResult: "preflight_passed"` vid finalize (`persist-telemetry.ts:221-226`) medan ReleaseGate (`triggerServerVerification`) startar först efteråt och aldrig uppdaterar raden — bara ett repair-pass stämplar om (`recordRepairPassedQualityGate`). Backoffice-legenden läser kolumnen som gate-utfall, så en F3-rad ser verifierad ut tills en reparation råkar köra. Samma familj: vid F2:s typecheck-advisory skickar `server-verify.ts:388-419` `outcome: "passed"` med `findings: []` — fynden finns kvar i `version_error_logs` och i `version.degraded`, men bussens konsumenter ser en ren pass. | 24-agentsvärm 2026-07-31, kodverifierad (M#gs9) | Stämpla `quality_gate_pending` vid finalize och skriv utfallet efter gaten (eller sluta läsa kolumnen som gate-status i backoffice/`assess.py`), och låt advisory-eventet bära sina findings. Ägare: `src/lib/gen/stream/finalize-version/persist-telemetry.ts`, `src/lib/gen/verify/server-verify.ts`, `backoffice/pages/generation_history.py`. |
| [ ] | Öppen kvalitet-risk | P3 | **Två grindar kan inte fela:** `scripts/scaffolds/eval-scaffold-selection.ts` skriver rapport och avslutar alltid 0 — ingen tröskel — och `mustHaveFeatures`/`mustNotBe` i `scaffold-eval.ts:13-14` poängsätts aldrig, så fälten är dekoration. `variant-integrity.test.ts` jämför bara id-mängden mot embeddings-indexet, inte texthashen, så en variant vars text ändrats behåller sin gamla vektor med grön CI. | 24-agentsvärm 2026-07-31, kodverifierad (M#gs10) | Exit≠0 under en beslutad tröskel + poängsätt must/mustNot; lagra `textHash` per embedding vid generering och jämför mot `buildEmbeddingText(variant)` i integritetstestet. Ägare: `scripts/scaffolds/`, `src/lib/gen/scaffold-variants/variant-integrity.test.ts`. |
| [ ] | Öppen kvalitet-risk | P3 | **Bakgrundsreparationens SSE-event tappas tyst efter stream-close:** `triggerServerVerification`/`triggerBuildErrorRepair` är fire-and-forget och deras `onRepairAvailable` skickar `version-repair-available` via `safeEnqueue` (`generation-stream-post-finalize.ts:663-685`) — men `safeClose()` har normalt redan körts, och `safeEnqueue` returnerar tyst när controllern är stängd. Inget kastas; eventet försvinner bara. Klienten hämtar samma tillstånd via readiness-polling, så effekten är fördröjd UI-uppdatering snarare än tappad reparation. | 24-agentsvärm 2026-07-31, kodverifierad (M#gs11) | Gör polling kanoniskt och ta bort callbacken (lägst risk), eller håll strömmen öppen till reparationen är klar (latenskostnad). Ägare: `src/lib/providers/own-engine/generation-stream-post-finalize.ts`. |
| [ ] | Öppen kvalitet-risk | P3 | **`rebuildContent`-fallbacken kan skriva i fel fil:** `pipeline.ts:1443-1469` byter först innehåll via en sökvägsmedveten fence-regex, men vid miss faller den tillbaka på `result.replace(orig.content, fixed.content)` — första förekomsten i hela den serialiserade strängen. Två filer med identiskt innehåll (vanligt för korta stubbar) plus en fence som inte matchar mönstret ger då en fix i den andra filen. Normal `parseCodeProject`/`serializeCodeProject`-output använder konsekventa fences, så vägen kräver avvikande format; inga träffar i prod-loggarna. | 24-agentsvärm 2026-07-31, kodverifierad (M#gs12) | Fail + warn i stället för blind replace, eller positionsbaserad splice. Ingen direkt testtäckning av `rebuildContent` idag. Ägare: `src/lib/gen/autofix/pipeline.ts`. |
| [ ] | Öppen bug | P3 | **Ingen återbetalning när finalize misslyckas efter att innehåll strömmats:** om `finalizeAndSaveVersion` kastar av annan orsak än tom output *och* fallback-finalizen också kastar, debiteras rundan ändå via `commitCreditsUnlessProviderFault` i finally-blocket — trots att ingen version sparades och användaren inte får något. `creditCheck.refund()` finns i `prepareCredits` men används inte på den vägen. Kräver dubbelt persist-fel, därav P3. | 24-agentsvärm 2026-07-31, kodverifierad (M#gs13); rundan har `reason: "stream_ended_without_version"` sedan PR:en 2026-07-31 så fallet går att räkna i loggarna | Ägarval: refundera när innehåll fanns men ingen version sparades, eller dokumentera uttryckligen att modellkörningen debiteras oavsett persist-fel (som tom output gör sedan 2026-07-30). Ägare: `src/lib/providers/own-engine/generation-stream.ts` + `credits-handler.ts`. |
| [ ] | Öppen kvalitet-risk | P3 | **Spökvariant i snapshotten efter en misslyckad rematch:** `orchestration-snapshot.ts:192-194` behåller `base.variantId` när nästa skrivning har `variantId: null`. Skyddet är avsiktligt (det stoppade mid-chat-flippar när snapshotten tappade id:t), men det gör också att ett explicit `null` — en rematch som inte kunde lösa varianten — lämnar det gamla id:t kvar. Följande turer låser då till en variant som rundan faktiskt inte använde. | 24-agentsvärm 2026-07-31, kodverifierad (M#gs14) | Skilj "fältet utelämnat" (behåll base) från explicit `null` (rensa), eller rensa id:t när `getVariantById` misslyckas (`matcher.ts` loggar redan `prior_variant_unresolved`). Rör inte utan test — `orchestration-snapshot-variant.test.ts:56-60` kodifierar nuvarande skydd. Ägare: `src/lib/gen/orchestration-snapshot.ts`. |
| [ ] | Öppen kvalitet-risk | P3 | **Init-promptens `## Layout & Theme Files` kan droppas i sin helhet:** blocket bär scaffold-källkoden i inspirational-läget (init) men har `priority: 85` utan `required` (`budget.ts`), och budgetpasset trunkerar bara required-block — övriga stryks helt. En trång budget kan alltså ta bort hela scaffold-källan medan `## Scaffold: …` (required) står kvar och lovar filer som prompten inte längre visar. Motsatsen är inte gratis: att markera blocket required innebär att det trunkeras mitt i en kodfence, vilket kan vara värre än att sakna det. Follow-up/`heavy` använder andra, required, block och drabbas inte. | 24-agentsvärm 2026-07-31, kodverifierad (M#gs15) | Ägarval: fence-medveten trunkering (klipp på hela filer) och därefter `required: true`, eller lämna som är och lita på att prio 85 sällan nås. Ägare: `src/lib/gen/system-prompt/budget.ts` + `src/lib/gen/scaffolds/serialize.ts`. |
| [ ] | Öppen bug | P2 | **Plan-lägets follow-up-turer kan sluta i total tystnad — fem user-meddelanden utan ett enda persisterat assistentsvar eller bygge:** prod chat `785c8d7a` (audit-entry, planläge, 2026-07-30): turn 1–2 svarade korrekt (plan + blockerarfråga, sedan "Plan skapad … redo för granskning"), men därefter gav "Hur går det?" ×2 samt reparera-knappens canned prompt ×3 (15:54, 16:05, 18:30) **ingen** assistentrad i `engine_messages`, ingen rad i `engine_generation_logs` och inga Vercel-runtime-fel i fönstret — först den sjätte sändningen (18:31, tier pro, utan audit-meta) byggde v1. Två separata hål: (a) planner-turer persisterar bara svar när utdatan parsas som plan (`persistAssistantSummary` i `plan-mode-turn.ts` — icke-plan-svar är SSE-only och försvinner vid reload), (b) tre sändningar producerade varken generation-logg eller fel — kandidater: credit-gate-402 på max-tier efter prompt-loggning, klientens job-kö/stream-lock, eller planMode-arv via köade jobb (`helpers.ts:149`). Delvis maskerad av att godkänn-kortet bara renderas i debug-vyn (se P3-raden om `BuildPlanCard` nedan) — användaren kunde aldrig godkänna planen och satt fast i 2,5 h. Reparera-knappens sändväg härdas separat (`handleFixPreview` skickar nu `sourceKind: "autofix"` — samma leverans som denna rad); raden avser (a) och (b), som kvarstår. | /logg-utredning 2026-07-31 (prod-DB `engine_messages` + `prompt_logs` + `engine_generation_logs` + Vercel `get_runtime_errors`, read-only) | Forensik i två steg: (1) persistera planner-svar även när utdatan inte parsas som plan (fallback-textrad i st.f. tystnad), (2) instrumentera codegen-turens tidiga exits (credit-gate, lock, kö) så en "död" sändning alltid lämnar ett spår i DB. Ägare: `src/lib/api/engine/chats/chat-message-stream/plan-mode-turn.ts` + `createOwnEnginePlanModeResponse` + `src/lib/hooks/chat/helpers.ts` (job-kön). |
| [ ] | Öppen kvalitet-risk | P3 | **"Bygg integrationer" kan debitera två gånger för det som känns som en handling (Ö4b — copy-delen Ö4a är levererad 2026-07-31):** knappen grenar server-side på `hasRequiredRealBuildKeys(gate.spec)` (`finalize-design/route.ts:212`). Utan byggnycklar tas vägen `deterministic_release` — F2-filerna kopieras byte för byte till en F3-rad, ingen LLM, **0 diamonds**. Med byggnycklar auto-skickas ett chattmeddelande och LLM bygger integrationskod, **4–6 diamonds** (`prompt.refine`). Knappen ser identisk ut i båda fallen. Dessutom kan första LLM-rundan svara med `suggestIntegration` i stället för kod — en fråga med snabbsvaren "Godkänn förslag" / "Avvisa" — och godkännandet startar en **ny** stream som debiteras igen. Båda rundorna brände riktiga tokens, så avgiften är försvarbar; problemet är att ingenting säger det i förväg. **Ingen risk för dataförlust eller felaktig debitering** — det är en förväntansfråga, men den träffar användarens plånbok, vilket är varför den inte bara är kosmetik. | Kodgenomgång av F3-flödet 2026-07-31 på ägarens fråga om vad knappen gör (Ö4 i builder-UI-planen) | **Bara prissidan är kvar — copy-delen (Ö4a) är levererad 2026-07-31.** Readiness bär nu `hasRealBuildIntegrations` (= `hasRequiredRealBuildKeys(spec)`, härledd ur samma anrop som `/finalize-design` grenar på), och knappens tooltip säger väg + kostnad; `undefined` ger ärlig villkorad text i stället för en gissning. Vägen härleds aldrig ur `buildBlockingKeys` — den ljuger när nyckeln redan är ifylld. **Kvar (Ö4b, ägarbeslut):** avgör om en förslagsrunda som **inte** skrev kod ska debiteras full `prompt.refine`, eller om förslag + godkännande ska räknas som en åtgärd. Rör inte prislogiken innan det är avgjort. Ägare: `src/lib/credits/` med `pricing.ts`. |
| [ ] | Öppen städning | P3 | **49 spårade filer ligger i git med CRLF trots att `.gitattributes` säger `eol=lf`:** `* text=auto eol=lf` normaliserar bara vid **check-in**, så blobbar som committades innan attributet fanns behåller sina CRLF för alltid — attributet rör aldrig befintlig historik. Effekten är att en liten ändring i en sådan fil kan visa hela filen som ändrad, vilket gömmer den verkliga diffen i granskning. Observerat skarpt på grenen `ema`: den innehåller **en** funktionell ändring (`"ema": true` i `vercel.json`) men visas som 42 insertions/41 deletions, eftersom commiten normaliserade filen till LF. Störst yta: `src/lib/auth/auth.ts` (587 rader), `src/lib/builder/promptOrchestration.ts` (493), `src/app/globals.css` (374), `src/components/auth/auth-modal.tsx` (362), `src/lib/db/client.ts` (275), nio routes under `src/app/api/**`, samt `vercel.json` (41). | Byte-scanning av varje spårad blob på HEAD 2026-07-30, i samband med mergen av #663/#664 (en cloud-agent rapporterade riktningen omvänt — CRLF ligger i master, inte på `ema`) | **Ägarbeslut 2026-07-30: logga, fixa inte nu.** `git add --renormalize` över 49 filer varav flera på protected paths är precis den breda formattering `workflow.mdc` säger att man ska pausa på, och vinsten är kosmetisk. Görs det någon gång: en egen PR med **ingenting** annat i, och med noteringen att den skriver om `git blame` för de raderna. Alternativ smal variant: bara `vercel.json`, som är den enda fil där problemet faktiskt observerats. Ägare: `.gitattributes` + de 49 blobbarna. |
| [ ] | Öppen bug | P2 | **RepairGate klarar inte kolliderande importer — två körningar i rad föll på fel den borde kunna laga:** prod-canaryn 2026-07-29 (chat `85f8db72`, frisörsalong med bokning) gav en innehållsrik 43-filssajt men **ingen** version nådde `passed`. F2 v1: verifieraren flaggade två blockerare korrekt (`app/api/contact/route.ts` använder `Resend` utan import; `components/booking-form.tsx` refererar `sv` som är importerad type-only), server-repair körde **två** försök och gav upp med "code is syntactically valid but typecheck/build still fails". F3 v2 föll sedan på `components/booking-form.tsx(14,85): error TS2440: Import declaration conflicts with local declaration of 'CircleDot'` i både typecheck och build. Alla tre felen är rena import-/symbolkollisioner — deterministiskt lagbara utan LLM (lägg till saknad import, byt `import type` mot värdeimport, döp om eller ta bort den kolliderande importen). Att RepairGate ändå misslyckas gör att en användare med en helt rimlig prompt inte kan publicera. | Prod-canary 2026-07-29 (`/logg-internet`-flöde körd manuellt, prod-DB + Vercel-loggar verifierade) | Utred om `Normalize` kan äga de tre klasserna deterministiskt innan RepairGate:s LLM-varv: (a) anrop till ett namn som inte är importerat, (b) `import type` av ett värde som används i runtime, (c) `TS2440`-kollision mellan import och lokal deklaration. (a) och (b) upptäcks redan av verifieraren — de har alltså exakt fil/symbol, bara ingen fixare. Ägare: `src/lib/gen/verify/` (detektering finns) + normalize-lagret. |
| [ ] | Öppen kvalitet-risk | P2 | **Kontraktsradernas filbevis är basversionens, inte rundans — en integration som byggs i samma runda märks "planerad" trots att den levererades:** `fileEvidenceCapabilities` beräknas EN gång vid orchestration-start ur `input.previousFilePaths` (`resolve-base.ts:591`) och skickas i det tidiga `meta`-SSE-eventet, varefter `buildModelInfoSteps` suffixar varje kontraktsrad utan bevis med "(planerad — kopplas in i nästa steg)" (`helpers.ts:829-840`). En F3-integrationsrunda skriver auth-/betalfilerna UNDER rundan, så dess egen leverans kan per konstruktion inte finnas i mängden: kortet säger "Auth: clerk (planerad)" om precis det varv som kopplade in Clerk. Samma staleness träffar snapshotens uppskjutna-lista, som rensas mot `next.fileEvidenceCapabilities` (`orchestration-snapshot.ts:193-205`) och därför behåller en levererad capability som uppskjuten till nästa meddelande räknar om den. **Omfattning:** bara chattens detaljkort ("Visa detaljer") — Byggblock-panelens `Planerad`-status kommer från presence-unionen via dossier-routen och självläker efter bygget. **Rättelse 2026-07-27:** den omfattningen höll inte i prod, men av en annan orsak än denna rad — snapshot-nyckelbudgeten tappade `mutedCapabilities` innan den nådde databasen, så panelen visade `planned: 0`. Det är fixat i #629 och gör omfattningen ovan sann igen; raden här handlar fortfarande om att *värdet* är basversionens, inte om att nyckeln saknas. Felet pekar dessutom åt det ofarliga hållet (understryker en leverans i st.f. att påstå en som inte finns), vilket är varför den loggas i st.f. att blockera. | Cursor-bugbot på #623 (medium), kodverifierad 2026-07-27 | Riktig fix: räkna om filbevis EFTER finalize ur rundans egna filer och låt kortet/snapshoten läsa den siffran — inte det förgenererings-värde som per definition saknar rundans leverans. Gena inte genom att hoppa över suffixet för `lifecycleStage === "integrations"`: det är exakt den rundan där bygget fortfarande kan misslyckas, så då vore proposal-som-faktum tillbaka. Ägare: `src/lib/gen/orchestrate/resolve-base.ts:591` + `src/lib/hooks/chat/helpers.ts:829-840` + `src/lib/gen/orchestration-snapshot.ts:193-205`. |
| [ ] | Öppen bug | P3 | **Plan-lägets godkänn-kort renderas bara i felsökningsvyn:** `BuildPlanCard` med "Godkänn plan och bygg" ligger bakom `showStructuredParts`, som är `vm.showStructuredChat` — default `false` och togglebar bara med `?debug=1` i URL:en (`MessageList.tsx:518-548`, `useBuilderState.ts:76`, `BuilderHeader.tsx:238`). Serversidan är fullt funktionell för både första prompt och follow-up (planner-modell, orkestrering, persistens, credits, `emitPlanArtifact`), och godkänn-vägen finns hela vägen till codegen via `buildApprovedPlanExecutionPrompt`. Men en användare som inte kör debug-läge ser bara textsummeringen och eventuella snabbsvar på blockerare — aldrig det strukturerade godkänn-flöde koden är byggd för. Plan-läget är alltså inte trasigt, det är oåtkomligt. | Kodläsning av plan-lägets funktionsduglighet 2026-07-27 (ägarfråga: fungerar läget alls?) | Ägarbeslut behövs: exponera plan-kortet i normal vy, eller dölj Plan-knappen i "Avancerat" tills flödet är helt. Ägaren valde 2026-07-27 att bara fixa fel-toasten (gjort i #629) och logga UI-exponeringen här. Ägare: `src/components/builder/MessageList.tsx` + `useBuilderState.ts` + `BuildPlanCard.tsx`. |
| [ ] | Öppen kvalitet-risk | P3 | **`contractAuthProvider` tappas ur snapshotten av den breda sensitive-regexen:** `SENSITIVE_KEY_SUBSTR` i `orchestration-snapshot.ts` matchar delsträngen `auth`, så `contractAuthProvider` filtreras bort vid persistering trots att värdet är ett leverantörsnamn (`clerk`), inte en hemlighet. Praktisk effekt idag är liten — alla konsumenter (`useCreateChat`, `stream-handlers`, `helpers`) läser fältet ur den efemära SSE-metan, som inte saniteras — men snapshotten kan därför inte återge auth-kontraktet för en follow-up, till skillnad från `contractDataMode` och `contractPaymentProvider` som båda överlever. Upptäckt under nyckelbudget-fixen i #629. | Kodläsning 2026-07-27 (`/auth/i.test("contractAuthProvider") === true`, verifierad) | Snäva regexen till hela nyckelnamn eller värdemönster i st.f. delsträngar, eller döp om fältet så det inte kolliderar. Rör den inte utan test: regexen skyddar mot att faktiska tokens/lösenord persisteras, så en för generös omskrivning är värre än fyndet. Ägare: `src/lib/gen/orchestration-snapshot.ts`. |
| [ ] | Öppen städning | P3 | **`template_cache`-tabellen är död yta:** inget i runtime skriver den (Blob-manifestet äger mall-galleriet sedan v0-syncen togs bort); bara admin-routens egen import skriver, och `project-cleanup.ts` raderar utgångna rader. Admin-UI:t för export/import/förläng/rensa togs bort 2026-07-24, men tabellen, `POST /api/admin/database`-actionsen (`export-templates`, `import-templates`, `clear-template-cache`, `extend-template-cache`) och `templateCache`-fälten i statistik-svaret finns kvar. | /admin-renoveringen 2026-07-24 (kodläsning: enda skribenten är admin-importen själv) | Beslut behövs: droppa tabell + actions (migration + kod) eller behålla som manuellt verktyg. Ingen funktionell risk idag — bara underhållsskuld och en tabell som ser aktiv ut. Ägare: `src/app/api/admin/database/route.ts`, `src/lib/db/schema.ts`, `src/lib/project-cleanup.ts`. |
| [ ] | Öppen kvalitet-risk | P3 | **Fire-and-forget server-verify läcker `running`-leases:** 3 utgångna `running`-leases i prod-jobbtabellen; troligast frös Vercel funktionen efter SSE-svaret mitt i transaktionen så release aldrig kördes (ofarligt post-expiry — nästa jobb tar över — men symptom på att release inte är garanterad). Exakt interleaving ej återskapbar (antagande). | M#vlane3 (samma prod-incident 2026-07-13, live-DB read-only) | `waitUntil()` runt verify+release i fire-and-forget-lanen, och/eller sweeper som stänger utgångna `running`-leases. Ägare: `server-verify.ts` (fire-and-forget-schemaläggningen). |
| [ ] | Öppen kvalitet-risk | P3 | **Klient-UX efter files_json-lease-guarden (#507):** spar-klienten visar nu svensk "verifieras just nu"-copy vid `version_busy`, men det finns ingen automatisk retry/auto-respar när leasen släpper, och spar-knappen är inte låst under `verifying` (användaren kan trigga förutsägbara 409). Ingen dataförlust (lokala utkast ligger kvar dirty). | Granska-svärm I3 på #507 | Egen liten UX-leverans: retry/backoff eller "spara igen när verifieringen är klar"-mekanism + disable/tooltip på spar-knappen under verifying. Ägare: `usePreviewPanelCodeFiles`/`usePreviewPanelCodeDrafts` + `PreviewPanel.tsx`. |
| [ ] | Öppen bug | P3 | **Template-galleriet, residual efter M#tmpl1-exkluderingen:** de 11 crash-on-load-mallarna är dolda via `EXCLUDED_TEMPLATE_IDS`, men ~6 klickbara mallar har kvar crash-on-route/API-500 (feature-döda vid interaktion) — user-synliga fel som inte fixas av boot-exkluderingen. Rad tillagd efter Codex på #505 så residualen inte bara lever i policy-prosan (M#tmpl1). | Blob-audit 2026-07-08 (`node scripts/v0-templates/audit-template-repos.mjs` sektion ENV PLACEMENT) + Codex på #505 | Beslut per mall: exkludera, stubba env-nycklar eller Normalize-läka; se M#tmpl1 i Beslut & policy för optionerna. Ägare: `src/lib/templates/template-data.ts` + stub-lagren. |
| [ ] | Öppen kvalitet-risk | P3 | **Verify-envens `.env.local`-kuvert saknar lifecycle-/projektkontext (båda assembly-grenarna):** `buildExportableProject` bygger placeholder-kuvertet utan `lifecycleStage`/projekt-env (full F2-katalog inkl. tier3-stubbar) medan live-preview via `buildPreviewEnvLocalContents` strippar tier3-stubbar i F3 och mergar sparade projekt-env-värden. Ingen false-green för nyckelkrav (tier3-readiness-gaten läser env-storen, inte `.env.local`), men verify-bygget kan i F3 se stub-värden som previewen inte har. Gäller icke-verbatim sedan tidigare och verbatim sedan parity-fixen 2026-07-24 — medvetet samma kuvert i båda. | BB (pre-push bugbot på `fix/bugfix-svep-0724`, 2026-07-24) | Tråda `lifecycleStage` + ev. projekt-env-nycklar in i `buildExportableProject`s env-kuvert (samma `ProjectEnvLocalOptions` som finalize-version använder) när området rörs. Ägare: `src/lib/gen/export/build-exportable-project.ts` + verify-callsites. |
| [ ] | Öppen säkerhets-hygien | P3 | **CI:s prod-DSN är skrivbar även där bara läsning behövs:** `POSTGRES_URL_PROD` är samma skrivbara connection-sträng som kör prod-migrationerna, och används nu även av `Eval Baseline Update` (#631) och den läsande `prod-migrations-applied`-checken. Credentialen är stegskopad sedan #631, så `npm ci` och `create-pull-request` ser den inte, men de steg som ändå får den kan tekniskt skriva. `DB_ALLOW_PROD_LIKE_WRITE` hjälper inte här — den hedras bara av skript som explicit kallar `assertSafeWriteTarget`, inte av runtime-klienten eller Drizzle. | Codex P1 på #631 (den stegskopade delen är fixad; rollförslaget är resten) | Skapa en Supabase-roll med bara `SELECT` (+ `USAGE` på schemat) och lägg dess DSN som en separat secret, t.ex. `POSTGRES_URL_PROD_READONLY`, som de läsande jobben använder i st.f. den skrivbara. Kräver DDL på prod + ny secret, alltså ägaråtgärd — inte något ett kod-PR gör självt. Ägare: Supabase-rollerna + `.github/workflows/eval-baseline-update.yml` + `ci.yml` (`prod-migrations-applied`). |
| [ ] | Öppen kvalitet-risk | P3 | **Leveransstatus skrevs in i en avklarad plandetaljfil:** #604 markerade Fas 3 levererad i `docs/plans/avklarat/2026-07-22-shadcn-registry-beskriv-komposition.md`, trots att `docs/plans/avklarat/README.md` redan bar samma information. Plan-livscykeln pekar ut det tunna indexet plus git-historiken som statusytan; två historier över samma leverans kan glida isär. | Codex P1 på mergad #604 (nedgraderad till P3: dokumentationsorganisation, ingen runtime-risk) | Bestäm en yta — sannolikt: ta bort statusraderna ur detaljfilen och låt indexet + git bära leveransen. Ägare: `docs/plans/avklarat/` + `plan-lifecycle.mdc`. |
| [ ] | Öppen kvalitet-risk | P3 | Stream-routens F3-gate och finalize-design läser versionens filer via `checkTier3ReadinessForVersion` UTAN lease — samma TOCTOU-mönster som #504 stängde i ReleaseGate finns kvar på de icke-promotande entry-punkterna (lägre risk: de promotar inte, men finalize forkar på `baseVersion.files_json`). Kodverifierad öppen 2026-07-22. | Granska-svärm F5 på #504 | Överväg samma preloadedFiles/lease-mönster när området rörs; ingen brådska (ingen promote-väg). Ägare: `src/lib/api/engine/chats/chat-message-stream/f3-readiness-gate.ts` + `finalize-design/route.ts` (ankare flyttade av #563-splitten). |
| [ ] | Öppen kvalitet-risk | P3 | Phase B (LLM-approve-vägen) persisterar user-raden FÖRE `consumeF3ContinuationMarker`; förlorad race nedgraderar rundan till icke-approval i stället för 409 — asymmetriskt mot deterministgrenens consume-först (#503). Pre-existerande beteende, medvetet lämnat (ändrad Phase B-ordning har bredare konsekvenser). Kodverifierad öppen 2026-07-22 (reject-grenen `addMessage` före consume). | Granska-svärm G6 på #503 (dokumenterad i `dossier-system.md` § Version-presence) | Utvärdera consume-först även i Phase B, eller idempotent `addMessage` på markerMessageId. Ägare: `src/lib/api/engine/chats/chat-message-stream/f3-continuation-phase.ts:221-227` (ankare flyttat av #563-splitten). |
| [ ] | Öppen kvalitet-risk | P3 | `f3.deterministic_release_exempted`-telemetrin når bara devLog (no-op i prod) — undantagsbeslutet i #503 saknar prod-observability (LLM-rundan själv är dock fullt telemetrerad). Kodverifierad öppen 2026-07-22. | Granska-svärm G6 på #503 | Emit via event-bus/generation-telemetri när området rörs. Ägare: `src/lib/api/engine/chats/chat-message-stream/f3-readiness-gate.ts:249-254` (ankare flyttat av #563-splitten). |
| [ ] | Öppen bug | P2 | Nav-förvirring one-pager vs multi-page: scaffold `landing-page` valdes men route-planen skapade ändå fullvärdiga `/om` + `/contact` parallellt med `#om-oss`/`#kontakt`-ankare (innehållsdubblett + inkonsekvent per menyval); "Förfina"-follow-up tog inte bort routes utan la `notFound()` i `app/om/page.tsx` (kodrest). | /logg-internet 2026-07-09 (fynd #18, DB-bevisad chat e9f578a6 v1-v2) | Route-plan ska respektera one-pager-scaffold (ankare ELLER routes, inte båda); "ta bort sida X" i chatt ska mappa till deterministisk route-borttagning (UI-knappens väg), inte LLM-förfining. Ägare: route-plan i orchestrate + follow-up-tolkning. |
| [ ] | Öppen kvalitet-risk | P2 | `postgres-drizzle`: `lib/db/schema.ts` är `rewritable` medan verbatim `lib/db/index.ts` importerar `./schema` — om modellen döper om/utelämnar schemat får bygget en obruten import (TS2307). | Codex P2 på #445 (2026-07-08) | Gör schemat till deterministisk emitted/restored-beroende av helpern (verbatim-policy-koppling), eller mjuka upp helper-importen. Ägare: `data/dossiers/hard/postgres-drizzle/` + `src/lib/gen/dossiers/verbatim-policy.ts`. |
| [ ] | Öppen kvalitet-risk | P3 | Sidflikarnas aktiva-markering följer inte iframe-INTERN navigering: `activePreviewRoute` härleds enbart ur `previewUrl`-prop (`PreviewPanel.tsx:713-725`); klick på länkar inne i den cross-origin:ade preview-sajten uppdaterar varken prop eller flik-highlight. Flikklick fungerar (fixat i preview-tabs-PR:en), men highlighten kan visa fel route. | Granska-svärm på preview-tabs-diffen (2026-07-09, djävulens advokat) | Kräver postMessage-bridge från preview (inspect-bridgen finns redan som transport) som rapporterar route-changes. Egen liten leverans. |
| [ ] | Öppen kvalitet-risk | P2 | `analytics` + `dashboard` dubbel-triggar capabilities utan veto i follow-up-vokabulären → båda kan aktiveras samtidigt trots överlapp. | Automat A#16 (#422) | Lägg capability-veto eller merge-regel i `follow-up-capability-vocabulary.ts`. |
| [ ] | Öppen bug | P2 | `/finalize-design` detektionsfullständighet: `detect-integrations.ts`-regex kan missa en integration på filer som finns | G#21 | Konkret false-green (tomma filer → `409`) redan fixad; kvar = regex-täckning. `detect-integrations.ts`. |
| [ ] | Öppen bug | P3 | Plan-mode token-budget skalas mot tier-build-modellen, inte planner-fasen → planner-kontext skalas mot fel kontextfönster på anthropic-tier | MB-4 | OBS: nuvarande `engineModelId: resolveEngineModelId(resolvedModelTier)` är ett medvetet Bug 04#3-val (spegla codegens BuildSpec). Att blint sätta `engineModelId: planModel` regressar det. Riktig fix = frikoppla token-budget-modell från BuildSpec-modell. Kodverifierad öppen 2026-07-22: `create-chat-stream-post.ts:512` · `chat-message-stream/plan-mode-turn.ts:136` (ankare flyttade av #563-splitten). |
| [ ] | Öppen kvalitet-risk | P2 | F3 pre-gen contracts divergerar från approval-scopad build-plan: när parent-versionen saknar filhärledda integrationer (`tier3BuildSpec.requirements` tom) men rundan har godkända providers, renderar `renderTier3IntegrationBlock` en approval-scopad "Tier-3 Integration Build Plan" (bara godkända providers) SAMTIDIGT som `## Pre-Generation Contracts` ändå renderas ur hela prompt-härledda kontraktsuppsättningen (suppressionen på rad 303 keyar bara på icke-tom `tier3BuildSpec`). Modellen kan få motstridiga integrationsinstruktioner i samma systemprompt. | BB#capsrc1 (Bugbot på #492, rebased) | Suppress `renderPreGenerationContractsBlock` även när en approval-scopad plan är aktiv (t.ex. `tier3ApprovedProviders.length > 0`), ELLER slå ihop approvals in i pre-gen contracts. Risk: naiv suppression kan tysta pre-gen contracts när de faktiskt behövs. Ägare: `src/lib/gen/system-prompt/build-dynamic-context.ts:290-307`. |
| [ ] | Öppen kvalitet-risk | P2 | OC_DEBUG: `buildDebugSystemPrompt()` lovar alltid full kod + fynd + repo-kontext, men kontext-bygget får `debug: debugOwned` som kräver BÅDE `chatId` och `activeVersionId`. Saknas versionsid (eller ownership-lookup faller) injiceras ingen filmanifest/fynd-block fast prompten säger att de finns. Ej säkerhet (mer restriktivt), bara prompt-vs-kontext-inkonsekvens. | BB#oc1 | Mjuka upp debug-systemprompten eller tillåt chatId-only-ägd full kod (`src/app/api/openclaw/chat/route.ts` debugOwned + `src/lib/openclaw/server-context.ts`). |
| [ ] | Öppen infra-risk | P2 | Preview-hosten (Fly `vm-fly-jakem`, single VM) har minnestryck: `npm install` OOM-killades 2× 2026-07-02 ~06:02–06:04Z, health-checks flappar, och verify-lanen blev onåbar ~06:53Z ("fetch failed" → chat 1c34592c v3 false-RED + v4 overifierad). Koden hanterar nu onåbar lane retrybart (repair-routen 503 + pending), men grundorsaken är kapacitet: samtidiga installs/verifies på en enda VM. | M#fly1 (prod-incident chat 1c34592c, Fly-loggar) | Delvis åtgärdat: (a) minne uppskalat till 8 GB + `swap_size_mb = 2048` (`preview-host/fly.toml`, sedan 2026-04) och (b) alla installs serialiseras via global `installQueue` (concurrency 1) i `preview-host/src/runtime.js` — men OOM inträffade EFTER (a), så skalning ensam räckte inte. **Kvarstående infra-beslut (ägare):** (c) separera verify-lanen från preview-VM:en (kör fortfarande på samma host). Egen leverans — inte kodfix i huvudrepot. |
| [ ] | Öppen kvalitet-risk | P3 | Quick-edit läser bas-filerna FÖRE lease: routen läser `baseFiles` och `runQuickEdit` applicerar ops på dem (`quick-edit/service.ts:79`) innan `acquireVersionLease` tas (`service.ts:99`). I fönstret läs→lease kan en accept-repair avancera basens `files_json`, så minorn skapas från en stale snapshot (konsistens-race; ej dataförlust — ny rad skapas). #351:s M#qe1-lease stänger inte ordningen. | M#qe3 (extern coach post-#351-review, kodverifierad) | Ta leasen före bas-fil-läsningen i routen, eller låt `runQuickEdit` re-läsa bas-snapshotet under lease och applicera ops på det. Ägare: `quick-edit/route.ts` + `quick-edit/service.ts`. |
| [ ] | Öppen kvalitet-risk | P3 | Klient-autofix får den M#rep1-FILTRERADE repair-kontexten när server-repair inte reparerar: `handleRepairOrAutofix` bygger `repair.qualityGate` av endast de tre kanoniska checkarna och skickar samma objekt till `onAutoFix` (`post-checks.ts:747-754`) — icke-kanoniska failade diagnostik-signaler (t.ex. install-relaterade) når aldrig LLM-fixerns kontext. Residual ur stängda PR #344; därifrån saknas också ett selection-regressiontest för fast-edit-valet (M#sel1-fixen i #351 shippade utan). | M#344r (residual ur stängd PR #344, kodverifierad) | Bevara icke-kanoniska diagnostics i ett separat kontextfält (inte `qualityGate`-arrayen som repair-routens zod validerar) + lägg selection-regressiontestet. Egen liten PR. |
| [ ] | Öppen kvalitet-risk | P3 | **Stale bus-`done` efter user-edit ger en inaktuell "Klar"-badge (NEDGRADERAD P2→P3 2026-07-25 — ingen false-green):** `reconcileTerminalDbState` låter terminal bus-`done` vinna över DB-`pending` efter `invalidateVerification` (`stale-verification.ts:121-124`). **Men det starkaste positiva tecknet är DB-härlett och faller korrekt:** `mapVersionStatusToDisplay` läser `promoted` ur `context.releaseState` (`version-status-display.ts:133,182`), som editen nollställer till `draft` → badgen går "Publicerad" → neutrala **"Klar"** ("generering färdig utan blockerande fel"), aldrig grön verifierad-claim. Deploy-gaten läser DB:n. Residual = "Klar" är stale w.r.t. editen. **Naiv fix är inte möjlig:** bus-`done` + DB-`pending` är ALSO det normala render-first-läget mellan finalize och bakgrundsverify, så DB-state kan inte skilja fallen — och bussen är per-instans in-memory (`readAll`), så en emit från `/files` når inte den instans som håller det stale `done`. Kräver innehållsrevision (se konsoliderad rad nedan). Invarianten som gör detta ofarligt är nu testlåst: `version-status-display.test.ts` ("user edit invalidates the promoted claim"). | Codex P1 på mergad #352; **re-triagerad + kodverifierad 2026-07-25** (`version-status-display.ts:178-183` + `version-history-status-labels.ts:60-73`) | Ingen punktfix — vänta på innehållsrevisions-primitiven. Rör inte `reconcileTerminalDbState` utan den: att låta DB-`pending` degradera terminal `done` skulle flappa varje normal generation till spinner. **Uppdatering 2026-07-31:** stängd på `/version-status` av steg 3 — degraderingen `stale_content_revision` läggs till **utan** att fasen rörs, så render-first-fönstret förblir en no-op (den flapp-regression raden varnar för är alltså undveken och testlåst). Residual: `/versions`-listan trådar inte revisionen, så historikbadgen kan fortfarande visa "Klar". Flaggan `SAJTMASKIN_CONTENT_REVISION_GATE` är av i prod. |
| [ ] | Öppen kvalitet-risk | P3 | preview-host saknar testharness: `preview-host/` har ingen test-/specfil för runtime-livscykeln (boot/verify-köer, readiness, reaper). #357 la riktade guard-tester (`npm run test:guards` — timeout-settle, idle-sweep no-op, socket-räkning, boot-serialisering per chat; körs sedan #469 även i CI-jobbet `preview-host-guards`) men `installQueue`⊕`verifyQueue`-ordningsgarantierna och reaper-integrationen (stop→hibernated→re-boot) är fortfarande otestade — regressions där syns först på Fly. | Codex P1 på #357 (delvis åtgärdad med guard-tester; harness-gapet kvarstår) | Bygg minimal harness: extrahera kö-/reaper-logik till testbara enheter eller kör `server.js` mot mockad store/child-spawner i CI. Ägare: `preview-host/scripts/test-runtime-guards.mjs` som bas. |
| [ ] | Öppen kvalitet-risk | P3 | **Stale quality-gate-telemetri efter `invalidateVerification` — false-green-riktningen är AVFÄRDAD 2026-07-25 (premissen höll inte):** raden påstod att en stale `passed` kan släppa igenom en promote av redigerat innehåll. `assertPromoteAllowed` är dock **allow-by-default** — bara `verifier_failed`/`preflight_failed` blockerar (`promote-guard.ts:22-25,88-94`) och `null` är medvetet fail-open (back-compat: template-import/rollback/legacy, se "Beslut & policy"). En stale `passed` ger därför **exakt samma utfall** som den föreslagna superseding-null-raden → den föreslagna fixen kan inte stänga något hål. Ekvivalensen är nu testlåst (`promote-guard.test.ts`, "treats a stale passed signal identically to no signal"). **Kvarvarande verklig effekt = bara för-strikt-riktningen:** en stale `verifier_failed` blockar en legitim promote av det nya innehållet — och den självläker så snart någon gate skriver en färsk rad (också testlåst). | Codex P2 på mergad #352; **re-triagerad + kodverifierad 2026-07-25** | Skriv INTE superseding-raden (no-op mot guarden). Vill man stänga för-strikt-riktningen deterministiskt: låt `invalidateVerification` skriva en färsk rad, men först när innehållsrevisionen finns så guarden kan resonera om VILKET innehåll som gate:ades. **Uppdatering 2026-07-31:** för-strikt-riktningen är stängd av steg 3 — ett mismatchat `verifier_failed` kastas symmetriskt och guarden svarar retrybart `indeterminate` i stället för terminalt `failed`. Ingen superseding-rad skrevs; den är fortfarande en no-op. Flaggan `SAJTMASKIN_CONTENT_REVISION_GATE` är av i prod. |
| [ ] | Öppen kvalitet-risk | P2 | **Innehållsrevisions-primitiven (konsoliderade 4 rader) — levererad, restsymptom kvar:** ursprungsfyndet var att `engine_versions` saknade fil-hash, så inget lager kunde avgöra "gäller detta kvitto/den här statusen det innehåll som ligger i `files_json` NU?" — sedan #642 finns `files_revision` (DB-genererad md5) och läsarna jämför bakom flagga (#693). Den saknade primitiven var rotorsaken till (a) stale bus-`done`-badgen efter user-edit, (b) stale quality-gate-telemetri (för-strikt-riktningen), (c) runtime-ready-kvittot som binds till versionId i st.f. innehållsrevision (egen rad nedan, M#pv4), och (d) **F10 från observationssessionen 2026-07-25: reparationsvarv skriver över samma version-id, så användaren saknar återgångspunkt efter en misslyckad reparation** (alla tre varven i prod-sessionen rapporterade `Version: f4aabb03` / 31 filer). Spår 02 i åtgärdsprogrammet hänvisade F10 hit i stället för att bygga en parallell fix — versionsidentiteten är samma primitiv. Kodverifierad 2026-07-25. | Konsolidering ur Codex P1/P2 på #352 + M#pv4 + F10 (observationssession 2026-07-25), re-triage 2026-07-26 | **Status 2026-08-01: primitiven är levererad** — steg 1–3 i #642/#674/#693, läsarna bakom flaggan `SAJTMASKIN_CONTENT_REVISION_GATE` (default av); planen är upplöst och indexerad i [`docs/plans/avklarat/README.md`](docs/plans/avklarat/README.md) § Innehållsrevision, med flagg-släpp + `/versions`-trådning som restlistans R14–R15. Raden bör om-triageras vid nästa svep: kvar av de fyra konsoliderade symptomen är främst (d)/F10 (ingen återgångspunkt efter misslyckad repair — löses inte av revisionen ensam). Skiss: additiv `files_revision` (hash av `files_json`) + `files_updated_at` på `engine_versions`; stämpla revisionen i `generation_telemetry`-raden, i preview-sessionens runtime-kvitto och i bus-projektionen; först då kan gate/status/kvitto säga "detta gäller revision X" och stale signaler avvisas deterministiskt. Ägare: `src/lib/db/schema.ts` + `chat-repository/version-files.ts` + `generation-telemetry.ts` + `stale-verification.ts`. **Uppdatering 2026-07-31:** primitiven finns (#642/#646, grind #674) och läsarna jämför sedan steg 3, bakom `SAJTMASKIN_CONTENT_REVISION_GATE` (av i prod). Delarna **(a), (b) och (c) är därmed kodmässigt stängda**; **(d) F10** — reparationsvarv skriver över samma version-id, så användaren saknar återgångspunkt — **är INTE adresserad** och är den enda kvarvarande delen. Arkivera raden först när flaggan släppts och F10 fått en egen rad. Notera också att `schema.ts:609-650` inte längre stämmer: `files_revision` ligger på ~662, så bevis-påståendet "ingen revision på innehållet" är historik, inte nuläge. |
| [ ] | Öppen infra-risk | P2 | **DB-pool-svält i prod under generering** (live-session 2026-07-03, chat `69aae3d5`): klustrade `timeout exceeded when trying to connect` på `/api/auth/me`, `/login`, `/google/callback`, `/analytics`, `/preview-session` (02:15–02:16). Prod kör Supabase transaction-pooler (`pooler.supabase.com:6543`) → `max: 3` per serverless-instans (`client.ts:153`, `POSTGRES_POOL_MAX` ej satt), connect-timeout 10s. Builderns polling (6+ samtidiga GET/s) + långlivade stream-anrop svälter poolen; row-lock-blockerade skrivningar förvärrade (de är nu åtgärdade — se arkiv 2026-07-03). | Vercel runtime-logg 2026-07-03 (ägarrapporterad) + kodverifierad pool-config | Infra-beslut (ägare): mät samtidiga sessions i Supabase-dashboarden under en generering, sedan ETT av: (a) höj `POSTGRES_POOL_MAX` försiktigt (risk: pooler-cap → EMAXCONNSESSION, se SAJ-7/B1), (b) route:a långlivade vägar via `POSTGRES_URL_NON_POOLING`, (c) höj Supabase-tier/pooler-cap. Ändra inte blint — koden varnar redan för capen. |
| [ ] | Öppen kvalitet-risk | P3 | **Runtime-ready-kvittot binds till versionId, inte till innehållsrevisionen VM:en servar:** `recordPreviewRuntimeOutcomeForVersion` stämplar alltid versionens SENASTE `generation_telemetry`-rad. Repro-scenario: (1) version v1 bootar och servas av preview-sessionen; (2) server-repair passerar sin gate → `recordRepairPassedQualityGate` skapar en NY telemetri-rad (preflight_passed, `preview_success=null`) för det REPARERADE innehållet som aldrig bootats; (3) nästa `/preview-status`-poll får `running:true` från den gamla sessionen (samma versionId, host servar fortfarande pre-repair-filerna) → stämpeln träffar den nya raden → `preview_success=true` för innehåll som aldrig körts. SQL-guarden (`IS DISTINCT FROM true`) hindrar det INTE — raden är `null` och `null→true` är en tillåten övergång; per-instans-cachen mildrar bara på samma instans. **Cache-aspekt (Codex P2 runda 4, samma rotorsak — versionId ≠ innehållsrevision):** confirmed-`true`-cachen i `generation-telemetry.ts` (`confirmedPreviewReadyVersionIds`) invalideras inte när repair/autofix finalize:ar OM samma versionId (`targetVersionId`-rewrite skapar ny telemetri-rad för samma version) — cachen kortsluter då stämplingen av den NYA raden på den instansen: ett startfel eller riktigt heartbeat-kvitto för det omskrivna innehållet hoppar över skrivningen och nya raden förblir pending. Felriktningen är konservativ (pending, aldrig false-green). | M#pv4 (Codex P2 på PR #377 runda 2 + runda 4 cache-aspekt, kodverifierad) | **Delar rotorsak med den konsoliderade innehållsrevisions-raden ovan (re-triage 2026-07-25) — lös dem tillsammans.** Bind kvittot till innehållsrevisionen: låt preview-sessionen bära telemetri-rad-id/`files_json`-hash och stämpla bara rader vars innehåll matchar det VM:en bootat; samma bindning löser cache-nyckeln (nyckla cachen på telemetri-rad-id/innehållsrevision i st.f. versionId, eller invalidera vid same-version-rewrite). Ägare: `src/lib/db/services/generation-telemetry.ts` (`recordPreviewRuntimeOutcomeForVersion` + `confirmedPreviewReadyVersionIds`) + `src/lib/gen/preview/session-store.ts`. **Uppdatering 2026-07-31:** stängd av steg 3 i **båda** halvor — revisionsgrind i kvittots UPDATE (samma sats som monotoniteten) plus revisionsnycklad confirmed-cache, bevisat mot riktig Postgres i `scripts/db/content-revision-readers.postgres.test.ts`. Residual: "det VM:en servar" härleds fortfarande ur versionens revision, inte ur preview-sessionen — exakt bindning kräver att `PreviewSessionEntry` bär revisionen. Flaggan `SAJTMASKIN_CONTENT_REVISION_GATE` är av i prod. |
| [ ] | Öppen kvalitet-risk | P3 | **F3-marker bär inte env-only-förslagsdetaljer:** när en no-code F3-runda bara bestod av `requestEnvVar` (eller en `suggestIntegration` med omappbar provider, t.ex. `"other"`) bär den persisterade markern `suggestedProviders` men INTE de efterfrågade env-nycklarna, och tool-/integrationskortet persisteras inte i `engineChat.messages`. Approval-rundan får då tom `approvedProviders`, prompten hänvisar till chathistoriken (där detaljerna saknas) och integrationsverktygen är avstängda → modellen bygger från generisk approval-prompt utan den föreslagna nyckelns namn. Konservativ riktning: fallback-instruktionen tvingar ändå byggbar kod med lazy-guard-mönstret; modellen definierar eget nyckelnamn som sedan syns via detect-integrations på den byggda koden. | Codex P2 på PR #383 (runda 2, 2026-07-04; nedgraderad till P3 — smal kant: env-only-förslag utan mappbar provider) | Persistera `requestedEnvKeys` i F3-markern (från rundans env-signaler) och inkludera dem i approval-promptens intro; alternativt behåll `requestEnvVar` i approval-rundans tool-set. Ägare: `src/lib/gen/stream/f3-continuation.ts` (marker-payload) + `chat-message-stream-post.ts` (approval-prompten). |
| [ ] | Öppen kvalitet-risk | P2 | Scaffold-matchern gate:ar embedding-override via ad-hoc keyword-heuristik (`canUseEmbeddingOverride`), inte via manifestens `allowedBuildIntents` → en scaffold vars manifest förbjuder aktuell build-intent (t.ex. dashboard/app-shell för website) kan väljas. | PR #355-triage #24 (kodverifierad 2026-07-08, `src/lib/gen/scaffolds/matcher.ts:555-592,749-782`) | Lägg en manifest-auktoritativ `scaffoldAllowsIntent`-gate som slutfilter på vald scaffold (keyword + embedding), fallback till generic vid krock. Core-path + stort testyta → egen PR med matcher.test-uppdateringar. |
| [ ] | Öppen kvalitet-risk | P3 | `selectPreferredEngineVersion` faller tillbaka till nyaste `failed`-versionen när ALLA versioner är failed (i st.f. `undefined`) → en failed version kan behandlas som preferred/aktuell. | PR #355-triage #41 (kodverifierad igen 2026-07-22, `src/lib/db/engine-version-lifecycle.ts:244` `?? sorted[0]` + `chat-repository/versions.ts:238-240`) | Returnera `undefined` när alla failed — MEN caller-audit 2026-07-22 visar att fixen är UX-synlig: `useBuilderDerivedState.ts:109` använder resultatet som aktiv version (builder skulle visa tomt i st.f. den failade versionen); route-callers har `?? getLatestVersion`-fallbacks som bevarar beteendet. Kräver designbeslut per caller, inte mekanisk ändring. |
| [ ] | Öppen kvalitet-risk | P3 | tier-2-preview kan rensa `iframeLoading` på rå iframe-`onLoad` (boot-sida ger HTTP 200) och det finns ett fönster efter `preview-ready` där boot-sida kan visas utan overlay → visuell false-ready. (Lifecycle "live" styrs dock av URL + `previewPending`, inte onLoad.) | PR #355-triage #1 (kodverifierad 2026-07-08, `src/components/builder/preview-panel/hooks/usePreviewIframe.ts:172-204`) | Gate:a tier-2-klar-signalen mot host-readiness/DOM-probe som shim-previewn redan gör. |
| [ ] | Öppen kvalitet-risk | P3 | Preview-hostens `/status` exponerar bara `running` (process-liveness), ingen separat content-`ready`-signal; resume/preview-status kräver `running` → en fortfarande-kompilerande boot kan behandlas som live. (PR #355:s egen `waitForReady→ready:true`-regression finns INTE i master.) | PR #355-triage #3 (kodverifierad 2026-07-08, `preview-host/src/server.js:408-410` + `preview-host/src/runtime.js` + `src/lib/gen/preview/preview-host-client.ts:170`) | Exponera content-`ready` (process alive + readiness-probe) skilt från `running` och kräv det i resume-vägen. Egen leverans (preview-host). |
| [ ] | Öppen kvalitet-risk | P2 | Dep-backfill för syskon-dossiers tappar provider-identitet: autofix-backstoppen `resolveCapabilityDependencies` (`src/lib/gen/autofix/dep-completer.ts:220`, anropad från `pipeline.ts:1281` med bara `requestedCapabilities`) replay:ar capabilityn utan promptText → default-Postgres-dossierns deps. Om modellen utelämnar en verbatim-fil kan `applyDossierVerbatimPolicy` återställa t.ex. mongo-helpern EFTER dep-completern → projektet får `import "mongodb"` men bara Postgres-fallback-deps → install/typecheck-fail. | Codex P2 på #445 (kodverifierad kedja) | Driv dependency-backfill från `selectedDossierIds` (finalize-version-runnern trådar redan explicit dossier-ids med capability-fallback, se `runner-dossier-threading.test.ts`) i stället för capability-replay när syskon-dossiers finns. Ägare: `dep-completer.ts` + `autofix/pipeline.ts` context. |
| [ ] | Öppen kvalitet-risk | P3 | Deploy-repair no-op:ar på orelaterad `repair_available`: `/api/v0/deployments/repair` kortsluter på ALLT `verification_state === "repair_available"` som `alreadyAvailable` utan att kontrollera att repairen kom från just den failade deployen. Har versionen redan en väntande preview/ReleaseGate-repair blir deploy-repair-knappen en no-op och hämtar aldrig Vercel-byggloggen för publiceringsfelet. | BB#deploy5 (bugbot på `feat/publicera-stringens` 2026-07-08, `src/app/api/v0/deployments/repair/route.ts:87-100`) | Scoppa idempotensen till deploy-originerad repair (spåra ursprung i meta), eller tillåt deploy-repair att köra även när en orelaterad `repair_available` finns. |
| [ ] | Öppen kvalitet-risk | P2 | **Preview-host boot-fel är osynliga för användaren (crash-loop utan felyta):** när ett boot misslyckas (t.ex. `pnpm install` exit 1) sätter `bootRuntimeForSession` `status="error"`, men splash-sidans auto-refresh (`GET /{chatId}`) och `/preview-status`-pollen re-triggar `ensureRuntimeForChat` → nytt boot-försök var ~4:e s tills TTL. Felet loggas bara till Fly-sessionens `logs`-array — aldrig till DB (`engine_version_error_logs` = 0) eller buildern, som bara visar evig "Startar preview"-splash utan orsak. (Install-rotorsaken för Tailwind v4/pnpm 11 är fixad i denna PR; visibility-gapet gäller alla framtida boot-fel.) | /logg prod-incident chat 11c3c1cd (2026-07-08, template `XmzC9oi7g4m`) + kodverifierad | Efter N misslyckade boots: markera sessionen terminalt `error`, sluta re-boota (backoff/cap) och logga en `preview`-rad till `engine_version_error_logs` + yt ett riktigt fel i buildern i st.f. splash. Ägare: `preview-host/src/runtime.js` (`bootRuntimeForSession` + boot-trigger i `server.js`) + app-sidans preview-status-projektion. |
| [ ] | Öppen kvalitet-risk | P3 | **Dämpad småtext klarar inte WCAG AA-kontrast i mörkt tema:** `--muted-foreground` (`220 6% 60%`) vid 70 % opacitet mot `--background` (`220 16% 7%`) ger ~3,75:1, och Tailwinds `text-gray-500` (#6b7280) mot samma bakgrund ~3,91:1 — båda under AA-kravet 4,5:1 för normal text. Nästan alla förekomster är `text-xs`/`text-[10px]`, så large-text-undantaget (3:1) gäller inte. Kontrasten är räknad ur tokenvärdena i `globals.css:13,24`, inte uppskattad. Bred yta: ~150 förekomster av `text-gray-500` + `text-muted-foreground/70` i `src/`. | M#a11y1 (Vercel-toolbar a11y-audit mot prod 2026-07-24, statiskt kontrasträknad) | Tokenbeslut, inte punktfix: höj ljusheten i `--muted-foreground` (eller slopa `/70` på redan dämpad text) i st.f. att patcha enskilda komponenter — en sök-och-ersätt över 150 ställen är precis den breda ändring som ska undvikas. Ägare: `src/app/globals.css` (tokens) + konventionen att nå gråtoner via `text-gray-500` i st.f. semantiska tokens. |
| [ ] | Öppen bug | P3 | **`merge-ready-freshness` tar bort labeln när agenten dokumenterar sin egen PR:** Cursor cloud-agenter postar sina kommentarer som **`cursor[bot]`** (`user.type: "Bot"`), och workflowens filter släpper igenom allt som inte är `dependabot[bot]`/`github-actions[bot]` — plus `vercel[bot]` på issue-vägen. En agent som skriver "mergarens verifiering" eller "triage av bot-fynden" på sin egen PR triggar därför `remove` med skälet "bot-fynd efter sign-off", fast inget fynd landat. Hände skarpt 2026-07-29 på **#655**: sign-off 17:29:10, agentens verifieringskommentar 17:34:36, labeln borttagen av workflowen 17:34:50 med grön grind och oförändrad head-SHA. Effekten är omvänd mot syftet: grinden städar bort ett giltigt godkännande och en mergare ser "författaren är inte klar". | Fas D/P2-leveransen 2026-07-29 (#656) — upptäckt när #655:s label försvann efter en ren dokumentationskommentar | Filtret behöver skilja **fynd** från **anteckning**, inte avsändartyp. Enklaste hållbara varianten: låt bara `cursor[bot]`-kommentarer som innehåller Bugbots fynd-markörer (`<!-- BUGBOT_REVIEW -->` / `BUGBOT_BUG_ID`) räknas, precis som `vercel[bot]` redan särbehandlas på issue-vägen; review- och inline-vägarna kan lämnas orörda eftersom en agent inte postar reviews. Beslutslogiken ligger i `scripts/ci/merge-ready-freshness.mjs` (enhetstestad) och avsändarfiltret i `.github/workflows/merge-ready-freshness.yml:34-47` — testa båda. Fram till dess: sätt labeln **efter** sista kommentaren, och räkna med att behöva sätta om den. |
| [ ] | Öppen städning | P3 | **Legacy-normaliseraren är den sista backoffice-LLM-ytan med hårdkodat modellval:** Fas D flyttade wizardens persona/guide och byggblocks-kurationen till `config/ai_models/manifest.json` (workloads `backoffice_scaffold_wizard_persona`/`_guide`/`backoffice_dossier_curation`), men `scripts/dossiers/normalize-legacy-prospect.ts:65` har kvar `const DEFAULT_MODEL = "gpt-5.5"` och `backoffice/pages/dossiers.py:_NORMALIZE_MODELS` sin egen tuple `("gpt-5.5", "gpt-5.4-mini")`. Ingen funktionell risk idag (värdena är rimliga och skriptet har redan `--model=`), men det är två sanningar om modellval kvar bredvid manifestet — exakt det Fas D tog bort på de tre andra ytorna. | Fas D-arbetet 2026-07-29 (kodläsning i samband med `--model`-flaggan i `curate-from-reference.ts`) | Registrera en fjärde workload (t.ex. `backoffice_dossier_legacy_normalize`) och läs den via `backoffice/ai_workloads.py` + `getWorkloadDefaultModelFromManifest`, precis som kurationen nu gör. Medvetet **utanför** Fas D:s scope: planen räknade upp tre ytor, och legacy-importen är en parkerad väg (materialet ligger utanför repot). Ägare: `scripts/dossiers/normalize-legacy-prospect.ts:65` + `backoffice/pages/dossiers.py` (`_NORMALIZE_MODELS`). |
| [ ] | Öppen bug | P2 | **`Eval Baseline Update` har failat varje vecka i minst åtta veckor utan att någon märkt det:** alla schemalagda körningar 2026-06-08 → 2026-07-27 slutade `failure` med `preflight_env: failed_env: missing database connection string` → 18/18 prompts `FAIL` och "PASS → FAIL" på hela baselinen. Den **första** grundorsaken (jobbet saknade `POSTGRES_URL`) är åtgärdad av #631. Två separata problem: (a) eval-gaten har inte mätt genereringskvalitet sedan juni, så `--gate`-jämförelsen i CI vilar på en baseline ingen validerat sedan dess; (b) åtta röda schemalagda körningar i rad gav ingen signal någon agerade på — workflowen saknar felnotifiering och syns inte på PR:er. **`OPENAI_API_KEY` är fortfarande ogiltig — den tidigare noteringen att den var roterad och fungerade var FEL.** Verifierat i run `30588420364` (2026-07-30 22:48, `dry_run=true`): `APICallError … Incorrect API key provided: sk-proj-…LWgA`, `statusCode: 401`, `code: invalid_api_key`, för varje scenario. Secreten uppdaterades 11:48 samma dag men den nya nyckeln autentiserar inte heller. **Felslutet som orsakade den felaktiga noteringen är värt att känna igen:** körningen rapporterade 72–76 % i stället för 0 %, vilket lästes som "LLM svarade". Det gjorde den inte — `files=0` och `time=2744ms` (baseline: ~67 s). Poängen kommer från att scaffold-/preflight-filer passerar de ~9 av 13 checks som inte kräver LLM-output. **En eval-poäng långt över noll bevisar alltså inte att generatorn körde**; läs `files=` och `time=` först. (a) ovan står därför kvar oförändrad: eval-gaten har inte mätt genereringskvalitet sedan mars. | Eval-arkeologi 2026-07-27 (#631); nyckeln roterad av ägaren 2026-07-30 men fortfarande ogiltig; verifierad om 2026-07-31 | **Ägaråtgärd: skaffa en fungerande `OPENAI_API_KEY` och sätt den som repo-secret.** Testa med `gh workflow run eval-baseline-update.yml -f dry_run=true` — ett ogiltigt nyckel-fall faller på ~1 min och kostar nästan inget. Därefter: kör om, och **spara ny baseline** (`--save-baseline`), eftersom den nuvarande är från **2026-03-18** och skapad med **`gpt-5.3-codex`** — 8 checks, annan pass-regel, och utan `blockingChecks`. Den falska "15 scenarier PASS → FAIL med nya blockerare"-rapporten är nu delvis åtgärdad i koden: `compareWithBaseline` hoppar över blockerar-diffen mot en baseline som saknar fältet i st.f. att rapportera varje blockerare som ny, och CLI:n skriver ut baselinens ålder och modell. Kvar oberört: (b) röda schemalagda körningar ger fortfarande ingen notifiering. Ägare: `src/lib/gen/eval/` + `.github/workflows/eval-baseline-update.yml`. |

## Behöver repro

Fynd som inte kan avgöras statiskt. Flytta till Aktiv kö när repro finns, eller till arkivet om de avfärdas.

| Fynd | Källa | Repro-krav |
| --- | --- | --- |
| Transient 502 på `/api/openclaw/health` observerades i prod-console men endpointen svarade senare 200. | /logg-internet 2026-07-09 (fynd #13) | Vid nästa incident: korrelera samma tidsfönster mot Vercel runtime-loggar för att skilja cold start från bestående fel. |
| F3 auto-kick `onF3Ready` kringgår stale-base-409-gaten | B12 | Parallell F2-follow-up + F3-send mot samma chatId → förvänta 409. `chat-message-stream-post.ts` + `PreviewPanelF3Trigger.tsx` + `useSendMessage.ts`. |
| `arcade-with-klarna` failar med merge-syntax | E#1, R#10 | `npm run eval:weird-smoke:dump` mot LLM-providers (nycklar + nät + kostnad). |
| Scaffold required files kan tappas i finalize/export-path | R#9 | Deterministisk preflight/export-verifiering när repro finns. |
| Font materializer träffar mest baseline Inter (variant→font-parning) | G#53 | Eval-verifiering mot faktisk output. |
| Element crop missar små element vid DPI/zoom | G#70, U#52 | Inspector-worker (Playwright DPI/zoom). |
| Media local fallback `/api/uploads/media` kanske ej nås av preview-VM | U#29 | Verifiering mot preview-VM-kontext. |
| Analytics före cookie-consent (integritet) | U#56 | App-bred audit av var analytics initieras + gate på consent-flagga. |
| Loopia auto-DNS kräver `OK` på både apex-A och www-CNAME; Loopia kan returnera icke-`OK` när en post redan finns → en re-link kan visa manuella instruktioner i onödan fast DNS är korrekt (godartat: posterna skapas ändå, instruktionerna är korrekta). Flyttad från Aktiv kö 2026-07-22: fixen kräver Loopias faktiska dubblett-svarskod. | Bugbot på PR #395 (`src/app/api/domains/link/route.ts:86-108`) | Provocera en re-link mot en domän med redan skapade poster och logga Loopias exakta svarskod (`src/lib/loopia/loopia-client.ts:189`); behandla den koden som success. |
| Socket connection lost flera ggr/min vid generering av användarsida | M#cr3 | Network HAR/SSE-logg från live-session: vilken endpoint tappar (`/stream`, `/versions`, `/readiness`, `/quality-gate`, `/repair` eller Next/Vercel HMR-socket)? #284/#286 sänkte route-timeouts men ingen direkt SSE/poll-reconnect-fix hittad. |
| `THREE.WebGLRenderer: Context Lost` på landningssidan i prod-konsolen. Misstanke: D-ID:s WebRTC-video och 3D-scenen (`how-it-works-scene.tsx`) konkurrerar om GPU-kontexten, vilket kan lämna scenen svart eller frusen. | M#gl1 (prod-konsol 2026-07-24, samma session som D-ID-avataren) | Öppna `/` i prod med avatar-widgeten aktiv och provocera context-loss (flera flikar / GPU-tryck); avgör om scenen återhämtar sig och jämför med avataren av. Utan repro går GPU-tryck inte att skilja från saknad `webglcontextrestored`-hantering. |
| Vercel-toolbaren rapporterar "visible text must be part of accessible name" på `#radix-_r_8_` / `.transition-opacity` / `.group` i prod. Syskonfynd till M#a11y1, men till skillnad från kontrasten går det här inte att peka ut statiskt. | M#a11y2 (Vercel-toolbar a11y-audit mot prod 2026-07-24) | Radix-id:t är autogenererat och byter värde mellan renderingar. Kör auditen igen och plocka elementet via DOM-inspektion för att se vilken `aria-label` som inte innehåller sin synliga text. |
| Gateway-dashboarden visar "Skills 53/53 active" samtidigt som ~40 av dem har saknade beroenden (`1password`, `apple-notes`, `apple-reminders` m.fl. — desktop-/macOS-integrationer i en Linux-container). Oklart om OpenClaw ändå exponerar dem som tool-definitioner mot modellen, vilket i så fall kostar tokens och kan få agenten att resonera om verktyg den inte kan köra. Prompten själv (`infra/openclaw/config/workspace/TOOLS.md`) påstår INTE att den har dem, så det är ingen promptinkonsekvens. | M#ocskill1 (Render-dashboard 2026-07-24) | Inspektera vad gatewayen faktiskt skickar som `tools` i ett `/v1/chat/completions`-anrop, eller OpenClaws skill-registry i containern, och avgör om skills med saknade deps filtreras bort. Filtreras de bort är raden avfärdad. |
## Beslut & policy (uppskjutet — ej aktiva buggar)

Systemet gör som tänkt; "fixen" är ett produkt-/arkitektur-**val** som medvetet skjutits upp. Räknas **inte** som risk i canvasen. Princip (Område 7): *systemet får misslyckas, men aldrig ljuga* — placeholder/stub/saknad hard-dossier får aldrig signalera success.

| Kluster | Källor | Val / blockare |
| --- | --- | --- |
| Browser-fel från F2-preview når varken DB eller en fixande LLM | /logg-utredning + kodkartläggning 2026-08-01 (chat `e8bd3ba6`: hydration-/script-fel bara synliga i DevTools) | Kartlagt gap: ingen aktiv väg fångar iframe:ns `console.error`/`window.onerror`/hydration-varningar — legacy `preview-error`-postMessage-bryggan är död (shim mintas inte i F2, ingen lyssnare i buildern), Product Postcheck (`SAJTMASKIN_F2_PRODUCT_POSTCHECK`, default av) ser bara fatala `pageerror`/overlay och matar inte RepairGate, preview-hosten ser bara serverprocessens stdout/stderr. RepairGate äter idag typecheck/build/lint + VM-byggfel. Val: bygga en error-bridge (VM-injicerad reporter eller postMessage + lyssnare) och tråda in i RepairGate/`error_log_events` = eget arkitekturpass med brus-/privacyavvägningar — inte ett punktfix. Rotorsaken till just hydration-klassen (layout-provider-fixerns mid-tree-injektion) är fixad deterministiskt 2026-08-01. |
| Okänd innehållsrevision är fail-open i preview-kvittot | Bugbot på #703 2026-08-01 | `revisionMatchesContent` (`generation-telemetry.ts`) godtar `files_revision IS NULL` som träff, även när anroparen sagt vilken revision VM:n bootade. En NULL-rad som är NYARE än raden för den bootade revisionen tar då stämpeln, medan cachen nycklas på den bootade revisionen och hoppar över vidare skrivningar. Beteendet är beslut 1b (okänd revision = fail-open, aldrig spärr — se `schema.ts` och `add-files-revision.sql`) och inte en regression: samma NULL-rad vann även före #703, då mot versionens nuvarande revision. Dubbelt latent — grinden är av bakom `SAJTMASKIN_CONTENT_REVISION_GATE`, och `resolveFilesRevision` sätter alltid en revision när `versionId` finns. **Val:** behåll fail-open, eller kräv exakt match när anroparen skickat `bootedFilesRevision` (då blir en okänd rad ostämplad i stället för fel-stämplad). Avgörs innan flaggan slås på. |
| Eval-gaten mäter inte verklig generering (ägaråtgärd) | Extern granskning 2026-08-01 (#670) | Baslinjen är `2026-03-18` (`eval-baseline.json`) och saknar `blockingChecks`; senaste körningen (`30588420364`, 2026-07-30) föll på `invalid_api_key` med `files=0` och ~2,7 s mot baslinjens ~67 s. Koden mitigerar redan det värsta (`baselineTracksBlockingChecks()` hoppar över blocker-diffen mot en legacy-baslinje) och gaten är **inte** kopplad till PR-CI — den kör på veckoschema i `eval-baseline-update.yml`, medvetet utanför merge-grinden per ADR 0001. Det är alltså ingen false-green i CI, men siffrorna i den senaste dokumenterade körningen beskriver inte generering. **Åtgärd är ägarens:** sätt en giltig `OPENAI_API_KEY` som secret och kör om med `--save-baseline`. |
| Degraded/placeholder-policy (placeholder = degraded, aldrig grön) | N#1, G#17, G#22, G#35, G#49, G#51, U#72 | Warning vs blocker per lane = gate-policybeslut; kan flippa version-status röd. |
| F2/F3 runtime-gate (Product Postcheck) | G#10, N#4, N#H3, R#6 | Default-off + fail-open idag; produktifiering till blockerande = pipeline-beslut. |
| Simplified-brief fallback sänker premium/3D | G#13 | Degraded-mode-signal = kvalitetspolicy (påverkar brief-compose + capability-threading). |
| Verifier-scope + recurring findings | G#33, N#5 | Hela filer vs snippets = verifier-arkitektur + token-budget; recurring-findings = nytt prompt-steg. |
| Verify-skip / cold cache | G#31, G#32 | Warm typecheck dokumenterat fail-open (`cache_cold`); cold-blockerande = infra-beslut. |
| Follow-up kvalitet/budget | G#57, N#3 | Jämka follow-up-gate mot init + hård regression-gate i CI = process-beslut. |
| Env-precedence / docs / refaktor | G#16, G#18, G#19 | `process.env`-refaktor (~100 filer), env-docs canonical-källa, `.env.local`-precedence = egna pass. |
| Säkerhet (deprio — eget pass) | G#40 (residual), #196, #197, B07, B-GA | TOCTOU/DNS-rebinding (IP-pinning), inspect spoof (nonce), publik media GET, OAuth-loggning. App-side SSRF redan fixad #196. |
| Quality-gate fail-open | B08, M#pg1 | **Härdat i två steg:** #299 gjorde `/quality-gate`-routen fail-closed-men-retrybar vid guard-/telemetri-läsfel (`onReadError:"indeterminate"` → ingen promotion, ingen terminal `failed`), och 2026-07-02 (branch `fix/bug-swarm-batch-0702`) fick även kanoniska `promoteVersion`/`acceptRepair` samma `onReadError:"indeterminate"` — läs-exception promotar aldrig. **Kvarvarande medvetet val:** no-telemetri-rader (`signal===null`) behåller fail-open överallt (back-compat för template-import/rollback/legacy). |
| Clerk-dossierns middleware fail-open utan riktiga nycklar | M#oc2 (residual, #351), extern coach post-#351-review "P1-B" | #351:s key-gate:ade `clerk-auth`-middleware returnerar `NextResponse.next()` för ALLA routes (även `/dashboard`, `/api/protected` osv.) när nycklar saknas/är placeholder — medvetet val så F2-preview renderar hela sajten i stället för 500/vit sida. Trade-off: deployar användaren utanför den gate:ade F3-vägen med placeholder-nycklar blir "skyddade" sidor tyst publika. Coach-rekommendation: fail-closed (redirect/503) för protected routes när Clerk är okonfigurerad. Beslut uppskjutet — fail-closed skulle blocka:a member-sidor i F2-preview, och F3/deploy-vägen kräver redan riktiga nycklar via tier3-readiness-gaten. Revisit om export/deploy-utanför-F3 blir en riktig väg. Ägare: `data/dossiers/hard/clerk-auth/components/middleware.ts`. |
| Capability-ytans ownership: prompt-kontrakt + Advisory, ingen deterministisk radering | M#dchat1 (prod-incident 2026-07-13, chat `747636c8`, DB/files_json-verifierad; omtriagerad 2026-07-28 efter #639) | **Ägarbeslut 2026-07-28: prompt-prevention + Advisory är slutläget.** #639 namnger dossierns komponent, importväg och route i follow-up-prompten (modellen ska adaptera eller ersätta, aldrig lämna två) och `runProjectSanityChecks` flaggar en komponent som anropar en API-väg utan route-handler. Det som medvetet **inte** byggs är ett `REPLACES:`-utdataprotokoll där modellen deklarerar ersatta paths och `finalize-merge.ts` raderar dem: en felaktig deklaration raderar användarens filer, alltså dataförlust i utbyte mot mindre brus. Kvarvarande trade-off: struntar modellen i prompt-blocket kan två chatt-ytor samexistera som i incidenten (LLM-byggd `chatbot-widget.tsx` + egen `/api/ai-chat` v5, dossiern `openai-chat` tillagd v6, F3-verify v7 föll på widgetens `TS2345 data.reply`) — vi upptäcker det i diagnostiken i stället för att stoppa det. Revisit bara om Advisory-träffarna visar att det händer ofta i prod. Ägare: `src/lib/gen/system-prompt/sections/dossiers.ts` + `src/lib/gen/validation/project-sanity.ts`. |
| Write-on-read i `/files`-GET (arkitektur) | M#files1 (residual) | Bägge persist-grenarna (heal + materialize) är nu fail-fast/best-effort (`lockTimeoutMs:2000` + try/catch) så läsvägen aldrig 429/500:ar på låskonflikt. Kvarvarande **arkitekturval**: flytta persist från GET till skrivvägen helt — eget pass, ingen aktiv defekt. |
| Draft-version utan parent-lease (vanlig generation) | M#qe1 (residual) | Quick-edit tar nu lease på basversionen (`quick_edit`-kind). Den generella `addAssistantMessageAndCreateDraftVersion`-vägen (vanlig generation/follow-up) tar medvetet INGEN parent-lease — den skapar alltid en ny rad från stream-output och muterar inte basen; stale-base-gaten (5-2) täcker klientsidan. |
| Scaffold/variant/font-tuning | G#50, G#52, G#54, G#66 | Scaffold-defaulttext, pre-match vs embedding, Geist-workaround, adaptiv `MAX_PAGES` = heuristik-/produkttuning. |
| Produkt-/UX-gap | U#10, U#23, U#42, U#47, U#49, G#71 | Optimistic conflict, fler transcribe-språk, scraper-prioritet, OpenClaw 180k-context, D-ID session, PDF print-flöde. |
| Logg-/observability-/storage-städ | G#59, G#60, G#63, U#53, U#63, U#66 | Bred städ med sampling-/namespace-policy = sammanhållet pass, inte punktfix. |
| Arkitektur (deploy-topologi/lane) | B3/E2, B1, B4, F4/F5, B7/#140 | Durable event-bus (multi-instans), S3-lane blockerande, canvas-PR-token, bus-emits/manifest-Zod, DB/Blob-gate-PR. |
| A7-2 kod-default | B05 | Kod-fix (scope till valda dossiers) mergad #211; kod-default OFF kvarstår som ditt val (env satt i Vercel). |
| CTA-knapp i init-fast-lane | M#1 (EGEN-05) | `simpleWebsitePath` blockar `CTA-knapp` från fast lane via bredare `cta`-match än follow-up-vocabulary. By-design: namngiven sektions-capability i init ska gå full dossier-pipeline (#242 Alt A, testat `simple-website-path.test.ts:134`). Skillnaden mot follow-up (styling-tweak) är medveten. |
| components/ui canonical-skydd (drop av LLM-emitterade shadcn-stem-filer) | #263 | Avsiktligt: `components/ui/<shadcn-stem>` behandlas som host-ägda canonical runtime-filer; drop sker bara när canonical ersättning finns. App-specifika exports i en canonical shadcn-path stöds ej → revisitas om det dyker upp i riktiga generationer. Codex P2 på PR #263. |
| Preview-retention (last-good, #345) rensas inte i efterhand — flyttad ur Aktiv kö 2026-07-22 | BB#345r7 (bugbot round 7) | Om en follow-up-version retainar föregående tier-2-frame och SENARE hård-failar utan egen `previewUrl` ligger gamla framen kvar (`didChangeVersion` one-shot). Status/chat visar failen sanningsenligt; smal repro (F2 typecheck-fail är advisory → klassen krympte). **Medvetet val** (raden var redan märkt "medvetet loggad i st.f. fixad"): undvik mer stateful komplexitet i retention-pathen. Ev. framtida effekt: rensa retained preview när aktiv version når terminal `failed` utan previewUrl (`useBuilderPageController.ts` preview-sync). |
| Deferred re-verify hoppas över av inflight | BB-265 (MEDIUM) | Stale-base-recovery i `repair/route.ts` schemalägger `triggerServerVerification` via `after()`; om `inflight` redan håller versionId returnerar den tidigt och callbacken no-op:ar medan raden står i `repairing`. Täcks av readiness-watchdogen (`failVersionVerificationIfUnleased`, lease-safe, targetar `repairing` när lease-tabellen finns) + den samtidiga körningen som äger versionen. Ingen ny race införd. Kommentar finns vid `after()`-siten. |
| Repair-loop pass 0 startar trots deadline | #286, M#cr4 | Avsiktligt: `pass > 0`-budgetguarden ger alltid ≥1 fixer-försök (`repair-loop.ts:569`). LLM-fixern har egen `AbortController(llmTimeoutMs)` men bounds inte av `repairDeadlineEpochMs`, så ett sent pass 0 kan löpa mot route-`maxDuration`. Final-gate-verifyn har separat budgetguard (`resolveFinalGateVerifyBudget`). Att låta pass 0 respektera deadline = risk att tappa "alltid ett försök"-garantin. |
| Dependabot auto-merge on-switch saknar sweep + protected-path-filter | Codex P2 pa #453 (2026-07-08, `.github/workflows/dependabot-safe-automerge.yml`) | Innan `DEPENDABOT_AUTOMERGE_ENABLED=true` ar sakert: (a) `--auto` kor ingen bot-fynd-sweep/settling → sena Codex/VADE-fynd kan auto-mergas otriagerade; (b) klassificeraren kollar bara patch + core-namn, sa en `github-actions`-patch (`actions/checkout`) som andrar `.github/workflows/*` passerar trots protected-path-policyn. Val: automatisera sweep-gate + lagg changed-files/protected-path-filter i workflow:et innan on-switchen anvands. Halls AV tills dess. |
| Master-ruleset required status checks — ATGARDAT 2026-07-08 | Repo-granskning 2026-07-08 | `quality`, `backoffice-tests`, `schema-drift` ar nu tillagda som required checks pa "Protect master" (icke-admin-PR kan inte merge:as med dem roda). Admin bypassar fortfarande allt, sa `--admin`-merge med rod CI ar fortsatt agent-disciplin. Kvar (separat val): satt `DEPENDABOT_AUTOMERGE_ENABLED=true` om dependabot ska auto-merga patch-PR:er. |
| CI-grindar som failar hårt men inte blockerar merge (required-status, forts. på raden ovan) | CI-genomgång 2026-07-29 (rulesetet `Protect master` + `gh pr checks` mot #644) | Fyra jobb failar hårt men står **inte** som required: det nya `build`, `preview-host-guards`, `dead-code` (orphan-filgrind) och `db-blob-sync`. Ett rött resultat i dem stoppas alltså bara av agent-/människodisciplin. Val per jobb, inte ett paketbeslut: `build` och `preview-host-guards` är deterministiska och nyckelfria → naturliga required-kandidater; `dead-code` blandar en blockerande orphan-grind med en rådgivande knip-rapport; `db-blob-sync` kör utan credentials på PR och är därför inte meningsfull som merge-grind i nuläget. Kräver rulesetändring, alltså ägaråtgärd — inte något ett kod-PR gör självt. Aktuell tabell: [`docs/testing.md`](docs/testing.md) → "Vilka CI-jobb blockerar faktiskt merge". |
| Warn-only-lanen har fyra tester ingen ser, och en engångsvakt lever kvar | CI-genomgång 2026-07-29 (`vitest.stability.config.ts` + `ci.yml`) | Två småval i samma område. (a) `stability`-jobbet är `continue-on-error` och innehåller sex `*.stability.test.ts` — men bara två (`followup-freeze`, `followup-capabilities`) hard-gate:as separat via `test:followup-contract`. De fyra övriga (`false-green-projection`, `status-resolver-single-writer`, `aao-invariant`, `refuse-dossier-stubs`) kan vara röda utan att någon märker det; "warn-only för alltid" är ingen signal. Val: befordra dem till blockerande (som followup-paret) eller radera dem. (b) `compat:v0-chat-boundary:check` är en engångsvakt för borttagna v0-chat-routes — låg återfallsrisk, permanent CI-kostnad. Val: behåll eller avveckla. Ingen av dem är en defekt idag. |
| Publikt repo exponerar admin/credit-mekanik + infra-id:n i docs — ÄGARVAL: PUBLIC 2026-07-18 | Repo-granskning 2026-07-08 → PUBLIC; omkontroll 2026-07-13 → PRIVATE; anonym GitHub API + `git ls-remote` 2026-07-18 → PUBLIC | Ägaren gjorde repot publikt igen. Det är inte i sig en kodbugg, men gör publik docs-hygien obligatorisk: inga secrets och sparsamt med admin-/credit-recept eller infra-id:n. Verifiera aktuell synlighet innan nya styrbeslut bygger på raden. |
| F2-demo-deploy utan placeholder-env (vit sida-risk) | Codex P1 på #443 (postad efter merge, triagerad 2026-07-09 → beslut, ej defekt) | En F2/design-version vars enda env-täckning är genererad `.env.local` deployas MEDVETET utan de värdena: `.env.local` strippas ur deploy-paketet (`route.ts` + `strip-env-local-for-zip.ts`) och Vercel får bara `projectEnv.configuredMap`; `envWarnings` säger uttryckligen "deployas utan riktiga värden tills du fyller i dem". Risk: en demo som kräver nycklarna i build/SSR kan ge vit sida. Val om beteendet ska ändras: injicera tier3-stub-värden för `placeholderCoveredKeys` i deploy-env-assembly, eller blocka F2-deploy när kod kräver nyckeln vid build. Ägare: `src/app/api/v0/deployments/route.ts` env-assembly. |
| Template-import env/drift (uppskjutet) | M#tmpl1 (audit 2026-07-08, `scripts/v0-templates/audit-template-repos.mjs`) | Importerade v0-mallar previewar verbatim och de flesta funkar; lågprio-val vi medvetet skjuter upp (ingen aktiv defekt): (1) preview-placeholders för de 11 nu **gömda** env-krasch-mallarna (modultopp, `EXCLUDED_TEMPLATE_IDS`) så de kan återinföras (nycklar t.ex. `FAL_KEY`, `NEON_DATABASE_URL`, `X_CLIENT_*`, `SUPABASE_SERVICE_ROLE_KEY`, `AI_GATEWAY_API_KEY`, QStash) i `config/ai_models/41-tier3-stub-placeholders.env.txt`; (2) bredare placeholders för ~19 "feature-döda" mallar (renderar men läser okuvrad server-nyckel lazy); ~~(3) verifiera om next/react-major-drift faktiskt bryter en mall vid första follow-up~~ ✅ ELIMINERAT 2026-07-23 (imported repo mode): follow-ups på `imported_repo`-chattar hoppar över `buildCompleteProject`/force-pins helt — mallens versioner + lockfil bevaras verbatim genom hela follow-up/verify/export-kedjan, och ingen scaffold auto-matchas/pinnas längre; se `docs/architecture/templates.md` §4. ~~(4) full borttagning av deprecerad legacy v0-auto-fetch~~ ✅ KLART 2026-07-08 (PR `chore/ta-bort-legacy-template-sync`): script, workflow, admin-route, `TEMPLATE_SYNC_*`-env och admin-UI raderade — Blob-vägen är enda katalogvägen. Kontext: `docs/architecture/templates.md`. |
| Preview-förvärmning (host-väckning + överlappad install, default OFF) | Codex P1 post-merge #480 + två `/granska`-härdningar (2026-07-11) | **Fixat:** explicit `prewarm`/`prewarmReplacementPending` döljer skelettet via HTTP och nekar alla WS; failed real replacement är stabil 503 utan refresh/auto-requeue, explicit real retry återgår till `starting`; ordinary booting behåller last-good och stale persisted `starting` återköas. Store-lås stoppar sent prewarm (`409`); idempotent recovery restartar bara missing/dead, inte healthy. `429` gör ingen auto-retry men släpper app-process-dedup för senare user-retry. Lease-HMAC kräver preview-host API-key + kanonisk user/trusted-IP-identitet; bootfel behåller cooldown, real claim/update/patch/destroy/cleanup/expiry/reset släpper, hibernate behåller. Fast kod-cap 4096 + admin count/expiry telemetry. **By design:** parallell andra chat får normal cold finalize (generation blockas inte); IP-rotation kan inte elimineras; `ip:unknown` är restriktivt; multi-machine-lock är framtida topologi. Tester: `test-prewarm-guards.mjs` + real HTTP/WS `test-preview-proxy-contract.mjs`. Flaggan förblir OFF; host-first deploy krävs. |
| Prod auto-migration parallellt med Vercel-deploy (race) | /granska-svärm 2026-07-11 (djävulens advokat + regression) | `prod-migrations-apply` väntar på `quality`+`schema-drift` medan Vercel bygger direkt på push → prod-kod kan bli live några minuter före schemat migrerats. **Medvetet val:** migrationerna är additiva (`ADD COLUMN IF NOT EXISTS`) så en parallell deploy är ofarlig (ny kod läser kolumner som strax finns; gammal kod bryr sig inte). Helt race-fritt kräver att Vercel-deployen gate:as bakom jobbet — egen infra-ändring, inte gjord nu. Ägare: `.github/workflows/ci.yml` + Vercel deploy-hook. |
| CI-verifiering `prod-migrations-applied` skippas vid röd `quality`/`schema-drift` | /granska-svärm 2026-07-11 (regression) | `needs: prod-migrations-apply` gör att ledger-checken **skippas** (inte körs) när apply-jobbet skippas pga failade gates. **Medvetet:** en trasig merge ska aldrig mutera prod-schemat, och master är redan röd via `quality`/`schema-drift` då — ingen separat migration-signal behövs. På nästa gröna push körs apply→applied normalt, så ingen bestående drift. |
| Capability-provenance (splittrat ägarskap) — flyttad ur Aktiv kö 2026-07-13 (coach-granskad) | BB#capsrc1 + #491/#492/#497 + extern coach (arkitektur) | **Ägarbeslut 2026-07-12 (etapp 7-upplåsning): accepterad som deferred.** Durable tombstone (#494→#497: merge + `buildFollowUpContract`) räcker; ORCHESTRATE-vägens rund-scopade removal (`orchestrate.ts:~1136/1590/1675/1157`) är mitigerad av strippad brief + tom-floor-auktoritet + #497:s subtraktion. Kanonisk `CapabilityIntentDelta`-refaktor tas som eget initiativ när området rörs — INGA fler capability-regexar/konsumentfall innan dess. |
| DATABASE_URL utan `integrationRegistry`-entry (parity-gap, ej false-green) — flyttad ur Aktiv kö 2026-07-13 | Codex P1→P2 på #445 | `ready:true` är **korrekt by design**: `DATABASE_URL` är feature-runtime + seed-fallback (samma mönster som ably/fal i #430, vars envs inte heller ligger i registryt). Val: ev. smal `postgres`-registry-entry + detektion MEN måste garantera feature-runtime-klassning i `deriveTier3BuildSpec` (naiv entry → falsk BLOCKER mot seed-fallback-beslutet). Ägare: `src/lib/integrations/registry.ts` + `detect-integrations.ts` + `tier3-build-spec.ts`. |
| CSP report-only-spam i console — flyttad ur Aktiv kö 2026-07-13, app-ytan åtgärdad 2026-07-25 | /logg-internet 2026-07-09 (#14) + ägarrapport 2026-07-25 (prod-console) | Report-only är fortsatt avsiktligt CSP-läge. `unsafe-eval`-bruset (×45) spårades 2026-07-25 till **Zod v4:s JIT** — `new Function` per objektschema, plus dess egen `Function("")`-probe — inte till preview-runtimen som raden först antog. Borta sedan `src/instrumentation-client.ts` sätter `z.config({ jitless: true })` i klienten; CSP:n behövde inte luckras upp. Kvar som medvetet val: report-only-läget självt, och nonce/allowlist för preview-dev-runtimen när CSP:n ändå revideras. |
| P11 orphan engine-chat save/restore-policy — flyttad ur Aktiv kö 2026-07-13 | M#p11b (extern post-merge-review R-P11) | `save`/`restore` tillåter `not_found` för olänkade chattar — rimligt idag. Öppen fråga är ett **policyval** (`allowed` vs `forbidden` när orphan saknar `project_id`), inte felbeteende. Lås valet + test när området rörs (`src/lib/tenant.ts` `resolveEngineChatOwnershipForRequest` + `save/route.ts`). |
| Admin-UI klient-gate (`localStorage["admin-auth"]`) vs server-allowlist — **ÅTGÄRDAT 2026-07-24** | PR #355-triage #33 | Klient-gaten och den egna inloggningsformen är borta. `/admin` har nu en server-gate i `src/app/admin/layout.tsx` (`getAdminUserForPage()` → samma `isAdminEmailEdge`-predikat som `requireAdminAccess` och proxy-gaten), så en inloggad admin kommer in direkt och en icke-admin redirectas av `src/proxy.ts`. `NEXT_PUBLIC_ADMIN_EMAILS` används bara för att visa/gömma genvägen i navbaren (`src/lib/auth/is-admin-client.ts`), aldrig som gate. |
| `/api/analytics` GET gate:ad på enbart `TEST_USER_EMAIL` — **ÅTGÄRDAT 2026-07-24** | Hittat vid /admin-renoveringen (manuell körning med admin-konto) | Statistikvyn gav `401` för varje riktigt admin-konto i `ADMIN_EMAILS`/`SUPERADMIN_EMAIL`, och gamla admin-UI:t tolkade 401 som utloggning. Routen använder nu `requireAdminAccess` som övriga admin-routes (predikatet inkluderar fortfarande `TEST_USER_EMAIL` → strikt superset, ingen åtkomst borttagen). |
| Bulkradering av Vercel-projekt i admin-UI kunde radera Sajtmaskins eget prod-projekt — **ÅTGÄRDAT 2026-07-24** | Hittat vid /admin-renoveringen (kodläsning + rutt-test) | `mega-cleanup` och `cleanup-vercel-projects` listade `api.vercel.com/v9/projects` och raderade **alla** projekt token såg, inklusive `VERCEL_PROJECT_ID`, bakom en två-klicks-knapp. `cleanup-vercel-projects` är pensionerad (410), `mega-cleanup` är nu en data-/cache-nollställning utan Vercel-anrop, och per-projekt-radering vägrar appens eget projekt (`src/lib/vercel/self-project-guard.ts`). Låst med tester. |
| Health-endpoints bevisar process-liveness, inte modellkapacitet | M#och1 (prod-felsökning 2026-07-24, Codex-kvoten utmattad) | Både `/api/openclaw/health` och gatewayens `/health` svarade 200/`"ok"` medan varje modell i fallback-kedjan var kvotslut och Sajtagenten inte kunde svara ett enda tecken — en falsk grön signal som kostade en hel felsökningssession. **Medvetet val:** en riktig modell-probe per health-anrop kostar pengar och kan själv slå i kvoten. I stället är fällan dokumenterad (`infra/openclaw/DEPLOY_INFO.txt` §6) och orsaken ytas nu i chatten i st.f. att sväljas (`src/lib/openclaw/gateway-response.ts`, PR #605). Revisit om en billig kapacitets-signal blir tillgänglig. Ägare: `src/lib/openclaw/status.ts`. |

Full detalj + alla `[x]`/avfärdade rader: [`backlog-arkiv-2026-07-02.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-07-02.md) · [`backlog-arkiv-2026-06-27.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-06-27.md) · [`backlog-arkiv-2026-06-24.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-06-24.md).

## Naming-debt: `v0ChatId`

Inte ett dött fält. Live DB-kolumn (`chats.v0_chat_id`, notNull/unique) + load-bearing konsument (`useBuilderVmPreview.ts` gatar VM-preview-bootstrap för legacy-mappade chattar). Full borttagning = tyst regression + bruten DB/payload-nyckel → kräver **migrationsplan** (byt internt symbolnamn, behåll DB/payload-kompat) per `docs/architecture/code-map.md`. Den säkra delmängden är **borttagen** 2026-06-28 (död `|| data.v0ChatId`-läsning i `useCreateChat.ts` + okonsumerade `v0ChatId`-svarsnycklar i `/api/projects/[id]/chat`); DB-kolumnen, `useBuilderVmPreview.ts`-gaten och interna DB-lookups behålls tills en migrationsplan finns.
