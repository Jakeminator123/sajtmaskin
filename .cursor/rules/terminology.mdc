---
description: Kärntermer, förväxlingar och signal-ownership
alwaysApply: true
---

# Terminology & signal ownership

**Kanonisk ordlista:** `docs/architecture/glossary.md`. **Ägarmatris + signalflöde:** `docs/architecture/llm-signal-flow.md`.

**Grundregel:** kod är source of truth. Ny term bara om befintlig inte räcker — kolla glossaryn först. Ny/omdöpt term → uppdatera glossaryn samma leverans. Konsolidera hellre än ny jargong.

## Signal-gate

Innan du lägger till en signal/ordlista/keyword-array: (1) finns canonical source redan? → ändra **ägaren**, inte konsumenten. (2) Ny signal → registrera i ägarmatrisen.

| Signal | Canonical source |
|--------|-----------------|
| Domän / site-type | `config/domain-rules.json` → `domain-inference.ts` |
| Prompt-heuristik | `config/prompt-heuristic-tokens.json` → `prompt-heuristics.ts` |
| Init-semantik | Deep Brief (`site-brief-generation.ts`) |
| Designregler | Core Rules (`config/prompt-core/`) + `buildDynamicContext` |
| Build intent | `BUILD_INTENT_GUIDANCE` i `system-prompt.ts` |

## Förväxlingstabell

| Säg | Inte |
|-----|------|
| `own-engine` | generatorn, v0-motorn |
| `preview` / `VM` / `preview_host` | sandbox (utom legacy) |
| `template-library` | "mallar" om du menar katalogen |
| `scaffolds` | mallar/templates (om runtime-startpunkter) |
| `scaffold variant` | `scaffold family`, `style direction` (borttaget) |
| `Deep Brief` | brief, StructuredBrief, PromptAssist-paraply |
| `Prompt Rewrite` / `Prompt Polish` | "Prompt Assist" som paraply |
| `previewUrl` | demoUrl (legacy) |
| `mekanisk fix` / **mekanisk autofix** | "autofix" blandat med LLM-fix |
| `LLM-fix` / **LLM-autofix** | "fixer", "syntax fixer" blandat |
| `backoffice/` | "dashboard" (legacy) |
| `Core Rules` | "prompt-static" (legacy) |

**`v0`:** (1) `/api/v0/` API-routes (2) naming debt i symboler (3) Mallar-tab. `v0-sdk`, `src/lib/v0/`, `V0_API_KEY` borta ur runtime.
