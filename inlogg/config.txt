POPUP_BROWSER_NORMAL_WINDOW=y
# Opens a clone Playwright tab to show QR if no popup appeared (fallback for scanning)
CLONE_TAB_FALLBACK=n
# Browser window diagnostics in skv6_session_log.txt (all open tabs, focus, URL snapshots)
TRACE_BROWSER_WINDOWS=y
TRACE_BROWSER_WINDOWS_INTERVAL=2
# After BankID loader disappears, wait and auto-scroll to expose "Next" area.
POST_AUTH_SCROLL_ON_LOADER_HIDDEN=y
POST_AUTH_SCROLL_DELAY_SECONDS=2
POST_AUTH_LOADER_SELECTOR=#loader
CLONE_QR_FROMPLAYWRIGHT_TO_SITE=y