# Bug-backlog — arkiv (2026-07-24)

> Flyttade rader från [`BUG-SWARM-BACKLOG.md`](../../../../BUG-SWARM-BACKLOG.md) efter bugfix-svepet 2026-07-24 (branch `fix/bugfix-svep-0724`, en commit per rotorsak, mot master `626f15e6`). Sex kodverifierade lågrisk-rader fixades; omtriage av rader i #593–#597-berörda områden (`sendMessage`-outcome, `version_busy`-UX, quick-edit-lease, autofix-kontext) bekräftade dem fortfarande öppna — inga ytterligare flyttar. Äldre historik: äldre snapshots jun–jul 22 i [`README.md`](README.md) (git-SHA).

## Fixade i bugfix-svepet 2026-07-24

| Klar | Status | Prio | Fynd | Källa | Fix-referens |
| --- | --- | --- | --- | --- | --- |
| [x] | Fixad | P2 | **Warm-cache-tsconfig bröt `@/*`-alias för genererade projekt:** `provision-warm-cache.ts` rå-kopierade repo-rotens `tsconfig.json` (`"@/*": ["./src/*"]`) till cache-dirren medan genererade projekt använder `"@/*": ["./*"]` → provisionerad cache + `SAJTMASKIN_PRE_VM_TYPECHECK`/F3-force gav bogus TS2307 för normala `@/components/ui/*`-importer och skickade ren kod in i repair-loopen. | Codex P2 på #354 | Commit `573c3d68` `fix(warm-cache)`: provision skriver cache-anpassad tsconfig (generated-project-baseline) i st.f. rå kopia; `check-warm-cache.mjs` asserterar aliaset så stale caches syns som COLD. Verifierad end-to-end: provision + smoke + tsc mot `@/`-import i cachen (exit 0). |
| [x] | Fixad | P2 | **Negations-fönstret tappade legitim positiv i blandade meningar:** `isTermFullyNegated` (fast 140-teckensfönster) applicerades per capability, så "no auth, add a map" / "lägg inte till auth, lägg till postgres" tystade map/postgres-signalen via det orelaterade auth-negationsfönstret. | Codex P2 på #592 (`follow-up-capability-detection.ts:314`) | Commit `c2f4daca` `fix(capability-detection)`: fönstret slutar nu vid klausulgräns — meningsslut, adversativt "men"/"but", eller komma följt av NY imperativ-sats; komma som fortsätter negerad substantivlista bryter INTE fönstret. 4 nya regressionstester (sv/en + list-kontroll); 149/149 detection-tester gröna. |
| [x] | Fixad | P3 | **F3-lineage kunde peka på annan version än build-basen:** direkt `/stream`-caller med ENDAST `parentVersionId` gate:ades/byggdes mot preferred men `codegen-turn.ts` persisterade rå `parsedMeta.parentVersionId` som lineage → `parent_version_id` kunde peka på en version som aldrig var byggbas. | Codex P2 på mergad #352 | Commit `004e0358` `fix(f3)`: `runF3ReadinessGate` returnerar sitt resolvade `gateVersionId` (`f3ResolvedBaseVersionId`) och codegen-turn persisterar lineage från samma resolution. Ny `f3-readiness-gate.test.ts` (4 tester) låser kontraktet. |
| [x] | Fixad | P3 | **`ably-realtime` hade svagast `mock: visual`-implementation:** dossiern skeppade ingen egen notis-/demo-komponent (till skillnad från stripe/clerk) — den ärliga "realtid ej kopplad"-ytan förlitade sig helt på att codegen-LLM:n följde prompthinten. | BB#dostax1 (bugbot-pass på feat/dossier-taxonomi 2026-07-22) | Commit `04665bbf` `fix(dossiers)`: ny verbatim `realtime-config-notice.tsx` (exposed `RealtimeConfigNotice`) probear auth-routen och renderar notisen deterministiskt på 503 `realtime-not-configured`; manifest/instructions uppdaterade, capability-map regenererad, `dossiers:validate-all` grön. |
| [x] | Fixad | P2 | **Custom env-blockers (icke-dossier-ägda `process.env.*`-nycklar) saknade editor-yta:** `LaunchReadinessCard` öppnade Byggblock-popovern för alla env-blockers men popovern renderade bara inputs för dossier-ägda nycklar — användaren kunde fastna med en deploy-/finalize-env-blocker som inte gick att åtgärda från buildern. | Codex P2 på #573 (`LaunchReadinessCard.tsx:65`) | Commit `353ceeac` `fix(builder)`: ny "Egna nycklar"-sektion i Inkopplade-fliken — oägda fokus-nycklar landar där med samma write-only-inputs mot samma POST `/env-vars`-API; manuell "lägg till egen nyckel" (UPPER_SNAKE-validering). 2 nya tester; 15/15 popover-tester gröna. |
| [x] | Fixad | P2 | **Verbatim verify saknade preview-`.env.local`-lagret:** `buildExportableProject({ verbatimRepo })` returnerade bara repo-filerna medan live-preview skriver runtime-`.env.local` — quality-gate/server-verify byggde i en annan miljö och en importerad mall som läste en placeholder-täckt nyckel vid build/modul-init kunde false-fail:a verify/repair fast previewen funkade. | Codex P2 på #594 (`build-exportable-project.ts:71-81`) | Commit `ee86a799` `fix(verify)`: verbatim-grenen speglar icke-verbatim-ägarskapsregeln — mall-egen `.env.local` förblir auktoritativ, annars skeppas samma placeholder-kuvert (inga riktiga värden); ZIP/deploy-gränserna strippar `.env.local` per path som förut. Uppdaterat passthrough-test + nytt mall-env-test. |

## Fixad i uppföljningen #603 (2026-07-24)

Raden nedan är **Clerk-delen** av den bredare dossier-SDK-raden som lades till i Aktiv kö när #600 mergades (Codex P2 triagerad som pre-existerande). #603 stängde bara den delen; de övriga 15 dossier-egna paketen har fortfarande samma lucka och ligger kvar som en restpost i Aktiv kö. Delningen gjordes efter Codex P1 på #606 — att arkivera hela den breda raden hade dolt de kvarvarande paketen och sänkt canvas-räknaren utan täckning.

| Klar | Status | Prio | Fynd | Källa | Fix-referens |
| --- | --- | --- | --- | --- | --- |
| [x] | Fixad | P3 | **Warm-cachen saknade `@clerk/nextjs` (+ `/server`-subpathen):** pre-VM-typechecken (`SAJTMASKIN_PRE_VM_TYPECHECK` / forcerad i F3) kör mot repots symlänkade `node_modules`, som inte innehåller dossier-egna SDK:er — genererade projekt med Clerk-auth fick TS2307 innan preview-VM:en installerat beroendet, och skickades i onödan in i repair. Gäller bara Clerk; övriga dossier-paket kvarstår (egen rad i Aktiv kö). | Codex P2 på #600 (2026-07-24, triagerad som pre-existerande) | PR #603 (`6820dc42`): provisioneringen kopierar `tests/stubs/clerk-nextjs{,-server}` till `<cache>/__sdk-stubs/` och aliasar båda i den cache-anpassade tsconfigen; `check-warm-cache.mjs` asserterar alias + stub-fil så stale caches syns som COLD. Stub-ytan täcker hela `CLERK_SERVER_IMPORTS` med lösa inerta typer (en smalare stub bytte bara TS2307 mot TS2305/TS2339), låst av `tests/stubs/clerk-nextjs-server.test.ts` som läser symbollistan direkt ur `ts2304-known-import-fixer`. Verifierad end-to-end: provision + smoke + tsc i cachen mot `getAuth`/`clerkClient`/`await auth()`/`(await currentUser())?.firstName` (exit 0). |
