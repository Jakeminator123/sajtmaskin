export default function BasicContactValues(props: Record<string, unknown>) {
  return (
    <div
      data-stub="@/components/basic-contact-values"
      style={{
        padding: "2rem",
        border: "2px dashed #666",
        borderRadius: "0.5rem",
        color: "#999",
        textAlign: "center",
        fontSize: "0.875rem",
      }}
    >
      [BasicContactValues]
    </div>
  );
}

export { BasicContactValues };
