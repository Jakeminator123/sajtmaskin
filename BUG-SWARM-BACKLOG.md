# Bug-backlog (konsoliderad)

**Enda aktiva bugglistan i repot.** Lokal markdown är källan till sanning — ingen Linear, ingen extern tracker. Preflight (`scripts/dev/check-bug-backlog.mjs`) och canvas-generatorn (`scripts/canvas/build-llm-flow-canvas.mjs`) läser **`## Aktiv kö`** nedan.

Avslutade rader flyttas till [`docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-07-02.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-07-02.md) (senaste), [`backlog-arkiv-2026-06-27.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-06-27.md) och [`backlog-arkiv-2026-06-24.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-06-24.md) (fryst historik). Grandmaster-svärmens B01–B15 = löst historik i [`docs/plans/avklarat/bug-swarm/README.md`](docs/plans/avklarat/bug-swarm/README.md).

## Hur den hålls sann

| Regel | Vad |
| --- | --- |
| **Aktiv kö = bara öppna defekter** | Rader där systemet gör fel. Alla rader är `[ ]`. När en rad fixas: **flytta** den till arkivfilen som `[x]` med fix-referens (commit/PR). Bocka inte av på plats. |
| **En sanning per rad** | Skriv aldrig "FIXAD" i prosan på en `[ ]`-rad. Det är en motsägelse och preflighten failar (se `check-bug-backlog.mjs`). |
| **Beslut ≠ bug** | Saker där systemet gör som tänkt men vi *kan* välja annorlunda (fail-open, default-off-flaggor, degraded-policy) hör hemma i "Beslut & policy", inte i Aktiv kö — de räknas inte som risk i canvasen. |
| **Kan ej verifieras statiskt** | Fynd som kräver repro/livekörning hör i "Behöver repro" tills någon kört dem. |
| **Inflöde** | Nya buggar: `/buggrapport` (skriver en `M#`-rad här) eller PR-review-gaten (`pr-merge-review-gate.mdc` → logga med fil-ankare). |

**Status-legend:** `[ ]` = öppen. Källa-taggar: `G#`/`U#` = gamla GPT/UI-svärmlistor, `N#` = defensiv triage, `R#` = äldre kodrapport, `B#` = grandmaster-svärm, `E#` = eval, `FEL#`/`MB#`/`BB#` = PR-review-fynd, `M#` = manuellt rapporterad.

## Aktiv kö

Verkliga öppna defekter. Detta är den enda listan att jobba ur. Canvasen visar dessa som öppna huvudrisker.

| Klar | Status | Prio | Fynd | Källa | Beslut / nästa steg |
| --- | --- | --- | --- | --- | --- |
| [ ] | Öppen kvalitet-risk | P2 | `postgres-drizzle`: `lib/db/schema.ts` är `rewritable` medan verbatim `lib/db/index.ts` importerar `./schema` — om modellen döper om/utelämnar schemat får bygget en obruten import (TS2307). | Codex P2 på #445 (2026-07-08) | Gör schemat till deterministisk emitted/restored-beroende av helpern (verbatim-policy-koppling), eller mjuka upp helper-importen. Ägare: `data/dossiers/hard/postgres-drizzle/` + `src/lib/gen/dossiers/verbatim-policy.ts`. |
| [ ] | Öppen bug | P2 | Follow-up "Ta bort Stripe/integration" stoppar dossier-reselection (PR #447) men (a) raderar inte befintliga dossier-filer i merge — `mergeVersionFilesWithWarnings` bevarar alltid föregående filer — och (b) removal-signalen når inte `inferCapabilities`/`buildCapabilityHints`/`inferPreGenerationContracts`, så payment-hints/contracts kan fortfarande byggas från råprompten. | /logg chat e298da50 + T15 scope-gap + Codex P1 på #447 (2026-07-08) | Tråda `removedCapabilities`/`removedDossierIds` genom contracts/hints + stream-meta → merge/post-merge delete (B3). Ägare: `orchestrate.ts` + `mergeGeneratedProjectFiles`. |
| [ ] | Öppen bug | P2 | `dashboard-charts` med `promptInstructionMode: compact` släpper client-only/avoid-regler i codegen-prompten → modellen kan bryta SSR/VChart-kontrakt. | Automat A#6 (#422) | Utöka compact-läget eller byt till full instructions; verifiera mot `data/dossiers/soft/dashboard-charts/manifest.json`. |
| [ ] | Öppen kvalitet-risk | P2 | `analytics` + `dashboard` dubbel-triggar capabilities utan veto i follow-up-vokabulären → båda kan aktiveras samtidigt trots överlapp. | Automat A#16 (#422) | Lägg capability-veto eller merge-regel i `follow-up-capability-vocabulary.ts`. |
| [ ] | Öppen kvalitet-risk | P2 | Dossier promotion i backoffice jämför inte `manifest.capability` mot plan — fel dossier kan promotas. | Automat A#14 (#419) | Validera capability-match i `backoffice/pages/dossiers.py:575-582`. |
| [ ] | Öppen bug | P3 | CLI `normalize-legacy-prospect` kraschar på trasig `prospects.json` (`undefined.filter`). | Automat A#15 (#419) | Guard + test i `scripts/dossiers/normalize-legacy-prospect.ts:439`. |
| [ ] | Öppen kvalitet-risk | P3 | `inventory-legacy.mjs` saknar npm-script trots planreferens; hårdkodad Windows-default. | Automat A#19–A#20 (#419) | Lägg script i `package.json` + plattformsoberoende default-path. |
| [ ] | Öppen kvalitet-risk | P3 | Thumbnail-route bumpar inte `updated_at` vid lyckad capture → projektlistan kan visa stale thumb. | Automat A#22 residual (#426, Codex P2 avfärdad som DB-trigger) | Utvärdera om explicit bump behövs utöver DB-trigger. |
| [ ] | Öppen kvalitet-risk | P3 | P11 cross-tenant: `save`-routen tillåter `not_found` för `chatId` (engine-chat utan `project_id` eller okänd) och sparar ändå. Rimligt för icke-länkade chattar, men en orphan engine-chat (saknar `project_id`) blir tvetydig — `forbidden` vs `not_found` kan inte skiljas av anroparen. | M#p11b (extern post-merge-review R-P11) | Lågprio hardening: bestäm explicit om orphan-chat (`project_id` saknas) ska vara `allowed` eller `forbidden` vid save/restore; lägg test som låser valet (`src/lib/tenant.ts` `resolveEngineChatOwnershipForRequest` + `save/route.ts`). |
| [ ] | Öppen bug | P2 | F3 build-plan härleds från `preGenerationContracts`, inte parent-versionens filer → tomt build-plan-block fast koden har integrationer | G#20 | Tråda fil-härledda integrationer in i F3-prompten (`system-prompt/sections/session-contracts.ts` vs `finalize-design/route.ts`). Signal-ägar-fix i orchestrate/contract-derivering. |
| [ ] | Öppen bug | P2 | Init och follow-up har olika capability-universum (drift). #242 sektions-capabilities nu trådade i follow-up (#250) + init fast-lane-gate (#253), men `capability-dossier-bridge` saknar fortfarande sektions-id:n | G#26 | Konsolidera capability-källan över init + follow-up + dossier-bridge + `orchestrate.ts` (signal-ägarmatris). Ihop med G#25. |
| [ ] | Öppen kvalitet-risk | P2 | Dossier/capability-threading svagt vissa paths (`capability-dossier-bridge.ts` → `orchestrate.ts`) | G#25 | Canonical-källa-konsolidering (samma spår som G#26). |
| [ ] | Öppen bug | P2 | `/finalize-design` detektionsfullständighet: `detect-integrations.ts`-regex kan missa en integration på filer som finns | G#21 | Konkret false-green (tomma filer → `409`) redan fixad; kvar = regex-täckning. `detect-integrations.ts`. |
| [ ] | Öppen bug | P3 | Plan-mode token-budget skalas mot tier-build-modellen, inte planner-fasen → planner-kontext skalas mot fel kontextfönster på anthropic-tier | MB-4 | OBS: nuvarande `engineModelId: resolveEngineModelId(resolvedModelTier)` är ett medvetet Bug 04#3-val (spegla codegens BuildSpec). Att blint sätta `engineModelId: planModel` regressar det. Riktig fix = frikoppla token-budget-modell från BuildSpec-modell. `create-chat-stream-post.ts:486-491` · `chat-message-stream-post.ts:754`. |
| [ ] | Öppen kvalitet-risk | P2 | Sektions-capabilities (`logo-cloud` … `stepper`): follow-up-delen täckt av #250 (vokabulär + tester i `follow-up-capability-vocabulary.ts:233-320`), men INIT saknar deterministisk detektor — `create-chat-stream-post.ts:653-696` sätter aldrig `requestedDossierCapabilities`, så init förlitar sig helt på att Deep Brief/LLM nominerar sektions-id:n. Fast-lane-gaten (#253, `simple-website-path.ts:199-200`) blockerar fast lane vid sektionsord men injicerar inga id:n → full pipeline kan köra utan rätt dossiers om briefen missar. | #242 (818-svärm 2026-07-02: follow-up-delen verifierad täckt, init-gap kvarstår) | Lägg deterministisk sektions-detektion på init-vägen (återanvänd follow-up-vokabulären) + tråda in i `capability-dossier-bridge.ts`. Samma spår som G#25/G#26. |
| [ ] | Öppen kvalitet-risk | P2 | OC_DEBUG: `buildDebugSystemPrompt()` lovar alltid full kod + fynd + repo-kontext, men kontext-bygget får `debug: debugOwned` som kräver BÅDE `chatId` och `activeVersionId`. Saknas versionsid (eller ownership-lookup faller) injiceras ingen filmanifest/fynd-block fast prompten säger att de finns. Ej säkerhet (mer restriktivt), bara prompt-vs-kontext-inkonsekvens. | BB#oc1 | Mjuka upp debug-systemprompten eller tillåt chatId-only-ägd full kod (`src/app/api/openclaw/chat/route.ts` debugOwned + `src/lib/openclaw/server-context.ts`). |
| [ ] | Öppen infra-risk | P2 | Preview-hosten (Fly `vm-fly-jakem`, single VM) har minnestryck: `npm install` OOM-killades 2× 2026-07-02 ~06:02–06:04Z, health-checks flappar, och verify-lanen blev onåbar ~06:53Z ("fetch failed" → chat 1c34592c v3 false-RED + v4 overifierad). Koden hanterar nu onåbar lane retrybart (repair-routen 503 + pending), men grundorsaken är kapacitet: samtidiga installs/verifies på en enda VM. | M#fly1 (prod-incident chat 1c34592c, Fly-loggar) | Infra-beslut: skala Fly-VM:ens minne, eller serialisera/köa `npm install`-jobb i preview-host (`preview-host/src/server.js`), eller separera verify-lane från preview-VM:en. Egen leverans — inte kodfix i huvudrepot. |
| [ ] | Öppen kvalitet-risk | P3 | Preview-retention (last-good, #345) rensas inte i efterhand: om en follow-up-version retainar föregående tier-2-frame och SENARE hård-failar utan att någonsin få egen `previewUrl` (preflight-block) ligger gamla framen kvar (`didChangeVersion` är one-shot; senare re-renders no-op:ar). Status/chat visar failen sanningsenligt; smal repro (F2 typecheck-fail är numera advisory, så klassen krympte). | BB#345r7 (bugbot round 7) | Ev. liten effekt som rensar retained preview när aktiv version når terminal `failed` utan previewUrl (`useBuilderPageController.ts` preview-sync). Medvetet loggad i st.f. fixad — undvik mer stateful komplexitet i retention-pathen. |
| [ ] | Öppen kvalitet-risk | P3 | Dossier-`configured`-flaggan läser PLATTFORMENS `process.env`, inte projektets env vars: `selectDossiersForRequest` → `isConfigured` (`src/lib/gen/dossiers/select.ts:32-40`) checkar `process.env[ev.key]` i Sajtmaskin-serverns egen miljö. Har plattformen själv t.ex. `RESEND_API_KEY`/`OPENAI_API_KEY` satta blir ALLA användarprojekts dossiers `[configured]` i codegen-prompten (fel signal → LLM:en kan skriva kod som antar riktiga nycklar); omvänt syns aldrig användarens ifyllda `projectEnvVars` → dossiern promptas `[UNCONFIGURED — render placeholder UI]` i F3 fast nycklarna är ifyllda i env-panelen. | M#doss1 (F3-email-gate-utredning 2026-07-02, kodverifierad) | Basera `configured` på projektets lagrade env-map (`getStoredProjectEnvVarMap`) i stället för server-`process.env`; kräver att projektkontext trådas in i dossier-selection (orchestrate-callsiten har chatId/projectId). Ägare: `src/lib/gen/dossiers/select.ts` + `orchestrate.ts`. |
| [ ] | Öppen kvalitet-risk | P3 | Quick-edit läser bas-filerna FÖRE lease: routen läser `baseFiles` och `runQuickEdit` applicerar ops på dem (`quick-edit/service.ts:79`) innan `acquireVersionLease` tas (`service.ts:99`). I fönstret läs→lease kan en accept-repair avancera basens `files_json`, så minorn skapas från en stale snapshot (konsistens-race; ej dataförlust — ny rad skapas). #351:s M#qe1-lease stänger inte ordningen. | M#qe3 (extern coach post-#351-review, kodverifierad) | Ta leasen före bas-fil-läsningen i routen, eller låt `runQuickEdit` re-läsa bas-snapshotet under lease och applicera ops på det. Ägare: `quick-edit/route.ts` + `quick-edit/service.ts`. |
| [ ] | Öppen kvalitet-risk | P3 | Klient-autofix får den M#rep1-FILTRERADE repair-kontexten när server-repair inte reparerar: `handleRepairOrAutofix` bygger `repair.qualityGate` av endast de tre kanoniska checkarna och skickar samma objekt till `onAutoFix` (`post-checks.ts:747-754`) — icke-kanoniska failade diagnostik-signaler (t.ex. install-relaterade) når aldrig LLM-fixerns kontext. Residual ur stängda PR #344; därifrån saknas också ett selection-regressiontest för fast-edit-valet (M#sel1-fixen i #351 shippade utan). | M#344r (residual ur stängd PR #344, kodverifierad) | Bevara icke-kanoniska diagnostics i ett separat kontextfält (inte `qualityGate`-arrayen som repair-routens zod validerar) + lägg selection-regressiontestet. Egen liten PR. |
| [ ] | Öppen bug | P2 | Warm-cache-tsconfig bryter `@/*`-alias för genererade projekt: `provision-warm-cache.ts` kopierar repo-rotens `tsconfig.json` (`"@/*": ["./src/*"]`) till cache-dirren, men genererade filer skrivs i cache-roten (`app/`, `components/`) och genererade projekt använder `"@/*": ["./*"]` (`project-scaffold.ts`) → provisionerad cache + `SAJTMASKIN_PRE_VM_TYPECHECK`/F3-force ger bogus TS2307 för normala `@/components/ui/*`-importer och skickar ren kod in i repair-loopen. Latent även pre-#354 för `landing-page`; #354 utökade exponeringen till alla 9 scaffolds (fortfarande enbart opt-in dev — prod/Vercel har kall cache → fail-open skip). | Codex P2 på #354 (kodverifierad av merging agent) | Skriv en cache-anpassad tsconfig vid provisionering (`"@/*": ["./*"]` + exkludera repo-specifika includes) i st.f. rå kopia; alternativt skriv genererade filer under `src/`. Ägare: `scripts/provision-warm-cache.ts` (+ smoke-checken bör då verifiera alias-resolution med en minimal `@/`-import). |
| [ ] | Öppen kvalitet-risk | P2 | Status-bussen nollställs inte när `updateVersionFiles(..., {invalidateVerification:true})` resettar en tidigare promotad/passerad version till draft/pending (user-edit via PUT/PATCH/DELETE `/files`): `reconcileTerminalDbState` låter terminal bus-`done` vinna över DB-`pending` (bara DB-`failed` degraderar) → builder-badges kan fortsatt visa grönt för redigerat, overifierat innehåll. Deploy-gaten läser DB:n, men master blockar idag bara `verification_state === "failed"` vid deploy (delad deploy-block-gate = egen rad nedan, PR #355-triage #10/#16; #355 stängd 2026-07-08). | Codex P1 på mergad #352 (otriagerad; kodverifierad mot `chat-repository-pg.ts:519` + `stale-verification.ts:112`) | Emittera/nollställ en bus-event vid invalidateVerification, eller lär projektionen honorera DB draft+pending över stale terminal-`done` för edited versions. Ägare: `src/lib/db/chat-repository-pg.ts` + `src/lib/gen/verify/stale-verification.ts`. |
| [ ] | Öppen kvalitet-risk | P3 | Tre otriagerade Codex-P2 från #353:s sista review-runda (kom ~19 s före merge): (1) retrybar 409/5xx från `/quality-gate` i resume-lanen kör `mutateVersions`-revalidering före `!res.ok`-grenen → retry-beslut på stale data (`useResumePendingVerification.ts`); (2) `tier3-readiness-gate.ts` inspekterar bara senaste 200 error-log-rader för `product_postcheck.summary` — postcheck skriver en rad per produktvarning så summary-raden kan skjutas utanför fönstret; (3) resume-selektorn behandlar alla `editKind: null` draft/pending-designrader som resumebara generationer utan positiv provenance. | Codex P2×3 på mergad #353 (otriagerade; svepta av merging agent 2026-07-02) | Triageras i egen liten PR: (2) query:a summary-raden direkt per kategori; (3) kräv generation-provenance-fält; (1) flytta revalideringen efter res.ok-grenen. |
| [ ] | Öppen kvalitet-risk | P3 | preview-host saknar testharness: `preview-host/` har ingen test-/specfil för runtime-livscykeln (boot/verify-köer, readiness, reaper). #357 la riktade guard-tester (`npm run test:guards` — timeout-settle, idle-sweep no-op, socket-räkning) men kö-serialiseringens ordningsgarantier och reaper-integrationen (stop→hibernated→re-boot) är fortfarande otestade — regressions i boot-ordning syns först på Fly. | Codex P1 på #357 (delvis åtgärdad med guard-tester; harness-gapet kvarstår) | Bygg minimal harness: extrahera kö-/reaper-logik till testbara enheter eller kör `server.js` mot mockad store/child-spawner i CI. Ägare: `preview-host/scripts/test-runtime-guards.mjs` som bas. |
| [ ] | Öppen kvalitet-risk | P2 | Stale quality-gate-telemetri överlever `invalidateVerification`: `updateVersionFiles(..., {invalidateVerification:true})` resettar DB-raden till draft/pending men rensar/superseder INTE senaste `generation_telemetry.quality_gate_result`-raden. Promote-guarden (`promoteVersion`/`acceptRepair`) läser senaste telemetrin: stale `passed` (från före editen) kan släppa igenom en direkt promote av redigerat, overifierat innehåll (false-green-riktningen); stale `verifier_failed` blockerar tills ny gate körs (för-strikt-riktningen). Syskonfynd till status-bus-raden ovan — samma ofullständiga reset. | Codex P2 på mergad #352 (otriagerad; kodverifierad mot `chat-repository-pg.ts:519-526` av merging agent 2026-07-02) | Skriv en superseding telemetrirad (eller nollställ signalen) i `invalidateVerification`-grenen så promote-guarden ser "ingen gate körd för detta innehåll". Ägare: `src/lib/db/chat-repository-pg.ts` + promote-guard-läsningen. |
| [ ] | Öppen kvalitet-risk | P3 | F3-lineage kan peka på annan version än build-basen: när en direkt `/stream`-caller skickar ENDAST `parentVersionId` (utan `engineBaseVersionId`) gate:ar och bygger M#f3g1-fixen mot preferred-versionen, men `parsedMeta.parentVersionId` persisteras fortfarande som lineage → versionsradens parent kan peka på en version som aldrig var byggbas (mismatch-409:an täcker bara fallet där BÅDA skickas och skiljer sig). Ren datakonsistens — gaten/bygget är korrekta. | Codex P2 på mergad #352 (otriagerad; kodverifierad mot `chat-message-stream-post.ts:406-438,1376` av merging agent 2026-07-02) | Persistera lineage från samma resolution som build-basen (gateVersionId) i st.f. rå `parsedMeta.parentVersionId` när `engineBaseVersionId` saknas. Ägare: `src/lib/api/engine/chats/chat-message-stream-post.ts`. |
| [ ] | Öppen infra-risk | P2 | **DB-pool-svält i prod under generering** (live-session 2026-07-03, chat `69aae3d5`): klustrade `timeout exceeded when trying to connect` på `/api/auth/me`, `/login`, `/google/callback`, `/analytics`, `/preview-session` (02:15–02:16). Prod kör Supabase transaction-pooler (`pooler.supabase.com:6543`) → `max: 3` per serverless-instans (`client.ts:153`, `POSTGRES_POOL_MAX` ej satt), connect-timeout 10s. Builderns polling (6+ samtidiga GET/s) + långlivade stream-anrop svälter poolen; row-lock-blockerade skrivningar förvärrade (de är nu åtgärdade — se arkiv 2026-07-03). | Vercel runtime-logg 2026-07-03 (ägarrapporterad) + kodverifierad pool-config | Infra-beslut (ägare): mät samtidiga sessions i Supabase-dashboarden under en generering, sedan ETT av: (a) höj `POSTGRES_POOL_MAX` försiktigt (risk: pooler-cap → EMAXCONNSESSION, se SAJ-7/B1), (b) route:a långlivade vägar via `POSTGRES_URL_NON_POOLING`, (c) höj Supabase-tier/pooler-cap. Ändra inte blint — koden varnar redan för capen. |
| [ ] | Öppen kvalitet-risk | P3 | **iframe kan studsa mellan versioner efter restore** ("dubbel"-känslan): efter en verifier_failed init-version + restore finns två versioner; `usePreviewSession` `version_mismatch` auto-resync (#362) kan forced-restart:a previewn mellan gamla (failade) versionens VM-session och den restore:ade versionen. Sanningsenligt i status, men visuellt förvirrande. | M#pv3 (prod-incident chat 3120c05c) | Undertryck auto-resync-studs när den aktiva versionen är terminalt `failed` utan egen `previewUrl`; föredra den restore:ade versionens session. **Status 2026-07-03:** implementerat i branch `fix/stabilisering-vag3-restore-readiness` + test i `src/app/builder/usePreviewSession.test.ts` (väntar merge). |
| [ ] | Öppen kvalitet-risk | P3 | **quality-gate-routens error-log-skrivningar saknar `lockTimeoutMs`**: `/error-log`-routen fick bunden rad-lås-väntan (branch `fix/error-log-lock-timeout-preview-throttle`), men `quality-gate/route.ts`:s egna `createEngineVersionErrorLogs(...)`-anrop (superseded/gate-summary/promote-guard-loggar) kör fortfarande utan `lockTimeoutMs` → blockerar till globala `statement_timeout` vid lease-kontention innan `.catch` degraderar till warning (långsamt, ej 500). | M#el2 (syskonfynd till /error-log-fixen 2026-07-03) | Skicka `{ lockTimeoutMs }` även i quality-gate-routens error-log-anrop. Ägare: `src/app/api/engine/chats/[chatId]/quality-gate/route.ts`. |
| [ ] | Öppen kvalitet-risk | P3 | **Runtime-ready-kvittot binds till versionId, inte till innehållsrevisionen VM:en servar:** `recordPreviewRuntimeOutcomeForVersion` stämplar alltid versionens SENASTE `generation_telemetry`-rad. Repro-scenario: (1) version v1 bootar och servas av preview-sessionen; (2) server-repair passerar sin gate → `recordRepairPassedQualityGate` skapar en NY telemetri-rad (preflight_passed, `preview_success=null`) för det REPARERADE innehållet som aldrig bootats; (3) nästa `/preview-status`-poll får `running:true` från den gamla sessionen (samma versionId, host servar fortfarande pre-repair-filerna) → stämpeln träffar den nya raden → `preview_success=true` för innehåll som aldrig körts. SQL-guarden (`IS DISTINCT FROM true`) hindrar det INTE — raden är `null` och `null→true` är en tillåten övergång; per-instans-cachen mildrar bara på samma instans. **Cache-aspekt (Codex P2 runda 4, samma rotorsak — versionId ≠ innehållsrevision):** confirmed-`true`-cachen i `generation-telemetry.ts` (`confirmedPreviewReadyVersionIds`) invalideras inte när repair/autofix finalize:ar OM samma versionId (`targetVersionId`-rewrite skapar ny telemetri-rad för samma version) — cachen kortsluter då stämplingen av den NYA raden på den instansen: ett startfel eller riktigt heartbeat-kvitto för det omskrivna innehållet hoppar över skrivningen och nya raden förblir pending. Felriktningen är konservativ (pending, aldrig false-green). | M#pv4 (Codex P2 på PR #377 runda 2 + runda 4 cache-aspekt, kodverifierad) | Bind kvittot till innehållsrevisionen: låt preview-sessionen bära telemetri-rad-id/`files_json`-hash och stämpla bara rader vars innehåll matchar det VM:en bootat; samma bindning löser cache-nyckeln (nyckla cachen på telemetri-rad-id/innehållsrevision i st.f. versionId, eller invalidera vid same-version-rewrite). Ägare: `src/lib/db/services/generation-telemetry.ts` (`recordPreviewRuntimeOutcomeForVersion` + `confirmedPreviewReadyVersionIds`) + `src/lib/gen/preview/session-store.ts`. |
| [ ] | Öppen kvalitet-risk | P3 | **F3-marker bär inte env-only-förslagsdetaljer:** när en no-code F3-runda bara bestod av `requestEnvVar` (eller en `suggestIntegration` med omappbar provider, t.ex. `"other"`) bär den persisterade markern `suggestedProviders` men INTE de efterfrågade env-nycklarna, och tool-/integrationskortet persisteras inte i `engineChat.messages`. Approval-rundan får då tom `approvedProviders`, prompten hänvisar till chathistoriken (där detaljerna saknas) och integrationsverktygen är avstängda → modellen bygger från generisk approval-prompt utan den föreslagna nyckelns namn. Konservativ riktning: fallback-instruktionen tvingar ändå byggbar kod med lazy-guard-mönstret; modellen definierar eget nyckelnamn som sedan syns via detect-integrations på den byggda koden. | Codex P2 på PR #383 (runda 2, 2026-07-04; nedgraderad till P3 — smal kant: env-only-förslag utan mappbar provider) | Persistera `requestedEnvKeys` i F3-markern (från rundans env-signaler) och inkludera dem i approval-promptens intro; alternativt behåll `requestEnvVar` i approval-rundans tool-set. Ägare: `src/lib/gen/stream/f3-continuation.ts` (marker-payload) + `chat-message-stream-post.ts` (approval-prompten). |
| [ ] | Öppen kvalitet-risk | P2 | Deploy-API och readiness-UI räknar blockerare olika: deploy blockar bara `verification_state === "failed"` + saknar delad `resolveDeployBlock`, och deploy-env-gaten får inte full F3-kontext (`lifecycleStage`/`selectedDossiers`/`allowPlaceholdersInF3`) → en version kan visa deploy-ready men få 409 vid deploy, eller tvärtom. (Product-postcheck-som-blocker är separat medvetet val, se Beslut & policy.) | PR #355-triage #10/#16 (kodverifierad 2026-07-08, `deployments/route.ts:439-447,506-509` + `readiness/route.ts:70-147,320-327`) | Inför en delad deploy-block/env-resolver som både deploy-API och readiness-UI läser. |
| [ ] | Öppen kvalitet-risk | P2 | Scaffold-matchern gate:ar embedding-override via ad-hoc keyword-heuristik (`canUseEmbeddingOverride`), inte via manifestens `allowedBuildIntents` → en scaffold vars manifest förbjuder aktuell build-intent (t.ex. dashboard/app-shell för website) kan väljas. | PR #355-triage #24 (kodverifierad 2026-07-08, `src/lib/gen/scaffolds/matcher.ts:555-592,749-782`) | Lägg en manifest-auktoritativ `scaffoldAllowsIntent`-gate som slutfilter på vald scaffold (keyword + embedding), fallback till generic vid krock. Core-path + stort testyta → egen PR med matcher.test-uppdateringar. |
| [ ] | Öppen bug | P2 | MCP generate kastar när preview-start fallerar i st.f. att returnera den sparade versionen/filerna med en `previewStartFailed`-flagga → en färdig generation kastas bort pga preview-fail. | PR #355-triage #40 (kodverifierad 2026-07-08, `src/lib/own-engine/generate-site-from-prompt.ts:275-292` + `src/lib/mcp/generate-site.ts:65-106`) | Returnera partial-success (sparad version + `previewStartFailed`) i MCP-vägen. |
| [ ] | Öppen kvalitet-risk | P2 | Fresh-entry chat-reset avbryter inte en pågående generation-stream (`cancelActiveGeneration` anropas inte) → en gammal stream kan fortsätta skriva efter reset. | PR #355-triage #7 (kodverifierad 2026-07-08, `src/app/builder/useBuilderPageController.ts:714-737` + `src/lib/hooks/chat/useChatMessaging.ts:102-114`) | Anropa `cancelActiveGeneration()` först i reset-vägen. Rör stream/abort-maskineriet → egen liten PR med noggrann test. |
| [ ] | Öppen kvalitet-risk | P3 | `selectPreferredEngineVersion` faller tillbaka till nyaste `failed`-versionen när ALLA versioner är failed (i st.f. `undefined`) → en failed version kan behandlas som preferred/aktuell. | PR #355-triage #41 (kodverifierad 2026-07-08, `src/lib/db/engine-version-lifecycle.ts:184-195` + `src/lib/db/chat-repository-pg.ts:476-478`) | Returnera `undefined` när alla failed; verifiera först att alla callers (getPreferredVersion → null) är null-safe. |
| [ ] | Öppen kvalitet-risk | P3 | `/messages`-nätverksfallbacken återanvänder inte 409 `stale_base_version`-reload-UX:en som stream-vägen har → stale-base-edit via fallback ger generiskt fel i st.f. reload-toast. | PR #355-triage #20 (kodverifierad 2026-07-08, `src/lib/api/engine/chats/chat-message-stream-post.ts:408-434` + `src/lib/hooks/chat/useSendMessage.ts:340-358`) | Dela `handleStaleBaseVersion` mellan stream- och /messages-fallback-vägen. |
| [ ] | Öppen kvalitet-risk | P3 | Env-panelens blockeringsräkning (`totalIssues` = klient-heuristiken `likelyRequiredEnvKeys`) speglar inte serverns auktoritativa `buildBlockingKeys` från readiness/finalize → badge-count kan avvika från faktisk finalize-gate. | PR #355-triage #31 (kodverifierad 2026-07-08, `src/components/builder/ProjectEnvVarsPanel.tsx:651-745` + `src/lib/project-env-resolver.ts:206-217`) | Hämta serverns `buildBlockingKeys` och basera räkningen på den (ny fetch/state i panelen). |
| [ ] | Öppen kvalitet-risk | P3 | Admin-UI:t gate:ar chrome på `localStorage["admin-auth"]` + `NEXT_PUBLIC_ADMIN_EMAILS`, medan server-API:erna gate:ar på `requireAdminAccess` (server-allowlist) → klient-gate kan avvika. Kosmetiskt: ingen dataåtkomst öppnas, servern nekar ändå. | PR #355-triage #33 (kodverifierad 2026-07-08, `src/app/admin/page.tsx:271-353` + `src/lib/auth/admin.ts`) | Härled admin-status från riktig session (t.ex. `isAdmin`-fält i `/api/auth/me` via server-`isAdminEmail`) i st.f. localStorage-flaggan. |
| [ ] | Öppen kvalitet-risk | P3 | tier-2-preview kan rensa `iframeLoading` på rå iframe-`onLoad` (boot-sida ger HTTP 200) och det finns ett fönster efter `preview-ready` där boot-sida kan visas utan overlay → visuell false-ready. (Lifecycle "live" styrs dock av URL + `previewPending`, inte onLoad.) | PR #355-triage #1 (kodverifierad 2026-07-08, `src/components/builder/preview-panel/hooks/usePreviewIframe.ts:172-204`) | Gate:a tier-2-klar-signalen mot host-readiness/DOM-probe som shim-previewn redan gör. |
| [ ] | Öppen kvalitet-risk | P3 | Preview-hostens `/status` exponerar bara `running` (process-liveness), ingen separat content-`ready`-signal; resume/preview-status kräver `running` → en fortfarande-kompilerande boot kan behandlas som live. (PR #355:s egen `waitForReady→ready:true`-regression finns INTE i master.) | PR #355-triage #3 (kodverifierad 2026-07-08, `preview-host/src/server.js:408-410` + `preview-host/src/runtime.js` + `src/lib/gen/preview/preview-host-client.ts:170`) | Exponera content-`ready` (process alive + readiness-probe) skilt från `running` och kräv det i resume-vägen. Egen leverans (preview-host). |
| [ ] | Öppen kvalitet-risk | P3 | Loopia auto-DNS kräver `OK` på både apex-A och www-CNAME; Loopia kan returnera icke-`OK` när en post redan finns → en re-link kan visa manuella instruktioner i onödan fast DNS är korrekt (godartat: posterna skapas ändå, instruktionerna är korrekta). | Bugbot på PR #395 (`src/app/api/domains/link/route.ts:86-108`) | Behandla "post finns redan"-svaret som success (kräver Loopias exakta dubblett-svarskod från `src/lib/loopia/loopia-client.ts:189`). |
| [ ] | Öppen kvalitet-risk | P2 | **Template-galleriet: 11 klickbara mallar dör på boot av env-läsning (crash-on-load)** — de läser icke-täckta server-secrets på modultopp i render-vägen (t.ex. `jSYDGSkRBFX` XAI_API_KEY; `hkRpZoLOrJC`/`hO33zwNmsQF` SESSION_SECRET/SUPABASE_SERVICE_ROLE_KEY/VERCEL_OAUTH_*; `Ln5i4EVw63N` X_CLIENT_*/JWT_SECRET; `a9HJA3gXB4w` LANGCACHE_*; `350IE4ojQXK` NEON_AUTH_*; `GB2Aln8GBDP` NEON_DATABASE_URL; `7YygHkO1oj4` MUX_*; `kz69KY1ux5H`, `t02Cx1KfaCS`, `xbwwZ9kD745`) → nyckeln är `undefined` i F2-previewens `.env.local` → `next dev` kraschar. Ytterligare 6 klickbara har crash-on-route (500 vid API-träff). Full lista: kör `node scripts/v0-templates/audit-template-repos.mjs` (sektion "ENV PLACEMENT"). | Blob-audit 2026-07-08 (alla 313 mallar, kodverifierad mot `env-local.ts`-täckningen) | Beslut per mall: (a) utöka stub-lagren i `config/ai_models/4x-*.env.txt` med harmless placeholders för de vanligaste nycklarna, (b) exkludera värstingarna via `EXCLUDED_TEMPLATE_IDS` (`template-data.ts`), eller (c) Normalize-läger som lazy-ar top-level-läsningar. Motion-lockstep-klassen (18 mallar) är åtgärdad via Normalize-injektionen (`normalize-imported-package-json.ts`). |
| [ ] | Öppen kvalitet-risk | P2 | Databas-dossiers saknar `integrationRegistry`-entry för generisk Postgres/Neon (`DATABASE_URL`) — till skillnad från mongodb (`MONGODB_URI`). Ett Postgres/Neon-DB-projekts `DATABASE_URL` surfas via custom-env + dossier-enforcement-overlay (feature-runtime, icke-blockerande), men får ingen setup-guide/parity i F3-build-plan-blocket som mongodb. `ready:true` är korrekt (feature-runtime + seed-fallback by design; samma mönster som ably/fal i #430 vars envs inte heller ligger i registryt). | Codex P1 på #445 (reklassad P2 — ej false-green: DATABASE_URL är feature-runtime by design och sajten degraderar till seed-data) | Överväg smal `postgres`-registry-entry + detektion (`@neondatabase/serverless`, `drizzle-orm/node-postgres`) MEN garantera feature-runtime-klassning i `deriveTier3BuildSpec` — en naiv entry riskerar en falsk BLOCKER på `DATABASE_URL` (mot seed-fallback-beslutet). Ägare: `src/lib/integrations/registry.ts` + `detect-integrations.ts` + `tier3-build-spec.ts`. |
| [ ] | Öppen bug | P3 | `database`-vokabulärens konkurrent-veto (`prisma\|mongoose\|supabase\|firebase\|…`) tystar hela `database`-capabilityn även när konkurrenten är NEGERAD ("lägg till postgres, inte prisma" / "use postgres not firebase") → ingen dossier väljs trots explicit positiv postgres-ask. | Codex P2 på #445 (kodverifierad, `follow-up-capability-vocabulary.ts` database-veto) | Gör konkurrent-veton negations-medveten (hoppa veto när termen föregås av inte/utan/no/without) — cross-cutting mot `prompt-negation.ts`; hellre holistiskt än ad-hoc. Ägare: `src/lib/builder/follow-up-capability-vocabulary.ts` + `prompt-negation.ts`. |
| [ ] | Öppen bug | P2 | Spegelfallet till raden ovan: en NEGERAD DB-provider i en annars orelaterad follow-up ("lägg till auth utan postgres", "add a contact form, no postgres") matchar ändå `database`-vokabulären positivt → detektorn emittar `database` och en F3-runda kan injicera default-Postgres-dossiern trots explicit negativ. | Codex P2 på #445 (`src/lib/builder/prompt-negation.ts:48` — provider-termer medvetet exkluderade ur negations-detektionen) | Filtrera capability-match per positiv/negerad provider-förekomst (behåll "mongodb, inte postgres"-fallet som fungerar via sibling-keywords). Samma cross-cutting negations-arbete som raden ovan — ta båda i ett svep. |
| [ ] | Öppen kvalitet-risk | P2 | Dep-backfill för syskon-dossiers tappar provider-identitet: autofix-backstoppen `resolveCapabilityDependencies` (`src/lib/gen/autofix/dep-completer.ts:220`, anropad från `pipeline.ts:1281` med bara `requestedCapabilities`) replay:ar capabilityn utan promptText → default-Postgres-dossierns deps. Om modellen utelämnar en verbatim-fil kan `applyDossierVerbatimPolicy` återställa t.ex. mongo-helpern EFTER dep-completern → projektet får `import "mongodb"` men bara Postgres-fallback-deps → install/typecheck-fail. | Codex P2 på #445 (kodverifierad kedja) | Driv dependency-backfill från `selectedDossierIds` (finalize-version-runnern trådar redan explicit dossier-ids med capability-fallback, se `runner-dossier-threading.test.ts`) i stället för capability-replay när syskon-dossiers finns. Ägare: `dep-completer.ts` + `autofix/pipeline.ts` context. |

## Behöver repro

Fynd som inte kan avgöras statiskt. Flytta till Aktiv kö när repro finns, eller till arkivet om de avfärdas.

| Fynd | Källa | Repro-krav |
| --- | --- | --- |
| F3 auto-kick `onF3Ready` kringgår stale-base-409-gaten | B12 | Parallell F2-follow-up + F3-send mot samma chatId → förvänta 409. `chat-message-stream-post.ts` + `PreviewPanelF3Trigger.tsx` + `useSendMessage.ts`. |
| clear-redesign delta-brief tappas vid contract-gate-retry (tur 2) | B13 | clear-redesign + contract-gate returnerar → tur 2 ska ha delta-brief. `chat-message-stream-post.ts` + `follow-up-orchestration-input.ts`. |
| `arcade-with-klarna` failar med merge-syntax | E#1, R#10 | `npm run eval:weird-smoke:dump` mot LLM-providers (nycklar + nät + kostnad). |
| Scaffold required files kan tappas i finalize/export-path | R#9 | Deterministisk preflight/export-verifiering när repro finns. |
| Font materializer träffar mest baseline Inter (variant→font-parning) | G#53 | Eval-verifiering mot faktisk output. |
| Element crop missar små element vid DPI/zoom | G#70, U#52 | Inspector-worker (Playwright DPI/zoom). |
| Media local fallback `/api/uploads/media` kanske ej nås av preview-VM | U#29 | Verifiering mot preview-VM-kontext. |
| Analytics före cookie-consent (integritet) | U#56 | App-bred audit av var analytics initieras + gate på consent-flagga. |
| `previewUrlHint` base path + chatId | U#77 | Jämförelse mot live preview-host (täcks delvis av tester). |
| Socket connection lost flera ggr/min vid generering av användarsida | M#cr3 | Network HAR/SSE-logg från live-session: vilken endpoint tappar (`/stream`, `/versions`, `/readiness`, `/quality-gate`, `/repair` eller Next/Vercel HMR-socket)? #284/#286 sänkte route-timeouts men ingen direkt SSE/poll-reconnect-fix hittad. |
| `promoteGuardUnavailable`-rad kan false-red:as av stale-verification-watchdog (B08 follow-up): indeterminate-grenen lämnar versionen `verifying` + en passerande `preflight:quality-gate`-logg, och readiness-watchdogen (`created_at` + ~`STALE_VERIFICATION_TIMEOUT_MS`) kan köra `failVersionVerificationIfUnleased` → terminal-fail av en VM-ren version efter transient telemetri-läsfel om klienten inte retri:ar i tid. Det undergräver "retrybar fail-closed"-designen. | BB#299 (Bugbot) | Repro: tvinga telemetri-läsfel i `/quality-gate` med VM-pass, vänta > timeout utan klient-retry → watchdog ska INTE terminal-faila en `promoteGuardUnavailable`-rad. `quality-gate/route.ts` (indeterminate-gren) + `readiness/route.ts` (watchdog) + `chat-repository-pg.ts` (`failVersionVerificationIfUnleased`). |

## Beslut & policy (uppskjutet — ej aktiva buggar)

Systemet gör som tänkt; "fixen" är ett produkt-/arkitektur-**val** som medvetet skjutits upp. Räknas **inte** som risk i canvasen. Princip (Område 7): *systemet får misslyckas, men aldrig ljuga* — placeholder/stub/saknad hard-dossier får aldrig signalera success.

| Kluster | Källor | Val / blockare |
| --- | --- | --- |
| Degraded/placeholder-policy (placeholder = degraded, aldrig grön) | N#1, G#17, G#22, G#35, G#49, G#51, U#72 | Warning vs blocker per lane = gate-policybeslut; kan flippa version-status röd. |
| F2/F3 runtime-gate (Product Postcheck) | G#10, N#4, N#H3, R#6 | Default-off + fail-open idag; produktifiering till blockerande = pipeline-beslut. |
| Simplified-brief fallback sänker premium/3D | G#13 | Degraded-mode-signal = kvalitetspolicy (påverkar brief-compose + capability-threading). |
| Verifier-scope + recurring findings | G#33, N#5 | Hela filer vs snippets = verifier-arkitektur + token-budget; recurring-findings = nytt prompt-steg. |
| Verify-skip / cold cache | G#31, G#32 | Warm typecheck dokumenterat fail-open (`cache_cold`); cold-blockerande = infra-beslut. |
| Follow-up kvalitet/budget | G#57, N#3 | Jämka follow-up-gate mot init + hård regression-gate i CI = process-beslut. |
| Env-precedence / docs / refaktor | G#16, G#18, G#19 | `process.env`-refaktor (~100 filer), env-docs canonical-källa, `.env.local`-precedence = egna pass. |
| Säkerhet (deprio — eget pass) | G#40 (residual), #196, #197, B07, B-GA | TOCTOU/DNS-rebinding (IP-pinning), inspect spoof (nonce), publik media GET, OAuth-loggning. App-side SSRF redan fixad #196. |
| Quality-gate fail-open | B08, M#pg1 | **Härdat i två steg:** #299 gjorde `/quality-gate`-routen fail-closed-men-retrybar vid guard-/telemetri-läsfel (`onReadError:"indeterminate"` → ingen promotion, ingen terminal `failed`), och 2026-07-02 (branch `fix/bug-swarm-batch-0702`) fick även kanoniska `promoteVersion`/`acceptRepair` samma `onReadError:"indeterminate"` — läs-exception promotar aldrig. **Kvarvarande medvetet val:** no-telemetri-rader (`signal===null`) behåller fail-open överallt (back-compat för template-import/rollback/legacy). |
| Clerk-dossierns middleware fail-open utan riktiga nycklar | M#oc2 (residual, #351), extern coach post-#351-review "P1-B" | #351:s key-gate:ade `clerk-auth`-middleware returnerar `NextResponse.next()` för ALLA routes (även `/dashboard`, `/api/protected` osv.) när nycklar saknas/är placeholder — medvetet val så F2-preview renderar hela sajten i stället för 500/vit sida. Trade-off: deployar användaren utanför den gate:ade F3-vägen med placeholder-nycklar blir "skyddade" sidor tyst publika. Coach-rekommendation: fail-closed (redirect/503) för protected routes när Clerk är okonfigurerad. Beslut uppskjutet — fail-closed skulle blocka:a member-sidor i F2-preview, och F3/deploy-vägen kräver redan riktiga nycklar via tier3-readiness-gaten. Revisit om export/deploy-utanför-F3 blir en riktig väg. Ägare: `data/dossiers/hard/clerk-auth/components/middleware.ts`. |
| Write-on-read i `/files`-GET (arkitektur) | M#files1 (residual) | Bägge persist-grenarna (heal + materialize) är nu fail-fast/best-effort (`lockTimeoutMs:2000` + try/catch) så läsvägen aldrig 429/500:ar på låskonflikt. Kvarvarande **arkitekturval**: flytta persist från GET till skrivvägen helt — eget pass, ingen aktiv defekt. |
| Draft-version utan parent-lease (vanlig generation) | M#qe1 (residual) | Quick-edit tar nu lease på basversionen (`quick_edit`-kind). Den generella `addAssistantMessageAndCreateDraftVersion`-vägen (vanlig generation/follow-up) tar medvetet INGEN parent-lease — den skapar alltid en ny rad från stream-output och muterar inte basen; stale-base-gaten (5-2) täcker klientsidan. |
| Scaffold/variant/font-tuning | G#50, G#52, G#54, G#66 | Scaffold-defaulttext, pre-match vs embedding, Geist-workaround, adaptiv `MAX_PAGES` = heuristik-/produkttuning. |
| Produkt-/UX-gap | U#10, U#23, U#42, U#47, U#49, G#71 | Optimistic conflict, fler transcribe-språk, scraper-prioritet, OpenClaw 180k-context, D-ID session, PDF print-flöde. |
| Logg-/observability-/storage-städ | G#59, G#60, G#63, U#53, U#63, U#66 | Bred städ med sampling-/namespace-policy = sammanhållet pass, inte punktfix. |
| Arkitektur (deploy-topologi/lane) | B3/E2, B1, B4, F4/F5, B7/#140 | Durable event-bus (multi-instans), S3-lane blockerande, canvas-PR-token, bus-emits/manifest-Zod, DB/Blob-gate-PR. |
| A7-2 kod-default | B05 | Kod-fix (scope till valda dossiers) mergad #211; kod-default OFF kvarstår som ditt val (env satt i Vercel). |
| CTA-knapp i init-fast-lane | M#1 (EGEN-05) | `simpleWebsitePath` blockar `CTA-knapp` från fast lane via bredare `cta`-match än follow-up-vocabulary. By-design: namngiven sektions-capability i init ska gå full dossier-pipeline (#242 Alt A, testat `simple-website-path.test.ts:134`). Skillnaden mot follow-up (styling-tweak) är medveten. |
| components/ui canonical-skydd (drop av LLM-emitterade shadcn-stem-filer) | #263 | Avsiktligt: `components/ui/<shadcn-stem>` behandlas som host-ägda canonical runtime-filer; drop sker bara när canonical ersättning finns. App-specifika exports i en canonical shadcn-path stöds ej → revisitas om det dyker upp i riktiga generationer. Codex P2 på PR #263. |
| Deferred re-verify hoppas över av inflight | BB-265 (MEDIUM) | Stale-base-recovery i `repair/route.ts` schemalägger `triggerServerVerification` via `after()`; om `inflight` redan håller versionId returnerar den tidigt och callbacken no-op:ar medan raden står i `repairing`. Täcks av readiness-watchdogen (`failVersionVerificationIfUnleased`, lease-safe, targetar `repairing` när lease-tabellen finns) + den samtidiga körningen som äger versionen. Ingen ny race införd. Kommentar finns vid `after()`-siten. |
| Repair-loop pass 0 startar trots deadline | #286, M#cr4 | Avsiktligt: `pass > 0`-budgetguarden ger alltid ≥1 fixer-försök (`repair-loop.ts:569`). LLM-fixern har egen `AbortController(llmTimeoutMs)` men bounds inte av `repairDeadlineEpochMs`, så ett sent pass 0 kan löpa mot route-`maxDuration`. Final-gate-verifyn har separat budgetguard (`resolveFinalGateVerifyBudget`). Att låta pass 0 respektera deadline = risk att tappa "alltid ett försök"-garantin. |
| Dependabot auto-merge on-switch saknar sweep + protected-path-filter | Codex P2 pa #453 (2026-07-08, `.github/workflows/dependabot-safe-automerge.yml`) | Innan `DEPENDABOT_AUTOMERGE_ENABLED=true` ar sakert: (a) `--auto` kor ingen bot-fynd-sweep/settling → sena Codex/VADE-fynd kan auto-mergas otriagerade; (b) klassificeraren kollar bara patch + core-namn, sa en `github-actions`-patch (`actions/checkout`) som andrar `.github/workflows/*` passerar trots protected-path-policyn. Val: automatisera sweep-gate + lagg changed-files/protected-path-filter i workflow:et innan on-switchen anvands. Halls AV tills dess. |
| Master-ruleset required status checks — ATGARDAT 2026-07-08 | Repo-granskning 2026-07-08 | `quality`, `backoffice-tests`, `schema-drift` ar nu tillagda som required checks pa "Protect master" (icke-admin-PR kan inte merge:as med dem roda). Admin bypassar fortfarande allt, sa `--admin`-merge med rod CI ar fortsatt agent-disciplin. Kvar (separat val): satt `DEPENDABOT_AUTOMERGE_ENABLED=true` om dependabot ska auto-merga patch-PR:er. |
| Publikt repo exponerar admin/credit-mekanik + infra-id:n i docs | Repo-granskning 2026-07-08 (`gh repo view` → PUBLIC) | Publik `AGENTS.md` beskriver i klartext admin-bootstrap-vägen, credit-grant och infra-identifierare (exakta värden/steg utelämnas medvetet här — kopiera inte receptet till ännu en publik fil). Ej trivialt exploaterbart (kräver env-kontroll) men onödig information disclosure. Val (ägare): privat repo, eller banta detaljerna i committade docs och lägg specifika i en privat not. |
| Template-import env/drift (uppskjutet) | M#tmpl1 (audit 2026-07-08, `scripts/v0-templates/audit-template-repos.mjs`) | Importerade v0-mallar previewar verbatim och de flesta funkar; lågprio-val vi medvetet skjuter upp (ingen aktiv defekt): (1) preview-placeholders för de 11 nu **gömda** env-krasch-mallarna (modultopp, `EXCLUDED_TEMPLATE_IDS`) så de kan återinföras (nycklar t.ex. `FAL_KEY`, `NEON_DATABASE_URL`, `X_CLIENT_*`, `SUPABASE_SERVICE_ROLE_KEY`, `AI_GATEWAY_API_KEY`, QStash) i `config/ai_models/41-tier3-stub-placeholders.env.txt`; (2) bredare placeholders för ~19 "feature-döda" mallar (renderar men läser okuvrad server-nyckel lazy); (3) verifiera om next/react-major-drift faktiskt bryter en mall vid första follow-up (kräver repro — bara `next`/`react`/`react-dom`/`lucide-react` force-pinnas, tailwind behålls). ~~(4) full borttagning av deprecerad legacy v0-auto-fetch~~ ✅ KLART 2026-07-08 (PR `chore/ta-bort-legacy-template-sync`): script, workflow, admin-route, `TEMPLATE_SYNC_*`-env och admin-UI raderade — Blob-vägen är enda katalogvägen. Kontext: `docs/architecture/templates.md`. |

Full detalj + alla `[x]`/avfärdade rader: [`backlog-arkiv-2026-07-02.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-07-02.md) · [`backlog-arkiv-2026-06-27.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-06-27.md) · [`backlog-arkiv-2026-06-24.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-06-24.md).

## Naming-debt: `v0ChatId`

Inte ett dött fält. Live DB-kolumn (`chats.v0_chat_id`, notNull/unique) + load-bearing konsument (`useBuilderVmPreview.ts` gatar VM-preview-bootstrap för legacy-mappade chattar). Full borttagning = tyst regression + bruten DB/payload-nyckel → kräver **migrationsplan** (byt internt symbolnamn, behåll DB/payload-kompat) per `docs/architecture/code-map.md`. Den säkra delmängden är **borttagen** 2026-06-28 (död `|| data.v0ChatId`-läsning i `useCreateChat.ts` + okonsumerade `v0ChatId`-svarsnycklar i `/api/projects/[id]/chat`); DB-kolumnen, `useBuilderVmPreview.ts`-gaten och interna DB-lookups behålls tills en migrationsplan finns.
