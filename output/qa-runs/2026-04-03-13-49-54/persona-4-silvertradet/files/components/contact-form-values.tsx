export default function ContactFormValues(props: Record<string, unknown>) {
  return (
    <div
      data-stub="@/components/contact-form-values"
      style={{
        padding: "2rem",
        border: "2px dashed #666",
        borderRadius: "0.5rem",
        color: "#999",
        textAlign: "center",
        fontSize: "0.875rem",
      }}
    >
      [ContactFormValues]
    </div>
  );
}

export { ContactFormValues };
