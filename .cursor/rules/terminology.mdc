---
description: Kanoniska begrepp som agenter ofta blandar ihop
alwaysApply: true
---

# Terminology

Kort ordlista för de begrepp som oftast blir fel. För repo-karta och pipelines: `docs/architecture/repository-and-platform.md` och `docs/architecture/repo-tree.md`.

## `v0` betyder tre olika saker

1. **API-versionering**: Sajtmaskins egna routes under `/api/v0/`. Det har inget med leverantören v0 att göra.
2. **Naming debt**: gamla symbolnamn eller payload-nycklar från tidigare v0-baserad codegen.
3. **Builderns Mallar-tab / mallgalleri**: användarvänd startyta med data synkad från `v0.app` via `src/lib/templates/`. Detta är **inte** runtime `template-library`, **inte** Vercel template research och **inte** scaffold.

`v0-sdk`, `src/lib/v0/` och `V0_API_KEY` är borttagna ur runtime. **own-engine** är den enda aktiva codegen-vägen.

### Namnge det som faktiskt händer

- Säg **own-engine** när du menar den aktiva generationmotorn.
- Säg **`/api/v0/`-route** eller **API-versionering** när du menar egna serverroutes.
- Säg **builderns Mallar-tab**, **mallgalleri** eller **mallkatalog** när du menar den användarvända katalogen i `src/lib/templates/`.
- Säg **`template-library`** när du menar runtime referensbiblioteket i `src/lib/gen/template-library`.
- Säg **Vercel template research** eller **`data/external-template-pipeline`** när du menar research-/importspåret.
- Säg **inte** "v0-provider", "v0-engine", "v0-generation" eller liknande om nuvarande runtime.
- Säg helst inte bara **"template"** eller **"mall"** utan förtydligande i tekniska resonemang; välj den specifika ytan ovan.
- Hitta inte på nya produktnamn för samma sak. Om du är osäker: använd repoets befintliga ord `own-engine`, `preview_host`, `VM`, `template-library`, `preview`. Använd `sandbox` när du faktiskt menar legacy-namn, delat tier-2-kontrakt eller Vercel Sandbox-fallback.

## Mall, preview, sandbox

- **Builderns Mallar-tab / mallgalleri**: den användarvända katalogen i `src/lib/templates/`, med kategorier och startpunkter synkade från `v0.app`. Inte samma sak som `template-library`, Vercel template research eller scaffold.
- **`template-library`**: internt runtime referensbibliotek i `src/lib/gen/template-library` som används för prompt guidance och retrieval. Inte samma sak som builderns Mallar-tab.
- **Vercel-mall / external-template research**: research-material i `data/external-template-pipeline/`. Inte samma sak som builderns Mallar-tab eller runtime `template-library`.
- **Preview**: det användaren ser i buildern.
- **VM / `preview_host`**: den aktuella primära tier-2-live-previewen via Fly.io. **Detta är riktningen framåt.** All ny preview-logik ska använda `preview_host` / VM-terminologi.
- **Sandbox (LEGACY — undvik i ny kod)**: äldre paraplyterm som lever kvar i DB-kolumner (`sandbox_url`), API-routes (`/sandbox-preview`, `/sandbox-status`, `/sandbox-heartbeat`, `/sandbox-destroy`), Redis-nycklar, TypeScript-typer och SSE-fält (`sandboxPending`, `sandboxUrl`, `sandboxId`). Dessa är **strukturella** och kan inte döpas om utan migration — men ny kod, kommentarer, loggar och docs ska använda **preview** / **VM** / **tier-2** istället. Skriv aldrig "sandbox" i ny kod utom när du refererar till dessa legacy-namn.
- **Vercel Sandbox (korrekt användning)**: `@vercel/sandbox` kan fortfarande förekomma i explicit Vercel-fallback för tier-2 preview, men **inte** som quality gate. Quality gate / server-verify kör nu via preview-hosts separata verify-lane.
- **demoUrl**: legacy-namn för URL eller preview-mål; tolka efter kontext.

## Projekt, chat och preview-ID

| Term | Kanonisk betydelse | Vanliga ställen | Blanda inte ihop med |
|------|---------------------|-----------------|----------------------|
| **`appProjectId` / `projects.id`** | Sajtmaskins riktiga projekt-id per användarprojekt | builder-state, ägarskap, project env vars, `engine_chats.project_id` | `chatId`, `VERCEL_PROJECT_ID` |
| **`chatId` / `engine_chats.id`** | own-engine-chattens id; den faktiska preview-lanen för iterationer | `/api/engine/chats/[chatId]`, versionsval, tier-2 session-store, preview-host path | `appProjectId` |
| **`versionId` / `engine_versions.id`** | specifik sparad version inom en chatt | preview-resume, bootstrap, heartbeat, versions-API | `chatId`, `sandboxId` |
| **`sandboxId`** | id för aktiv tier-2-runtime/session | `sandbox-status`, `sandbox-heartbeat`, destroy/resume | `chatId`, `appProjectId` |
| **`VERCEL_PROJECT_ID`** | Vercel-projekt-id för Vercel Sandbox / API-auth | env, `runtime-url.ts` | Sajtmaskins `appProjectId` |

- **Preview-host-pathen är `/{chatId}`**, inte `/{appProjectId}`.
- Om du ser preview-host-payload eller äldre kommentar med **`projectId`** för denna path/key: läs det som **legacy alias för `chatId`**, inte som det riktiga app-projekt-id:t.

## AI Gateway (LEGACY — rör dig bort)

- **Vercel AI Gateway** (`https://ai-gateway.vercel.sh/v1`) är **borttagen ur runtime**. Huvudkedjan och sido-routes använder **direkt OpenAI/Anthropic API** via `createDirectModel()` eller officiella SDK-klienter.
- **`AI_GATEWAY_API_KEY`** ska inte längre finnas i ny kod, ny docs eller ny env-policy.
- **Ny kod ska aldrig använda AI Gateway.** Använd `OPENAI_API_KEY` / `ANTHROPIC_API_KEY`.
- **`VERCEL_OIDC_TOKEN`** är nu endast relevant för Vercel-plattformsflöden som faktiskt använder Vercel-auth, inte för LLM-routes.

## Fidelity

- **1**: enklare eller statisk preview (kan också vara legacy).
- **2**: (VI JOBBAR I 90% AV FALLEN MOT DETTA) körbar live-preview via `preview_host` / VM, med vissa legacy `sandbox`-namn kvar i kod och DB. Detta är den previewnivå projektet i allmänhet strävar efter att ge användaren.
- **3**: byggbar och deploybar produktionstro version.

`docs/architecture/preview-deploy.md` är kanonisk källa för den faktiska preview-kedjan.

## Env: flera lager (blanda inte ihop)

1. **Sajtmaskin-plattformen (detta repo)**
   - Filer i **repo-roten**: `.env.local`, `.env.production`, `.env.*` — **gitignorade** och ofta **`.cursorignore`** (agenter ser dem inte i index; öppna med `@` / Read om användaren delar).
   - **Vercel Environment Variables** för den deployade Sajtmaskin-appen. Tre Vercel-targets: **development** (= `vercel dev`), **preview** (PR-deploys), **production** (live). I praktiken: **development = lokal dev** (via `.env.local` + `npm run dev`), **production = Vercel** (deployer från `master`), **preview = speglar production** (samma env-värden; sällan använt). Sätt preview-variabler identiskt med production.
   - **Kanon för *namn* som appen läser:** `src/lib/env.ts` (Zod `serverSchema`). **Klassificering / audit:** `config/env-policy.json`. Människotext: `docs/ENV.md`.

2. **Användarens genererade sajt (Next-projekt i sandbox eller efter export)**
   - En **egen** `.env.local` (eller motsvarande) **inne i sandlådans / byggprojektets filträd** — inte samma fil som i Sajtmaskin-roten.
   - Byggs/mergas i kod (`src/lib/gen/sandbox-env-local.ts`): placeholders från `config/ai_models/`, ev. `project_data.meta.projectEnvVars`, m.m.
   - När någon säger **"användarens .env"**, **"kundprojektets env"** eller **"genererad sajts nycklar"** → mena detta lager, inte Sajtmaskins `.env.local`.

3. **Driftordning**
   - Felsök **preview/sandbox**: börja med plattforms-env (`VERCEL_OIDC_TOKEN`, m.m.) och `docs/architecture/preview-deploy.md`.
   - Felsök **"saknas API_KEY i byggd sajt"**: användarprojektets env / deploy-precheck, inte automatiskt Sajtmaskins rot-env.

Agenter: säg explicit vilket lager du menar om det kan misstolkas.

## own-engine, `src/lib/gen/`, `src/lib/mcp/`

- **own-engine**: produktens kanoniska genereringsbana.
- **`src/lib/gen/`**: delad genereringskärna och verktyg som own-engine bygger på.
- **`src/lib/mcp/`**: adapterlager för att använda befintliga flöden programmässigt; inte en separat motor.

## OpenClaw / Sajtagenten vs builderns LLM-flöde

- **OpenClaw / Sajtagenten**: separat assistent-/agentyta för råd, tips, textfältsförslag, avatar-bridge och löpande hjälp. Vanliga ytor: `src/components/openclaw/*`, `/api/openclaw/*`, den isolerade `D-ID`-pilotytan `/avatar` och `/api/did/chat`.
- **Builderns `LLM`-flöde / `LLM`-pipeline**: samlingsnamn för de övriga AI-stegen runt själva byggandet, t.ex. prompt assist, deep brief, spec-first, own-engine generation, verifiering, polish och repair.
- **Blanda inte ihop dem**: OpenClaw bygger normalt **inte** sajten. own-engine och övriga builder-steg är **inte** samma sak som Sajtagenten, även om de kan referera till samma builderkontext.
- **Djup granskning på begäran**: Sajtagenten kan få mer builder-/kodinsyn för review, felsökning eller förbättringsförslag, men den publika ytan förblir samma assistent. Säg inte att en separat publik agent används om repot inte faktiskt exponerar den.
- När du skriver docs eller förklaringar: säg **OpenClaw / Sajtagenten** för assistentytan, och säg **builderns `LLM`-flöde** eller den specifika fasen när du menar generationskedjan.

## LLM-lager i buildern

- **Prompt assist / Förbättra**: hjälpassistent **före build**. Förbättrar eller skriver om användarens prompt men bygger inte sajten.
- **Prompt polish / `Skriv om`**: den lätta copy-editor-varianten. Den ska främst rätta språk, tydlighet och form, inte skapa en full brief eller ersätta codegen.
- **Post-generation polish pass**: separat LLM-pass **efter** codegen som arbetar på genererade filer, inte på användarens prompt. Blanda inte ihop detta med prompt-polish.
- **Deep brief / site brief**: strukturerad brief före första builden (`sidor`, `sektioner`, `tone`, `SEO`, `imagery`, m.m.). Genereras via `/api/ai/brief` (klient) eller `tryGenerateServerAutoBrief` (server-fallback). Briefen genereras alltid från **originalprompt**, inte summarerad text. Server auto-brief blockeras för follow-ups.
- **Spec-first chain / `WebsiteSpec`**: valfri kedja (`specMode=true` default) som konverterar brief → strukturerad spec (`promptAssistContext.ts`). Specen bifogas systemprompten som `sajtmaskin.spec.json`-referens. Separat från deep brief — spec *bygger på* briefen.
- **`orchestratePromptMessage()`**: deterministisk promptvakt/budgetering — **inte** en LLM. Den väljer `direct`, `preserved` (full handoff över mjuk gräns), `phase_plan_build_polish`, eller sällan `summarize` som nödfallback vid hård cap.
- **Verifier-pass (post-generation)**: read-only LLM-granskning **efter** syntax i finalize; returnerar bl.a. `polishCandidates` (målfiler). Styrs av `phaseRouting.verifier` och `postGenerationPasses` i manifest.
- **`resolveOrchestrationBase()` / `BuildSpec`**: den faktiska orkestreringen före codegen. Här väljs scaffold, route plan, kontrakt, context-policy, preview-policy och verification-policy.
- **Plan mode**: separat planner-LLM som returnerar plan-/JSON-artifact, **inte kod**.
- **own-engine generator**: huvud-LLM:n som faktiskt genererar kod.
- **`thinking`**: resonemangs-/reasoning-flagga på generatorbanan, inte en separat modellbana.
- **Repair / fixer**: eftergenereringskedjor för fel. Deterministisk autofix loopas (`DETERMINISTIC_AUTOFIX_MAX_PASSES`, manifest-styrt), sedan eskalering till LLM-fixer. Fixer-modellen matchar generatorns tier via `resolvePhaseModel(tier, "fixer")`. Alla reparationsparametrar styrs via `repairPolicies` i `manifest.json`.
- **Prompt wizard**: 5-stegs wizard med AI-drivna adaptiva följdfrågor via `/api/wizard/enrich`. Producerar en rik prompt som sedan kan gå genom deep brief → orchestrering → generation. Inte "12 frågor" — antalet varierar dynamiskt.
- **Audit / analyze-routes**: separata analysbanor utanför own-engine-build. Blanda inte ihop dem med builderns codegen-flöde.

## Integrationer och källor

- Provider- och integrationsnamn ska följa repoets faktiska källor, främst `sajtmaskin.integration-manifest.json` om den finns, pre-generation contracts, `config/env-policy.json`, `src/lib/env.ts` och `docs/ENV.md`.
- Hitta inte på nya providernamn i copy eller frågor när projektets befintliga konfiguration redan visar vad som gäller.

## Aliaser

Varicell / Vericell → **Vercel**. Veenol / Venol → **v0**.

## Överlappande ord och när den här filen uppdateras

Samma vardagsord (**preview**, **mall**, **template**, **v0**, …) förekommer ofta i **flera olika betydelser** i repo och produkt. Den här ordlistan kan inte fånga allt; den ska bara täcka det som **faktiskt** brukar bli fel.

- **Uppdatera sällan men ibland:** lägg till eller skärp en rad här när samma begreppsförvirring **återkommer** (t.ex. två agenter eller två issues blandar ihop samma par). Undvik att växa listan för varje engångsfråga eller för varje nytt internnamn i kod.
- **Varför:** andra agenter ska veta att överlapp finns och var de ska leta — inte att varje ny synonym måste in här direkt.
- **Var:** håll tillägg korta; koppla till befintlig sektion (t.ex. mall/preview) hellre än nya långa stycken.
