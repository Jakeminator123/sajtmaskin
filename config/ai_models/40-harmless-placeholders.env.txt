# Generated user-site preview: HARMLESS placeholder environment variables
#
# Single source of KEY=value lines that are SAFE to leave as fake values
# even in F3 ("bygg integrationer"). Test/publishable keys, generic
# secrets that just need a non-empty value, and analytics IDs.
#
# Tier-3 stub placeholders live in the sibling file
# `41-tier3-stub-placeholders.env.txt` and are stripped from F3 merges so
# the project can require real values via `tier3-build-spec.ts`.
#
# Source-of-truth set: src/lib/integrations/placeholder-harmless.ts
# Read at runtime via src/lib/ai-models/load-generated-site-placeholders.ts

# Stripe — publishable key is designed to be exposed in the browser; test key is fine.
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_placeholder_000000000000000000000000

# Clerk — publishable key only.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_placeholder

# Analytics / public IDs
NEXT_PUBLIC_GA_ID=G-PLACEHOLDER000
NEXT_PUBLIC_GTM_ID=GTM-PLACEHOLDER
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=preview.local
NEXT_PUBLIC_POSTHOG_KEY=phc_placeholder_preview
NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# Sentry DSN — public, harmless if events go to a dummy project.
# SENTRY_DSN is the integrationRegistry key; NEXT_PUBLIC_SENTRY_DSN is what the
# sentry-error-tracking dossier's init shims actually read (client-visible DSN).
SENTRY_DSN=https://placeholder@example.ingest.sentry.io/0
NEXT_PUBLIC_SENTRY_DSN=https://placeholder@example.ingest.sentry.io/0

# Auth.js / NextAuth-style — any 32-char string works for AUTH_SECRET.
AUTH_SECRET=preview_auth_secret_min_32_chars_long_dummy_value
NEXTAUTH_URL=http://localhost:3000

# Auth0 — base URLs and AUTH0_SECRET are harmless; client/issuer secrets are tier-3.
AUTH0_SECRET=placeholder_auth0_secret_long_enough_for_preview
AUTH0_BASE_URL=http://localhost:3000

# CMS / search — public read-only keys are harmless.
NEXT_PUBLIC_SANITY_PROJECT_ID=placeholder_project_id
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_STORYBLOK_ACCESS_TOKEN=placeholder_storyblok_public_token
NEXT_PUBLIC_ALGOLIA_APPLICATION_ID=PLACEHOLDER_APP_ID
NEXT_PUBLIC_ALGOLIA_SEARCH_API_KEY=placeholder_search_only_key
NEXT_PUBLIC_MEILISEARCH_HOST=https://placeholder.meili.local
NEXT_PUBLIC_MEILISEARCH_SEARCH_API_KEY=placeholder_search_only_key
NEXT_PUBLIC_TYPESENSE_HOST=https://placeholder.typesense.local
NEXT_PUBLIC_TYPESENSE_SEARCH_ONLY_API_KEY=placeholder_search_only_key
NEXT_PUBLIC_ELASTICSEARCH_NODE_URL=https://placeholder.es.local
NEXT_PUBLIC_ELASTICSEARCH_SEARCH_API_KEY=placeholder_search_only_key

# Misc URLs — local-only base URLs.
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_BASE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000
BASE_URL=http://localhost:3000

# Generic non-vendor secrets — any non-empty value satisfies these.
HEADLESS_SECRET=preview_headless_secret_minimum_length_dummy
INSERT_RANDOM_SECRET_KEY=preview_insert_random_secret_minimum_length_dummy
PAYLOAD_SECRET=preview_payload_secret_minimum_length_dummy
CRON_SECRET=preview_cron_secret_dummy
