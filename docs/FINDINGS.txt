Tidsbunden sessionslogg — inte kanonisk dokumentation.
Flytta viktiga slutsatser till PROJECT-STATE-AND-DIRECTION.md eller relevanta
arkitekturdokument; rensa eller arkivera denna fil periodiskt.

---

2026-03-27 (konsoliderad — tidigare poster arkiverade i git-historik)

Nuvarande tillstand:

Own-engine ar enda produktionsbanan. V0 SDK, src/lib/v0/, V0_API_KEY borttagna.
"/api/v0/"-prefix ar HTTP API versioning, inte V0 Platform — se terminology.mdc.

Exportpipeline:
- `buildExportableProject()` i src/lib/gen/build-exportable-project.ts ar enda
  ingangen for download, export, quality-gate och engine-version-zip.
- Baseline: Next 16.2.1, React 19.2.4, inga lint-scripts i exporterade projekt.

Deterministiska fixar i autofix-pipeline:
- react-import-fixer (default React import)
- react-hook-import-fixer (namngivna hooks: useState, useEffect etc.)
- tailwind-font-arbitrary-fixer (font-[family-name:var(--x)] -> inline style)
- font-import-fixer, lucide-image-fixer, metadata-import-fixer
- as-const-boolean-keys (readonly tuples -> mutable for Next.js Metadata)
- dep-completer (saknade beroenden mot baseline)

System-prompt-regler (website-intent):
- Scroll-reveal aldrig pa hero/above-the-fold.
- CSS blur forbjudet i reveal-transitioner pa text.
- font-[family-name:var(--x)] forbjudet.

Autofix-prompt: regel 5 — LLM far aldrig fraga anvandaren under autofix.

---

Oppna gap (prioritetsordning):

1. Forbattra felkontext till autofix-LLM — exakta tsc/build-fel bor skickas som
   strukturerad kontext, inte bara sammanfattning.
2. [x] Centralisera sandbox-policy — isSafeRelativePath, template-URL etc.
   anvander nu delade helpers fran src/lib/mcp/runtime-url.ts (2026-03-28).
3. [x] Synka bildregler — picsum.photos/via.placeholder.com borttagna fran
   genererad next.config; prompt och scaffold ar nu konsekventa (2026-03-28).
4. Visuell QA med screenshots — nuvarande visual-qa.ts gor statisk kodanalys;
   riktig QA kraver Playwright-screenshots fran sandbox (bakom feature-flag).
5. Autofix-persistering — resultat loggas men sparas inte permanent per version i DB.

Se docs/handoffs/export-reliability-och-nasta-steg.md for detaljerat handoff.
