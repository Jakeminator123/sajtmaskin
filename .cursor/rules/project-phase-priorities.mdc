---
description: "Projektprioritet: bygg/stabilisera utan onödiga säkerhetslager, men sänk aldrig grundhygien."
alwaysApply: true
---

# Projektprioritet

Prioritera fungerande end-to-end och små kontrakt. Lägg inte till stora säkerhets-/governance-lager utan explicit scope.

## Alltid grundhygien

- Inga secrets i git eller loggar.
- Cross-tenant-läckage stoppar alltid.
- Dataförlust och tyst korruption fixas alltid.
- Ta inte bort fungerande krypto/nyckelhantering om det kan göra sparad data oläsbar.

## Tooling-säkerhet (alltid)

- **Supabase-MCP: `supabase` = dev, `supabase-prod` = prod.** Svara aldrig om prod utifrån dev-projektet. Ingen destruktiv SQL via MCP.
- `.cursor/mcp.json` är gitignorerad och kan hålla tokens — committa aldrig.
- Appens `VERCEL_PROJECT_ID` ≠ användarsajters per-deploy `vercel_project_id`.

Full tooling-setup: [`local-tooling-mcp.mdc`](local-tooling-mcp.mdc).

## Undvik utan explicit scope

- ny auth-gate
- ny kryptering
- ny rate-limit
- ny signatur-/tokenmodell
- stora "governance"-lager

## Vercel-env

- Föredra läsbara (non-sensitive, `encrypted`-typ) Vercel-env så värden kan inspekteras; markera inte `sensitive` i onödan.
- Ta inte bort `ENV_VAR_ENCRYPTION_KEY` (lagrade projektEnvVars blir odekrypterbara → tyst dataförlust).

## Repo-synlighet (obs)

Repot är **publikt** sedan 2026-07-18 (GitHub API utan credentials →
`private: false`, `visibility: public`; anonym `git ls-remote` fungerar). Publik
hygien är därför obligatorisk: committa inga secrets, och var sparsam med
admin-/credit-mekanik och infra-id:n i docs (t.ex. exakt "bli admin → 10 000
diamonds"-recept). "Läsbar env" ovan gäller Vercel-dashboarden — inte en
licens att skriva känsligt i git. Verifiera synligheten på nytt innan ett
framtida styrbeslut bygger på den; ägaren kan ändra den igen.

Skriv säkerhetsfynd som tydlig backlog om de inte är P0/P1.
