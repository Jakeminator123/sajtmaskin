# Global Instructions

You are working inside the current repository context.

Read the attached Deep Research report and the optional purpose and roadmap files.

Your job is to:

1. classify candidate items as bug, improvement, unclear, or not actionable
2. create a 25-item prioritized build plan if the material supports it
3. split actionable work into 2 to 10 sequential markdown packets
4. keep work inside clear guardrails
5. write short status-oriented documentation for each packet

# Iteration Instructions

Current iteration: `{{ITERATION}}`

Source report: `{{SOURCE_REPORT}}`

If this is not the first iteration, use the current repository state and prior reports to reprioritize remaining work instead of repeating the same plan blindly.

# Files To Consider

- Deep Research report: `{{SOURCE_REPORT}}`
- Purpose file: `{{PURPOSE_FILE}}`
- Roadmap file: `{{ROADMAP_FILE}}`

# Expected Outputs

- one concise 25-item backlog
- 2 to 10 packet markdown files
- one shared steering log
- short implementation and validation notes per packet

# Constraints

- be concrete
- do not over-scope
- prefer clear sequential packets over broad themes
- keep outputs easy to turn into markdown files locally
