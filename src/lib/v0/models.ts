/**
 * Compatibility bridge for historical imports.
 *
 * Shared builder model definitions now live in `@/lib/models`.
 * Keep this file as a thin re-export while the v0 adapter is decoupled.
 */

export * from "@/lib/models/catalog";
