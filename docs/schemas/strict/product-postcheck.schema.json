{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "product-postcheck.schema.json",
  "title": "Product Postcheck",
  "description": "Subset-kontrakt för product_postcheck.* logs. Product Postcheck kör en begränsad DOM-/runtimekontroll av previewn. Den ersätter inte F3 build/typecheck men productBlocked kan stoppa integrationsreadiness.",
  "type": "object",
  "additionalProperties": false,
  "required": ["level", "category", "message"],
  "properties": {
    "level": {
      "type": "string",
      "enum": ["info", "warning", "error"]
    },
    "category": {
      "type": "string",
      "pattern": "^product_postcheck\\."
    },
    "message": {
      "type": "string",
      "minLength": 1
    },
    "meta": {
      "type": ["object", "null"],
      "additionalProperties": true,
      "properties": {
        "warningCount": { "type": "integer", "minimum": 0 },
        "productBlocked": { "type": "boolean" },
        "durationMs": { "type": ["number", "null"], "minimum": 0 },
        "checkedUrl": { "type": ["string", "null"] },
        "skippedReason": { "type": ["string", "null"] },
        "code": {
          "type": "string",
          "enum": [
            "broken_anchor",
            "broken_image",
            "cta_no_handler",
            "mobile_menu_failed",
            "fake_form",
            "runtime_crash",
            "preview_boot_page",
            "preview_probe_unreadable",
            "hydration_mismatch",
            "hydration_dom_loss",
            "console_error",
            "request_failed",
            "http_error"
          ]
        },
        "href": { "type": ["string", "null"] },
        "src": { "type": ["string", "null"] },
        "alt": { "type": ["string", "null"] },
        "selector": { "type": ["string", "null"] },
        "text": { "type": ["string", "null"] },
        "formId": { "type": ["string", "null"] }
      }
    }
  }
}
