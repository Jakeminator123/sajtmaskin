Sajtmaskin — kvarvarande uppgifter
Konsoliderad: 2026-04-14

══════════════════════════════════════════════════
UPPSKJUTET (inget blockerar)
══════════════════════════════════════════════════

[ ] Konsolidera dashboards
    3 500+ rader Python. Ren refaktor.

[ ] Template-library pipeline: hydrate repo-cache
    133 git clones. Behövs bara vid research-uppdatering.

[ ] Dossier-manifests: recommendedScaffoldFamilies → recommendedScaffoldIds
    Runtime-shim i catalog.ts hanterar båda. Uppdateras vid nästa pipeline-körning.

[ ] searchTemplateLibrary() exporterad men oanvänd i runtime
    Global search förblir inaktiv. Scaffold-ankrad guidance via
    resolveTemplateGuidance() är opt-in (SAJTMASKIN_RUNTIME_TEMPLATE_GUIDANCE).

[ ] BUILD_INTENT_GUIDANCE dubblett
    Duplicerad i system-prompt.ts och promptAssist.ts med "keep in sync"-kommentar.
    Cirkulär import-risk förhindrar enkel sammanslagning.

[ ] Fallback-guidance (MOTION/VISUAL/QUALITY) i promptAssist.ts
    Behövs för non-init-paths men parkerat.

[ ] Scaffold-specifik toolkit-lista per scaffold
[ ] Komponentpool per scaffold
[ ] Nya scaffolds: AI-chat, docs-site, realtime-app
[ ] Automatisk baseline-uppdatering (CI/script)

[ ] Structural references: bredda urval av strukturfiler
    Idag filtreras bara layout.tsx, page.tsx, middleware.ts.
    Nästa steg: inkludera pricing/page.tsx, checkout/page.tsx, login/page.tsx,
    signup/page.tsx, dashboard/page.tsx som strukturfiler i
    selectStructuralFilesForEntry() (structural-files.ts).
    Kräver att build-pipeline (build-template-library.ts) också väljer dessa filer
    med höjd excerpt-gräns (4800 tecken). Cursorignore blockerar inte runtime,
    bara agent-sökning i dossier-filer.

[ ] shadcn blocks-integration i init-generering
    FEATURED_BLOCKS och buildShadcnBlockPrompt() i registry-service.ts / registry-utils.ts
    används idag bara av builder-UI:t (element picker). Potential att koppla in i
    init-genereringen som capability-matchade block-referenser.

══════════════════════════════════════════════════
KLART (2026-04-14)
══════════════════════════════════════════════════

[x] file-logger.ts borttagen — helt oanvänd (ingen import i src/).
    SAJTMASKIN_LOG borttagen ur env-policy.json och ENV.md.
[x] Loggning: shared.ts med gemensamma helpers (paths, slug, gating, sensitive-pattern).
    devLog.ts, dev-log-reader.ts, generation-log-writer.ts rensat från dubbletter.
[x] Vercel-sync: manage_env.py läser nu .vercel/project.json (vercel link).
    Nya npm-scripts: vercel:link, env:pull, env:pull:prod-snapshot, env:status, env:audit, env:reconcile.
[x] Builder-bugg: modellval/scaffold/inställningar disabled under isCreatingChat fixat.
    Nu använder config-kontroller isConfigLocked (=isAnyStreaming) istället för isBusy.
[x] findLastNumber() borttagen ur generation-log-writer.ts (oanvänd, eslint-varning).

══════════════════════════════════════════════════
OPERATIVT / MILJÖ
══════════════════════════════════════════════════

[ ] WSS/HMR till Fly — WebSocket-proxy tappar connection
[ ] Hydration error overlay — pre-existerande på landningssidan



---


@/hooks/use-mobile / @/hooks/use-toast i runtime-imports.ts — behålls som bakåtkompatibel fallback bredvid nya @/lib/hooks/-vägarna, eftersom äldre genererade sajter kan använda dem.
useDeploymentStatus lyssnar på /api/v0/deployments/ — API-versioning (v0 i URL:en) är naming debt men inte trasigt; hanteras som en separat städ.
useIntegrationStatus har previewUrl i dependency-array utan att använda den i fetch — funktionellt korrekt (re-triggrar när preview blir tillgänglig) men semantiskt förvirrande. Inte värt att bryta nu.
35 filer under src/lib/hooks/ granskade — alla aktiva i runtime, inga döda.
