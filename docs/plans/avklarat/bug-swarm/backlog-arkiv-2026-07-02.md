# Bug-backlog — arkiv (2026-07-02)

> Flyttade `[x]`-rader från [`BUG-SWARM-BACKLOG.md`](../../../../BUG-SWARM-BACKLOG.md) efter 818-svärmens verifiering + batch-fix-PR (branch `fix/bug-swarm-batch-0702`) 2026-07-02. Äldre historik: [`backlog-arkiv-2026-06-27.md`](backlog-arkiv-2026-06-27.md) · [`backlog-arkiv-2026-06-24.md`](backlog-arkiv-2026-06-24.md).

## SSRF-guard-konsolidering (2026-07-08, PR #390)

`is-disallowed-host.ts` fanns inte längre på disk vid en repo-hygien-genomgång 2026-07-08 (read-only dead-code-granskning). Verifierat mot git-historik: PR #390 ("konsolidera SSRF-guard till ssrf-guard.ts") stängde redan båda raderna nedan samma dag, men de blev aldrig flyttade ur Aktiv kö.

| Klar | Status | Prio | Fynd | Källa | Fix-referens |
| --- | --- | --- | --- | --- | --- |
| [x] | Fixad | P2 | SSRF-guard duplicerad: `src/lib/security/is-disallowed-host.ts` (skapad i #385) exporterade en egen `isDisallowedHost` som redan fanns i `src/lib/ssrf-guard.ts`. Två parallella guards med samma namn = drift-risk och svagare skydd på inspector-vägen. | MB#385 (merge-gate-review på mergad #385, kodverifierad) | `is-disallowed-host.ts` borttagen; `inspector-capture/route.ts` + `inspector-element-map/route.ts` importerar nu `isDisallowedHost`/`isLoopbackHost`/`hostResolvesToPrivate` direkt från `ssrf-guard.ts`. Commit `9e8924381`, PR #390. |
| [x] | Fixad | P3 | DNS-rebinding på `/api/inspector-element-map`: den gamla guarden (`is-disallowed-host.ts`) kollade bara den literala hostnamnssträngen, så en publik host som resolvar till privat/metadata-IP kunde passera. | MB#385 (merge-gate-review på mergad #385, kodverifierad) | Löst av samma konsolidering: `ssrf-guard.ts`s `hostResolvesToPrivate()` (DNS-lookup + privat-IP-check) anropas nu från `/api/inspector-element-map` via den delade guarden. Commit `9e8924381`, PR #390. |

## Fixar flyttade från Aktiv kö

| Klar | Status | Prio | Fynd | Källa | Fix-referens |
| --- | --- | --- | --- | --- | --- |
| [x] | Fixad | P1 | Removal-tombstone-resurrection: `removedCapabilities` är rund-scopad (räknas ur varje prompt) och den ytliga snapshot-mergen (`mergePersistedOrchestrationSnapshots`) lät en senare neutral rundas tomma lista skriva över en tidigare removal. När tombstonen var borta såg `buildFollowUpContract` ett tomt floor utan explicit-removal-markör och ärvde tillbaka den borttagna capabilityn från den persisterade briefen. Empiriskt bekräftad för "tog bort enda integrationen"-fallet (t.ex. Stripe-only-sajt → "ta bort Stripe" → neutral follow-up → Stripe åter). | BB#caprem1 (Bugbot på #491) + Codex-P1 & extern coach-review på #494 (kodverifierad + reproducerad) | **Två steg.** #494 (`fix/capability-removal-durable-tombstone`) gjorde tombstonen varaktig med `(prev ∪ next) \ floor` — men floor-subtraktionen var själv en residual-P1 (Codex + coach): floor kan repopulera capabilityn ur brief/filbevis/F3-approval/inferens, inte bara ur ett explicit re-add, så en removal kunde återuppstå vid F2→F3. **Komplett fix (`fix/capability-tombstone-readd`, PR #497):** (1) merge = durable set-union rensad ENBART av en explicit `readdedCapabilities`-signal (aldrig floor); (2) `detectCapabilityRemoval` surfar `readdedCapabilities` ur den aktuella prompten ("lägg tillbaka Stripe" — additiv-verb utökad); (3) `buildFollowUpContract` subtraherar den varaktiga tombstonen från effektiva capabilities så en borttagen integration stannar ute även när floor/brief återinför den. Tester: readd-detektion, merge (durable/floor-repopulation-guard/explicit-readd), full round-trip incl. F3-resurrection-guard. Ägare: `orchestration-snapshot.ts` + `follow-up-capability-removal.ts` + `orchestrate.ts`. |
| [x] | Fixad | P2 | Deploy-repair (A3) tappades vid reload: "Publicera om med fix"-knappen + deploy-felstaten bands till in-session `activeDeploymentId`, som inte återställdes från deployment-historiken vid sidladdning → efter reload visade headern varken felstate eller byggloggslänk och `republishWithFix` avbröt med "Ingen failad publicering att reparera". | BB#deploy3 (bugbot på `feat/publicera-stringens` 2026-07-08) | `useDeploymentHistory` exponerar `latestFailedDeployment` (nyaste rad = terminal `error`); `useBuilderPageController` hydrerar `activeDeploymentId` från den vid mount (SSE-endpointen skickar terminal-snapshot direkt). Kräver även engine-first-auth-fixen i `events/route.ts` (A#3, samma leverans). 2 tester i `useDeploymentHistory.test.ts`. PR #456. |
| [x] | Fixad | P2 | Deploy-fel dubbel-loggades webhook-vs-poll: både Vercel-webhooken (`deployment.error`) och SSE-poll-fallbacken anropade `logDeployError` för samma deployment utan idempotensnyckel → dubbla `engine_version_error_logs`-rader + dubbla RAG/bus-signaler för ETT publiceringsfel. | BB#deploy2 (bugbot på `feat/publicera-stringens` 2026-07-08) | `updateDeploymentStatus` gör nu en atomisk villkorlig UPDATE (`status IS DISTINCT FROM 'error' RETURNING id`) och returnerar `{ transitionedToError }`; ALLA error-skrivvägar (webhook, SSE-poll, list-/single-GET-refresh, POST:ens initiala statusskrivning) loggar bara när den vägen vann övergången → exakt en logg per build-fel, race-säkert. Poll-strömmen hålls öppen vid failad error-persist så en senare iteration kan göra om övergången. Metadata (inspectorUrl) skrivs fortfarande på duplicerade error-events. Residual (by design): loggning är best-effort — vid permanent DB-nere under hela fönstret loggas inget (loggen kräver DB). Tester i `deployment.test.ts` (4), `events/route.test.ts` (poll-transition + retry), `webhooks/vercel/route.test.ts` (dedup + metadata), `deployments/route.test.ts` (POST-refresh-logg). PR #469. |
| [x] | Fixad | P3 | Preview-hostens pnpm-build-approval (`PNPM_CONFIG_DANGEROUSLY_ALLOW_ALL_BUILDS`, #454) no-op:ade under pnpm 10: corepack väljer pnpm-version per projektets `packageManager`-fält, så en importerad mall som pinnade `pnpm@10.x` kunde crash-loopa med `ERR_PNPM_IGNORED_BUILDS`. | VADE på #454 (residual-kant) | `normalizeImportedRepoFiles` strippar nu ett `packageManager: "pnpm@<11>"`-pin vid mallimport → corepack faller till VM-default pnpm 11 som honorerar flaggan. pnpm 11+/yarn/npm-pins lämnas orörda. 6 tester i `normalize-imported-package-json.test.ts`. PR #469. |
| [x] | Fixad | P3 | Dossier-`configured` läste plattformens `process.env` och kunde både läcka Sajtmaskins konfiguration till användarprojektets prompt och missa användarens sparade `projectEnvVars`. | M#doss1 (F3-email-gate-utredning 2026-07-02) | `configuredEnvKeys` trådas från projektets `getStoredProjectEnvVarMap` genom init, follow-up, snapshot- och version-presence-selection; plattformens `process.env` är bara deprecerad fallback för projektlösa backstops. Regressionstester i `select.test.ts`, `snapshot-selection.test.ts` och `version-presence.test.ts`. Commit `c4b9627b1`, PR #483. |
| [x] | Fixad | P3 | Genererade projekts pipeline-ägda `files_json`-`.env.local` dumpade hela placeholder-katalogen i kodvyn i stället för det valda Byggblocks-scope som redan styrde `env.example`. | Granska-svärm på preview-tabs-diffen (2026-07-09) | `ProjectEnvLocalOptions` trådas genom finalize/eval; nya och äldre pipeline-markerade env-artefakter skrivs dossier-scopat (eller utelämnas vid tomt scope), medan modell-emitterade filer och preview-runtime förblir separata. Tester i `project-scaffold.test.ts`, `preview-session.test.ts`, `project-env-file.test.ts`, `preview-prewarm.test.ts`, finalize-sviter. Branch `fix/env-artifact-scope-v2`. |
| [x] | Fixad | P2 | Follow-up "Ta bort Stripe/integration" stoppade reselection men lät capability-hints/contracts och gamla dossier-filer överleva union-merge, så nästa version kunde bli identisk. | /logg chat e298da50 + T15 scope-gap + Codex P1 på #447 (2026-07-08) | Explicit removal subtraheras nu från inference, Deep Brief, contracts, F3 approvals och stream-meta; finalize raderar file-evidenced dossier-paths med shared-path-skydd och kör importkontroll efteråt. Snapshotens tomma top-level floor är auktoritativ. Tester täcker design/F3, snapshot, meta, merge och importpass. Branch `fix/capability-removal-e2e`. |
| [x] | Fixad | P2 | F3 build-plan härleddes bara från `preGenerationContracts`, så parent-versionens verkliga integrationer kunde saknas i prompten. | G#20 | Readiness-gatens filhärledda `Tier3BuildSpec` trådas från exakt parent-version till Dynamic Context; explicit godkända providers i aktuell runda adderas, övriga promptkontrakt är fallback vid tom/saknad filspec. Branch `fix/capability-source-parity`. |
| [x] | Fixad | P2 | Init och follow-up hade olika named capability-universum; init blockerade simple lane på några sektionsord men skickade inte capability-id:n till dossier selection. | G#25, G#26, #242 init-gap | Båda faser kör nu `detectFollowUpCapabilities` + explicit dossier-id-resolution före orchestrate. Simple lane läser samma resultat, inklusive capabilities utanför legacy-regex. Route-/prompttester täcker init, follow-up och F3. Branch `fix/capability-source-parity`. |
| [x] | Fixad | P3 | **pg DeprecationWarning (`client.query()` överlapp på nya kopplingar):** `pool.on("connect")` körde `SET TIME ZONE 'UTC'` fire-and-forget (utan await) i `src/lib/db/client.ts` → poolen lämnade samma klient till Drizzle innan timezone-queryn hunnit klart, så första riktiga queryn överlappade → `Calling client.query() when the client is already executing a query` (syns på `/api/auth/me`, `/api/analytics`, `/api/webhooks/vercel` i prod-loggarna 2026-07-09). | M#pg1 (prod runtime-logg 2026-07-09 via Vercel MCP, kodverifierad) | Ny migration `set-role-timezone-utc.sql`: `ALTER ROLE CURRENT_USER SET timezone TO 'UTC'` — pooler-säker (överlever transaction-pooling på port 6543, till skillnad från sessions-`SET`). Race-queryn i `pool.on("connect")` borttagen ur `client.ts`. 2h-driftskyddet bevaras (kolumnerna redan TIMESTAMPTZ). `timezone-contract.test.ts` låser båda halvorna av överlämningen (migration finns i `MIGRATION_ORDER` + `client.ts` återinför aldrig per-connection-queryn). PR #472. `npm run db:migrate:prod` körd 2026-07-09 (21/21 migrationer, ledgern bekräftat ikapp via `db:migrate:check:prod`). |
| [x] | Fixad | P1 | `{{MEDIA_x}}`-token läcker till persist → `next/image` "Failed to parse src" kraschar build/SSG. `expandUrls` kördes bara EN gång i finalize; LLM-repair-pass efteråt kunde återinföra token utan re-expand. | M#oc1 | Ny deterministisk `media-alias-fixer` i `runAutoFix` (körs efter VARJE LLM-repair-pass: verifier-fixer, partial-file, server repair-loop) ersätter läckta alias med samma `/placeholder.svg`-fallback som `expandUrls`; `expandUrls`-regexen breddad (whitespace + `-`-separator, normaliserad key-lookup). Registrerad i `fixer-registry.ts`; 7 tester i `media-alias-fixer.test.ts`. Residual (policy): `build` i F2-gaten är fortsatt ett medvetet val — spåras under "F2/F3 runtime-gate" i Beslut & policy. Branch `fix/bug-swarm-batch-0702`. |
| [x] | Fixad | P1 | Genererad Clerk-`middleware.ts` verbatim/oskyddad → placeholder-nyckel (`pk_test_placeholder`) kastar "Publishable key not valid" → 500/vit sida. Provider-shellen guardade bara SAKNAD nyckel, inte placeholder. | M#oc2 | Dossier-källan key-gate:ar sig själv: `clerkMiddleware` konstrueras bara vid strukturellt giltiga nycklar (pk base64→`$`-check + sk-format), annars `NextResponse.next()` (`data/dossiers/hard/clerk-auth/components/middleware.ts`). `clerk-provider-shell.tsx` validerar nyckeln strukturellt (banner i st.f. krasch). Manifest + instructions.md uppdaterade. Verbatim-policyn oförändrad — den skyddade filen ÄR nu canonical. Branch `fix/bug-swarm-batch-0702`. |
| [x] | Fixad | P3 | Skriv-på-läs i `/files`: `materialize=1`-grenen skrev `updateVersionFiles` utan `lockTimeoutMs`/try/catch — samma kontentionsrisk som ursprungsbuggen (heal-persist-delen fixad i #341). | M#files1 | Materialize-persisten har nu samma fail-fast/best-effort-mönster (`lockTimeoutMs:2000` + try/catch + warn-logg) som heal-persisten (`files/route.ts`). Residual (arkitektur): write-on-read-flytten till skrivvägen → Beslut & policy. Branch `fix/bug-swarm-batch-0702`. |
| [x] | Fixad | P3 | `preview_url`-persist sväljdes tyst i quick-edit: `.catch(() => null)` utan logg. | M#qe2 | `warnLog("engine", "[quick-edit] Failed to persist previewUrl", …)` med chatId/versionId/fel (`quick-edit/service.ts`), mönster från post-finalize. Test i `service.test.ts`. Branch `fix/bug-swarm-batch-0702`. |
| [x] | Fixad | P2 | Quick-edit gick förbi versions-lease: minor kunde skapas från bas som samtidigt verifierades/reparerades. | M#qe1 | `runQuickEdit` tar `acquireVersionLease(baseVersion.id, "quick_edit")` runt persist; ägd lease → retrybar `base_busy` (409); lease-infra-fel degraderar till olåst väg (samma fail-safe som `repair/route.ts`); release i finally. Nytt `VersionJobKind: "quick_edit"` (kolumnen är TEXT — ingen migration). 4 tester i `service.test.ts`. Kvarvarande generella `addAssistantMessageAndCreateDraftVersion`-varianten (vanlig generation) är medveten — den vägen skapar alltid ny rad från stream-output, inte från en muterbar bas. Branch `fix/bug-swarm-batch-0702`. |
| [x] | Fixad | P3 | LATENT `/repair`-schema-mismatch: `post-checks.ts` castade `c.check` ovaliderat; en info-signal med `passed:false` hade 400:at repair-POST:en. | M#rep1 | Filter till de tre kanoniska checkarna (`typecheck`/`build`/`lint`) i `handleRepairOrAutofix` innan cast (`post-checks.ts`). Branch `fix/bug-swarm-batch-0702`. |
| [x] | Fixad | P2 | Bug-hunt batch (`--all`): ett enda kastat engine-anrop (400/409/502) avbröt hela `POST /api/openclaw/debug/run` med 500 och slängde alla andra scenariers resultat. | BB#oc2 | Try/catch per scenario i `runBugHunt`-loopen: felet loggas + skrivs som `oc-debug:scenario-error`-warning-finding, resterande scenarier fortsätter (`bug-hunt.ts`). Test "continues with remaining scenarios when one scenario throws". Branch `fix/bug-swarm-batch-0702`. |
| [x] | Fixad | P3 | Asymmetri: oresolverat follow-up-ref loggade bara och `break`:ade utan finding (init-vägen skrev warning-finding). | BB#oc3 | Samma `buildOutcomeFinding`-warning skrivs nu för oresolverat follow-up-ref innan chain-break (`bug-hunt.ts`). Test uppdaterat. Branch `fix/bug-swarm-batch-0702`. |
| [x] | Fixad | P2 | TS2304-import-repair: Clerk-serversymboler resolvades till `@clerk/nextjs/server` för ALLA filer → server-only-import kunde injiceras i klient-komponent i F3. | BB#291 | Content-gate i stället för naiv path-gate (path-gate hade brutit giltig `middleware.ts`-resolvering): `"use client"`-filer får aldrig server-only-moduler (`@clerk/nextjs/server`, `stripe`) injicerade — namnet lämnas residual för LLM-fixern; servercomponenter/middleware/route-handlers resolverar som förut (`ts2304-known-import-fixer.ts`). 2 nya tester. Branch `fix/bug-swarm-batch-0702`. |
| [x] | Fixad | P3 | import-validator hoppade över ikoner som BARA var `import type`-importerade → `import type { PawPrint }` + `icon: PawPrint` kunde skeppa TS1361/`ReferenceError` (vit skärm) på export/preview-pathen. | M#cr1 | `collectImportedBindings` spårar value- vs type-bindings separat; ny `convertLucideTypeImportToValue` konverterar (inline `type X`-spec strippas; single-spec `import type`-rad flippas; multi-spec splittras till typ-rad + value-import). Bifix: `fixLucideImports` fuzzy-korrupterade inline `type X`-specs (`Type as type PawPrint`) — type-specs lämnas nu orörda. 4 nya tester. Branch `fix/bug-swarm-batch-0702`. |
| [x] | Fixad | P2 | import-validator destrukturerings-falskpositiv: lokalt destrukturerat namn = lucide-ikonnamn fick dublett lucide-import (TS2440): `const { icon: PawPrint } = x`, `function Card({ icon: PawPrint })`, arrow-params. | M#cr2, BB#296 | `fileDeclaresSymbolLocally` är nu destrukturerings- och parameter-medveten (const/let/var-destrukturering, `function Name({…})`, `= ({…}) =>`); anrops-argument (`fn({ icon: X })`) matchar avsiktligt INTE och importeras fortfarande. 4 nya tester inkl. call-site-negativtest. Branch `fix/bug-swarm-batch-0702`. |
| [x] | Fixad | P2 | Selection-guard-race vid fast-edit: `handleFilesSaved` satte nytt versions-id innan `mutateVersions()`-refetchen landat; guarden nollade valet → fallback till gammal version. | M#sel1 | `pendingCreatedVersionRef` registrerar det nyskapade id:t före select; versionIdSet-guarden tolererar det inom 15 s grace tills refetchen landat (då rensas ref:en) (`useBuilderPageController.ts`). Branch `fix/bug-swarm-batch-0702`. |
| [x] | Fixad | P2 | Status-flap: terminal bus-`failed` var sticky i `reconcileTerminalDbState` — gate-fail → repair → accept-repair → promoted lämnade UI på "Verifiering misslyckades" fast DB sa promoted/passed (prod-observerad chat 47008c3b). | M#flap1 | Policybeslut implementerat: DB `passed` **+ `release_state=promoted`** uppgraderar nu även en terminal bus-`failed` till `done` (promote/deploy läser ändå DB; promoted är systemets starkaste positiva signal — ingen false-green-risk). `release_state` trådad från `/version-status` + `/versions`. Degradations bevaras. 3 nya tester. Branch `fix/bug-swarm-batch-0702`. |
| [x] | Fixad | P2 | Snapshot-brief tappade init-brief-fält vid rehydrering: `primaryCTA` persisterades men lästes aldrig tillbaka; `seasonalHints` persisterades aldrig. | M#818-1 | `seasonalHints` persisteras i `extractBriefSummary`; `BriefSummarySnapshot` + `extractBriefSummaryFromSnapshot` + `buildFollowUpBriefFromSnapshot` rehydrerar `primaryCTA` → `primaryCallToAction` och `seasonalHints` under konsument-nycklarna. Beslut dokumenterat i kod: brief-`pages[]` snapshotas MEDVETET inte — follow-up-IA härleds från basversionens filer via route-frysningen (färskare sanning). Test uppdaterat + nytt. Branch `fix/bug-swarm-batch-0702`. |
| [x] | Fixad | P2 | F3-stream kringgick tier-3 env-readiness: `validateTier3Readiness` anropades bara i `/finalize-design`; `POST .../stream` med `meta.lifecycleStage:"integrations"` startade F3-codegen utan env-gate → brända credits. | M#818-2 | Ny delad ägare `src/lib/integrations/tier3-readiness-gate.ts` (`checkTier3ReadinessForVersion` + flyttad `deriveTier3BuildSpecForVersion`); stream-routen kör samma gate server-auktoritativt före F3-start (412 + missingByIntegration vid saknade nycklar, 409 vid oläsbara filer; gate-fel failar öppet med logg — F3-gaten build+lint fångar ändå). `finalize-design` konsumerar samma modul. 4 tester i `tier3-readiness-gate.test.ts`. Branch `fix/bug-swarm-batch-0702`. |
| [x] | Fixad | P2 | Degenererad output kunde trigga KLIENT-driven autofix-retry trots terminal server-fail (WP4-vakt) — retry fail-fastade bara i vakten igen och brände capped försök. | M#dgc (Bugbot #322) | Klient-skip i `post-checks-results.ts`: `previewBlockingReason` som börjar med "Degenerate output blocked" rapporteras sanningsenligt (readiness fail) men köar ALDRIG autofix (`autoFixReasons=[]`, `verifyPending=false`). Test "does NOT queue client autofix for degenerate output". Branch `fix/bug-swarm-batch-0702`. |
| [x] | Fixad | P2 | Inspect-bridge-klustret (flag-gated `NEXT_PUBLIC_SAJTMASKIN_INSPECT_BRIDGE`): (A) ingen fallback när `ready` saknades → inert inspektor; (B) capture skickade element-center, inte klickpunkt; (C) `?inspect=1` nådde genererad apps `searchParams`; (D) flaggan saknade env-policy-regel. | #164, #197 | A: ready-timeout (5 s) i `usePreviewInspectBridge` → status + `onBridgeUnavailable` → `PreviewPanel` växlar till kartmotorn. B: bridge-scriptet skickar faktisk klickpunkt (`click:{x,y}`) i pick-payload; parent föredrar den (rect-center = fallback för äldre script). C: preview-host-proxyn strippar `inspect`-parametern före upstream-forward (`preview-host/src/runtime.js` `stripInspectParam` — kräver Fly-deploy). D: `NEXT_PUBLIC_SAJTMASKIN_INSPECT_BRIDGE` registrerad i `config/env-policy.json` (knownEmptyOk + extraKnownKeys). Branch `fix/bug-swarm-batch-0702`. |
| [x] | Fixad | P3 | Promote-guard fail-open vid läsfel på kanoniska vägarna: `promoteVersion`/`acceptRepair` anropade `assertPromoteAllowed` utan `onReadError` → smalt false-green-fönster vid telemetri-läsfel (quality-gate-routen redan härdad i #299). | M#pg1 (B08 follow-up) | Policybeslutet implementerat: båda callsites kör nu `onReadError:"indeterminate"` — läs-exception → ingen promotion (retrybar, `null`-retur terminal-failar aldrig); no-telemetry (`signal===null`) behåller historisk fail-open (template-import/rollback/legacy). Loggen skiljer indeterminate (retryable) från explicit block. Guard-tester fanns redan (#299); callsite-beteendet täcks av accept-repair-sviten. Branch `fix/bug-swarm-batch-0702`. |
| [x] | Fixad | P1 | Deploy-API:t blockade env på `missingEnvKeys` medan readiness (F3) blockar på `buildBlockingKeys` → feature-runtime/warn-only-nycklar (t.ex. Resend `EMAIL_FROM`/`CONTACT_EMAIL_TO`) stoppade publicering med `DEPLOY_MISSING_ENV` fast readiness visade dem som warning → `canDeploy:true` i UI men 409 vid klick. | Codex P1 på #443 (postad ~3 min efter merge, otriagerad tills 2026-07-09; kodverifierad mot `project-env-resolver.test.ts:84-90`) | Deploy-gatens env-blocker bytt från `missingEnvKeys` till `envRequirements.buildBlockingKeys` (samma resolver-fält som readiness, `project-env-resolver.ts:181-209`); resolvern defaultar nycklar utan enforcement-metadata till `build`, så legacy-beteendet är oförändrat. `missingEnvKeys` behålls i `deployReadiness`/varningar. Kontraktstester i `src/app/api/v0/deployments/route.test.ts` (saknad feature-runtime-nyckel → 200 + warning; saknad build-blocking-nyckel → 409 `DEPLOY_MISSING_ENV`). Branch `fix/deploy-env-gate-parity`. |
| [x] | Fixad | P2 | `envSyncWarnings` från deploy-API:t (partiellt misslyckad env-synk mot hosting-leverantören) nådde aldrig användaren — deploy-klienten typade inte fältet och visade bara success-toast vid HTTP 200 → en publicering där env-synken delvis failade såg helt lyckad ut. | Codex P2 på #443 (postad efter merge, otriagerad tills 2026-07-09, kodverifierad) | `useBuilderDeployActions` (deploy-anroparen) typar + parsar nu `envSyncWarnings` **och** `envWarnings` ur deploy-svaret och visar `toast.warning` efter success-toasten (aggregerad, leverantörsneutral svensk copy). `republishWithFix` (#456) orörd. Branch `fix/deploy-env-gate-parity`. |

## Scaffold-freeze på game-follow-up (2026-07-03, PR #368, branch `fix/scaffold-freeze-followup`)

Live `/logg-internet`-session (prod-chat `69aae3d5`) upptäckte att en capability-follow-up ("bygg ett minispel på /spel") rebasade en `landing-page`-sajt till `base-nextjs` mitt i chatten. Rotorsaken var avsiktlig scaffold-unlock för spel/canvas — borttagen per ägarbeslut (scaffold ~97% fryst på follow-ups).

| Klar | Status | Prio | Fynd | Källa | Fix-referens |
| --- | --- | --- | --- | --- | --- |
| [x] | Fixad (policy) | P1 | Capability-follow-up bytte scaffold mitt i chatten (v3 `base-nextjs`/`agreement` medan v2 var `landing-page`/`persisted`). Rotorsak: `hasMajorChangeUnlockSignal` / `FOLLOW_UP_MAJOR_CHANGE_UNLOCK_PATTERNS` lät spel/playable/canvas-follow-ups låsa upp scaffold-frysen. EJ orsakad av kontrollflöde-PR:arna #360–#367 (koden senast rörd i #297). | /logg-analys live-session 2026-07-03 (`generation_telemetry`), kodspårning | Game/canvas-auto-unlock borttagen ur `shouldIgnorePersistedScaffoldForMatch` + död pattern-bank raderad (`follow-up-clarification.ts`); follow-up behåller nu fryst scaffold och lägger funktionen som ny route. Escape-hatch kvar: explicit `clear-redesign`-intent + `PERSISTED_SCAFFOLD_UNLOCK_SUPPLEMENT_PATTERNS`. Init opåverkat. Tester i `follow-up-clarification.test.ts` omvända; doc-sync `follow-up-capability-vocabulary.ts`. Reversibelt via git. Branch `fix/scaffold-freeze-followup`. |

## Stabilisering våg 1 — init-grön (2026-07-03, branch `fix/stabilisering-vag1-init-gron`)

Rotorsaksanalys av prod-sessionen chat `cc10e7de` (8 versioner, v1/v5/v8 röda på "symbol utan import"-klassen). Kärnfyndet omkullkastade M#jsx1:s antagande: prod-filerna hade ALDRIG `<HTMLFormElement/>` som JSX — verifier-scannen flaggade den giltiga typannoteringen `FormEvent<HTMLFormElement>` som JSX (falsk positiv). M#imp1-mekanismen var guard-revert i import-validator (radbaserad bindnings-scan såg inte flerradiga importblock). Nytt öppet fynd (F3-flödet körde v8 i F2-lane) loggat som P1-rad i Aktiv kö.

| Klar | Status | Prio | Fynd | Källa | Fix-referens |
| --- | --- | --- | --- | --- | --- |
| [x] | Fixad | P2 | **DOM-interface "som JSX" var en scanner-falskpositiv** — omklassad med ny evidens: alla tre prod-versionerna (v1 5ed674b9, v5 856909d5, v8 4a29c7b4, verifierat mot `files_json` i prod-DB) innehöll ENDAST den giltiga typannoteringen `FormEvent<HTMLFormElement>`; ingen JSX-misuse fanns. `checkUndefinedJsxSymbols` (verifier-pass) JSX-tag-regex matchade `<HTMLFormElement>` inne i typ-generics → oåtgärdbar `undefined-jsx-symbol`-Blocker (inget att fixa → RepairGate loggar alltid `still-failing`). Den efterfrågade normalize-regeln (`dom-builtin-jsx-fixer`) fanns redan och fungerar — den har negativ lookbehind och no-op:ade korrekt. | M#jsx1 (prod chat 3120c05c + cc10e7de; omklassad i stabilisering våg 1) | Samma negativa lookbehind (`(?<![A-Za-z0-9_$.])`) som `dom-builtin-jsx-fixer` tillagd på `JSX_OPENING_TAG` i `verifier-pass.ts` — typ-generics (`FormEvent<HTMLFormElement>`, `Promise<Response>`, `ChangeEvent<HTMLInputElement>`) kan inte längre nå verifiern som Blockers; äkta JSX-misuse flaggas fortfarande (och fixas av dom-prefixet före verifiern). Failing-test-först dokumenterad i PR-body; tester i `verifier-pass.test.ts` (prod-mönstret + typ-generics + äkta misuse) och full normalize+verifier-vägen i `verifier-phase.test.ts`. Branch `fix/stabilisering-vag1-init-gron`. |
| [x] | Fixad | P2 | **Normalize-JSX-scannen missade kända imports i prod** — rotorsakad: `runImportValidator` fixade Badge/Button korrekt, men (a) `detectMissingImports`/`fixMissingIconValueImports` samlade importbindningar RADBASERAT och såg inte flerradiga importblock (`import {\n Flame,\n} from "lucide-react"`) → redan importerade ikoner re-importerades, och (b) nyimport-insättningen kunde splitta MITT I ett flerradigt block. Guard-wrappern (`runImportValidatorGuarded`) upptäckte de introducerade dubbletterna och reverterade HELA resultatet inkl. de korrekta fixarna — reproducerat exakt på v8:s `app/page.tsx` (revert-orsak: `Flame, Gem, Hammer, MapPin, Waves, Zap`). | M#imp1 (prod chat e8420220 + cc10e7de v8; reproducerad i stabilisering våg 1) | Delad flerradsmedveten bindnings-kollektor (`collectImportBoundNames`) för båda scanarna + blockmedveten insättningspunkt (aldrig inne i ett öppet importblock) i `import-validator.ts` och `ts2304-known-import-fixer.ts`. Repro-testet (v8-page-mönstret) i `import-validator.test.ts` verifierar not-reverted + korrekta imports. Telemetrifält `cannotFindSummary` (seenCodes/resolvedNames/residual + reason) på `validate.tsc.import-repair` och `verifier-pass.deterministic-import-fix` gör klassen observerbar i prod. Branch `fix/stabilisering-vag1-init-gron`. |
| [x] | Fixad | P2 | `LucideIcon` (type-only) lämnades residual av `ts2304-known-import-fixer`: resolvern hade bara `kind: "named" \| "default"` — ingen type-import-emission. Eval-fall EVAL-IMPORT-005 dokumenterade residual-beteendet. | Extern coach-review 2026-07-03 (lucka 2); fixbeslut tidigarelagt av stabilisering våg 1 (v8-evidens: LucideIcon-mönstret i `future-gear-shop.tsx`) | Ny `kind: "type-named"` i resolvern: `LucideIcon` → `import type { LucideIcon } from "lucide-react"`, mergas endast i befintlig `import type`-rad (aldrig value-rad). Samtidigt: `Resend` → named import från `"resend"`, server-route-gated exakt som Stripe + `SERVER_ONLY_MODULES` + tier-3-gate (F2 → residual, reason `tier3_gated`). EVAL-IMPORT-005-förväntan flippad; nya tester i `ts2304-known-import-fixer.test.ts` + `deterministic-import-repair.test.ts`; hela v8-klassen låst av `v8-f3-import-repair.eval.test.ts` (F3: 0 residual på known-set; F2: gate intakt). Branch `fix/stabilisering-vag1-init-gron`. |

## DB-kontention prod-natt 2026-07-03 (branch `fix/db-lock-contention`)

Live-sessionens Vercel-loggar visade två DB-felklasser: `57014 statement timeout` på `update engine_versions set preview_url` (rad-lås-kontention mot verify/lease) och `SELECT … FOR UPDATE` utan lock-budget i lease-vägarna (P2 loggad av Fas 4). Pool-svälten (connect-timeouts) är kvar som öppen infra-rad — den kräver ägarens mätning/beslut.

| Klar | Status | Prio | Fynd | Källa | Fix-referens |
| --- | --- | --- | --- | --- | --- |
| [x] | Fixad | P1 | `POST /preview-session` 500:ade på `canceling statement due to statement timeout` (57014) när `preview_url`-persisten blockerade på `engine_versions`-radlåset (hölls av verify/lease) — trots att preview-sessionen redan startat. | Vercel runtime-logg 2026-07-03 (chat `69aae3d5`, tuple (4,15)) | `updateVersionPreviewUrl` har nu opt-in `lockTimeoutMs` (samma mönster som M#files1 heal-persist): transaktionslokal `lock_timeout`, kastar aldrig, returnerar false vid kontention. Preview-session-routen anropar med `lockTimeoutMs: 2000` + warn-logg — sessionen returneras alltid; URL:en re-persisteras vid nästa start. Test i `preview-session/route.test.ts`. Branch `fix/db-lock-contention`. |
| [x] | Fixad | P2 | `SELECT … FOR UPDATE` utan `lock_timeout` i lease-vägarna: `acquireVersionLease` + `failVersionVerificationIfUnleased` kunde blockera till globala `statement_timeout` (57014) under kontention — observerad prod-timeout under quality gate. Fas 4 loggade fixen som P2 (krävde godkännande, stop-ruled banor); ägaren godkände 2026-07-03 ("fixa allt"). | Fas 4 kontrollflöde + Vercel-logg 2026-07-03 | `LEASE_LOCK_TIMEOUT_MS = 5000` sätts transaktionslokalt i båda vägarna; `55P03` (lock_not_available, inkl. cause-kedja via `isLockTimeoutError`) översätts till `null` = "kontenderad/annan ägare, kör inte" resp. watchdog-no-op — semantiskt identiskt med befintlig retur, alla callers hanterar `null`. Lease-tester gröna. Branch `fix/db-lock-contention`. |
| [x] | Fixad | P2 | CI/Vercel-paritetslucka: Vercels prebuild kör `preflight:common` (inkl. `check-bug-backlog`) men GitHub-CI:s `quality`-jobb gjorde det inte → commit kunde bli CI-grön och ändå fälla prod-deployen (hänt: #368 → 7s build-error på master tills #369). | Prod-deploy-fel 2026-07-03 (`sajtmaskin-503dzqv0q`) | Nytt blockerande CI-steg "Preflight parity with Vercel prebuild" (`npm run preflight:common`) i `quality`-jobbet (`.github/workflows/ci.yml`). Branch `fix/db-lock-contention`. |

## Prod-incident chat 1c34592c (2026-07-02, branch `fix/autofix-false-reds-badge-collision`)

Rotorsaksanalys av användarens follow-up-incident (v3 "Repair crashed: fetch failed" + hydration mismatch i preview). Tre kodfixar i samma PR; infra-residualen loggad som öppen rad (M#fly1).

| Klar | Status | Prio | Fynd | Källa | Fix-referens |
| --- | --- | --- | --- | --- | --- |
| [x] | Fixad | P2 | Badge-klassen shadcn∩lucide-namnkrock lämnades medvetet av deterministisk import-repair samtidigt som system-prompten kräver lucide-import för ikoner → `<Badge variant=…>text</Badge>` importerad från `lucide-react` renderade svg-glyf med children (ogiltig HTML i svg) → hydration mismatch; TS2322 kunde bara LLM-loopen ta. | M#badge1 (extern agent-rapport + prod chat 1c34592c v3, kodverifierad) | Användningsmedveten kollisionslösning på alla tre ägar-vägar: (1) ny mekanisk `lucide-shadcn-collision-fixer` i `runAutoFix` (children/`variant=`/`asChild` ⇒ skriv om importen till `@/components/ui/*`; ikon-användning i samma fil alias:as `<XIcon/>`), (2) `ts2304-known-import-fixer` löser krocknamn via `classifyShadcnLucideCollisionUsage` (shadcn/lucide/ambiguous — ambiguous lämnas kvar till LLM), (3) `jsx-checker.fixMissingImports` bucket:ar krocknamn efter användning i st.f. lucide-först. Registrerad i `fixer-registry.ts`; docs-sync `docs/schemas/quality-gate.md`. Tester i `lucide-misuse-fixer.test.ts`, `ts2304-known-import-fixer.test.ts`, `jsx-checker.test.ts`. |
| [x] | Fixad | P1 | `checkTagMatching` i jsx-checker räknade TS-generics med globala DOM-typer som JSX-taggar: `useRef<HTMLCanvasElement \| null>` → falsk `Tag mismatch for <HTMLCanvasElement>: 1 opening vs 0 closing`, eskalerad till preview-blocking verifier-finding → hela v3 markerades failed + skickades till repair i onödan. | Prod chat 1c34592c v3 (fish-pinball.tsx) | `checkTagMatching` exkluderar nu `isGlobalTypeName`-namn (samma guard som `fixMissingImports`); dessutom är `isPreviewCriticalJsxFile`s innehålls-regex case-sensitiv så vanligt HTML-`<canvas>` inte klassas som R3F-`<Canvas>` (filnamns-heuristiken släppte också `canvas` som trigger). 2 nya tester i `jsx-checker.test.ts`. |
| [x] | Fixad | P1 | Repair-routens catch-all hard-failade versionen med `Repair crashed: fetch failed` när post-repair-gaten kastade retrybar `QualityGateUnavailableError` (verify-lane onåbar) — false-RED om infra, medan `/quality-gate`-routen redan hanterade samma fel som 503 + reset-till-pending. | Prod chat 1c34592c v3 + Vercel runtime-logg | `repair/route.ts` catch har nu en typad gren: `QualityGateUnavailableError` ⇒ `resetVersionVerificationToPending` (lease-villkorad, hoppar över vid stale-base) + retrybar 503 `quality_gate_unavailable` i st.f. `failAfterRepair` + 500. Regressionstest i `repair/route.test.ts` ("unreachable verify lane must not fail the version"). |

## Prod-incident retro-3D "Monster 3D Sälja TV-spel i Stockholm AB" (2026-07-02, branch `fix/jsx-tag-mismatch-parse-gate`)

Uppföljare till 1c34592c-klassen ovan: #356:s guard täckte bara globala DOM-typnamn + lokala deklarationer. Kört repro (`.cursor/tmp/jsx-false-positive-check.mjs`) bevisade tre kvarvarande false-positive-vektorer på giltig JSX.

| Klar | Status | Prio | Fynd | Källa | Fix-referens |
| --- | --- | --- | --- | --- | --- |
| [x] | Fixad | P1 | `checkTagMatching`-regexarna felräknade tre GILTIGA JSX-former: (A) nested self-closing i prop — `fallback={<SceneFallback />}` (obligatoriskt API i three-fiber-canvas-dossiern) fick strip-regexens `[^>]*` att äta öppningstaggen → `0 opening vs 1 closing` för `<ThreeCanvasShell>`; (B) arrow-funktion i props på self-closing tagg — `[^>]*` kan inte korsa `>` i `=>` → `<Group … />` räknades som öppning; (C) importerade typnamn i generic-position — `useRef<Group>(null)` + `import type { Group } from "three"` (#356-guarden täckte bara globala DOM-typer + lokala deklarationer). Falsk `Tag mismatch` eskalerades till `preview-blocking:` → `autofix-preview-blocking` verifier-finding → versionen failades + skickades till repair som aldrig konvergerar (koden är redan giltig; checkern re-rapporterar deterministiskt). | Prod-generation "Monster 3D Sälja TV-spel i Stockholm AB" (retro-store, 3D, `components/retro-3d-scene.tsx`) + exekverat repro | Parse-gate i `runJsxChecker`: tag-mismatch-varningar emitteras bara när TS-parsern (`countParseErrors`, samma ägare som `guardFixerSyntax`) bekräftar att filen inte parsar — en genuint obalanserad JSX-tagg gör alltid filen oparsebar. Lazy (parsern körs bara när räkningen flaggat något); vid äkta parse-fel behålls varningstext + `preview-blocking:`-eskalering oförändrade som locator-hints. Regexarna lämnas medvetet orörda (arrow-props/nested JSX är inte regex-territorium — gaten gör deras oprecision ofarlig). 4 nya tester i `jsx-checker.test.ts` (vektor A/B/C + bevarad true-positive). |

## Post-#351 hotfix (extern coach-review, fixade direkt utan Aktiv kö-rad)

Fynd ur den externa post-merge-reviewen av #351, kodverifierade och fixade i samma hotfix-PR (branch `fix/post-351-hardening`) i stället för att först loggas som öppna rader.

| Klar | Status | Prio | Fynd | Källa | Fix-referens |
| --- | --- | --- | --- | --- | --- |
| [x] | Fixad | P1 | False-green vid in-place-filmutation: `updateVersionFiles` bevarade `release_state`/`verification_state`/`promoted_at` (utom `repair_available`-fallet) → en promoted/passed-version som fick nya filer via PUT/PATCH/DELETE `/files` (default-sparvägen när quick-edit-flaggan är av, och F3-fallbacken när quick-edit declinar `integrations_base`) såg fortfarande deploy-grön ut fast verdiktet gällde de GAMLA filerna. | M#uvf1 (extern coach post-#351-review "P1-C", kodverifierad) | Ny option `invalidateVerification` i `updateVersionFiles` (`chat-repository-pg.ts`): materiella user-edits (PUT/PATCH/DELETE i `files/route.ts` via `saveOwnEngineFiles`) resettar raden till `draft`/`pending` + nollar `promoted_at`/`verification_summary`. Idempotenta heal-/materialize-persists (GET `/files`) och deterministiska heal-routes (`normalize-text`/`validate-images`/`validate-css`) behåller default-beteendet medvetet. Route-tester uppdaterade. Branch `fix/post-351-hardening`. |
| [x] | Fixad | P2 | F3-gate-residual efter #351:s Codex-fix: gaten läste `metaEngineBaseVersionId ?? parsedMeta.parentVersionId` — en caller som skickade ENBART `parentVersionId` gate:ades mot den, medan generationen bygger från `engineBaseVersionId ?? preferred` (`resolveFollowUpPreviousFiles`) → gate-bas ≠ byggbas (mismatch-409:an fångar bara fallet där BÅDA skickas). | M#f3g1 (extern coach post-#351-review "P1-A" residual, kodverifierad) | Gate-id deriveras nu ENBART från `engineBaseVersionId` (chat-scopad, annars preferred) — exakt samma resolution som byggbasen; `parentVersionId` är lineage-only. Route-test: only-parentVersionId → gaten inspekterar preferred (412 vid saknade nycklar). `chat-message-stream-post.ts`. Branch `fix/post-351-hardening`. |
| [x] | Fixad | P2 | Degenererad output startade verify-lane: M#dgc-fixen (#351) nollade `autoFixReasons` och satte `verifyPending=false`, men callern startade verify-lane på enbart `autoFixReasons.length === 0` → VM-verify kördes på en terminalt failad version (bortkastat arbete + förvirrande status). | M#dgv1 (extern coach post-#351-review, kodverifierad) | Verify-lane-gaten kräver nu `verifyPending` också (`post-checks.ts`); degenerate-fallet får ett ärligt skipped-kort ("terminalt failad — verify-lane hoppas över"). Test: degenerate → ingen `/quality-gate`-fetch, ingen autofix. Branch `fix/post-351-hardening`. |

## Stabilisering våg 2 — preview-telemetri (2026-07-03, branch `fix/stabilisering-vag2-preview-telemetri`)

Två P2 om att preview-telemetrin ljög grönt: `preview_success` sattes till success FÖRE runtime var uppe, och `preview_url` kunde förbli null när preview-sessionen konsekvent krockade med verify-leasen. Ingen DB-schema-ändring — semantiken bor i skrivare/läsare; ingen `preview-host/`-ändring.

| Klar | Status | Prio | Fynd | Källa | Fix-referens |
| --- | --- | --- | --- | --- | --- |
| [x] | Fixad | P2 | **False-green `preview_success`:** `generation_telemetry.preview_success=true` sattes vid finalize från `!hasPreviewBlockingPreflightErrors` — FÖRE previewförsöket — så verifier-blockerade versioner och versioner vars runtime dog (EADDRINUSE/orphan-loop, hibernerad utan fungerande preview) loggades som gröna. Session-skapad ≠ runtime-överlevde. | M#pv1 (prod-incident chat e8420220/cc10e7de, kodverifierad mot Fly-loggar + `generation_telemetry`) | Ärlig tri-state (`true`=runtime-ready-kvitto, `false`=bekräftat ingen preview, `null`=pending). Finalize-writern slutar hävda `true` (`persist-telemetry.ts` → `hasPreviewBlockingPreflightErrors ? false : null`); bekräftat utfall stämplas via best-effort **monoton** `recordPreviewRuntimeOutcomeForVersion` (`generation-telemetry.ts`: `null→true/false`, `false→true` OK; `true` terminal; idempotent). Runtime-ready-kvittot = preview-host `/status running:true`, stämplat vid kvittopunkterna: `POST /preview-heartbeat` (normalvägen — klienten heartbeat:ar ~25 s medan iframen lever; routen verifierar kvittot med ETT host-`/status`-anrop, one-shot per version/instans), `GET /preview-status` running-grenen (suspect/recovery-vägen), `POST /preview-session` resume-verifierad väg (`PreviewSessionResult.runtimeReady`) och post-finalize (start-fel → `false`); färsk köad boot förblir `null` tills kvitto. Läsarna var redan tri-state-medvetna (scaffold-scoring SAJ-49); `control-stats.mjs` skiljer `preview_ready`/`preview_failed`/`preview_pending` + hård semantik-cutoff (`PREVIEW_SUCCESS_SEMANTIC_CUTOFF` 2026-07-03T14:00Z, pre-cutoff-rader = `legacy_preview_flag`, aldrig i KPI:n), backoffice `_preview_label` = ready/failed/pending. Gammal→ny + historik dokumenterat i `docs/schemas/quality-gate.md`. Tester i `generation-stream-post-finalize.test.ts`, `generation-telemetry.record-preview.test.ts`, `preview-status/route.test.ts`, `preview-session/route.test.ts`. Branch `fix/stabilisering-vag2-preview-telemetri` (PR #377 + review-fixar). |
| [x] | Fixad | P2 | **`preview_url` kunde förbli `null` vid verify-lease-krock:** `updateVersionPreviewUrl` best-effort (`lockTimeoutMs`, #373) hoppade över persist vid rad-kontention mot quality-gates `acquireVersionLease` (`FOR UPDATE`). En version vars preview-session konsekvent sammanföll med verify-leasen fick aldrig sin `preview_url` sparad (prod chat 3120c05c v1: `preview_url=null` trots preview). | M#pv2 (prod-incident chat 3120c05c) | Bounded retry-after-lease-release i `updateVersionPreviewUrl` (`chat-repository-pg.ts`): nya `maxRetries`/`retryDelayMs`; vid kontention (55P03) väntas en kort awaited paus och skrivningen görs om (max N försök, ingen busy-wait) så den landar i en lucka när det korta lease/verify-radlåset släpper. Bara kontention retry:as (saknad rad / annat fel → ge upp direkt); kastar aldrig i het väg (best-effort). `preview-session/route.ts` anropar med `maxRetries: 3, retryDelayMs: 300`. Retry-vägen (kontention→success, missing-row, permanent give-up) täckt av `chat-repository-pg.preview-url.test.ts`. Branch `fix/stabilisering-vag2-preview-telemetri`. |

## Stabilisering våg 3 — F3-entry (2026-07-03, branch `fix/stabilisering-vag3-f3-entry`)

Adjudikering av våg 1-fyndet "F3-flödet körde v8 i F2-lane" (prod chat `cc10e7de`). Den ursprungliga radens gissade mekanism ("env-nycklarna okonfigurerade → finalize-design delade aldrig ut F3-meta") stämde INTE: F3-steg 1 (auto-kickat efter finalize-design) körde korrekt F3. Verklig rotorsak: F3-stadiet bars ENBART av det auto-kickade meddelandet (`BuilderShellContent.onF3Ready` → `meta.lifecycleStage`); när F3-steget slutade tool-only (`tool_only_empty_generation`, awaiting-input) gick användarens "Godkänn förslag"-svar som vanligt follow-up utan lifecycle-meta → servern defaultade till design/F2 (`parse-chat-request-meta.ts`) → v8 körde `fidelity2` och F2-guarden strippade SDK-importerna.

| Klar | Status | Prio | Fynd | Källa | Fix-referens |
| --- | --- | --- | --- | --- | --- |
| [x] | Fixad | P2 | **F3 "Bygg integrationer" loopar: modellen om-föreslår i stället för att bygga efter godkännande** (prod chat `fa6515bc` 2026-07-03): välformade Stripe Checkout-förslag varje runda men ingen kod trots "Godkänn förslag" ×3 (F3-arvet #382 fungerade — modellen om-föreslog); "Avvisa förslag" körde en tyst tom generation och gav ny "Svar krävs"-dialog; sajtens kod hade inga betalytor — förslaget drevs av tier-3-stub-placeholders i genererade env-filer som modellen läste som integrationsbevis. | Ägar-session + chatutdrag + Vercel runtime-logg + `files_json` v2 | **PR #383** (`cb12eec04`, 2026-07-04): approval-rundan drar `suggestIntegration`/`requestEnvVar` ur tool-setet + tvingar codegen med #374:s not-configured-fallback; godkänd provider → dossier-capability (strikt matchning, ej kategori) → `selectDossiersForRequest`; stub-placeholders filtreras ur `detect-integrations` + follow-up-filkontext (inkl. kommentarrader); loop-breaker med rundräknare (tysta OCH tool-only-rundor delar räknare, 2:a upprepningen = lugnt terminalt avslut); reject avslutar F3 utan generation med race-säker consume-bekräftelse; config-notice-instruktionen gate:ad till dossiers som skeppar komponenten. Syskon kvar i Aktiv kö: G#20 + P3-raden om env-only-förslagens nyckeldetaljer. |
| [x] | Fixad | P1 | **F3-flödet körde v8 i F2-lane:** prod chat `cc10e7de` v8 (4a29c7b4) — användarens "Bygg integrationer → Godkänn förslag"-steg genererade riktig Stripe/Resend/toast-kod, men streamen kördes med `previewPolicy: "fidelity2"` och versionen fick `lifecycle_stage: "design"`. Följd: F2-guarden `tier3-sdk-guard-fixer` strippade modellens SDK-importer och tier-3-gaten i deterministic import-repair vägrade (korrekt per F2-kontrakt) återinföra dem → versionen var garanterat röd oavsett repair. | Stabilisering våg 1 rotorsaksanalys 2026-07-03 (prod-DB `generation_telemetry` v8); adjudikerad + korrigerad mekanism i våg 3 | Server-side stage-arv: ny delad kontraktsmodul `src/lib/gen/stream/f3-continuation.ts` — F3-streamen persisterar awaiting-input-frågan som assistant-meddelande MED `f3Continuation`-marker (`generation-stream.ts`, endast `previewPolicy === "fidelity3"`), och follow-up-routen härleder stadiet ur persisterad historik (`resolvePendingF3Continuation` i `chat-message-stream-post.ts`) i stället för att lita på klient-meta. Konservativt arv: ENDAST det direkta svaret på F3-frågan (varje senare user-meddelande konsumerar markern), aldrig plan-mode eller tekniska pass (autofix/preserve-payload), och det ärvda stadiet går genom SAMMA M#818-2 env-gate (412 vid saknade nycklar — finalize-design-striktheten kringgås inte). F2-mute-lagren orörda. Tester: `f3-continuation.test.ts` (marker-walk), route-tester i `[chatId]/stream/route.test.ts` (arv → integrations-lane + gate engaged; 412 bevarad; två negativa: inget arv utan marker resp. efter konsumerad marker), golden-tester i `generation-stream.golden.test.ts` (marker persisteras i F3 tool-only; INTE i F2). PR #382, branch `fix/stabilisering-vag3-f3-entry`. |
