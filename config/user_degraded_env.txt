# Generated user-site preview: degraded / placeholder env policy
#
# This file is human/agent documentation only — it is NOT parsed.
# For the canonical narrative + merge order + F3 stripping, see:
#   config/ai_models/_READ_ME_FIRST.md
#   config/ai_models/00-overview.md
#
# Source-of-truth pointers
# ------------------------
# - Per-key classification (harmless vs tier-3):
#   src/lib/integrations/placeholder-harmless.ts
# - KEY=value fragment files (merged into preview .env.local):
#   config/ai_models/40-harmless-placeholders.env.txt   (always on)
#   config/ai_models/41-tier3-stub-placeholders.env.txt (F2 on, F3 off)
# - Merge logic + provenance layers:
#   src/lib/gen/preview/env-local.ts (harmless -> tier3-stub -> project-preview -> user -> generated)
# - F3 readiness gate (refuses F3 generation until every required real key is set):
#   src/lib/integrations/tier3-build-spec.ts -> validateTier3Readiness()
# - Parity tests that keep these surfaces in sync:
#   src/lib/integrations/placeholder-harmless.parity.test.ts
#   src/lib/integrations/registry-parity.test.ts
#
# Edge-case note (Redis / SQL / Auth0)
# ------------------------------------
# Generated code that creates Redis/DB clients at module scope and connects
# eagerly may still time out on placeholder URLs. Prefer lazy clients or skip
# init when the URL equals a known preview sentinel. Postgres-shaped URLs in
# `41-` are local-only so `next dev` doesn't crash on `import postgres`.
#
# Different target than `config/env-policy.json`
# -----------------------------------------------
# `config/env-policy.json` classifies the *Sajtmaskin app's* runtime env
# (BACKOFFICE_PASSWORD, VERCEL_TOKEN, etc.). Don't conflate the two — they
# describe different processes.
