===================================================================
  SAJTMASKIN — EGNA KOMMANDON (snabbreferens)
  Senast uppdaterad: 2026-04-21 (städ efter v2-omstart av dossier-pipen)
===================================================================

---  TEMPLATE-PIPELINE (LEGACY — avvecklad 2026-04-17)  ---

# Den gamla template-library-pipen (scrape Vercel -> generated.json -> runtime
# guidance) har avvecklats helt. data/external-template-pipeline/, scripts/
# template-library/, scaffold_cli.py och rebuild_artifacts.py ligger nu i
# legacy-stuff/ utanfor repot. Backoffice-flikarna "Template pipeline" och
# "Artifacts pipeline" ar avregistrerade. 11 npm scripts borttagna.
#
# Per-integration-guidance hanteras nu helt av dossier-pipen v2 (data/dossiers/).

---  SCAFFOLDS  ---

npm run scaffolds:validate                 # Validera manifest-filer (vitest)

# Note (2026-04-17): scaffolds:status / import / hydrate / build / embeddings /
# eval / verify / all / curate / promote har avvecklats tillsammans med
# scaffold_cli.py och external-template-pipeline. Variant-underhall sker via
# scripts nedan.

---  SCAFFOLD-VARIANTER  ---

npm run scaffolds:variant-embeddings       # Embeddings for alla 21 variants (OpenAI)
npm run scaffolds:variant-patterns         # AI-curate signaturePatterns for alla variants
npm run scaffolds:variant-patterns:dry     # Forhandsgranska utan att skriva

---  DOSSIERS v2 (legoklossar: integrationer + UI-sektioner)  ---

# Inga embeddings, inget rebuild. Runtime walkar data/dossiers/{hard,soft}/
# direkt. Capability-driven urval (deterministic) — det brief-LLM:n deklarerar
# i requestedCapabilities -> matchande dossier injiceras 1:1.
#
# Pool idag (2026-04-21): 3 dossiers — stripe-checkout, openai-chat, pricing-tier-table.
# Plan for utbyggnad: docs/plans/active/M-medium-hard-layer.md (M2).

# AI-kurera EN dossier fran ett klonat referens-repo:
npm run dossiers:curate -- --reference=<reference-id> --class=<hard|soft> --id=<dossier-id>
# Exempel:
#   npm run dossiers:curate -- --reference=auth-clerk-authentication-starter --class=hard --id=clerk

# Klonade referens-repon ligger i data/template-references/repos/.
# Capability-map regenereras via backoffice "Dossiers" -> "Capability map" -> "Bygg om".

# AVVECKLADE v1-kommandon (existerar inte langre):
# - dossiers:rebuild, dossiers:embeddings, dossiers:index
# - dossiers:scrape, dossiers:enrich, dossiers:import, dossiers:queue
# - dossiers:promote, dossiers:clone-repos, dossiers:extract-files
# - dossiers:recommend:merge, dossiers:recommend:force
# - smoke-select.ts
# Allt detta ligger i archive/dossiers-legacy-2026-04-20/.

---  SHADCN-REGISTER  ---

npm run shadcn:sync                        # Kontrollera shadcn-registret mot components.json
npm run shadcn:sync:write                  # Synka + skriv uppdateringar
npm run shadcn:sync-examples               # Uppdatera data/shadcn-examples/

---  DATABAS  ---

npm run db:init                            # Bootstrapa tabeller + migrationer (vagrar mot prod)
npm run db:migrate                         # Kor SQL-migrationer fran src/lib/db/migrations/
npm run db:push                            # Drizzle-kit push (guardad mot prod)
npm run db:check                           # Read-only sanity-koll av kerntabeller
npm run db:rows                            # Read-only COUNT(*) per utvald tabell

---  V0-MALLAR (builderns Mallar-tab, lokal data)  ---

npm run templates:local:refresh            # Sync + validering
npm run templates:local:refresh:embeddings # Sync + validering + regenerera embeddings
npm run templates:embeddings               # Bara embeddings

---  VERCEL-TEMPLATES DISCOVERY (LEGACY)  ---

# Den gamla Playwright-discovery-pipen (references:discover*, dossiers:scrape,
# dossiers:enrich) ar avvecklad 2026-04-20. e2e/vercel-templates/scrape-catalog-light.spec.ts
# finns kvar som referens-spec men koppling till v1-pipen ar dod (header-not).
# For nya dossiers: klona repo manuellt till data/template-references/repos/
# och kor `npm run dossiers:curate`.

---  DEV / TEST / TYPECHECK  ---

npm run dev                                # Starta dev-server (kor predev automatiskt)
npm run typecheck                          # TypeScript-kontroll
npm run lint                               # ESLint
npm run test:ci                            # Vitest i CI-lage
npm run devtest                            # typecheck + validate + test + lint

---  EVAL  ---

npm run eval                               # Kor eval-harnessen

---  BACKOFFICE  ---

npm run backoffice                         # Starta Streamlit backoffice (scaffolds, dossiers, observability, autofix)

---  ENV-HANTERING  ---

python scripts/env/manage_env.py           # Interaktiv env-kontrollpanel
python scripts/env/manage_env.py audit     # Audit env-konfiguration
python scripts/env/manage_env.py audit --strict  # Strict audit

---  OBSERVABILITY  ---

# Prometheus-metrics endpoint:
curl http://localhost:3000/api/metrics -H "Authorization: Bearer $SAJTMASKIN_METRICS_TOKEN"
# Streamlit-panel: backoffice -> "Observability"

---  VIKTIGA ENV-FLAGGOR  ---

SAJTMASKIN_DOSSIER_PIPELINE=true|false              # Aktivera dossier-injektion (default: on, sätt false/0 för opt-out)
SAJTMASKIN_DEFER_EXTRA_ROUTES_ON_INIT=true|false    # Defer extra routes till shells i init-generering
SAJTMASKIN_STRICT_GENERATED_ARTIFACTS=true|false    # Krav genererade JSON-artefakter
SAJTMASKIN_DEFAULT_THINKING=true                    # Server-side default for reasoning/thinking
SAJTMASKIN_STRICT_SYSTEM_PROMPT_ASSERT=1            # Hard fail vid prompt-korruption (anvand i CI)
SAJTMASKIN_AUTO_REPAIR_BUILD_ERROR=1                # Auto-repair vid VM build-error (default: dev/preview=on, prod=off)
SAJTMASKIN_BUILD_SPEC_ENABLED=true|false            # Bara SSE-meta-fält, inte BuildSpec-koden (alltid pa)
SAJTMASKIN_VERIFIER_PASS=1|0                        # Pa/av (default: pa, sta)
SAJTMASKIN_PROMPT_DUMP=1                            # Dumpa promptar till data/prompt-dumps/
GENERATIONSLOGG=1                                   # Skriv logs/generationslogg/
SAJTMASKIN_DEV_LOG=true                             # Bredare dev-logg

===================================================================
  Kanonisk kalla: package.json (scripts), scripts/README.md
===================================================================
