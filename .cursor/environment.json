{
  "name": "sajtmaskin-node22-cloud",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "install": "npm ci --no-audit --no-fund && npm run embeddings:ensure && python3 -m pip install --disable-pip-version-check --user --break-system-packages -r requirements.backoffice.dev.txt",
  "start": "node scripts/db/db-init.mjs || true",
  "terminals": [
    { "name": "dev", "command": "npm run dev" },
    { "name": "typecheck", "command": "npm run typecheck" },
    { "name": "lint", "command": "npm run lint" }
  ]
}
