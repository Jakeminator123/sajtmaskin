// AUTO-GENERERAD av scripts/canvas/build-llm-flow-canvas.mjs - redigera inte for hand.
// Lagras som .txt sa repots tsc/eslint aldrig ror den. Rendera lokalt med:
//   node scripts/canvas/sync-to-cursor.mjs
import {
  Callout,
  Card,
  CardBody,
  CardHeader,
  Grid,
  H1,
  H2,
  Pill,
  Row,
  Stack,
  Stat,
  Table,
  Text,
  type StatTone,
  type TableRowTone,
} from "cursor/canvas";

type Status = "done" | "ongoing" | "shaky" | "blocked";

type Process = {
  name: string;
  status: Status;
  statusLabel: string;
  tone: TableRowTone;
  openBugs: number;
  openByPrio: { P0: number; P1: number; P2: number; P3: number; other: number };
  churn: number;
  summary: string;
  note: string;
};

type CanvasData = {
  meta: { repo: string; commit: string; commitDate: string; sinceDays: number };
  totals: {
    processes: number;
    openP0: number;
    openP1: number;
    openP2: number;
    openP3: number;
    blocked: number;
    shaky: number;
    evalExactHitPct: number | null;
  };
  processes: Process[];
  shaky: { name: string; statusLabel: string; tone: TableRowTone; reason: string }[];
  topOpenRisks: { prio: string; blocker: boolean; fynd: string }[];
  topOpenRisksOmitted: number;
  evals: {
    exactHitPct: number | null;
    acceptableHitPct: number | null;
    total: number | null;
    errors: number | null;
    rows: { id: string; ok: boolean; method: string; confidence: string }[];
  } | null;
  phases: string[];
  legend: string[];
};

const DATA: CanvasData = {
  "meta": {
    "repo": "Jakeminator123/sajtmaskin",
    "commit": "69cddb4f4",
    "commitDate": "2026-08-22",
    "sinceDays": 14
  },
  "totals": {
    "processes": 8,
    "openP0": 0,
    "openP1": 2,
    "openP2": 13,
    "openP3": 8,
    "blocked": 1,
    "shaky": 3,
    "evalExactHitPct": null
  },
  "processes": [
    {
      "name": "LLM-faser & runtime-sanning",
      "status": "ongoing",
      "statusLabel": "Pagar",
      "tone": "info",
      "openBugs": 0,
      "openByPrio": {
        "P0": 0,
        "P1": 0,
        "P2": 0,
        "P3": 0,
        "other": 0
      },
      "churn": 34,
      "summary": "Överblick: manifestets buildProfiles + phaseRouting, resolverade fixer/verifier per tier, inläst DEFAULT_MODEL_ID från catalog.ts, fas-readme under logs/llm-segmentts-and-index/rea",
      "note": "Aktiv: 34 commits / 14d"
    },
    {
      "name": "Codegen core",
      "status": "shaky",
      "statusLabel": "Skakigt",
      "tone": "warning",
      "openBugs": 2,
      "openByPrio": {
        "P0": 0,
        "P1": 0,
        "P2": 2,
        "P3": 0,
        "other": 0
      },
      "churn": 9,
      "summary": "Ordning och lista över Core Rules-fragment (= statisk systemprompt) för own-engine. Själva texterna ligger som filer under prompt-core/. Sidan husar också tre kringliggande config-",
      "note": "2 oppna backlog-rader (P0:0 P1:0 P2:2 P3:0)"
    },
    {
      "name": "prompt-core",
      "status": "done",
      "statusLabel": "Klart",
      "tone": "success",
      "openBugs": 0,
      "openByPrio": {
        "P0": 0,
        "P1": 0,
        "P2": 0,
        "P3": 0,
        "other": 0
      },
      "churn": 2,
      "summary": "Markdown-fragment (= Core Rules) som concateneras till statisk systemprompt (se codegen-core-manifest.json). Sätter stack, output-format, behavior, komponentkontrakt. Aldrig per-re",
      "note": "2 commits / 14d"
    },
    {
      "name": "ai_models",
      "status": "shaky",
      "statusLabel": "Skakigt",
      "tone": "warning",
      "openBugs": 2,
      "openByPrio": {
        "P0": 0,
        "P1": 0,
        "P2": 1,
        "P3": 1,
        "other": 0
      },
      "churn": 49,
      "summary": "Modellregister och LLM-kedja: byggprofiler, prompt assist, brief/defaults, phase-routing för planner/fixer/verifier, repair-passgränser, tokenbudgetar, route-timeouts och placehold",
      "note": "2 oppna backlog-rader (P0:0 P1:0 P2:1 P3:1)"
    },
    {
      "name": "Scaffolds: titta & justera",
      "status": "shaky",
      "statusLabel": "Skakigt",
      "tone": "warning",
      "openBugs": 7,
      "openByPrio": {
        "P0": 0,
        "P1": 0,
        "P2": 3,
        "P3": 4,
        "other": 0
      },
      "churn": 46,
      "summary": "Samlad scaffold-vy (konsoliderad 2026-07-21 från Runtime scaffolds + Scaffolds + Mental modell): dynamiska filträd direkt från respektive scaffold-rot, kopiering till webbläsarens ",
      "note": "7 oppna backlog-rader (P0:0 P1:0 P2:3 P3:4)"
    },
    {
      "name": "Scaffolds & varianter: skapa, redigera, klona, ta bort",
      "status": "ongoing",
      "statusLabel": "Pagar",
      "tone": "info",
      "openBugs": 0,
      "openByPrio": {
        "P0": 0,
        "P1": 0,
        "P2": 0,
        "P3": 0,
        "other": 0
      },
      "churn": 76,
      "summary": "Backoffice-yta för runtime-scaffolds och deras scaffold-variants: tradvy scaffold -> varianter, create scaffold-shell via kloning av befintlig scaffold, CRUD för variant-JSON, bero",
      "note": "Aktiv: 76 commits / 14d"
    },
    {
      "name": "Preview och versioner",
      "status": "blocked",
      "statusLabel": "Blockerat",
      "tone": "danger",
      "openBugs": 14,
      "openByPrio": {
        "P0": 0,
        "P1": 2,
        "P2": 8,
        "P3": 4,
        "other": 0
      },
      "churn": 21,
      "summary": "Tier-2 VM-preview, F2/F3-livscykel (`engine_versions.lifecycle_stage`), repair/promotion, prompt-dumps och logg/telemetri för buildern. `done` kan bära `previewPending` och `previe",
      "note": "BLOCKER: `SM-007` **Domänköpets restlista är en releasegrind, inte en aktiv incident:** tolv fynd …"
    },
    {
      "name": "Eval",
      "status": "ongoing",
      "statusLabel": "Pagar",
      "tone": "info",
      "openBugs": 0,
      "openByPrio": {
        "P0": 0,
        "P1": 0,
        "P2": 0,
        "P3": 0,
        "other": 0
      },
      "churn": 30,
      "summary": "Backoffice-yta för den canonicala evalen (`npm run eval`). Ett läge (gratis / smoke / full), en knapp. Scaffold-lanen läser senaste lokala JSON-rapport. Codegen-lanen läser data/ev",
      "note": "Aktiv: 30 commits / 14d"
    }
  ],
  "shaky": [
    {
      "name": "Preview och versioner",
      "statusLabel": "Blockerat",
      "tone": "danger",
      "reason": "BLOCKER: `SM-007` **Domänköpets restlista är en releasegrind, inte en aktiv incident:** tolv fynd …"
    },
    {
      "name": "Scaffolds: titta & justera",
      "statusLabel": "Skakigt",
      "tone": "warning",
      "reason": "7 oppna backlog-rader (P0:0 P1:0 P2:3 P3:4)"
    },
    {
      "name": "ai_models",
      "statusLabel": "Skakigt",
      "tone": "warning",
      "reason": "2 oppna backlog-rader (P0:0 P1:0 P2:1 P3:1)"
    },
    {
      "name": "Codegen core",
      "statusLabel": "Skakigt",
      "tone": "warning",
      "reason": "2 oppna backlog-rader (P0:0 P1:0 P2:2 P3:0)"
    }
  ],
  "topOpenRisks": [
    {
      "prio": "P1",
      "blocker": true,
      "fynd": "`SM-007` **Domänköpets restlista är en releasegrind, inte en aktiv incident:** tolv fynd (a)–(l) blir verklig…"
    },
    {
      "prio": "P1",
      "blocker": false,
      "fynd": "`SM-035` **Fly preview-host: `npm install --no-audit --include=dev` exit 254:** fem träffar i tre chattar sed…"
    },
    {
      "prio": "P2",
      "blocker": false,
      "fynd": "`SM-070` **Live-review-aktivering är en grind, inte tre lösa buggar:** [#1052](https://github.com/Jakeminator…"
    },
    {
      "prio": "P2",
      "blocker": false,
      "fynd": "`SM-001` **Repair/autofix skriver över samma version utan återgångspunkt:** existing-version-vägen uppdaterar…"
    },
    {
      "prio": "P2",
      "blocker": false,
      "fynd": "`SM-002` **Klient-autofix tappar konkret install-/repairkontext:** filtreringen som är gjord för serverrepair…"
    },
    {
      "prio": "P2",
      "blocker": false,
      "fynd": "`SM-003` **Deploy-repair no-op:ar på orelaterad `repair_available`:** idempotenskontrollen tittar på globalt …"
    },
    {
      "prio": "P2",
      "blocker": false,
      "fynd": "`SM-012` **Scaffold-valet kan bryta manifestets `allowedBuildIntents`:** efter avsiktlig promotion görs ingen…"
    },
    {
      "prio": "P2",
      "blocker": false,
      "fynd": "`SM-013` **Misslyckad template-init lämnar \"Läser in mallen\"-laddläget kvar tills omladdning:** `pendingTempl…"
    },
    {
      "prio": "P2",
      "blocker": false,
      "fynd": "`SM-014` **Tier-2-iframens `onLoad` avtäcker previewn före runtime är redo:** rå `onLoad` sätter `iframeLoadi…"
    },
    {
      "prio": "P2",
      "blocker": false,
      "fynd": "`SM-015` **Dämpad normaltext klarar inte AA:** `muted-foreground/70` och `text-gray-500` ligger under 4,5:1 m…"
    },
    {
      "prio": "P2",
      "blocker": false,
      "fynd": "`SM-032` **Sajtmaskins CSP blockerar Google Maps Places på startsidan:** prodloggen visar `script-src-elem` m…"
    },
    {
      "prio": "P2",
      "blocker": false,
      "fynd": "`SM-033` **Wizard-rutterna slår i sina egna tidsgränser:** `/api/wizard/competitors` gav 504 vid `maxDuration…"
    }
  ],
  "topOpenRisksOmitted": 3,
  "evals": null,
  "phases": [
    "FAS 1 - Intent: prompt, brief, scaffold/variant/dossiers, route plan",
    "FAS 2 - Orkestrering & build: system prompt, codegen, finalize, autofix, verifier, persist",
    "FAS 3 - Preview & deploy: preview-host/VM, quality-gate, repair, deploy"
  ],
  "legend": [
    "Klart",
    "Pagar",
    "Skakigt",
    "Blockerat"
  ]
};

const STATUS_TONE: Record<Status, TableRowTone> = {
  done: "success",
  ongoing: "info",
  shaky: "warning",
  blocked: "danger",
};

const CALLOUT_TONE: Record<string, "info" | "success" | "warning" | "danger" | "neutral"> = {
  success: "success",
  info: "info",
  warning: "warning",
  danger: "danger",
  neutral: "neutral",
};

export default function LLMFlowCanvas() {
  const d = DATA;
  return (
    <Stack gap={24} style={{ maxWidth: 1100, margin: "0 auto", padding: 24 }}>
      <Stack gap={6}>
        <H1>LLM-flode - status mot master</H1>
        <Text tone="secondary">
          {d.meta.repo} {"·"} commit {d.meta.commit}
          {d.meta.commitDate ? " (" + d.meta.commitDate + ")" : ""} {"·"} auto-genererad fran repo-signaler
        </Text>
        <Row gap={8} wrap>
          {d.legend.map((l) => (
            <Pill key={l}>{l}</Pill>
          ))}
        </Row>
      </Stack>

      <Grid columns={6} gap={12}>
        <Stat value={String(d.totals.processes)} label="Processer" />
        <Stat value={String(d.totals.blocked)} label="Blockerade" tone={(d.totals.blocked ? "danger" : "success") as StatTone} />
        <Stat value={String(d.totals.shaky)} label="Skakiga" tone={(d.totals.shaky ? "warning" : "success") as StatTone} />
        <Stat value={String(d.totals.openP0)} label="Oppna P0" tone={(d.totals.openP0 ? "danger" : "success") as StatTone} />
        <Stat value={String(d.totals.openP1)} label="Oppna P1" tone={(d.totals.openP1 ? "warning" : "success") as StatTone} />
        <Stat
          value={d.totals.evalExactHitPct == null ? "-" : d.totals.evalExactHitPct + "%"}
          label="Eval exact-hit"
          tone={"info" as StatTone}
        />
      </Grid>

      {d.phases.length > 0 && (
        <Stack gap={8}>
          <H2>Faskedja</H2>
          <Stack gap={6}>
            {d.phases.map((p, i) => (
              <Row key={i} gap={8} align="start">
                <Pill>{i + 1}</Pill>
                <Text>{p}</Text>
              </Row>
            ))}
          </Stack>
        </Stack>
      )}

      <Stack gap={10}>
        <H2>Processer</H2>
        <Table
          headers={["Process", "Status", "Oppna buggar", "Churn", "Not"]}
          columnAlign={["left", "left", "right", "right", "left"]}
          rowTone={d.processes.map((p) => STATUS_TONE[p.status])}
          rows={d.processes.map((p) => [
            p.name,
            p.statusLabel,
            String(p.openBugs),
            String(p.churn),
            p.note,
          ])}
        />
      </Stack>

      {d.shaky.length > 0 && (
        <Stack gap={10}>
          <H2>Skakigast just nu</H2>
          {d.shaky.map((s) => (
            <Callout key={s.name} tone={CALLOUT_TONE[s.tone] ?? "neutral"} title={s.name + " - " + s.statusLabel}>
              {s.reason}
            </Callout>
          ))}
        </Stack>
      )}

      {d.topOpenRisks.length > 0 && (
        <Stack gap={10}>
          <H2>Oppna huvudrisker (backlog)</H2>
          <Text tone="secondary">
            Oppna P0/BLOCKER/P1/P2-rader direkt ur BUG-SWARM-BACKLOG.md. Mappas inte alltid till en enskild
            process - visas globalt sa inget tappas. P0 ar hogsta allvar och visas alltid overst.
          </Text>
          <Table
            headers={["Prio", "Typ", "Fynd"]}
            columnAlign={["left", "left", "left"]}
            rowTone={d.topOpenRisks.map((r) => (r.prio === "P0" || r.blocker ? "danger" : r.prio === "P1" ? "warning" : "info") as TableRowTone)}
            rows={d.topOpenRisks.map((r) => [r.prio, r.blocker ? "BLOCKER" : "oppen", r.fynd])}
          />
          {d.topOpenRisksOmitted > 0 && (
            <Text size="small" tone="tertiary">
              {"+" + d.topOpenRisksOmitted + " fler lagre-prioriterade rader (se BUG-SWARM-BACKLOG.md) - inga P0 doljs."}
            </Text>
          )}
        </Stack>
      )}

      {d.evals && d.evals.rows.length > 0 && (
        <Stack gap={10}>
          <H2>Eval-scorecard (scaffold-selection, baseline-master)</H2>
          <Text tone="secondary">
            exact-hit {d.evals.exactHitPct ?? "-"}% {"·"} acceptable {d.evals.acceptableHitPct ?? "-"}% {"·"}{" "}
            {d.evals.total ?? d.evals.rows.length} prompts {"·"} fel: {d.evals.errors ?? 0}
          </Text>
          <Table
            headers={["Prompt", "Traff", "Metod", "Confidence"]}
            columnAlign={["left", "left", "left", "left"]}
            rowTone={d.evals.rows.map((r) => (r.ok ? "success" : "warning") as TableRowTone)}
            rows={d.evals.rows.map((r) => [r.id, r.ok ? "ja" : "nej", r.method, r.confidence])}
          />
        </Stack>
      )}

      <Card>
        <CardHeader>Hur den uppdateras</CardHeader>
        <CardBody>
          <Stack gap={6}>
            <Text size="small" tone="secondary">
              Ogonblicksbild vid korningstillfallet. Fil-kallorna (config/backoffice/domain-map.json,
              BUG-SWARM-BACKLOG.md, eval-rapporten) lases fran working tree, medan commit {d.meta.commit}
              och churn speglar committat lage (git log).
            </Text>
            <Text size="small" tone="secondary">
              Uppdateras MANUELLT - auto-PR-workflowen togs bort i #191. Kor om for farsk status:
              npm run canvas:build (sparad .txt + .json) eller npm run canvas:open (HTML).
            </Text>
            <Text size="small" tone="tertiary">
              Lokalt i Cursor: git pull {"→"} node scripts/canvas/sync-to-cursor.mjs {"→"} oppna canvasen bredvid chatten.
            </Text>
          </Stack>
        </CardBody>
      </Card>
    </Stack>
  );
}
