"use client";

/**
 * Public entry for versions panel. Row lifecycle badges read per-row busStatus
 * (event-bus projection) inside ./version-history/version-history-view.
 */
export { VersionHistory } from "./version-history/version-history-view";
