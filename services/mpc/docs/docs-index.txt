Sajtmaskin Documentation Index
==============================

Single källa: all dokumentation bor i `services/mpc/docs` och läses via MCP.

Så läser du
-----------
- I Cursor MCP: använd `search_docs` (för att hitta) och `get_doc` (för att läsa).
- Top-level-filer i `docs/` exponeras som `docs://local/{name}`.
- Alla filer i underkataloger kan läsas via relativa sökvägar med `get_doc`.

Prioritet
---------
1. WHEN-TO-USE-MCP.md — När och hur du ska använda MCP-servern (LÄS FÖRST!)
2. quick-reference.txt — snabböversikt och vanliga mönster
3. overview.txt — server/struktur
4. error-playbook.txt — felrapportering/loggning
5. local/*.txt — projekt-/team-specifika anteckningar

Lokala quickrefs (docs://local/local/...)
-----------------------------------------
- local/ARCHITECTURE-AI-FLOW
- local/README_sources_and_freshness
- local/ai_sdk_tool_calls_structured_output_quickref
- local/v0_model_api_openai_compat_format
- local/v0_platform_api_json_formats
- local/vercel_ai_gateway_openai_compat_quickref
- local/oauth_sammanfattning_sajtmaksin
- local/session-summary
- local/cursor-mcp-config
- local/v0doc.dev — v0 API docs: Model API + Platform API overview, endpoints, SDK usage
- local/shadcn-ui-integration — shadcn/ui setup: components.json, lib/utils.ts, CSS vars, deps

Lägga till eller flytta docs
----------------------------
- Lägg nya filer som .txt/.md/.json under `services/mpc/docs/local/`.
- Starta om MPC-servern om den körs (`npm run mpc`) så indexet uppdateras.

Källor via `search_docs` (source-param)
---------------------------------------
- local: allt under `docs/` (inkl. dina filer och scraped innehåll)
- ai-sdk / openai / vercel / v0: scraped-mappar `docgrab__*` i `docs/`

Tips
----
- Ingen README/MD i roten behövs; håll allt i `services/mpc/docs`.
- Lägg inte hemligheter i docs/.
