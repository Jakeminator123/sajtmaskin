2026-03-27

Own-engine / gen / mcp:
- own-engine = den kanoniska produktbanan och Sajtmaskins "svarta låda" for generation.
- src/lib/gen = delad genereringskarna som own-engine bygger pa; inte en separat motor.
- src/lib/mcp = adapter- och verktygslager for programmatisk atkomst till own-engine-resultat (preview, sandbox, generate-site), inte platsen for core promptlogik.

MCP-mappen:
- src/lib/mcp/runtime-url.ts ar aktivt inkopplad i own-engine/sandbox-floden och dess riktade tester passerar.
- src/lib/mcp/generate-site.ts och src/lib/mcp/local-engine.ts ser ut som bra seams for verktyg/agent-anrop, men ar mindre bevisade i produktflodet an runtime-url.ts.
- Non-SSE own-engine-generation ar nu extraherad till src/lib/own-engine/generate-site-from-prompt.ts, sa mcp/generate-site inte langer behover aga core-generationlogiken sjalv.

Template-init:
- POST /api/template ar nu own-engine-baserad och returnerar engine-chat-id i stallet for att ga via V0 Platform.
- Gamla template-cacheposter som inte pekar pa en engine-chat ignoreras i stallet for att aterintroducera legacy-vagen.

Vidare utfasing:
- POST /api/v0/chats/init ar nu own-engine-baserad import av repo/ZIP till forsta engine-version.
- Direct registry-init via API ar avvecklad; UI faller tillbaka till vanlig own-engine-promptstart.
- src/lib/v0.ts, src/lib/v0/ och npm-paketet v0-sdk ar borttagna ur appflodet.
- Den tillfalliga feature-flaggan SAJTMASKIN_V0_PLATFORM_ENABLED togs bort igen nar den inte langre styrde nagon aktiv appvag.
- Kvarvarande admin-cleanup mot V0 API togs bort, tillsammans med de sista runtime-/health-/trace-sporen av V0_API_KEY.

OpenClaw:
- OpenClaw lever idag separat under src/lib/openclaw/ och src/app/api/openclaw/.
- Ingen direkt runtime-koppling mellan OpenClaw och src/lib/mcp idag.
- src/lib/mcp kan pa sikt vara en bra adapteryta om OpenClaw senare ska trigga own-engine programmatisk generation eller runtime-start.

Dokumentstädning:
- Historiska v0-filer togs bort: docs/handoffs/v0-fasning-och-forenkling.md, docs/handoffs/gpt-external-review.txt och docs/architecture/v0-platform-sunset-audit.md.
- Kvarvarande handoff-filer pekar nu till aktiva arkitektur-/handoff-dokument i stallet for borttagna historikfiler.

Kontraktsforenkling / integrationer (del 4):
- `buildContractClarificationQuestion` har nu `previewFirst`-parameter (default `true`) — returnerar `null` direkt i sandbox/preview-flodet. Blockering bara vid explicit `previewFirst: false` (produktion).
- `inferPreGenerationContracts` invariant: `unresolvedDecisions` alltid tom i default-flode; dokumenterat i JSDoc.
- Stripe env: fixat mismatch `STRIPE_PUBLISHABLE_KEY` -> `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` i registry och PROVIDER_RULES for paritet med placeholder-filen.
- Generated-site env konsoliderat: `loadPlaceholderRecord` + `formatDotenvBody` exporteras fran `sandbox-env-local.ts`; `project-scaffold.ts` importerar dessa istallet for att duplicera logik. Lazy import av `project-env-vars` undviker transitiv DB-laddning.
- MCP/own-engine sandbox (`generateOwnEngineSiteFromPrompt`) anvander nu `buildSandboxEnvLocalContents` — samma env-merge som builder-sandbox; Fidelity 2-paritet sakerstelld.
- `registry-parity.test.ts`: ny test att core registry envVars ar tackta i placeholder-filen.
- `pre-generation-contracts.test.ts`: ny test att infererade Stripe-env anvander NEXT_PUBLIC_ prefix.
- `contract-clarification.test.ts`: tva nya tester — previewFirst=true (default) returnerar null; previewFirst=false med auth blockerar.
- Verifierat med `npx vitest run src/lib/gen/pre-generation-contracts.test.ts src/lib/gen/contract-clarification.test.ts src/lib/gen/sandbox-env-local.test.ts src/lib/gen/project-scaffold.test.ts src/lib/integrations/integration-manifest.test.ts src/lib/integrations/registry-parity.test.ts` -> 6 testfiler, 34 tester, allt passerade.
- `npx tsc --noEmit` -> rent (exit 0).

Autofix / quality-gate hardening:
- `dep-completer` ar synkad med baseline for `zod` (nu `^4`) och har regressions-test mot `project-scaffold.ts` PACKAGE_JSON.
- `validateAndFix` rapporterar nu explicit `pipeline-error` i stallet for falskt gront resultat nar valideringspipen kastar.
- `runLlmFixer` har strict/partial-signal med `requiredFiles`; partial fixar kan fortfarande provas och valideras, men markeras tydligt i logg.
- `quality-gate` i sandbox loggar nu stage-specifikt med tydligare output-klippning och explicit install-stage (`npm install`) innan typecheck/build/lint.
- `sandbox_disabled` persisteras i builder-state och visas som synlig varning i previewn (inte bara dev-toast).
- `runtime-url` kommentar for preview mode-default ar synkad med faktisk kod (`dev_then_build`), och `docs/ENV.md` pekar pa `sandbox-env-local.ts` for merged generated-site env.
- Verifierat med:
  `npm run test:ci -- src/lib/gen/autofix/dep-completer.test.ts src/lib/gen/autofix/validate-and-fix.test.ts src/lib/hooks/chat/post-checks.test.ts src/lib/mcp/runtime-url-sandbox-output.test.ts src/lib/integrations/integration-manifest.test.ts`
  -> 5 testfiler, 31 tester, allt passerade.
- Verifierat med:
  `npm run baseline-deps:verify`
  -> OK (46 poster i PACKAGE_JSON-baseline, npm registry).
