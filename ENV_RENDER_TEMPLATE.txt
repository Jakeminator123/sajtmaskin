# =============================================================================
# SAJTMASKIN - Render Production Checklist
# =============================================================================
# 
# Set these variables in Render Dashboard → Environment Variables.
# All required variables are marked with (REQUIRED).
#
# RENDER SETTINGS:
# - Build Command:  npm install && npm run build
# - Start Command:  npm run start
# - Health Check:   /api/health
# - Root Directory: . (project root)
#
# =============================================================================

# CORE (REQUIRED)
NODE_ENV=production
DATA_DIR=/var/data
NEXT_PUBLIC_BASE_URL=https://YOUR-APP.onrender.com
JWT_SECRET=<generate-a-strong-secret>

# AI SERVICES (REQUIRED)
V0_API_KEY=<from-v0.dev/settings>
OPENAI_API_KEY=<from-platform.openai.com/api-keys>

# STORAGE (REQUIRED)
BLOB_READ_WRITE_TOKEN=<from-vercel.com/dashboard/stores>

# AUTHENTICATION
GOOGLE_CLIENT_ID=<from-google-cloud-console>
GOOGLE_CLIENT_SECRET=<from-google-cloud-console>
GITHUB_CLIENT_ID=<prod-github-oauth-app>
GITHUB_CLIENT_SECRET=<prod-github-oauth-app>

# PAYMENTS (use LIVE keys for production)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=<pk_live_...>
STRIPE_SECRET_KEY=<sk_live_...>
STRIPE_WEBHOOK_SECRET=<whsec_...>

# CACHING (optional but recommended)
REDIS_HOST=<redis-host>
REDIS_PORT=<port>
REDIS_USERNAME=default
REDIS_PASSWORD=<password>

# EXTERNAL APIS (optional)
UNSPLASH_ACCESS_KEY=<from-unsplash.com>
VERCEL_API_TOKEN=<from-vercel.com/account/tokens>

# ADMIN ACCOUNT (REQUIRED - change from defaults!)
SUPERADMIN_EMAIL=<your-real-email>
SUPERADMIN_PASSWORD=<strong-password>

# DISABLE IN PRODUCTION
MCP_ENABLED=false
