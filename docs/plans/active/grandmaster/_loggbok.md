# Grandmaster — loggbok & protokoll

> Orchestrator-loggbok (ej en planfil). Jake + orchestrator-agenten för korta notiser här medan grandmaster-planen körs. Master-plan: [`00-master-plan.md`](00-master-plan.md). Committad för handoff-kontinuitet — nästa agent läser denna + [`docs/handoffs/2026-06-21-grandmaster-execution-handoff.md`](../../../handoffs/2026-06-21-grandmaster-execution-handoff.md) (aktuell; 2026-06-18-handoffen = historik).

## Hur den används

- **Orchestratorn** (den långlivade Cursor-sessionen) äger filen. Builder-agenter loggar inte själva (undviker merge-konflikter i parallella branches) — de rapporterar tillbaka, orchestratorn skriver hit.
- **Efter varje "sprint"** (en avklarad/mergad aktivitet): uppdatera `Progress`-tabellen + notera städ-/omorg-kandidater nedan.
- En rad per händelse i `Logg`. Kort. Pekar tillbaka på aktivitet/PR.

## Merge-policy (Jake, 2026-06-18 — uppdaterad)

**Ny policy:** Om ALLT är grönt + bot-rent **och orchestratorn känner sig trygg** → merga till master. Annars stanna och fråga (som förut). Beteende-/runtime-PR med minsta osäkerhet (område 4/5/6/7) = fortfarande fråga.

**OBS auto-merge-automation:** en Cursor-side automation (`app/cursor`) kan merga gröna öppna PR:er på egen hand (mergade #152 C1 utan att byggaren bad om det). Risk-PR:er ska därför öppnas som **draft** tills granskade. Se [`.cursor/rules/auto-merge-automation.mdc`](../../../../.cursor/rules/auto-merge-automation.mdc).

## Protokoll — aktiviteter (batch 1)

| ID | Aktivitet | Område | blocked_by | Status | PR | Granskad |
|---|---|---|---|---|---|---|
| H0 | Branch-hygien | — | — | klar | — | — |
| D1 | active/README → router | 3 | — | klar | — | — |
| S1 | test:stability-lane | 2 | — | **MERGAD** | [#147](https://github.com/Jakeminator123/sajtmaskin/pull/147) | grön; Codex-P2 åtgärdad `f86f68399` |
| S2 | åäö-invariant | 2 | ~~S1~~ | **MERGAD** | [#151](https://github.com/Jakeminator123/sajtmaskin/pull/151) | grön + bot-rent |
| S3 | statusresolver-invariant | 2 | S1 | väntar (todo tills omr 6) | — | — |
| S4 | db:schema-drift gate | 2 | ~~S1~~ | **MERGAD** | [#150](https://github.com/Jakeminator123/sajtmaskin/pull/150) | grön + bot-rent |
| C1 | plan-file-schema deprecate | 1 | — | **MERGAD** (av automation) | [#152](https://github.com/Jakeminator123/sajtmaskin/pull/152) | grön |
| C2 | ordlista/glossary-check | 1 | — | **MERGAD** | [#153](https://github.com/Jakeminator123/sajtmaskin/pull/153) | grön; Codex-P2 åtgärdad `ad60f3455` |
| D2 | repo-tree/README-synk | 3 | — | **MERGAD** | [#148](https://github.com/Jakeminator123/sajtmaskin/pull/148) | grön + bot-rent |

## Protokoll — aktiviteter (batch 2: område 7, ur sekvens)

| ID | Aktivitet | Område | blocked_by | Status | PR | Granskad |
|---|---|---|---|---|---|---|
| A7-1 | false-green stabilitetstest (`done` ≠ raderar degraderingar) | 7 | — | **MERGAD** | [#155](https://github.com/Jakeminator123/sajtmaskin/pull/155) | grön + bot-rent (test-only) |
| A7-2 | autofix vägrar dossier-stub (flag-gated, default-OFF, N#1) | 7 | — | **draft, väntar Jakes review** (master oförändrat) | [#156](https://github.com/Jakeminator123/sajtmaskin/pull/156) | grön; default-off |

## Progress (% av 100 — grov uppskattning)

| Steg (§6 körordning) | Vikt | Klart |
|---|---:|---:|
| 0 · Branch-hygien + foundation (FF, CI-build bort, regel-tightening, plan på master) | 10 | 10 |
| 1 · Stabilitetstester (S1–S4) | 20 | 20 (S1+S2+S4 + S3 single-writer-invariant #163 mergade) |
| 2 · Dokumentation & kartor (D1+D2) | 10 | 6 (D1+D2 mergade) |
| 3 · Kontrakt & regler (C1/C2) | 10 | 10 (C1+C2 mergade) |
| 4 · Status & UI/UX (event-bus) | 15 | 15 (6-1 #159 + 6-2 #160 + #161 + 6-3 p1 #162 + 6-3 p2 #163 mergade — **Område 6 klart**: legacy-resolver raderad, S3-invariant aktiv) |
| 5 · Follow-up & preview-kontrakt | 20 | 18 (5-1..5-5 + 5-3b + 5-7 lane-gate #176 + 5-Z doc-drift; **kärnan klar** — 5-6 previewSessionId parkerad/backlog) |
| 6 · False-green-härdning | 15 | 13 (#149 runtime-kärna + A7-1 + A7-2 #156 + #179 missing_preview_url + #180 productBlocked-degrade mergade; #140 parkerad) |
| **Totalt** | **100** | **~89%** (Område 5 kärnan klar; A7-2 env-aktiv #177; #179 + #180 stänger F2 false-green (skip + productBlocked); Område 4 redan täckt. Återstår: bred Område 8-cleanup + katalogiserad backlog (DB-badge finding 02, finding 11-lane, B1–B7)) |

## Städ-/omorg-kandidater (löpande — körs i område 8 / per-områdes Z-städ)

> **Strukturerad, fil:rad-ankrad backlog (B1–B7) + parallell-säkerhet + worktree-exekveringsplan:** [`_backlog-deferrad.md`](_backlog-deferrad.md) — **PAUSAD 2026-06-19** (kontext sparad åt LLM-flöde-/Område 5-spåret). Det dokumentet är kanonisk detalj; punkterna nedan + i avslutningsrundan är terse index.

- `version-2637fe59.zip` i repo-roten — nedladdad version-zip, untracked skräp → radera eller `.gitignore`.
- `.tmp/commit-cleanup.txt`, `.tmp/duck-ceab-duck-mesh.json` — agent-scratch, bör inte tracked:as.
- ~~Worktree `sajtmaskin-hydration` — tom~~ → **AKTIVT** (annan agent kör där): PR #149 promote-guard / false-green (område 7). RÖR INTE branchen/worktreet. Inte en städkandidat.
- ~~(S1-obs) länka `docs/testing.md` + `docs/delivery-bias.md` från `docs/README.md`~~ → **klart** (omr 8-städ, PR `chore/omr8-stadning`: båda länkade i `docs/README.md` Key navigation).
- (S1-obs) `stability`-CI-jobbet kör egen `npm ci` (extra minuter) — kan slås ihop med `quality` om kostnaden skaver.
- Master-plan §7-kandidater: cursorignore-logs, pensionera `plan-file.schema.json` (C1), arkivera källdokument, `next`-bump.
- (D2-obs) Trackad scratch i roten: `blandat/` → **raderad** (omr 8-städ, PR `chore/omr8-stadning`). OBS: `test_förslag_templates_blob/` är **INTE** scratch — load-bearing `DEFAULT_SOURCE` för `npm run templates:blob:sync` (`scripts/v0-templates/sync-blob-catalog.mjs:27`, dokumenterad i `scripts/README.md:127`); behålls.
- ~~(D2-obs) `config/dashboard/` (Streamlit `app.py`) parallellt med `backoffice/`~~ → **löst** (#158): legacy-wrappers raderade, bara load-bearing `domain-map.json` kvar (mappnamn fortf. legacy men ej runtime-problem).
- (D2-obs) `.gitignore`/`.cursorignore` har kvar `archive/...`- och `research/_sidor/`-mönster fast mapparna är borta → städ (omr 8).
- (6-2-obs / VADE-perf) `/versions`-routen kör `selectVersionStatus(readAll(v.id))` **per rad** på en pollad yta → synkron fil-I/O som skalar med versioner×runs (+ O(events²)-dedup i `readAll`). Medel/perf, ej korrekthet. **Backlog (eget pass):** cacha/batcha `readAll` (Set-baserad dedup + ev. chat-scoped läsare eller kort TTL-cache) — rör alla bus-konsumenter, ska testas isolerat, smygs ej in i en risk-PR.

## Avslutningsrunda 2026-06-19 — handoff-snapshot (inför nästa agent)

> Orchestratorns avslutningsrunda. Master HEAD `ca4a7974a` (status nedan uppdaterad 2026-06-20 efter #168 → #169 → #170 → #172 → #174-merge). Detta avsnitt är **aktuell öppen-status** och ersätter tidigare spridda "öppet för Jake"-listor.

**Master-hälsa (lokalt Windows/pwsh, verifierad vid `501928c85`, typecheck om vid `f85af7b78` = 0 fel):** `typecheck` 0 fel · `lint` 0 fel · `test:ci` 294 filer / 2297 tester gröna · `test:stability` 6/6 (schema-drift 6 + false-green 2 + åäö 4). **Inga regressions** från de mergade grandmaster-PR:erna.

**Mergade 2026-06-19 (Jakes go):** **#163** (6-3 p2, S3-invariant + radera legacy-resolver) → `1713d4fdf`; **#156** (A7-2 dossier-stub-flagga, default-OFF) → `c2f2a25db`; **#154** (LLM-flöde-canvas + P0-backlograd-surfacing-fix) → `674a862ac`. Med #163 är **Område 6 klart** och S3-invarianten aktiv (warn-only stability-lane = medvetet S1-designval; lane-härdning = backlog).

**Öppna PR:er — actionlista:**

| PR | Vad | CI/bot | Status / nästa |
|---|---|---|---|
| **#140** (DB+Blob sync-gate, **parkerad**) | fristående Python live-infra-gate (`pydatabastest.py` + workflow) | grönt; **2× Codex P1 (säkerhet):** prod-secrets mot PR-kod + `workflow_dispatch` trusted-ref. Bugbot **High**: EMPTY-check → gate exitar 0 falskt. + ~15 P2. | **Park kvar** (Jakes beslut 2026-06-19). P1-säkerhet = grundhygien-blocker; överlappar S4:s keyless `db:schema-drift`-gate. **Omgörningsform (extern review-rek):** **PR A** = lokal read-only diagnos, inga secrets i PR-CI; **PR B** = trusted push/manual-only mot master, minimal `permissions`, `persist-credentials: false`, fail om creds saknas på trusted run. **OBS:** en annan agent kör infra-/inspector-spåret just nu — ev. omarbetning samordnas där, ej av denna orchestrator. |

> **Öppna PR:er:** Inga öppna grandmaster-PR:er. #168 (5-3 frys-enforcement) + #169 (5-4 F1 clear-redesign-brief) + #170 (F3-städ) + **#172 (5-3b route-hard-clamp)** **mergade 2026-06-20** (squash `dff837572` / `bda57c488` / `b1b9285e8` / `3f0d4c0a7`). Kvarvarande öppna (orchestrator rör ej): **#167** (annan ägares Sajtbyggaren UI-port, 188 filer — separat, ej grandmaster) · **#164** (inspect-bridge, parkerad) · **#140** (parkerad). **#173 + #171** = chgenbergs Viewser/Sajtbyggaren-produkt-PR:er (separata, ej grandmaster, nu blockade från master av rulesetet "Protect master"). **Grandmaster Område 5: #165 + #166 + #168 + #169 + #172 + #174 mergade**; #149/#151/#152/#153/#155/#157–#163 + #154/#156 + #170 mergade.

**Canvas-fråga (Jake):** Finns GitHub-renderad canvas för LLM-flödet? → **Nej, inte renderad på GitHub.** #154 inför en **Cursor-canvas** lagrad som `docs/canvases/llm-flow.canvas.txt` (TSX-källa sparad som `.txt` så repots tsc/eslint inte rör den; renderas lokalt i Cursor via `node scripts/canvas/sync-to-cursor.mjs`). Auto-uppdateras av workflow `llm-flow-canvas.yml` (push→master → genererar → rullande draft-PR `chore/llm-flow-canvas`). **GitHub-renderade diagram som FINNS idag:** Mermaid i `docs/architecture/llm-signal-flow.md` (signal-ägarflöde) + `system-overview.md`; `llm-flow-target-worldclass.md` och `mental-model-vs-actual-flow.md` är prosa/ASCII (ingen Mermaid).

**Överlämnings-luckor (bevakningspunkter):**

- **Inspector-rendering-spår (eget agentspår):** worktree `..\sajtmaskin-inspect-bridge` på `feat/inspect-bridge` (vid master HEAD). Underlag: `docs/plans/active/2026-06-19-inspector-rendering-arkitektur.md` (var untracked i huvudträdet, **flyttades till inspect-bridge-spåret under denna runda** — ej på master ännu, ingen master-länk). Option A = lyft worker till nytt render-konto · Option B = instrumenterad preview + `postMessage`-brygga (rekommenderad). Idag auto-startar inspector-workern på **port 3310** via `scripts/dev/next-runner.mjs` (Option B pensionerar den). **Git-läge (verifierat 2026-06-19):** spåret är **ocommittat WIP** i sitt worktree (15 filer: 10 ändrade + 5 nya, bl.a. `src/app/api/inspect-bridge/`, `usePreviewInspectBridge.ts`, `inspect-bridge-feature.ts/-script.ts`) — **ingen commit/push/PR**, branchen står på master-ancestor `501928c85`, inget har nått eller läckt till master. **Ska landa via egen PR** (rör runtime + protected `src/app/api/**` → review-gate / NEEDS_HUMAN, **ej** direkt-to-master, **ej** insmuget i annan PR): ägande agenten gör commit → push `feat/inspect-bridge` → draft-PR **innan** worktreet tas bort (`worktree remove --force` på ocommittat = dataförlust). **Orchestratorn rör inte detta spår.** **Säkerhetsyta (separat PR, ej grandmaster):** inspector-workern bär SSRF-risken **G#40** (publik DNS→privat IP före `page.goto`), auth-token är **valfri** om env saknas, och servern lyssnar `0.0.0.0` → fix = DNS-resolve-guard före `page.goto` + auth/local-only-policy. Detalj: [`bug-swarm-koppling.md`](bug-swarm-koppling.md) § G#40. (Bekräftat av extern review 2026-06-19.)
- **Område 5 (Follow-up & preview-kontrakt) = Fas 3, 5-1..5-4 + 5-3b + 5-5 mergade 2026-06-20.** **5-1 #165** + **5-2 #166** (stale-base-409 + S1–S5 i CI) + **5-3 #168** (frys-enforcement, squash `dff837572`) + **5-4 #169** (F1-fix, squash `bda57c488`) + **5-3b #172** (route-hard-clamp, squash `3f0d4c0a7`) **mergade**. 5-3: `orchestrate` läser `FollowUpContract` och clampar drifted scaffold/variant tillbaka på neutral follow-up (stänger `scaffoldMode:"manual"`-kringgången); clear-redesign exempt. **5-3b: route är nu FLOOR** — en tyst tappad fryst route återinförs (`enforceFollowUpRouteFreeze`); clear-redesign + explicit route-removal (`collectExplicitRouteRemovals`) är undantag (tidigare bara drift-signal). 5-4: `parsedMeta.brief = metaBrief` så clear-redesign-deltan når orchestration i st.f. snapshot-fallbacken. **5-5 (capabilities-floor) mergad #174** (`ca4a7974a`): ren `enforceFollowUpCapabilityFloor` union:ar `FollowUpContract`-golvet efter prompt-filtret → follow-up tappar aldrig tyst en bas-capability; clear-redesign ej undantaget; telemetri `emitFollowUpFreezeDrift("capabilities")`. Drop-väg 2 (tom snapshot) = telemetri/backlog. **5-7 lane-gate mergad #176** (`091bd39a1`): frys/clamp/capability-invarianterna är nu blockerande `test:ci` (nytt `test:followup-contract`-steg i `quality`-jobbet). **5-Z klar:** doc-drift F1/F2/F3 + nominerings-drift rättad i matris/flowchart/end-to-end. **5-6 previewSessionId PARKERAD** (tunt/redundant). **→ Område 5 kärnan stängd.** **A7-2 env-knapp registrerad #177** (`refuseDossierStubs` nu env-aktiverbar, default OFF; kod-flipp = ägar-/prod-beslut, ej blind-flippad). NÄSTA (Jakes val): prod-flippa A7-2 via env · omr 8-svans · backlog-härdning. **F3 åtgärdad i #170** (squash `b1b9285e8`: död follow-up-intent-LLM-fallback borttagen + doc-drift); F4–F5 = backlog (F1 åtgärdad i #169). Yt-karta `docs/architecture/llm-callsite-matrix.md`.
- **Backlog (egna pass):** VADE-perf `readAll`-per-rad på pollade `/versions`; E2 in-memory/efemär event-bus → multi-instans serverless kan splittra finalize-POST/status-GET; S3-lane-härdning (warn-only stability-lane → blockerande `test:ci` om invarianten ska stoppa regression); **canvas auto-PR CI:** `llm-flow-canvas.yml` skapar PR via `GITHUB_TOKEN` → triggar ej CI på auto-canvas-PR:er (Codex #154 P2 b, medvetet kvarlämnad) → provisionera dedikerad `CANVAS_PR_TOKEN` (PAT) om branch protection kräver checks; område 8-svans = `.gitignore`/`.cursorignore`-prune + arkivera källdocs + `next`-bump + eval-namnskugga (`scripts/eval/` vs `scripts/evals/`).
- **Auto-merge:** "PR-mergare" auto-mergar bara risk 1–2 utan protected paths; #140 (enda öppna) → `NEEDS_HUMAN`. Builder-coexistence-regeln gäller.
- **Worktrees (rör ej andras HEAD):** main `ca4a7974a [master]` · `db-sync-test` `[feat/pydatabastest-sync-gate]` (#140, parkerad) · `inspect-bridge` `[feat/inspect-bridge]` (#164, inspector-spår, annan agent, parkerad). omr5-6 (#172) + omr5-3 (#168) + omr5-4 (#169) + f3 (#170)-worktrees + brancher (lokal+remote) **städade efter merge 2026-06-20**; omr5-1 (#165) + omr5-2 (#166) + #163/#156/#154-worktrees + brancher städade i tidigare rundor.

## Logg

- **2026-06-18** — Foundation: lokal master FF → `0ad1ef53a`; `Build`-steg bort ur `ci.yml`; 10 remote + 2 lokala merged/closed-brancher rensade; agent-worktree/plan-lifecycle/observatory-regler tightade. Jake committade + pushade → `b528034ce`. #146 mergat av separat agent.
- **2026-06-18** — S1 dispatchad (isolerat worktree) → PR #147. CI grön. Codex P2: `*.stability.test.ts` fångas av blockerande `quality`-jobbet → åtgärdas på samma branch före merge.
- **2026-06-18** — D2 dispatchad (parallell, docs-only, distinkt owner_files mot S1).
- **2026-06-18** — D2 klar → PR #148 (repo-tree+README synkade mot HEAD, fantommappar bort, döda länkar fixade). CI pending; auto-merge vid grön+bot-rent (lågrisk).
- **2026-06-18** — D2 #148 **mergad** (grön + bot-rent: Bugbot pass, inga inline-fynd). Worktree + lokal branch borta, master FF → `6b8c8cffc`. S1 Codex-P2 fix pushad (`f86f68399`) — `*.stability.test.ts(x)` exkluderas nu från blockerande `test:ci`, körs bara av `test:stability`. `quality` kör om på #147.
- **2026-06-18** — S1 #147 **mergad** (allt grönt, Codex-P2 åtgärdad+besvarad, inga nya fynd). Worktree + lokal branch borta, master FF → `b45b6a4a4`. Konstaterat: S1 la redan `db:schema-drift` i `test:stability` + ett **warn-only** `stability`-jobb i `ci.yml`. S4:s package.json-del är därmed klar; S4 återstår = lyft schema-drift till hård gate.
- **2026-06-18** — Nästa våg (avsluta omr 2): S2 (åäö-invariant, ny stabilitetstestfil) + S4 (schema-drift hård gate i `ci.yml`) dispatchade i isolerade worktrees. Distinkta `owner_files` → parallell-säkra. **C2 rör samma `ci.yml` som S4 → får ej parallellköras med S4.** S2 auto-merge vid grön; S4 = fråga Jake först.
- **2026-06-18** — S4 klar → PR #150. Nytt blockerande `schema-drift`-jobb (`npm run db:schema-drift`, ingen continue-on-error) mellan `quality` och warn-only `stability`. `db:schema-drift` grön utan DB-creds, typecheck 0, ci.yml giltig YAML. Worktree kvar för städ efter merge. **Väntar Jakes ja före merge** (medel-risk: CI-gate-beteende). PR-CI: nya `schema-drift`-jobbet **pass** (gaten fungerar).
- **2026-06-18** — S2 klar → PR #151. Ny `src/lib/builder/aao-invariant.stability.test.ts` (4 tests) som round-trippar prompttext genom `createCodeGenSSEStream`→`consumeSseResponse` med 1-byte-chunkning → fångar mojibake om `{ stream: true }` tappas. Ingen live-builder rörd. `test:stability` 4/4, typecheck/eslint 0. Auto-merge vid grön+bot-rent.
- **2026-06-18** — **Automation-koll (Jakes fråga):** ingen auto-merge-automation finns (inget mergify/kodiak/`gh pr merge`-workflow i `.github`, inga lokala Cursor-automations, `#151.autoMergeRequest = null`). Workflows = `ci`/`eval-baseline-update`/`weekly-template-sync`, ingen mergar PR. → Merge är manuell via orchestrator-policy. Ingen regel skapad (skulle dokumentera en automation som inte finns).
- **2026-06-18** — S2 #151 **mergad** (helt grön, bot-rent). Worktree + branch borta, master FF → `06d7e70b3`.
- **2026-06-18** — **Auto-merge-automation upptäckt:** PR #152 (C1) mergades av `app/cursor`/`cursor[bot]` 13:21Z — byggaren var instruerad att INTE merga, övriga 14 senaste PR:er mergades manuellt av Jake. → Cursor-side automation (cloud, ej i repo) kan merga gröna öppna PR:er. Ny regel skapad: `.cursor/rules/auto-merge-automation.mdc` (alwaysApply) + indexerad i `.cursor/README.md`. Risk-PR:er ska öppnas som **draft**.
- **2026-06-18** — S4 #150 **mergad** (grön + bot-rent, ny policy: trygg+grön→merga). master FF → `5aed2fd28`. **Område 2 (stabilitet) klart:** S1✓ S2✓ S4✓; S3 flyttad till område 6. C1 #152 mergad (automation). Progress ~39%.
- **2026-06-18** — C2 dispatchad (ordlista-check, warn-först). `ci.yml` fritt nu (S4 mergad) → C2 wirar sin warn-only-check i stability-jobbet. Owner: `config/naming-dictionary.json` + `scripts/dev/check-term-coverage.mjs` + `ci.yml`-steg.
- **2026-06-18** — C2 klar → PR #153 (`check:terms` warn-först, exit 0; demoUrl 158/sandbox 67/dashboard 79/v0 92 m.fl. flaggade). **Auto-mergaren "PR-mergare" identifierad i detalj** via dess `NEEDS_HUMAN`-verdict på #153: Cursor Automation (ID `59ae4961-…`), `pull_request`-triggad, auto-mergar BARA risk 1–2 utan protected paths (`.github/workflows/**`, `package.json`, `src/lib/db|auth/**`, `src/app/api/**`). #152 (C1, ren docs) auto-mergades; #153 (rör ci.yml+package.json) → NEEDS_HUMAN. Regel `auto-merge-automation.mdc` uppdaterad med faktisk guard-logik.
- **2026-06-18** — Codex **P2** på #153 (giltig, false-green-nära): C2-steget efter `test:stability` hoppas över om stability-steget fallerar (default `success()`), så checken försvinner när den behövs. C2-byggaren återupptagen → fix `if: ${{ !cancelled() }}` (`ad60f3455`), besvarade Codex-tråden.
- **2026-06-18** — C2 #153 **mergad** manuellt (grön på nya head, P2 åtgärdad+besvarad, inga nya fynd, PR-mergaren NEEDS_HUMAN pga protected path). master FF → `06c0c0770`. **Område 3 (kontrakt C1/C2) klart.** Progress ~44%. Alla orchestrator-byggare klara+städade. Kvar-worktrees: huvud, `db-sync-test` (Jakes), `hydration` (#149, annan agent).
- **2026-06-18** — **Område 7 startad ur sekvens** (Jakes val A; samordnat med #149, ej överlapp). Beslut inhämtade: merga bara grön+okej; A7-1+A7-2 scope; lämna #149 till sin agent; max 2 byggare; #140 parkerad. Kartlade ytor (explore): kanonisk status = `selectVersionStatus` (event-bus-projection.ts); stubbar är **avsiktligt warning-only** på master; N#1 dossier-stub-refusal är flag-gated P5+ (`FEATURES.refuseDossierStubs`, default-off) — "kan flippa röd/bryta gen". Skapade A7-1/A7-2-aktivitetsdocs. Dispatchade 2 byggare: **A7-1** (test-only, låser `done`≠solid-green, auto-merge vid grön) + **A7-2** (flag-gated default-OFF dossier-stub-guard, **draft** för Jakes review).
- **2026-06-18** — A7-1 #155 **mergad** (grön + bot-rent, 1 testfil +137/-0, noll runtime). master FF → `a76f77eed`. PR-mergaren gav `NEEDS_HUMAN` Risk 5 trots test-only → **`src/lib/logging/**` är också protected path** (lade till i `auto-merge-automation.mdc`). A7-2 (draft) kör vidare.
- **2026-06-18** — A7-2 klar → **draft PR #156** (ej mergad). `FEATURES.refuseDossierStubs` default-OFF (`src/lib/env.ts` + `config.ts`); flag-gated gren i `cross-file-import-checker.ts` (nytt optional `refused`-fält). Flagga PÅ → ingen tyst stub, oresolvad import fångas av **befintlig** `runProjectSanityChecks` "Unresolved local import" → degrade/block (ingen pipeline-ändring behövdes). Default-off-paritet bevisad (checker-test 13/13, stability 7/7, typecheck/eslint 0). **Batch 2 klar — orchestrator stannar här (Jakes "max 2 → stanna").**
- **2026-06-18** — ÖPPET FÖR JAKE: (1) granska + merga **#156** (område 7 runtime, default-off); (2) beslut om att flippa `SAJTMASKIN_REFUSE_DOSSIER_STUBS` PÅ — kräver att område 5/6 landat först (annars kan status flippa röd); (3) vid default-flipp: följd-PR som lägger env-nyckeln i `config/env-policy.json` + `docs/ENV.md`; (4) **#149** (annan agent, promote-guard) kvarvarande P2/P4/P5 + merge-beslut; (5) **#140** parkerad tills bot-trådar hanterade.
- **2026-06-18** — **Sidospår (annan agent, ur sekvens):** PR #149 promote-guard / false-green = **område 7** (wave 3, normalt sist), landad tidigt pga live prod-incident. `/quality-gate`-routen returnerade `passed:true` även vid promote-block → nu `promotionBlocked:true` + `promotionBlockedReason`. Draft, MERGEABLE, 10/10 tester. **Område 7 = beteende/runtime → fråga Jake före merge** (ej orchestratorns auto). Den agentens lokala `_loggbok.txt` i worktree-roten är efemär → kanonisk loggbok = denna fil (`docs/plans/active/grandmaster/_loggbok.md`); #149-posten bor här. OBS: agentens föreslagna "nästa slice = S1" är **inaktuell** — S1 redan mergad (#147), S2/S4 i review.
- **2026-06-18** — **Område 6 startad (§6 steg 4) — `6-1` → PR #159 mergad** (squash → master FF `499ab2266`→`b9eb43297`). Event-bus-status-flip för shell + preview-empty-state: ny ren mapper `src/lib/builder/version-status-display.ts` (18 tester; härleder `retrying`/`promoted`, false-green-vakt: degraderad `done`/`promoted` → `degraded`, aldrig solid grön). `BuilderShellContent` + `PreviewPanelEmptyState` läser nu status via `useVersionStatus` + mappern i st f legacy `resolveEngineVersionDisplayStatus`. `VersionHistory`/S3 orört (→ 6-2/6-3). Verifiering: typecheck/lint 0, 46/46 vitest (PreviewPanel-svit grön utan teständringar = bevarat beteende). Allt CI grönt + **bot-rent** (Bugbot pass/inga fynd, Vercel Agent pass, GitGuardian pass); PR-mergaren `NEEDS_HUMAN` risk 5 (preview/status/false-green-yta — deferrar, ej fynd). Jake godkände merge + 2 dokumenterade legacy↔bus-mismatchar (poll-latens ~4s; `failed`+supersedd visar `failed` ej `retrying` = ärligare). **Nästa:** 6-2 = server-enricha `/versions` + flippa `VersionHistory` (protected path `src/app/api/**` → NEEDS_HUMAN väntat), sedan 6-3 aktiverar S3.
- **2026-06-18** — **Område 8-städ (batch 2) → PR #158 mergad** (squash → master FF `0a1853277`→`dbb97e9b9`). Punkt 3: raderat `docs/schemas/strict/plan-file.schema.json` (0 runtime-konsumenter; planering = regel via `plan-lifecycle.mdc`). Punkt 2: raderat legacy-wrappers `config/dashboard/{app.py,run.ps1,shared_overhead.py,requirements.txt}`, **behållit** load-bearing `domain-map.json` (parity-test grön — inget deletat path refereras i kartan). Punkt 1: eval-README förtydligar `scripts/eval/` vs `scripts/evals/`-namnskuggan (ingen kod rörd). Doc-synk: 7 ytor + `pyrightconfig.json`. Allt grönt + bot-rent (risk 1); PR-mergaren `NEEDS_HUMAN` pga protected path `src/lib/gen/**` (eval-README), orchestrator-mergade. **Kvarvarande omr 8:** bred `.gitignore`/`.cursorignore`-prune, arkivera källdokument, `next`-bump, ev. eval-konsolidering — alla öppna/egna pass.
- **2026-06-18** — **Område 8-städ (batch 1) → PR #157 mergad** (squash → master FF `e416ae077`→`ca5467fd0`). Raderat `blandat/` (8 scratch-filer) + `version-2637fe59.zip`; länkat `docs/testing.md`+`docs/delivery-bias.md` i `docs/README.md` (S1-obs klar). **Beroendespårning fångade fel:** `test_förslag_templates_blob/` är load-bearing (`DEFAULT_SOURCE` för `npm run templates:blob:sync`, `sync-blob-catalog.mjs:27`) — **uteslöts** ur raderingen, loggbok-rad korrigerad. Allt grönt + bot-rent (Bugbot/Vercel Agent/GitGuardian rena, risk 1); PR-mergaren gav `NEEDS_HUMAN` enbart pga protected path (`docs/plans/active/grandmaster/**`), orchestrator-mergade per policy. **Kvarvarande omr 8-beslut (kräver Jake):** eval-namnskugga (`scripts/eval/` vs `scripts/evals/`), `config/dashboard/`-vs-`backoffice/`-konsolidering, flytta deprecated `plan-file.schema.json`, bred `.gitignore`-prune. Parallellt: omr-6 (event-bus-status) kartläggs read-only.
- **2026-06-18** — **#149 övertagen + mergad** (Jakes val "ta över helt" efter coach-review). Verifierade read-only mot `49c51c8a1` att alla botfynd är åtgärdade i kod: P1 route-läcka → `passed:false`+`promotionBlocked` (+ `post-checks.ts`-konsument), P2 promote-fel → egen `promoteError`-gren, P2 stale-telemetri → `recordRepairPassedQualityGate`, eget fynd → `acceptRepair` kör `assertPromoteAllowed`. Körde 11/11 tester i hydration-worktreet. Städade: samlad re-review-kommentar, **6 trådar resolvade**, stale `CHANGES_REQUESTED` (review 4525185685) **dismissad**, body 6→7 filer + 10→11 tester. Squash-mergad → master FF `6d34a3707`→`b8d85a338`. Worktree + branch (lokal+remote) borta. **Område 7 runtime-kärna klar; progress ~55%.** #149:s P2/P4/P5 = separata follow-ups.
- **2026-06-18** — **Område 6 `6-2` → PR #160 mergad** (squash → master FF `0c27010ed`). `VersionHistory`-livscykelbadgen läser nu event-bus: `/versions` enrichar varje rad additivt med `busStatus = selectVersionStatus(readAll(id))`, nytt rent label-lager `src/lib/builder/version-history-status-labels.ts` (+ test) mappar `VersionDisplayStatus`→badge, legacy `resolveEngineVersionDisplayStatus`-importen borttagen ur komponenten (resolvern kvar i `engine-version-lifecycle.ts`; enda kvarvarande app-konsument var denna + redan-flippade `BuilderShellContent`). False-green-vakt bevarad (degraderad → amber outline, aldrig solid grön). Verifiering: typecheck/lint 0, 53 vitest gröna. Review-gate: CI grönt, **Bugbot pass/inga fynd**, PR-mergaren `NEEDS_HUMAN` risk 5 (protected path `src/app/api/**` + false-green/lifecycle-yta — deferrar, ej fynd). **VADE 1 inline-fynd (medel/perf):** `readAll` per rad på pollade `/versions` → loggat som backlog ovan (eget pass, ej i denna PR per delivery-bias + no-scope-creep). Verifierade kosmetiska skillnader: historisk `failed`-utan-bus-events → "Draft" (promoted-rader bär DB-sanning via `release_state`, ofarligt); "Fix redo" ej längre livscykel-badge (surfas via verification-badge + "Acceptera fix"). Jake delegerade merge-beslutet (hoppade frågan), orchestrator-mergade per policy. **Nästa: 6-3 = aktivera S3 statusresolver-invariant** (single-writer). Progress ~66%.
- **2026-06-18** — Handoff + reconcile committat till master (`f48fbaeab`): körnings-handoff, denna loggbok, auto-merge-regel. Coach-review (säkerhet 90%) flaggade **stale planstatus** (äkta fynd) + loggbok-saknad (falskt: såg GitHub före push). **Docs-only reconcile:** aktivitetsfiler S1/S2/S4/C1/C2/D2 `status: ready`→`done` med PR-ref; S3 förtydligad som enda öppna (gated omr 6); `aktiviteter/README.md`-tabell + `00-master-plan §9` synkade mot verkligheten. Coachens PR-domar (#149 re-review=annan agent, #140 parkera, #154 blocka, #156 draft) = redan i linje, ingen orchestrator-åtgärd.
- **2026-06-19** — **#161 (område 6, bus-status-härdning) mergad** (squash → master FF `29947a3b3`). Härdar `selectVersionStatus`-projektionen: (1) **terminal-settle** — verifier-completion (passed/skipped) settlar versionen `done` i st f att hänga kvar i `verifying`; (2) **false-green-vakt** — `skipped`-verifier utan explicit `version.degraded` härleder en `verifier_skipped_by_policy`-degradering (skipped ≠ solid grön); (3) **stale-outcome-gate** — terminal-settle kräver `lastSignal === "verifying"` så en repair-pass efter en tidigare passerad verifier ej visar falskt `done`. Tre P2:or från bot-svärmen (Codex/VADE) åtgärdade iterativt + regressionstester (`event-bus-projection.test.ts` +192, `version-status-display.test.ts` +16; typecheck/lint/vitest/stability gröna). Review-gate: allt CI grönt, **Vercel Agent Review pass**, PR-mergaren `NEEDS_HUMAN` risk 5 (protected `src/lib/logging/**` + false-green-yta → deferrar, ej fynd), Jake gav merge-mandat. **Känd fast-follow → 6-3 punkt 1 (Finding A, Codex P2, medvetet uppskjuten):** en `passed`-verifier som settlar `done` kan få klienten (`useVersionStatus` stoppar polling vid `done`) att missa en **sen** `product-postcheck`-`version.degraded` → smalt net-new falsk-grön-fönster. Verifierat mot koden: `version.verifier.done` emit:as **server-side vid finalize** (`generation-stream-post-finalize.ts`/`server-verify.ts`), postcheck-degraderingen **client-side efteråt** (`post-checks.ts`). Vanliga F2/`skipped`-vägen redan skyddad av (2). Fix kräver en post-check→`version-status`-refresh-signal på klientytan (~5-6 filer i `src/lib/hooks/**` + `builder/**`) → egen liten PR, inte indränkt i #161. **Nästa: 6-3** = (1) Finding A-fix, (2) aktivera S3 single-writer-statusinvariant + beslut legacy-resolver (`resolveEngineVersionDisplayStatus` radera/behåll).
- **2026-06-19** — **6-3 punkt 1 (Finding A, false-green) → PR #162 mergad** (squash → master FF `879ac0c6b`). Stänger det smala net-new falsk-grön-fönstret där `useVersionStatus` slutade polla på `done` innan en **sen** client-side `product-postcheck`-`version.degraded` hann emit:as. **Deterministisk completion-signal:** `runPostGenerationChecks` `finally` → `onComplete` → `refreshNonce`-bump (via `onVersionStatusRefresh` genom messaging/stream → `useBuilderPageController.versionStatusNonce`) → garanterad status-refetch EFTER att `/product-postcheck` emit:at degraderingen (routen emit:ar före svaret, klienten awaitar). Poll-until-stable kvar som **sekundär** billig vakt. **Plus två Codex-P2:** stale-poll-guard i interval-callbacken (delad `prevEventCountRef` skyddad per nyckel) + `mutateVersions()` i samma `finally` så VersionHistory (`busStatus` via `/versions`) revalideras tillsammans med preview-badgen (ingen kort falsk-grön historik-rad). 3× Codex-P2 stängda+resolvade; Codex summary "no major issues"; CI/VADE grönt; 0 olösta trådar; PR-mergaren `NEEDS_HUMAN` risk 5 (false-green/lifecycle, draft) → Jake gav merge-go. 9 filer (`src/lib/hooks/chat/**` + `src/app/builder/**`), +478/-13 inkl. tester. **Backlog (i PR-body):** event-bussen in-memory/efemär → multi-instans serverless kan splittra finalize-POST/status-GET (E2). **Nästa: 6-3 punkt 2** = aktivera S3 import-invariant + radera nu-döda `resolveEngineVersionDisplayStatus` (verifierat: 0 app-konsumenter, bara egen def+test; typen `EngineVersionDisplayStatus` likaså).
- **2026-06-19** — **Avslutningsrunda (handoff-snapshot ovan).** Master-hälsa grön (typecheck/lint 0; `test:ci` 2297 tester / 294 filer; `test:stability` 6/6) — inga regressions från de ~14 mergade grandmaster-PR:erna. Re-gatade **#163**: CI allt grönt (quality 4m17s), summonade Codex → **1×P2** (S3-guarden ligger i warn-only `continue-on-error`-stability-lanen → blockar ej merge) + **1×P3** (substräng-matchning ej faktisk bus-read), inga P1/High; **ej mergad, ej tagen ur draft** (risk-5 → Jakes merge-go + lane-beslut). Auditade #156/#154/#140 (actionlista ovan). Canvas-svar: ingen GitHub-renderad canvas; #154 = **Cursor-canvas** (`.txt`→lokal render via `sync-to-cursor.mjs`), Mermaid-flöden finns i `llm-signal-flow.md` + `system-overview.md`. Upptäckte **5:e worktree** `feat/inspect-bridge` (inspector Option B, eget spår — ej rört). Endast denna `_loggbok.md` rörd + committad. Mitt under rundan reverterade en annan agent (delat working tree) `docs/plans/active/README.md` och flyttade ut den untracked:ade inspector-planen (HEAD oförändrad `501928c85`, divergens `0 0`); `.vscode/launch.json` kvar som pre-existerande ändring — lämnad för sin ägare.
- **2026-06-19** — **PR-triage efter avslutningsrunda (Jakes 4 beslut).** **#163 mergad** (squash → master FF `1713d4fdf`) — 6-3 p2: S3 single-writer-statusinvariant aktiverad + nu-döda `resolveEngineVersionDisplayStatus` (+ typ + testblock) raderad (0 app-konsumenter), doc-synk klar. **Område 6 helt klart.** Codex-P2 (S3-guard i warn-only stability-lane) = konsekvent med S1-design → lane-härdning till blockerande `test:ci` = backlog (ej blint åtgärdad). **#156 mergad** (squash → master FF `c2f2a25db`) — A7-2 flag-gate `SAJTMASKIN_REFUSE_DOSSIER_STUBS` default-OFF (runtime oförändrat); Codex-P2:or (refuse-nedgradering + env-policy-nyckel) gäller bara vid flagg-flipp PÅ → backlog tills omr 5/6 + env-rad landat. Båda risk-5/protected → PR-mergaren `NEEDS_HUMAN`, orchestrator-mergade på Jakes go. **#154:** byggar-agent dispatchad (eget worktree `..\sajtmaskin-canvas-p0fix`) att stänga P0-backlograd-tappet (canvas kan ljuga grönt) — mergas ej förrän fixad+re-gatad. **#140:** park kvar (Jakes beslut; P1-säkerhet = grundhygien-blocker) — annan agent kör infra-/inspector-spåret, ev. omarbetning samordnas där. Worktrees omr6-3b + a7-2 + deras brancher (lokal+remote) städade. Progress ~68% → **~72%**.
- **2026-06-19** — **#154 (LLM-flöde-canvas) mergad** (squash → master FF `674a862ac`) efter Jakes "fixa P0 först, sen merga". Byggar-agent stängde Codex-P0-fyndet i `scripts/canvas/build-llm-flow-canvas.mjs`: öppna P0-backlograder utan `BLOCKER`-tag tappades ur "Öppna huvudrisker" → canvasen kunde underdriva allvar (falsk-grön observability-yta). Fix = P0 i `topOpenRisks`-filtret + `prioRank{P0:0,…}` + garanterad P0-plats vid trunkering ("+N fler"-rad) + egen P0-hink/stat; `parseBacklogRows`/`selectTopOpenRisks` exporteras + import-säker `main()`. 4 regressionstester (`build-llm-flow-canvas.test.ts`), typecheck/lint 0, CI grön på fix-commiten (`98c132cf4` → quality 4m14s pass; manuell push triggar riktig CI eftersom `scripts/` rörs). Bonus: 14 redan tyst trunkerade P1/P2-rader redovisas nu öppet. Alla 3 Codex-P2 värderade: P0-surfacing **fixad**, återanvänd-branch **redan uppfylld** (stabil `chore/llm-flow-canvas`), GITHUB_TOKEN-CI **medvetet kvarlämnad** → backlog (`CANVAS_PR_TOKEN`/PAT + säkerhetsbeslut; docs-only-påverkan). Inga P1/High. PR-mergaren `NEEDS_HUMAN` risk 4 (`.github/workflows/**` protected), orchestrator-mergade på Jakes mandat. Worktree `canvas-p0fix` + branch (lokal+remote) städade. **OBS framåt:** `llm-flow-canvas.yml` är nu på master → kör vid push→master och skapar rullande draft-canvas-PR `chore/llm-flow-canvas` (utan CI tills GITHUB_TOKEN-fyndet åtgärdas). **Alla aktuella PR:er utom parkerade #140 är nu mergade.**
- **2026-06-19** — **Read-only backlog-utredning + paus (ny orchestrator).** Bisittar-agent kartlade alla deferrade poster mot HEAD `cccc843dd` (B1 S3-lane warn-only, B2 `/versions` `readAll`-dedup, B3 multi-instans event-bus, B4 canvas-token, B5 A7-2 env-doc, B6 omr8-svans, B7 #140-bot-fynd) med fil:rad-ankare, allvar/effort, isolerbarhet + parallell-/worktree-exekveringsplan. Konsoliderad till [`_backlog-deferrad.md`](_backlog-deferrad.md) (kanonisk detalj; spridda backlog-punkter ovan = terse index). **Parallell-säker delmängd** (eget worktree → PR, disjunkt mot Område 5): B5 env-doc + stäng B6 next-bump (redan `16.2.9`). **PAUSAD medvetet** för att spara kontext åt LLM-flöde-/Område 5-spåret (coachens Fas 0). Ingen kod rörd, inget committat av denna runda än. HEAD oförändrad, divergens `0 0`.
- **2026-06-19** — **Område 5 Fas 0+1+2 (LLM-flöde-kartläggning, ny orchestrator, autonomt).** 5 parallella read-only `explore`-agenter kartlade hela LLM-anropsytan mot HEAD `cccc843dd` (A intake/brief `f128b388` · B orchestration/codegen `9921fa31` · C verify/repair/autofix `8d6c10d2` · D model-routing/övrigt `e8818123` · E follow-up/preview-kontrakt `9c504801`). **Fas 1 (karta):** ny kanonisk [`docs/architecture/llm-callsite-matrix.md`](../../../architecture/llm-callsite-matrix.md) — var varje LLM/embedding-anrop sker (fil:rad, fas, modellkälla, API-stil), deterministiska steg, modell-routing, föreslagen modul-karta (ingen kodflytt), länkad från `docs/llm/README.md`. **Orchestrator-verifierade kandidat-buggar (kod-kollade själv):** **F1** clear-redesign-delta-brief når aldrig orchestrate (`metaBrief` sätts `chat-message-stream-post.ts:477` men orchestrate läser `parsedMeta.brief ?? snapshot` `follow-up-orchestration-input.ts:82-84`; `metaBrief` bara telemetri `:1049/1133`) → bortkastat LLM-anrop; **F2** stale `baseVersionId` saknar gate i follow-up-strömmen (tyst accept `version-manager.ts:82-107`) medan `finalize-design` har 409 (`route.ts:126-143`). Plus F3 (död intent-LLM-fallback), F4 (4 odefinierade bus-emits), F5 (manifest `perTier*` ej i Zod-schema + hårdkodade modeller). **Fas 2 (nivå-3):** [`05-followup-och-preview-kontrakt.md`](05-followup-och-preview-kontrakt.md) uppgraderad `scope→active` med verifierad owner-yta + FollowUpContract-gap-tabell + ordnade aktiviteter **5-1..5-7 + 5-Z**; körbara specar `aktiviteter/5-1-followup-contract-type.md` (kontraktstyp+builder, additiv) + `aktiviteter/5-2-stale-baseversion-409.md` (F2-fix, högst korrekthetsvärde). **Allt docs-only, ingen runtime rörd. Fas 3 (kontraktskod) väntar Jakes go.** Byggar-prompter för 5-1/5-2 levererade i chatten.
- **2026-06-19** — **Område 5 Fas 3 start: 5-1 (#165) mergad + 5-2 (#166) finaliserad (draft) — autonomt, Jakes plan A.** Två byggar-subagenter, isolerade worktrees, disjunkta filer. **#165 FollowUpContract:** additiv kontraktstyp + ren builder (`buildFollowUpContract`, `orchestration-snapshot.ts`) + additivt optional-fält på `OrchestrationInput` — **orchestrate läser inte fältet än** (paritet bevisad, 0 beteendeändring); reviewer-fix = defensiva array-kopior (`existingRoutePaths`/`existingShellRoutePaths`/`capabilities`, `972ac55b1`). Orchestrator-docs-commitsen (matris + nivå-3 + backlog) FF-pushades till master **före** merge (`cccc843dd→805855ca7`) så squashen blev ren kod (`follow-up-contract.test.ts` +215; 4 filer +363). CI grönt, VADE pass, PR-mergaren `NEEDS_HUMAN` (väntat). Squash → master FF **`c469a4598`**; omr5-1-worktree + branch (lokal+remote) städade. **#166 stale-baseVersion 409 (F2-fix):** 409-gate i follow-up-strömmen (speglar `finalize-design`) via ny `resolveChatPreferredVersionId` (`version-manager.ts`) + klientsignal `engineLatestKnownVersionId` (`useSendMessage.ts`; utelämnas vid explicit override) + 409→reload-toast. Smal motiverad scope-avvikelse: `types.ts` + `useBuilderPageController.ts` (klientsignalen). Rebasad rent på nya master → HEAD **`e5399e8a6`** (disjunkt mot 5-1). **Härdning:** alla fem stale-base smoke-scenarier låsta i CI (S1 stale→409 · S2 current→OK · S3 medveten edit av äldre→ej blockerad · S4 ingen signal→OK · S5 klient-409→reload utan state-trasa); ny `useSendMessage.test.ts` 4/4 täcker tidigare 0-testad klienthalva. typecheck/eslint 0, vitest 32/32. **#166 förblir DRAFT — inväntar Jakes merge-go** (han ville smoke-/koll-beslut själv; äkta live 2-klients-smoke ej körd pga builder-coexistence-regeln → därför CI-låsta scenarier i stället). **F1 (clear-redesign-delta-brief når ej orchestrate) + F3–F5 = backlog i `llm-callsite-matrix.md`. Nästa Område 5: 5-3..5-7 (konsumera FollowUpContract i orchestrate; preview-kontrakt) + 5-Z.** Progress ~72%→~73%.
- **2026-06-19** — **5-2 (#166) mergad → Område 5 Fas 3 (5-1+5-2) klar (Jakes go + coach-dom 90%).** #166 ur draft → squash → master FF **`7034b0085`** (8 filer, +511/−6: `chat-message-stream-post.ts` 409-gate, `version-manager.ts` `resolveChatPreferredVersionId`, `useSendMessage.ts` klientsignal+409→reload, `types.ts`+`useBuilderPageController.ts` signal-wiring, 3 testfiler inkl. ny `useSendMessage.test.ts`). **Plan-väktarens 2 kontrollpunkter verifierade i kod före merge:** (C1) racet stoppas — S1-test server→`ver_new`/klient på `ver_old` med stale known-latest → 409, `createGenerationPipeline` ej körd; resolver speglar finalize-design `preferred ?? latest` (in-progress-icke-preferred = ärvd semantik, uttryckligt accepterad); (C2) exempt-väg ej exploaterbar — gaten kräver BÅDE base+known-latest, bare-base-test bevisar att resolvern ej konsulteras, klienten skickar alltid signalen utom vid explicit override. CI helt grön (quality 4m19s), VADE pass, inga inline/bot-fynd, PR-mergaren `NEEDS_HUMAN` (stale på gamla headen; draft → ingen auto-merge). Manuell 2-klients-smoke medvetet ej körd (builder-coexistence) → ersatt av S1–S5 i CI (Jake accepterade). omr5-2-worktree + branch (lokal+remote) städade; lokal typecheck körd efter merge. **Öppna icke-grandmaster-spår lämnade orörda:** #164 (inspect-bridge — header-fynd-fix + live-smoke först), #167 (Sajtbyggaren UI-port, annat agentspår), #140 (parkerad). **Nästa: 5-3 frys-enforcement** = lås scaffold/variant/route via `FollowUpContract` (undantag clear-redesign), stabilitetstest först, smal PR. Progress ~73%→~75%.
- **2026-06-19** — **5-3 frys-enforcement byggd + gatad → grön draft #168 (autonomt, väntar Jakes go).** Bygg-subagent i eget worktree (`feat/omr5-freeze-enforcement`). **`orchestrate` läser nu `input.followUpContract`** och clampar drifted scaffold/variant tillbaka till frysta kontraktet på neutral follow-up — stänger även `scaffoldMode:"manual"`-kringgången; **clear-redesign exempt** (`ignorePersistedScaffoldForMatch` / `followUpIntent="clear-redesign"`, verifierat satta upstream). Wiring lagd i `orchestrate.ts` (inte input-lagret) så även direkta callers skyddas; `follow-up-orchestration-input.ts` orörd. Test-först: integrationstest rött före fix, grönt efter. **Review-gate (delegerad):** CI helt grön (quality 4m17s), inga Bugbot/Codex/VADE/GitGuardian-fynd; **plan-väktaren 1 Medium** — route-drift-telemetrin täckte bara `existingRoutePaths`, ej frysta `existingShellRoutePaths` → falsk-grön drift-signal om shell-route droppas. **Tätat in-PR** (`2159f32f6`): drift-detekteringen täcker nu hela frysta `routePlan` (union av båda route-arrayerna) + 3 regressionstester (dropp av fryst shell-route → `followup_freeze_drift` fyrar; clear-redesign-exempt även för shell). **Hård route-clamp medvetet uppskjuten** (kräver `explicitRouteRemovals`-kontext = utanför 5-3-scope) — nu bara telemetri-täckning, ingen tyst route-mutation. **Re-gate GO:** CI grön på `2159f32f6`, inga nya fynd, plan-väktarens Medium omklassad till icke-blockerande, PR-mergaren `NEEDS_HUMAN` Risk 5 (draft + protected `src/lib/gen/**`, väntat). **#168 = ren granskad draft — inväntar Jakes Risk-5-review + smoke** (vanlig follow-up behåller scaffold/variant/layout; clear-redesign får byta) **+ merge-go.** Progress oförändrad ~75% tills #168 mergas; sen 5-4 (F1-fix: clear-redesign-delta-brief når orchestrate).
- **2026-06-19** — **5-4 (F1-fix) byggd + gatad → grön draft #169 (autonomt, Jakes "kör på ditt omdöme").** Bygg-subagent i eget worktree (`feat/omr5-clear-redesign-brief`, branchad från master). **Root-cause verifierad mot kod:** clear-redesign genererar en färsk delta-brief (`metaBrief`, satt `chat-message-stream-post.ts:531`) men den skrevs aldrig tillbaka till `parsedMeta.brief`; orchestration läser `parsedMeta.brief ?? buildFollowUpBriefFromSnapshot(...)` (`follow-up-orchestration-input.ts:85-87`) → snapshot-fallbacken vann alltid, deltan användes bara i telemetri. **Fix:** en rad — `parsedMeta.brief = metaBrief;` direkt efter delta-tilldelningen (täcker både plan- och codegen-call-site). **Test-först:** F1-test RÖD på master (brief = snapshot-sentinel) → GRÖN efter fix (brief = delta-sentinel) + neutral-follow-up-regressionsvakt (grön före+efter, neutral väg rör aldrig delta-genereringen). **Filstaket hållet:** `orchestrate.ts` + `followup-freeze.stability.test.ts` (#168:s yta) orörda — ingen kollision med 5-3. Filer: `chat-message-stream-post.ts` (fix), `[chatId]/stream/route.test.ts` (test), ny spec `aktiviteter/5-4-clear-redesign-delta-brief.md` + README-rad. **Review-gate (delegerad, read-only):** CI helt grön (quality 4m25s, stability/schema-drift/VADE/Vercel/GitGuardian pass), inga Bugbot/Codex/VADE/GitGuardian-fynd, 0 inline-kommentarer, plan-väktaren hittade ingen drift-lucka; PR-mergaren `NEEDS_HUMAN` Risk 3 (protected `src/app/api/**` + grandmaster-plan, väntat). typecheck/lint 0, vitest 79/79. **#169 = ren granskad draft — inväntar Jakes merge-go** (kan smokas ihop med #168: clear-redesign ska nu faktiskt byta design). Progress ~75% tills #168+#169 mergas. Nästa Område 5-slice = Jakes val (capability single-source · preview/version-pinning · finalize-kontrakt · stabilitetstester).
- **2026-06-20** — **F3-städ byggt + gatad → grön draft #170 (autonomt, Jakes "båda"-val: F3 nu + 5-5 efter #168-merge).** Bygg-subagent i eget worktree (`feat/f3-remove-dead-followup-llm-fallback`, branchad från master). **Bevisad död först:** repo-grep visade `classifyFollowUpIntentWithLlmFallback` (`follow-up-clarification.ts`) bara i (1) egen def, (2) dedikerad P22-testsvit, (3) docs — noll runtime-callers; routes kör regex `classifyFollowUpIntent`. **Borttaget:** död funktion + privata helpers (`defaultLlmCaller`, `parseLlmIntentLabel`, cache m.fl.) + `LLM_FALLBACK_*`-konstanter + nu oanvända imports (`generateText`, `createDirectModel`, `FOLLOW_UP_INTENT_MODES`) + P22-testsviten (6 tester). Regex/clarification orört. **Doc-drift rättad:** `llm-chain-flowchart.md:121` (falska "≥80 ord LLM-fallback") + F3-raden i `llm-callsite-matrix.md` markerad åtgärdad. **Review-gate (delegerad, read-only):** alla 8 CI gröna (quality 3m20s, stability/schema-drift/VADE/Vercel/GitGuardian pass), inga Bugbot/Codex/VADE/GitGuardian-fynd, 0 inline; PR-mergaren `NEEDS_HUMAN` Risk 4 (draft + `src/lib/providers/**`, väntat). typecheck/lint 0, vitest 35. **Enda nit (Low, ej blocker, lämnad):** `docs/plans/archived/Kvarvarande-uppgifter.md:55` nämner ännu funktionen som "P32-planerad" — stale men i `archived/` (historik) och påstår ej att den körs. **#170 = ren granskad draft — inväntar Jakes merge-go.** Filstaket mot #168/#169 hållet. Rör ej Område 5-progress (~75%).
- **2026-06-20** — **#168 → #169 → #170 mergade i en runda (coach-godkänd ordning, ingen re-gate) → Område 5 Fas 3 (5-3+5-4) klar + F3-städ landad.** Tre grandmaster-draft-PR:er squash-mergade i coachens ordning **#168 → #169 → #170**; ingen re-gate behövdes (heads oförändrade sedan gaten, alla `MERGEABLE`/`CLEAN`); lokal master FF efter varje. **Mergat UTAN lokal builder-smoke** — coachen dömde på status/CI/diff vid 90% konfidens (builder-coexistence → ingen live 2-klients-smoke). **#168 (5-3 frys-enforcement) → squash `dff837572`:** `FollowUpContract` är nu aktiv källa för follow-up-frys — neutral follow-up får ej byta scaffold/variant bort från basversionens kontrakt; clear-redesign exempt. **Route hanteras som DRIFT-SIGNAL ENDAST** (`followup_freeze_drift`-telemetri, inkl. shell-routes), **inte** hård clamp. Filer: `src/lib/gen/orchestrate.ts`, `orchestration-snapshot.ts`, `followup-freeze.stability.test.ts`. **#169 (5-4 clear-redesign delta-brief, F1-fix) → squash `bda57c488`:** en rad write-back `parsedMeta.brief = metaBrief;` så orchestrate får den färska clear-redesign-deltan i st.f. snapshot-fallbacken. Filer: `chat-message-stream-post.ts`, `[chatId]/stream/route.test.ts`, ny `aktiviteter/5-4-clear-redesign-delta-brief.md`, `aktiviteter/README.md`. **#170 (F3-städ) → squash `b1b9285e8` (nuvarande master HEAD):** raderade död `classifyFollowUpIntentWithLlmFallback` + helpers/cache/LLM-call/test; rättade docs som falskeligen påstod en LLM-fallback för långa follow-up-prompter (runtime kör bara regex `classifyFollowUpIntent`). Filer: `follow-up-clarification.ts`, `follow-up-clarification.test.ts`, `docs/llm/llm-chain-flowchart.md`, `docs/architecture/llm-callsite-matrix.md`. **Städat:** worktrees `sajtmaskin-omr5-3` / `-omr5-4` / `-f3` borttagna; brancher (lokal+remote) `feat/omr5-freeze-enforcement` · `feat/omr5-clear-redesign-brief` · `feat/f3-remove-dead-followup-llm-fallback` raderade. Kvarvarande worktrees: main (master) + `sajtmaskin-db-sync-test` (#140, parkerad) + `sajtmaskin-inspect-bridge` (#164, parkerad). **Coach-notering — KVARSTÅR efter #168:** hård route-clamp + explicit route-removal (route är idag signal-only). **Coachens nästa-slice-prioritet:** (1) route hard-clamp + explicit route-removal, (2) capability single-source (init/follow-up/dossier/orchestrate), därefter preview-session/version-pinning, finalize/readiness-kontrakt, samt ett konsoliderande follow-up-contract-stabilitetstest. **Öppna PR:er efter rundan:** #167 (annan ägares Sajtbyggaren UI-port, 188 filer — separat, ej grandmaster) · #164 (parkerad, inspect-bridge-header-fynd kvar) · #140 (parkerad). #165/#166 mergade i tidigare runda (redan i loggboken).
- **2026-06-20** — **#172 (route hard-clamp + explicit route-removal) squash-mergad till master (`3f0d4c0a7`) via ADMIN BYPASS.** Admin mergade trots den nya rulesetens krav på code-owner-review → bekräftar att Jakes admin-frihet är intakt. Oberoende diff-review (färsk subagent) = alla 4 beteendekontroller **PASS** (floor-not-ceiling, explicit-removal hedrad, clear-redesign exempt, try/catch-omslutet). Impl = `enforceFollowUpRouteFreeze` i `orchestrate.ts`; kanonisk removal-signal = `collectExplicitRouteRemovals` (ägare `route-plan/planning-helpers.ts`), re-exporterad via `route-plan/index.ts` (ingen ägarmatris-ändring). Worktree omr5-6 + branch `feat/omr5-route-hard-clamp` (lokal+remote) städade. **Numreringskrock löst:** route-hard-clamp = **5-3b** (kompletterar 5-3:s route-frys), `5-6` förblir `previewSessionId` (5-5/5-6/5-7/5-Z ej omnumrerade → alla "5-6 = previewSessionId/finalize"-korsreferenser giltiga). **OCKSÅ — governance-lockdown landad:** `.github/CODEOWNERS` (`* @Jakeminator123`, `a31f9ce35`) + aktiv ruleset **"Protect master"** (id `17926309`: require PR + code-owner review + non_fast_forward + deletion, **admin always-bypass**) → chgenberg (write) blockad från att pusha/merga master, Cursor auto-mergaren GitHub-neutraliserad för master-PR:er. **Non-blocking follow-up noterad:** frys-/clamp-beteendetesterna (`followup-freeze.stability.test.ts`) ligger i den warn-only `stability`-lanen (gatar **ej** blockerande `quality`-CI) → kandidat att lyfta in i den gatande lanen.
- **2026-06-20** — **5-5 (capabilities can-only-grow) byggd, gatad, mergad → #174 (`ca4a7974a`).** Eget worktree (`feat/omr5-capabilities-floor`), test-först. Ny ren helper `enforceFollowUpCapabilityFloor` (speglar `enforceFollowUp*Freeze`) union:ar `FollowUpContract.capabilities`-golvet tillbaka **efter** `filterDossierCapabilitiesForPrompt` i orchestrates dossier-block → en follow-up kan aldrig tyst tappa en bas-capability (t.ex. init-`contact-form` eller F3-`payments` som F2-filtret annars kapar på ett neutralt meddelande). **Golv ej tak** (nya caps flödar igenom), **init = no-op**, och — till skillnad från scaffold/variant/route — **clear-redesign INTE undantaget** (en omdesign får ej tappa en betald integration). Telemetri: `emitFollowUpFreezeDrift("capabilities", {floorApplied, restoredCapabilities})` — **signal-gate-val:** utökade den kanoniska `FollowUpFreezeSurface`-ägaren i st.f. ny `incIngressEvent` (smalare diff, dokumenterat i PR; medveten avvikelse från specens ord). Ny `followup-capabilities.stability.test.ts` (9 tester inkl. deterministiskt `filter+floor`-bevis; stability-lanen 5→6 filer / 50 tester). **Drop-väg 2 (tom snapshot → golv lika tomt) lämnad som telemetri/backlog** — kräver snapshot-persistens vid finalize = egen slice (rörde ej finalize). Gate: CI helt grön (quality/schema-drift/stability/Vercel/GitGuardian), inga Bugbot/Codex/inline-fynd, PR-mergaren `NEEDS_HUMAN` Risk 4 (draft + protected `src/lib/gen/**`, väntat; kallade ändringen "anti-falskt-grönt-garanti"). Jakes go → squash-merge **admin-bypass** → master FF `9099f7b63→ca4a7974a`. Worktree + branch (lokal+remote) städade; huvudträdets node_modules-junction borttagen länk-bart (target intakt). **Lane-not (→5-7):** capability-/frys-/clamp-testerna ligger i warn-only `stability`-lanen → ska lyftas till blockerande `test:ci` i 5-7 (Jakes beslut denna runda). Progress ~80%→~82%. **Nästa: 5-6 previewSessionId.**
- **2026-06-21** — **5-7 (#176) + 5-Z → Område 5 kärnan stängd.** **5-7 lane-promotion mergad #176** (admin-bypass, squash `091bd39a1`): nytt `test:followup-contract`-script + blockerande steg i `quality`-jobbet kör follow-up-kontrakt-invarianterna (`followup-freeze` = 5-3/5-3b + `followup-capabilities` = 5-5) → en regress **rodnar nu CI** i st.f. att bara varna i warn-only-lanen. Inga nya tester (åäö-intent redan blockerande via `follow-up-clarification.test.ts`; stale-base-409 via stream-tester) — ren lane-promotion per Jakes fold_57. CI grön, nya steget bevisat `success` i `quality`, PR-mergaren `NEEDS_HUMAN` Risk 4 (väntat), inga bot-fynd. Worktree + branch (lokal+remote) städade (fysisk mapp krävde manuell `Remove-Item` efter cwd-lås + `git worktree prune`). **5-Z (doc-drift Z-städ) klar** (docs-only, direkt master-push): `llm-callsite-matrix.md` (F1 #169 / F2 #166 / F3 #170 markerade åtgärdade; doc-drift-tabell → "rättad"; F4/F5 + vestigial nominerings-kod noterade som backlog), `llm-chain-flowchart.md` (brief-fallback `gpt-4.1`→Anthropic `claude-sonnet-4.6`; brief-fält mot faktiska `siteBriefSchema`), `llm-flow-end-to-end.md` (nominerings-drift: scaffold/variant = **deterministisk** pick, `scaffoldNomination`/`variantNomination` vestigialt/alltid-`null` eftersom `siteBriefSchema` ej emitterar dem; clear-redesign-delta-brief-nyans). Kod-verifierat: `siteBriefSchema:128-198` saknar `*Nomination`/`mustHave`/`avoid`; orchestrate läser dem defensivt → alltid null → drift-logg fyrar aldrig. **5-6 previewSessionId PARKERAD** (kod-grundat: kontraktet bär redan fältet sedan 5-1, inget konsumerar det, `preview-session/route.ts` version-pinnar redan vid session-start → "validering vid follow-up-start" tunt/redundant; den värdefulla "preview re-pinnar efter follow-up-finalize / anti-stale-falsk-grön"-varianten = egen scopad backlog, finalize-yta). **Område 5 kärnan stängd** (~82%→~87%) — "Klart när"-kriterierna uppfyllda + blockerande CI-gatade. **Backlog kvar:** re-pin-varianten, F4 (odefinierade bus-emits), F5 (manifest `perTier*`/hårdkodade modeller), vestigial nominerings-kod. **Nästa: A7-2-flipp** (Område 7, `refuseDossierStubs` default-ON — nu upplåst av omr 5/6; kräver env-policy/docs-synk + hård gate).
- **2026-06-21** — **A7-2 env-knapp registrerad → #177 (master `9b346297b`).** Backlog B5(a): registrerade den befintliga `SAJTMASKIN_REFUSE_DOSSIER_STUBS`-flaggan i `config/env-policy.json` (knownEmptyOk + runtimeOnlyKeys + rule med dev/preview/**production**-targets, speglar `SAJTMASKIN_F2_PRODUCT_POSTCHECK`) + dokumenterade i `docs/ENV.md`. **Runtime oförändrat** (kod-default OFF i `config.ts`) → A7-2 false-green-härdning är nu **aktiverbar + inspekterbar via env** utan kodändring, reversibelt. **Kod-grundad riskbedömning av flippen (läste finalize-preflight + project-sanity):** refuse ON → en dossier-exponerad import som LLM:n inte emitterade blir oresolvad → `runProjectSanityChecks` `code_structure_failure`/error → `buildPreviewStartContract` `canStartPreview:false` + `previewBlockingReason`. Alltså **ärlig preview-block/degrade** (anti-false-green), INTE krasch/data-loss. MEN frekvensen (hur ofta LLM refererar icke-emittad dossier-path) är **LLM-runtime-beroende och ej bevisbar av grön CI**. **Därför INTE blind-flippad kod-default medan Jake är borta** — den produktionsbeteende-ändringen lämnas åt ägar-omdöme: sätt `SAJTMASKIN_REFUSE_DOSSIER_STUBS=true` i Vercel (preview först, bevaka block-frekvens, sen production — `production` finns i targets så manage_env ej flaggar overreach). Gate: CI helt grön, **Bugbot ("Find critical bugs") pass**, 1 Codex-P2 (production saknades i targets → manage_env `TARGET_OVERREACH`) **åtgärdad** (`182ddf3b3`), PR-mergaren `NEEDS_HUMAN` Risk 4 → admin-merge per Jakes "merge-when-green"-mandat (Jake borta några timmar). Worktree + branch städade. **Område 7:** kärnan (#149/A7-1/A7-2-flag) klar; A7-2 nu **env-aktiverbar**, kod-default-flipp = ägar-/prod-beslut. **Nästa (Jakes val):** prod-flippa A7-2 via env · omr 8-svans (per-rad ignore-prune, ej autonom) · backlog-härdning (VADE-perf/E2/B2).
- **2026-06-21** — **A7-2 env-AKTIVERAD i Vercel + eval-baseline-spår borttaget (#178) + eval-output rensad** (Jake åter, cleanup-begäran). **A7-2 (Jakes go):** satte `SAJTMASKIN_REFUSE_DOSSIER_STUBS=true` i **alla tre** Vercel-miljöer via CLI/`vercel api` — **non-sensitive** (`type:encrypted`, läsbar, speglar `DOSSIER_PIPELINE`), alla preview-branches (gitBranch null). CLI 54.4.1 gör Production/Preview sensitive by default → tog bort den och återskapade via `vercel api` POST med `type:encrypted` + `target:[production,preview,development]`. Aktiveras vid nästa deploy per miljö (env ej retroaktiv) → går live i prod vid nästa master-push (Jakes val `next_push`); reversibelt. **#178 eval-cleanup (admin-merge, squash `d6791c9ab`, −2088 rader):** raderade det otrackade-i-CI OMTAG-02 scaffold-selection-baseline-spåret `evals/` (10 prompts + `results/baseline-master/` 11 JSON + README + .gitkeep) + `scripts/evals/` (`probe.ts`/`run-baseline.mjs`/`diff-results.mjs`) — stale april-baseline, ingen npm/CI-koppling. **Behållet orört:** `scripts/eval/` (`npm run eval`) + `src/lib/gen/eval/**` (`eval:suite`/veckovis CI) — tester 43/43. **Canvas** (`build-llm-flow-canvas.mjs`) lämnad — degraderar snyggt (kört utan baseline → exit 0, eval-scorecard utelämnas). **Backoffice** `eval_page.py` läser `data/scaffold-eval/` (annat system) — opåverkat. Refs städade: `repo-tree.md`, `eval/README.md`, `.gitignore`, grandmaster `03`/`D2`/`_backlog-deferrad` B6/`08-cleanup` (eval-namnskugga **markerad löst**); archived/avklarat = historik, lämnade. Lokal otrackad `evals/`-scratch (33 filer) rensad ur arbetsträdet. `eval-output/` (gitignored) rensad. Gate: CI grön, Bugbot pass, inga inline-fynd, PR-mergaren NEEDS_HUMAN Risk 3 (grandmaster-docs) → admin-merge. **Eval-namnskuggan (omr 8) löst.** (Ej mina, lämnade ostagade i arbetsträdet: `launch.json`, `.cursor/settings.json`, `docs/canvases/llm-flow.canvas.txt` — canvas regenereras av `llm-flow-canvas.yml` vid push.) Progress ~87% (cleanup = hygien). **Nästa:** A7-2 prod-aktiveras vid nästa push; återstår omr 8-svans (ignore-prune/arkivera källdocs) + backlog-härdning.
- **2026-06-21** — **Avslutningsrunda + ny handoff.** Verifierat: master `96b27f8ef`, div 0 0, typecheck grön, **inga öppna grandmaster-PR:er** (5-5 #174 / 5-7 #176 / A7-2-env #177 / eval-cleanup #178 alla mergade; 5-Z direkt på master), worktrees rena (main + parkerade #140/#164). Öppna PR:er totalt = chgenbergs **#175** (Viewser/Sajtbyggaren, ej grandmaster, master-blockad) + #140/#164. Ostagat i arbetsträdet (ej mina, lämnade): `launch.json`, `.cursor/settings.json`, `docs/canvases/llm-flow.canvas.txt`. Skrev durabel handoff [`docs/handoffs/2026-06-21-grandmaster-execution-handoff.md`](../../../handoffs/2026-06-21-grandmaster-execution-handoff.md) — 3-nivåmodellen + status per område + governance + arbetssätt + copy-paste-startprompt. **⚠️ Jake startar en PARALLELL bugg-letar-agent** bredvid nästa orchestrator → master kan röra sig under nästa agent: `git fetch` ofta, rebasa worktrees, kollidera inte om brancher/worktrees, append (skriv ej över) i loggboken. **Klara områden:** 1, 2, 3, 6 + Område 5 kärnan + 5-Z; Område 7 env-aktiverbart; Område 8 pågående. ~87%.
- **2026-06-21** — **#179 (Område 7, F2 false-green) byggd → gatad → admin-mergad → master `d06c33533`** (ny orchestrator, parallell bugg-agent kör). Smal slice (818-svärm för semantik-beslut + egen kod-verifiering): `product-postcheck/route.ts` resolvar versionen **före** `missing_preview_url`-skippen och emitterar `version.degraded {kind: product_postcheck_skipped, missing_preview_url}` där (speglar post-run-skip-vägen `route.ts:114-122`). Stänger bekräftad falsk-grön-lucka (bug-swarm finding 01/07): klientens vanliga `previewUrl:null`-anrop lämnade tidigare livscykel-badgen solid grön fast DOM-postchecken aldrig kördes → nu degraderad. **Falsk-RÖD-vakt:** `feature_disabled` ligger kvar **före** scope → default-OFF-deploys gör ingen DB-läsning + emitterar inget (annars rödmarkerats varje version); låst med test. Gate: CI grön (quality/schema-drift/stability/GitGuardian/Vercel), auto-mergaren bekräftade **inga Bugbot/Codex/VADE-fynd**, route-test 7/7 (var 4), typecheck/eslint 0, `test:stability` 6/50, riktade suiter 70. Auto-mergaren NEEDS_HUMAN Risk 5 (draft + protected `src/app/api/**` + false-green) → admin-squash på Jakes go. Worktree + branch (lokal+remote) städade (junction-bort-före-remove; fysisk mapp force-removed efter Windows-lås + `git worktree prune`). **Medvetet utanför scope (separata valfria slices):** `productBlocked`→degradera livscykeln (kräver ny `VersionDegradationKind` + UX/backoffice/glossary), DB-`Verifierad`-vs-bus-`Degraderad`-split (finding 02 → `VersionHistory`-gate), finding 11 (lyfta "degraderingar överlever `version.done`" warn-only → blockerande lane). De två första överlappar bugg-agentens findings → koordineras/defereras. **Nästa:** ev. omr-7-restslices / Område 4 (frivillig) som egna PR:er, sedan bred Område 8-cleanup ihop med Jake. ~87→~88%.
- **2026-06-21** — **#180 (Område 7, F2 `productBlocked` → degraderad) byggd → gatad → Bugbot-subagent-granskad → admin-mergad → master `b4993271a`.** Stänger Område 7:s "Placeholder = degraded, aldrig grön": en lyckad F2-postcheck som dömde produkten trasig (`productBlocked` = död mobilmeny / 2+ brutna in-page-ankare) emitterade tidigare **ingen** `version.degraded` → livscykel-badgen stod solid grön fast en kärninteraktion var trasig. Ny `VersionDegradationKind` `product_postcheck_blocked` (distinkt från `_skipped` = aldrig kört) med full signal-yta per closed-enum-regeln: enum (`event-bus-types.ts`) + emitter (`product-postcheck/route.ts`, `emitPostcheckBlocked` när `!skipped && productBlocked`, meta `blockingCodes`/`warningCount`) + UX-copy (`version-history-status-labels.ts`) + backoffice-telemetri (`llm_flode_telemetry.py`) + glossary. Projektionen är kind-agnostisk → degraderad, aldrig grön. Ren körning + skippar + default-OFF F2 = oförändrade (emitterar inget). Regressionstest i false-green-stability-lanen (`product_postcheck_blocked` överlever `version.done`; 50→51) + 2 route-tester (emit + clean→no-emit; 7→9). Gate: CI grön (quality 4m17s / schema-drift / stability / GitGuardian / Vercel), typecheck/eslint 0, riktade suiter 98. **OBS: båda Cursor-automationerna (`Find critical bugs` + `PR-mergare`) SKIPPADE #180** (oklart varför — pausade el. samtidighet med bugg-agenten) → ingen automatisk bot-review denna gång; ersatt med en **read-only Bugbot-subagent** som rapporterade **inga buggar** + orchestratorns egen diff-granskning. Admin-squash på Jakes go (omr-7-runtime = fråga-policy). Worktree + branch (lokal+remote) städade (fysisk mapp Windows-låst → force-removed + `git worktree prune`). **Område 7 false-green-kärna nu stängd** (missing_url #179 + productBlocked #180 + F3 G#21 + A7-1/A7-2). **Område 4** verifierat redan uppfyllt ("Klart när" = follow-up-intent-tester + S2 åäö + terminologi-lås + omr 5-kontrakt; init-prompt-overhaul = oscope:ad, ej "Klart när"). ~88→~89%. **Nästa: hälsokontroller + handoff** som lämnar bred Område 8-cleanup + backlog (DB-badge finding 02 = bugg-agentens yta, finding 11-lane, B1–B7) åt nästa agent.
- **2026-06-21** — **Område 8-städpass del 1 (ihop med Jake, per-rad-ok + direct-master): arkiv + next-bump + `.gitignore`-prune committat; `.cursorignore`-prune blockerad av verktygsskydd.** (1) **Arkiv:** `2026-06-17-cleanup-forenkling-handoff.md` `git mv` `docs/handoffs/` → `_parkering/` (joinar `deep-research-report.md` som redan låg av-indexerad där → grandmaster-källdocs konsoliderade i parkeringsytan). (2) **next-bump stängd** som inaktuell (`^16.2.9` = senaste stabila; 16.3.0 = preview; noll kod). (3) **`.gitignore`-prune** (Jakes "dead+dedup"-ok): −51 rader döda legacy-paths (archive/scripts-labs, EGEN_MOTOR_V2, scrape-rötter, research/external-templates, `_sidor`, STOR_MIGRATION, SCARFOLDS_ALTERNATIV, dossier-legacy + loggar) + dedup (`.vercel` ×4→1, `.env*.local` ×2→1). **Alla secret-guards + regenererings-mönster verifierat intakta.** (4) **`.cursorignore`-prune EJ gjord** — Cursors edit-verktyg nekar skrivning till `.cursorignore` (self-visibility-skydd); exakta rader överlämnade till Jake (archive-block 127–130, `pot_buggs/`/`internt_jakob/`, `_sidor/`/`research/_sidor/`, `templates_v0/downloads/*`). Synkat: `00 §0`, `03`, `08-cleanup` (arkiv klar + next-bump stängd), `_backlog B6`. Handoffsen orörda (frysta snapshots). **Committat direkt på master** (docs/ignore-config, ej runtime). Rörde ej `launch.json`/`.cursor/settings.json`/`canvas`/`bug-swarm/`-findings. **Kvar i omr 8:** `.cursorignore`-prune (Jake) + ev. fler döda `.cursorignore`-rader (`old/`, `skrapning/`, `EGEN_MOTOR_V2/`, `vercel_templates_levels/`, `manual_log/`, `input_files_for_cursor/`) + `.gitignore:309` (flyttad fil-ref) / `:310` (self-ref) i följdrunda.
- **2026-06-21** — **`.cursorignore`-prune slutförd + master-sync.** Jakes deletions-commit (`6c47bc9de`: −2716 rader avklarat/avklarade-historik + `bug-swarm/README.md` + 3 config/canvas-filer) pushad till master (admin-bypass; `div 0 0`). Sedan `.cursorignore`-prunen som edit-verktyget blockerade gjord via sanktionerad pwsh-fallback (`Set-Content` + rad-filtrering, radslut/encoding bevarat, secret-mirror-sektionen orörd): **−23 döda legacy-rader** (archive-block, `pot_buggs/`/`internt_jakob/`, `_sidor/`/`research/_sidor/`, hela `templates_v0/downloads/*`-blocket). **Område 8 ignore-prune i praktiken klar.** Kvar (frivilligt, ej i godkänt set): enstaka döda `.cursorignore`-rader (`old/`, `skrapning/`, `EGEN_MOTOR_V2/`, `vercel_templates_levels/`, `manual_log/`, `input_files_for_cursor/`) + `.gitignore:309/:310`.
- **2026-06-21** — **Bugg-swarm-triage-runda (rank 1/3/10) → draft [#181](https://github.com/Jakeminator123/sajtmaskin/pull/181) (mot master, väntar Jakes review).** Ny orchestrator triagerade den externa topp-10-listan mot master `1c2b9ace` (parallell bugg-agent kör — branch baserad på master, undviker dess ytor; #181 öppnad som draft per auto-merge/risk-policy). **Verifierat aktuellt + åtgärdat i EN PR (Jakes val "alla buggar i en omgång"):** **rank 3 (kapabilitets-single-source)** — `resolveSelectedDossiersFromSnapshot` läste `snapshot.brief.requestedCapabilities` men persistad snapshot bär `briefSummary.requestedCapabilities` (`own-engine-build-session`→`extractBriefSummary`); F3 readiness + finalize-design skickar rå snapshot → resolverade **alltid 0 dossiers** → feature-runtime-env-nycklar felklassade som build-blocker; läser nu kanoniska `briefSummary` (+ `brief`/top-level-fallback); enhetstestet kodade fel `{brief}`-form (grönt fast prod trasig) → rättat + regressionstest på persistad form. **rank 1 (preview version-pin)** — `fetchPreviewHostStatus` accepterade `running:true` utan att kolla host-rapporterad `versionId` → en session pinnad till X kunde resume:a "running" mot en VM som kör äldre build (stale/vit iframe som "live"); trär nu `entry.versionId` via `tryResumeTier2Runtime`, avvisar **bara** vid känd avvikande version (äldre host utan `versionId` = oförändrat, ingen regress). **rank 10 (migration-ordning)** — `run-migrations` körde `readdir().sort()` (alfabetisk, ej beroende-medveten): `add-generation-telemetry-scaffold-selection.sql` (ALTER) sorterar före `add-generation-telemetry.sql` (CREATE); ersatt med explicit `MIGRATION_ORDER`-manifest + `resolveMigrationRunOrder` (kastar vid manifest/disk-drift). **Medvetet UTANFÖR scope (motiv):** rank 2 = stale (#179/#180); **rank 4 = redan korrekt** (`buildFollowUpContract` läser `briefSummary` via `buildFollowUpBriefFromSnapshot`); rank 5 = gated default-OFF `refuseDossierStubs`, ägar-flipp deferrad (A7-2); rank 6 = färsk omr5-route-hot-path (5-3b), blast radius oviss; **rank 7 (media-auth) + rank 8 (fail-open) = Jake deferrade** (produkt-/policybeslut); rank 9 = `VersionHistory.tsx`-badge = bugg-agentens finding 02 (dubbel-fixas ej). Gate: riktade `vitest` 39/39, `typecheck` 0 fel, `lint` 0 (touched). **Draft #181 → väntar Jakes review/merge** (admin; master CODEOWNERS-låst; runtime-yta = fråga-policy). ~89% (bugg-/städrunda — bred Område 8 fortsatt orörd).
