# Generated user-site preview: TIER-3 STUB placeholder environment variables
#
# These keys are stubbed during F2 ("design preview") so the project boots
# without crashing - but they MUST be replaced by real values before F3
# ("bygg integrationer") can succeed at runtime. F3 merge in
# `src/lib/gen/preview/env-local.ts` strips this layer entirely; F3 then
# reads the user's project env vars and refuses to generate when keys
# from this list are missing (validated via `tier3-build-spec.ts`).
#
# Source-of-truth set: src/lib/integrations/placeholder-harmless.ts
#   (all keys NOT in `PLACEHOLDER_HARMLESS_ENV_KEYS` belong here)
#
# Parity is enforced by:
#   src/lib/integrations/placeholder-harmless.parity.test.ts
#   src/lib/integrations/registry-parity.test.ts

# Stripe - secret key + webhook secret. Real preview/test values needed in F3.
STRIPE_SECRET_KEY=sk_test_placeholder_preview_not_real
STRIPE_WEBHOOK_SECRET=whsec_placeholder_preview

# Supabase-shaped - URL + anon key. Even "anon" must be a real Supabase URL.
NEXT_PUBLIC_SUPABASE_URL=https://placeholder.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.preview_placeholder
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.preview_placeholder

# Clerk - secret key, server-side only.
CLERK_SECRET_KEY=sk_test_placeholder_preview

# Auth0 - issuer + client id + secret are tenant-bound; placeholder cannot reach a tenant.
AUTH0_ISSUER_BASE_URL=https://placeholder.auth0.com
AUTH0_CLIENT_ID=placeholder_client_id
AUTH0_CLIENT_SECRET=placeholder_client_secret

# Google OAuth - client id + secret per project, can't be faked.
GOOGLE_CLIENT_ID=placeholder.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=placeholder_google_client_secret

# AI - OpenAI key reaches a real API in production calls.
OPENAI_API_KEY=sk-placeholder-preview-not-real

# Vercel Blob - write token controls real cloud storage.
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_placeholder_preview

# Vercel KV - REST URL + token, hosted Redis behind Upstash.
KV_REST_API_URL=https://placeholder.kv.vercel-storage.com
KV_REST_API_TOKEN=placeholder_vercel_kv_token

# Upstash Redis REST - placeholder host will not respond.
UPSTASH_REDIS_REST_URL=https://placeholder.upstash.io
UPSTASH_REDIS_REST_TOKEN=placeholder_upstash_token_preview

# MongoDB - Atlas-shaped connection string; placeholder host unreachable.
MONGODB_URI=mongodb+srv://preview:preview@placeholder.mongodb.net/preview?retryWrites=true&w=majority

# CMS - read tokens that hit real APIs.
CONTENTFUL_SPACE_ID=placeholder_space
CONTENTFUL_ACCESS_TOKEN=placeholder_cda_token
SANITY_API_TOKEN=skSanityPlaceholderTokenForPreviewOnly

# Email - Resend API rejects placeholder.
RESEND_API_KEY=re_placeholder_preview_not_a_real_key

# Cache + DB - local placeholder URLs WILL fail in deployed runtime.
# Kept here even without a dedicated registry integration: many scaffolds
# import a Postgres/Redis client at module scope, and an empty value
# crashes `next dev` before any preview can render.
REDIS_URL=redis://127.0.0.1:6379
DATABASE_URL=postgresql://preview:preview@127.0.0.1:5432/preview?sslmode=disable
POSTGRES_URL=postgresql://preview:preview@127.0.0.1:5432/preview?sslmode=disable
