Tidsbunden sessionslogg — inte kanonisk dokumentation.
Flytta viktiga slutsatser till PROJECT-STATE-AND-DIRECTION.md eller relevanta
arkitekturdokument; rensa eller arkivera denna fil periodiskt.

---

2026-03-28 (konsoliderad)

Nuvarande tillstand:

Own-engine ar enda produktionsbanan. V0 SDK, src/lib/v0/, V0_API_KEY, v0Stream.ts
och gen/fallback.ts ar borttagna.
"/api/v0/"-prefix ar HTTP API versioning, inte V0 Platform.

Kanoniska ytor:
- buildExportableProject() — enda artefaktbyggare for export/verify/download.
- GenerationInputPackage + computeLineageHash() — kanonisk fan-in fore generation.
- server-verify.ts — serveragd verify + capped repair-loop efter finalize.
- repairing verification state — DB-driven, visas i VersionHistory.
- Server repair ar default efter quality-gate-fel; klientautofix ar fallback.
- Template-katalog via src/lib/templates/index.ts barrel.
- Sandbox-policy centraliserad i src/lib/mcp/runtime-url.ts.

Oppna gap:

1. Visuell QA med screenshots — visual-qa.ts gor statisk kodanalys;
   riktig QA kraver Playwright-screenshots fran sandbox (bakom feature-flag).
2. GenerationInputPackage-fan-in ar inte helt genomford i alla routes —
   streaming routes beraknar delar lokalt istallet for att bara hela paketet.
3. /api/v0/*-naming debt lever kvar i routes och konsumenter.

Se docs/plans/active/CONSOLIDATED-own-engine-platform-and-quality-v2.md for plan.
