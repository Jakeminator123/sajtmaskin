---
description: Grok = default för ALLA subagenter inkl. bugg-/kodgranskning; slå upp slugen per session, kopiera den inte
alwaysApply: true
---

# Subagent-modeller

**En Grok-modell är standard för ALLA subagenter** — scan, omdöme, destillering,
kedja-runner **och** bugg-/kodgranskning (`bugbot`). Detta är ägarens stående
begäran, så `Task`-verktygets default (`inherit`) gäller inte här.

**Vilken Grok (ägarbeslut 2026-08-14):** slå upp slugen i din egen sessions
`<available_subagent_models>`. Preferens så länge den är gratis:
`cursor-grok-4.6-xhigh-fast`. Finns den inte: nästa Grok-post i **samma lista**
(historiskt `cursor-grok-4.5`). Hittar du ingen Grok: stoppa och säg till —
ärv inte Opus eller Composer.

- **Skicka alltid `model`** i `Task`-anropet, inklusive `bugbot`. Utelämnas den
  ärver subagenten din egen, ofta dyra, modell — tyst.
- **Slå upp slugen, skriv inte av den.** `<grok-4.5>` i kommandon och skills är
  en **roll-platshållare** för den Grok-post stycket ovan pekar på — inte
  slugen `cursor-grok-4.5`. I `git.mdc` / `pr-merge.mdc` står det «slug enligt
  denna fil». Formen varierar mellan sessioner.
- Välj **inte** Composer (`composer-2.5` / `composer-2.5-fast`) som default.
- **Hård regel:** dyra tänkande modeller — i synnerhet
  `claude-opus-5-thinking-xhigh` — får inte vara buggranskare/review-subagent
  som default. Bara när ägaren uttryckligen ber om en specialkoll.

Kommandot eller skillen som startar subagenten äger rollens exakta uppdrag och
antal. Den här regeln äger enbart modellvalet.
