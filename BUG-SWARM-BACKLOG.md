# Bug-backlog (konsoliderad)

**Enda aktiva bugglistan i repot.** Lokal markdown är källan till sanning — ingen Linear, ingen extern tracker. Formatkontrollen (`npm run check:bug-backlog` → `scripts/dev/check-bug-backlog.mjs`, blockerande CI-steg i `quality`) och canvas-generatorn (`scripts/canvas/build-llm-flow-canvas.mjs`) läser **`## Aktiv kö`** nedan.

Filen omstrukturerades 2026-08-05: `Aktiv kö` gick från 55 rader till 21, eftersom 34 av dem inte var bekräftade produktfel utan repro-behov, ägarbeslut, teknisk skuld eller redan löst. Ingen rad raderades — granskningsgrund, full beslutsmatris (`R1`–`R55` → nya ID) och en fryst kopia av den gamla filen ligger i [`backlog-omstrukturering-2026-08-05.md`](docs/plans/avklarat/bug-swarm/backlog-omstrukturering-2026-08-05.md).

Avslutade rader flyttas till [`backlog-arkiv-2026-07-25.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-07-25.md) (senaste), [`backlog-arkiv-2026-07-24.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-07-24.md), [`backlog-arkiv-2026-07-22.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-07-22.md), [`backlog-arkiv-2026-07-02.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-07-02.md), [`backlog-arkiv-2026-06-27.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-06-27.md) och [`backlog-arkiv-2026-06-24.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-06-24.md) (fryst historik). Grandmaster-svärmens B01–B15 = löst historik i [`docs/plans/avklarat/bug-swarm/README.md`](docs/plans/avklarat/bug-swarm/README.md).

## Hur den hålls sann

| Regel | Vad |
| --- | --- |
| **Sex sektioner, en dom per rad** | `Aktiv kö` (bekräftade fel) · `Release blocker` (bakom avstängd flagga) · `Behöver repro` (trovärdig men obevisad) · `Väntar på ägarbeslut` (systemet gör som tänkt) · `Säkerhet, infra och teknisk skuld` (ingen produktbugg) · `Arkiv`. Hör raden i två sektioner: **dela den**, skriv den inte i båda. |
| **Aktiv kö = bara öppna defekter** | Rader där systemet gör fel. Alla rader är `[ ]`. När en rad fixas: **flytta** den till arkivfilen som `[x]` med fix-referens (commit/PR). Bocka inte av på plats. |
| **Stabilt ID** | Varje rad i `Aktiv kö` börjar med `` `SM-###` `` i `Fynd`-cellen. ID:t är stabilt, återanvänds **aldrig**, och säger ingenting om prio eller position — nya rader tar nästa lediga nummer. Refera till en rad med ID, inte med "raden ovan" eller ett radnummer. **ID:t följer med raden till arkivfilen** när den fixas: nästa lediga nummer räknas ut ur kön **plus** arkiven, så en arkiverad rad som tappar sitt ID gör att numret tystnadsfullt kan delas ut igen. Är ett ID förbrukat utan att raden finns kvar någonstans (t.ex. arkiverad innan formatet fanns): skriv en rad om det, som för `SM-010` längst ned. |
| **En falsifierbar premiss** | Fyndcellen ska gå att motbevisa med kod eller en körning. Är premissen bred: dela raden. Lång historik och gamla PR-resonemang hör i arkivet/journalen, inte i kön. |
| **En sanning per rad** | Skriv aldrig "FIXAD" i prosan på en `[ ]`-rad. Det är en motsägelse och formatkontrollen failar (`npm run check:bug-backlog`). Ligger fixen i en **öppen** PR står raden kvar tills den mergas. |
| **Beslut ≠ bug** | Saker där systemet gör som tänkt men vi *kan* välja annorlunda (fail-open, default-off-flaggor, degraded-policy) hör i `Väntar på ägarbeslut` — de räknas inte som risk i canvasen. Ett beslut utan **beslutsägare och deadline** blir en permanent pseudobugg. |
| **Kan ej verifieras statiskt** | Fynd som kräver repro/livekörning hör i `Behöver repro` tills någon kört dem, med exakt vad som ska köras för att raden får flytta. |
| **Skuld ≠ bug** | Hardening, testluckor, dokumentations- och arkitekturskuld räknas inte som produktbuggar och ska inte inflatera kön. |
| **Inflöde** | Nya buggar: `/buggrapport` (skriver en `M#`-rad + nästa lediga `SM`-ID) eller PR-review-gaten (`pr-merge.mdc` → logga med fil-ankare). |

**Status-legend:** `[ ]` = öppen. Källa-taggar: `G#`/`U#` = gamla GPT/UI-svärmlistor, `N#` = defensiv triage, `R#` = äldre kodrapport, `B#` = grandmaster-svärm, `E#` = eval, `FEL#`/`MB#`/`BB#` = PR-review-fynd, `M#` = manuellt rapporterad, `journal R#` = raden i den frysta för-migrationssnapshoten.

## Aktiv kö

Bekräftade öppna defekter. Detta är den enda listan att jobba ur. Canvasen visar dessa som öppna huvudrisker.

> **Senast helverifierad:** 2026-08-05 mot master `2044877c` (sex områdesgranskningar + korsgranskning). Varje rad här kodverifierades som fortfarande öppen; 34 rader flyttades ur kön och 4 arkiverades. Beslutsmatris och fryst historik: [`backlog-omstrukturering-2026-08-05.md`](docs/plans/avklarat/bug-swarm/backlog-omstrukturering-2026-08-05.md).
>
> **Framburen till master `2a9f02050`** (2026-08-05): master hann ändra den gamla filen efter verifieringsbasen, så två deltan är applicerade för hand i stället för att skrivas över. `SM-010` (preview-host boot-fel) arkiverades när #799 mergades, och den enda rad master lade till — `worktree:remove` mot `.env.local`-symlänk — lever nu som `SM-022`. Kön är fortfarande 21 rader.

| Klar | Status | Prio | Fynd | Källa | Beslut / nästa steg |
| --- | --- | --- | --- | --- | --- |
| [ ] | Release blocker (flagga av) | P1 | `SM-007` **Domänköpets restlista är en releasegrind, inte en aktiv incident:** tolv fynd (a)–(l) blir verkliga i samma sekund `SAJTMASKIN_DOMAIN_PURCHASE` slås på, och flera kostar riktiga pengar. Ingen av dem är nåbar med flaggan av. | Codex på #706 2026-08-01 (11 P1) + två oberoende externa granskningar; samtliga kodverifierade · journal R55 | Behandla som **en grind, inte tolv lösa buggar** — ingen del får kvarstå när flaggan vänds. Ordning: (a)+(b) först, sedan pengavägen (c)–(e) med DB-backade kraschtester, sedan (f)–(h), sist UI (i)–(l). Delpunkter i sektionen `Release blocker bakom feature flag: domänköp` nedan. Ägare: `src/lib/domains/`, `src/app/api/domains/`, `src/app/api/stripe/webhook/route.ts`, `src/components/builder/DomainManager.tsx`. |
| [ ] | Öppen bug | P2 | `SM-001` **Repair/autofix skriver över samma version utan återgångspunkt:** existing-version-vägen uppdaterar `files_json` in-place, så en reparation som gör det värre har ingen punkt att återgå till. | F10-residualen ur åtgärdsprogrammet 2026-07-25 · journal R40 | Skapa en immutable repair-version (eller snapshot före skrivning) med exponerad rollback. Ägare: repair-/autofix-skrivvägen i `src/app/api/engine/chats/[chatId]/repair/`. |
| [ ] | Öppen bug | P2 | `SM-002` **Klient-autofix tappar konkret install-/repairkontext:** filtreringen som är gjord för serverrepair används även för `onAutoFix`, så klienten får en urvattnad lista i stället för de faktiska repairable checks. | Bugg-svep 2026-07-24, kodverifierad · journal R36 | Filtrera **endast** repair-requesten; skicka fulla repairable checks till autofix. |
| [ ] | Öppen bug | P2 | `SM-003` **Deploy-repair no-op:ar på orelaterad `repair_available`:** idempotenskontrollen tittar på globalt versions-state i stället för på deployment eller ursprung, så en repair som hör till något annat får den här att tro att jobbet redan är gjort (`deployments/repair/route.ts:93-102`). | Enkelhetssvep 2026-08-05 (ankare rättat) + #798:s omverifiering · journal R49 | Lagra repair-provenance och gör idempotensen per `deploymentId`/origin. |
| [ ] | Öppen kvalitet-risk | P2 | `SM-005` **F3-prompten kan få två integrationsauktoriteter samtidigt:** approval-planen renderas medan pre-generation-kontrakten inte alltid suppressas, så modellen ser två källor som båda gör anspråk på att vara sanningen. | Granskning F3-promptvägen · journal R33 | Suppressa eller slå ihop kontrakten när `approvedProviders` är aktivt. |
| [ ] | Öppen bug | P3 | `SM-008` **F3-markern tappar env-only-förslagets detaljer:** markern bär provider men inte `requestedEnvKeys` efter reload/approve, så ett env-only-förslag glömmer vilka nycklar det bad om. | F3-granskning · journal R43 | Persistera `requestedEnvKeys` och lägg regressionstest på env-only-flödet. |
| [ ] | Öppen kvalitet-risk | P3 | `SM-009` **Aktuellt F3-detaljkort kan säga "planerad" efter leverans i samma runda:** snapshot och panel är post-merge-korrekta, men tidig SSE/meta räknar på basfilerna. Felet pekar åt det ofarliga hållet (understryker en leverans) och träffar bara detaljkortet. | Cursor-bugbot på #623 (medium, loggad i st.f. fixad) · journal R16 | Räkna om eller ersätt detaljkortets `fileEvidence` efter finalize. |
| [ ] | Öppen bug | P2 | `SM-022` **`npm run worktree:remove` kraschar på en worktree som har `.env.local` som symlänk:** teardown anropar `rmdirSync` på fil-symlänken och failar med ENOENT, så borttagningen avbryts halvvägs och worktreet blir kvar registrerat. Allvarligare än det ser ut: rå `git worktree remove` är förbjuden (den följer junctionen och tömmer **huvudcheckoutens** `node_modules`) och blockeras av `worktree-force-guard.mjs`, så skriptet är den enda sanktionerade vägen — en agent som möter felet kan lockas att kringgå vakten i stället. Workaround: koppla loss symlänken och kör om. | Mergeagentens städsvep 2026-08-05, reproducerat på `sajtmaskin-stadning-0805` (M#ma2) | Hantera fil-symlänkar i teardown (`lstatSync` + `unlink` innan `rmdirSync`). Ägare: `scripts/cursor/worktree.mjs`. |
| [ ] | Öppen bug | P2 | `SM-011` **Sync-scaffoldmatchern väljer första tröskelträff och ignorerar capabilities:** `auth` kan returneras innan `ecommerce`/`app` ens jämförs, så en sämre scaffold vinner på ordning. | Scaffold-granskning · journal R6 | Rangordna de samlade scoresen och låt högsta **tillåtna** scaffold vinna. |
| [ ] | Öppen bug | P2 | `SM-012` **Scaffold-valet kan bryta manifestets `allowedBuildIntents`:** efter avsiktlig promotion görs ingen manifestauktoritativ slutkontroll, så slutresultatet kan ligga utanför vad manifestet tillåter. | Scaffold-/manifestgranskning · journal R44 | Validera effective intent efter promotion och fall tillbaka säkert när den inte är tillåten. |
| [ ] | Öppen bug | P2 | `SM-013` **Misslyckad template-init lämnar "Läser in mallen"-laddläget kvar tills omladdning:** `pendingTemplateInit` härleds bara ur `templateId` i URL:en + saknad `chatId`, så ett failat `POST /api/template` lämnar spinnern kvar utan felvy eller åtgärdsknapp — felet syns bara i en toast. | Bugbot-subagent på #737:s diff 2026-08-02, kodverifierad (M#li11) · journal R2 | Tråda template-init-felet till empty-staten (felvy + "försök igen") och återställ attempt-key vid retry. Ägare: `src/components/builder/preview-panel/PreviewPanelEmptyState.tsx` + `useBuilderEffects`-felvägen. |
| [ ] | Öppen bug | P2 | `SM-014` **Tier-2-iframens `onLoad` avtäcker previewn före runtime är redo:** rå `onLoad` sätter `iframeLoading=false` utan readiness-gate, så användaren ser en halvfärdig sida. | Preview-granskning · journal R46 | Håll overlayen till sessionbunden runtime-/bridge-ready eller timeout. |
| [ ] | Öppen a11y-bug | P2 | `SM-015` **Dämpad normaltext klarar inte AA:** `muted-foreground/70` och `text-gray-500` ligger under 4,5:1 mot mörk bakgrund. | A11y-granskning · journal R51 | Inför en semantisk AA-token och kontrasttest per bakgrund. |
| [ ] | Öppen a11y-bug | P3 | `SM-016` **Primärknappens hover-text ligger under AA:** default `hover:bg-primary/90` ger ca 4,14:1 med nuvarande tokens. | Extern djupgranskning 2026-08-01 · journal R4 | Inför en AA-testad **opak** hovernyans och kontrasttest för hover-läget. |
| [ ] | Öppen rapporteringsbug | P2 | `SM-017` **Kvalitetsgrinden stämplas grön innan grinden körts:** finalize skriver `preflight_passed` i det fält som backoffice visar som *slutlig* grind, så en preflight-signal läses som ett gate-utfall. | Telemetri-/statusgranskning · journal R8 | Separera preflight från slutligt gate-utfall och bevara advisory-fynden. |
| [ ] | Öppen UI-bug | P3 | `SM-018` **Aktiv previewflik följer inte intern iframe-navigation:** parentens `previewUrl` ändras inte när användaren klickar sig runt inne i en multipage-preview. | Preview-granskning · journal R29 | Skicka ett validerat route-change via preview-bryggan. |
| [ ] | Öppen workflowbug | P3 | `SM-019` **Vanlig Cursor-kommentar tar bort `merge:ready`-labeln:** freshness-workflowen saknar kontroll av Bugbot-markör/innehåll, så en status- eller dokumentationskommentar river labeln på en färdig PR. | CI-granskning · journal R52 | Invalidera bara för verkliga fynd, inte för status-/doc-kommentarer. Ägare: `scripts/ci/merge-ready-freshness.mjs`. |
| [ ] | Öppen adminbug | P3 | `SM-020` **Legacy-normaliseraren erbjuder ett retired modell-ID:** adminverktyget kan skicka `gpt-5.4-mini` direkt till providern. | Enkelhetssvep 2026-08-05 (enda rent mekaniska raden av åtta) · journal R53 | Läs modellerna ur workload-manifestet och validera/aliasera före anrop. |
| [ ] | Öppen bug | P3 | `SM-021` **`starter-neutral` säger `either` men skickar mörka EXACT-tokens:** metadata och promptinstruktion motsäger varandra, så scaffoldens ljusläge inte är valbart i praktiken. | Scaffold-granskning · journal R7 | Gör metadata och tokens konsekventa. Dashboard-light-gapet är skuld, inte den här raden — se skuldsektionen. |
| [ ] | Öppen bug (halva kvar) | P2 | `SM-025` **Chromium-konkurrens dödar Product Postcheck mitt i körningen:** ~~skip-klassificeraren mappar browser-closed till #783:s installationsdiagnos~~ — **etikettdelen levererad i #841** (navigering testas före browser-mönstret; `page.goto`/`page.evaluate` mot previewen ger `navigation_failed`, äkta launch-fel behåller `playwright_unavailable`). **Kvar:** webbläsaren dör fortfarande mitt i kontrollen. Tidskorrelationen med thumbnail-capturens Chromium (`launchCaptureBrowser` i samma serverless-funktion) är trolig men obevisad orsak. Prod-bevis: v2/v3 i chat `3a6c5472` (2026-08-05, `page.evaluate`) och tre versioner i rad i chat `1b906aa1` (2026-08-08, `page.goto`) — kontrollen har alltså inte kört på tre av fyra versioner i två separata körningar. | M#logg0805 — generation_telemetry + Vercel runtime-logg 2026-08-05; bekräftad igen 2026-08-08 | Utred Chromium-konkurrensen postcheck↔thumbnail (serialisera eller isolera). Ägare: `src/lib/projects/thumbnail-capture.ts` + `runProductPostcheck`. |
| [ ] | Öppen bug | P2 | `SM-026` **OpenClaw armerad auto-send kan aldrig klicka skicka:** `parseOpenClawMessage(msg.content)` ger nytt `action`-objekt per render och ligger i auto-send-effektens dependencylista (`OpenClawMessage.tsx` ~75 + ~414), så varje omrendering kör cleanup (`cancelled = true`, timer rensas) och omstarten bailar direkt på `startedRef` — retryloopen dör före `MAX_ATTEMPTS` och kortet står kvar på "Fyller fältet och skickar…" utan felstatus. `useSmoothText` garanterar omrenderingar under textavslöjandet, så felet är i praktiken deterministiskt. Prod-observerat 2026-08-05: fältet fylldes 20:51, skickaknappen enabled, inget klick på >10 min. | M#logg0805 — live-DOM-observation + kodverifiering | Memoisera parse:n på `msg.content` eller nyckla effekten på `messageId` + primitiva fält i stället för objektreferensen. Ägare: `src/components/openclaw/OpenClawMessage.tsx`. |
| [ ] | Öppen härdning | P3 | `SM-029` **`version-presence` kan klassa ett gammalt RAG-projekts chat-route som openai-chat-bevis:** rag-chat och openai-chat delade filvägen `api/chat/route.ts`, och etapp 4 tog bort presence-distinktionen mellan dem — ett historiskt RAG-projekt skulle därmed få `ai-chat`-filbevis i F3-scopet och kunna underhållas som vanlig chat. Population idag: **noll** (rag-chat selekterades aldrig i prod — parkeringskriteriet), så raden är härdning, inte aktiv defekt. Fyndkälla: extern tungmodellsreview 2026-08-06. | Extern review + kodverifiering (`version-presence.ts`, etapp 4-diffen) | Om äkta RAG-innehåll ska särskiljas: matcha på rag-unika filer (`lib/rag/*`) i stället för delad route-path innan `ai-chat`-bevis stämplas. Ägare: `src/lib/gen/dossiers/version-presence.ts`. |
| [ ] | Öppen bug | P3 | `SM-030` **Explicit Mongo-ask kan ge Postgres i F2 och Mongo i F3 i samma resa:** sedan etapp 3 triggar "MongoDB" capability `database` → postgres-drizzle (F2), men kontraktslagret kan fortfarande namnge `mongodb` som provider (registry-raden lever, medvetet) → ett F3-godkännande bygger generisk Mongo ovanpå en Postgres-F2. Samma lagerkonflikt-klass som `SM-027`. Fyndkälla: extern review 2026-08-06, ej livereproducerad. | Extern review + kodläsning (`registry.ts` mongodb-rad + `detect-integrations`) | Ta i samma pass som `SM-027`: kontraktslagret ska följa capability-lagrets val (postgres-drizzle) eller uttryckligen fråga vid brand-mismatch — aldrig två databaser tyst. Ägare: `src/lib/gen/contract/pre-generation-contracts.ts` + provider-rules. |
| [ ] | Öppen bug | P2 | `SM-027` **Kontraktsinferensen routar fortfarande prenumerationsvokabulär till Stripe:** Stripe-providerregeln matchar `\bsubscription\b`/`\bbilling\b` (`config/ai_models/manifest.json` ~570) och Stripe-placeholder-fallbacken matchar `betalning` i sammansättningar som "återkommande betalning" (`applyDefaultStripePlaceholderWhenPaymentNeeded`, `src/lib/gen/contract/pre-generation-contracts.ts` ~238–240). En recurring-ask får därmed `paymentProvider: stripe` + Stripe-integration i kontrakten trots att capability-lagret sedan etapp 2 (paddle-parkeringen 2026-08-06) medvetet INTE routar recurring till `payments`. För-existerande (ingen paddle-regel har någonsin funnits i kontraktslagret — beteendet fanns även när `subscriptions` levde), men etapp 2 gör motsägelsen till enda rutt. Fyndkälla: bugbot-local på etapp 2-diffen. | Bugbot-pass 2026-08-06 + kodverifiering mot manifest + contracts | Ta bort `subscription`/`billing` ur Stripe-regelns patterns och låt fallbackens `needsPayment` lita på `capabilities.needsPayments` (som redan vetoar recurring) i stället för egen corpus-regex — med motprov att "checkout"/"engångsbetalning"/"kassa" fortfarande ger Stripe-placeholder. Via `/kedja`. Ägare: `config/ai_models/manifest.json` (providerRules) + `src/lib/gen/contract/pre-generation-contracts.ts`. |

**Fixordning** (teman, inte ID-ordning; verifier-/repair-sanningstemat `SM-023`+`SM-024` stängdes 2026-08-08 via #839/#842 — rader i arkivet):

1. Dataintegritet och felåterhämtning: `SM-001`, `SM-002`, `SM-003`.
2. Dossier-/F3-kontrakt: `SM-005`, `SM-008`, `SM-009`, `SM-027` (+ `SM-030` i samma pass), `SM-029`.
3. Scaffold och rendering: `SM-011`, `SM-012`, `SM-013`, `SM-014`.
4. Tillgänglighet: `SM-015`, sedan `SM-016`.
5. Telemetri och mindre UI/CI: `SM-017`, `SM-018`, `SM-019`, `SM-020`, `SM-021`, `SM-025`.
6. OpenClaw-autonomi: `SM-026`.

`SM-007` går inte i den ordningen — den är en grind som ska vara stängd innan `SAJTMASKIN_DOMAIN_PURCHASE` slås på. `SM-022` är verktygsskuld i worktree-teardown och blockerar inget produktarbete, så den kan tas när som helst av den som ändå rör `scripts/cursor/worktree.mjs`.

## Release blocker bakom feature flag: domänköp

Barnpunkterna till `SM-007`. Flaggan `SAJTMASKIN_DOMAIN_PURCHASE` är **av**; #706 landade köpvägen som ett medvetet ofärdigt skelett (ägarbeslut 2026-08-01: registrantdata samlas inte in nu). Ingen punkt här är åtkomlig med flaggan av — alla blir verkliga samtidigt när den vänds.

| Del | Dom | Vad som måste stängas |
| --- | --- | --- |
| a | Blocker + policy | `vercel-client.ts` postar mot utfasade `/v5/domains/buy`; nuvarande anrop är `POST /v1/registrar/domains/{domain}/buy` och kräver `contactInformation` som appen aldrig samlar in — varje betalt köp faller på registrarsteget. Rör även ägarbeslutet om registrantdata (UI, lagring, GDPR). |
| b | Pengabugg | `fulfilment.ts` återbetalar varje undantag efter `markRegistrarCalled()`, så ett tappat svar ger kunden både domänen och pengarna. Inför post-dispatch reconciliation; refundera inte blint efter timeout eller okänd registrarstatus. |
| c | Recovery-lucka | En order som fastnar i `registering` efter en krasch återhämtas aldrig — Stripe slutar leverera och kunden står debiterad (`webhook/route.ts:223`). Lease/watchdog/takeover krävs. |
| d | Recovery-/ledgerlucka | En misslyckad återbetalning för en okänd order svarar 200 utan spårbar rad, och `deleteProject` kan radera `domain_orders` medan en Checkout-session fortfarande kan slutföras (`webhook/route.ts:279`). Persistera unmatched payment/refund-resultat. |
| e | State-modellbrist | `registration_failed` ligger utanför `LIVE_DOMAIN_ORDER_STATUSES`, så ett post-dispatch-fel släpper namnet till en andra köpare fast den första kan äga det (`fulfilment.ts:115`). Inför `registration_unknown`/`manual_review` som håller namnet reserverat. |
| f | Configbugg | `useDomainPurchase` blir sant utan `STRIPE_WEBHOOK_SECRET`, alltså kan kort debiteras medan varje settlement-webhook svarar 500 (`config.ts:494`). |
| g | Urvalsbugg | Tillgänglighet och bindande pris väljs oberoende, så en provider som säger "ledig" kan göra ett odeliverbart erbjudande köpbart (`registrar/index.ts:87`). Samma fulfiller måste svara på båda. |
| h | Kontradiktion | Köpfälten kopieras före `enrichWithWhois`, så RDAP kan säga ledig medan `purchasable: false` står kvar (`domains/check/route.ts:66`). Räkna om `purchasable` efter WHOIS. |
| i | Öppen (residual UX) | Avbruten Checkout: `cancel_url` sätter `domainPurchase=canceled` (`domains/purchase/route.ts:201`) men **ingen klientyta läser parametern** — pollningen startas bara av `domainOrder`, så `pending_payment` fortsätter tills Stripe-expiry byter status. Konsumera cancel-signalen och stoppa pollningen direkt. (Bugbot-verifierad 2026-08-05; förslaget klassade den som "delvis löst", vilket den inte är.) |
| j | Delvis löst; residual P3 | Betalningsstatus återhämtas nu, men retur-URL:en kan tappa chat-/projektkontexten (`domains/purchase/route.ts:200`). |
| k | Recovery-lucka | En registrerad men olänkad domän saknar väg tillbaka — senare sökning visar bara "Upptagen" (`DomainManager.tsx:91`). Lägg en retry-link. |
| l | Produktlucka | `DomainSearchDialog` konsumerar aldrig `purchasable`, så reservera-före-publicering är oåtkomligt (`DomainSearchDialog.tsx:29`). Gör in-app-köpet nåbart från pre-publication-dialogen, eller ta bort den falska köpsignalen. |

## Behöver repro

Fynd som inte kan avgöras statiskt. De är **inte** bekräftade produktbuggar. Flytta till `Aktiv kö` när repro finns, eller till arkivet om de avfärdas. Raden ska ange exakt vad som ska köras.

| Fynd | Källa | Repro-krav |
| --- | --- | --- |
| Automatisk boot av en persisterad `starting`-session kan nollställa clean-exit-budgeten. | Granskning 2026-08-05 · journal R5 | Testa host-omstart/krasch under boot. Håller premissen: flytta resetet till en explicit start-option. |
| Ett gammalt variant-ID kan överleva en lyckad explicit clear/rematch. | journal R12 | Visa den normala vägen där finalize ger `null` med tidigare variant satt. Går det inte: arkivera raden. |
| Synliga template-gallerimallar kan fortfarande krascha eller lata-ladda. | journal R20 | Kör aktuell catalog-/blob-audit + click-smoke på **endast** de synliga mallarna (evidensen är gammal). |
| Verify/export använder F2-placeholderkuvert även i F3. | journal R21 | Reproducera en konkret build-/runtime-avvikelse innan kontraktet breddas. |
| F3-readiness och finalize kan läsa olika `files_json` vid samtidighet. | journal R24 | Concurrencytest med user-edit/repair som verifierar mismatchen. |
| Dubbla approve-svar kan lämna en missvisande user-rad och köra förloraren som F2. | journal R25 | Dubbel-submit-test; förloraren ska ge explicit conflict/no-op. |
| `landing-page` kan få separata `/om` och `/contact` trots one-page. | journal R27 | Deterministiskt route-evalfall utan explicit sidantal. |
| `analytics` + dashboard-charts kan dubbelaktiveras felaktigt. | journal R30 | Kräv konkret prompt och fil-/providerkollision. Annars arkivera. |
| Integrationsdetektorns regex kan missa en integration. | journal R31 | Kräv provider, filsnippet och regressionfixture — manifestet är redan primär källa. |
| Quick-edit applicerar basfiler före lease och kan bli stale. | journal R35 | Samtidig repair/quick-edit-test; läs om snapshoten under lease. |
| DB-pool-svält kan kvarstå trots backoff och observability (P2 vid träff). | journal R41 | Fånga pool `x/3`, idle, waiting och server-headroom under en prodgenerering. Mät före åtgärd. |
| Transient 502 på `/api/openclaw/health` observerades i prod-console men endpointen svarade senare 200. | /logg-internet 2026-07-09 (fynd #13) | Vid nästa incident: korrelera samma tidsfönster mot Vercel runtime-loggar för att skilja cold start från bestående fel. |
| F3 auto-kick `onF3Ready` kringgår stale-base-409-gaten | B12 | Parallell F2-follow-up + F3-send mot samma chatId → förvänta 409. `chat-message-stream-post.ts` + `PreviewPanelF3Trigger.tsx` + `useSendMessage.ts`. |
| `arcade-with-klarna` failar med merge-syntax | E#1, R#10 | `npm run eval:weird-smoke:dump` mot LLM-providers (nycklar + nät + kostnad). |
| Scaffold required files kan tappas i finalize/export-path | R#9 | Deterministisk preflight/export-verifiering när repro finns. |
| Font materializer träffar mest baseline Inter (variant→font-parning) | G#53 | Eval-verifiering mot faktisk output. |
| Element crop missar små element vid DPI/zoom | G#70, U#52 | Inspector-worker (Playwright DPI/zoom). |
| Media local fallback `/api/uploads/media` kanske ej nås av preview-VM | U#29 | Verifiering mot preview-VM-kontext. |
| Analytics före cookie-consent (integritet) | U#56 | App-bred audit av var analytics initieras + gate på consent-flagga. |
| Loopia auto-DNS kräver `OK` på både apex-A och www-CNAME; Loopia kan returnera icke-`OK` när en post redan finns → en re-link kan visa manuella instruktioner i onödan fast DNS är korrekt (godartat: posterna skapas ändå, instruktionerna är korrekta). Flyttad från Aktiv kö 2026-07-22: fixen kräver Loopias faktiska dubblett-svarskod. | Bugbot på PR #395 (`src/app/api/domains/link/route.ts:86-108`) | Provocera en re-link mot en domän med redan skapade poster och logga Loopias exakta svarskod (`src/lib/loopia/loopia-client.ts:189`); behandla den koden som success. |
| Socket connection lost flera ggr/min vid generering av användarsida | M#cr3 | Network HAR/SSE-logg från live-session: vilken endpoint tappar (`/stream`, `/versions`, `/readiness`, `/quality-gate`, `/repair` eller Next/Vercel HMR-socket)? #284/#286 sänkte route-timeouts men ingen direkt SSE/poll-reconnect-fix hittad. |
| `THREE.WebGLRenderer: Context Lost` på landningssidan i prod-konsolen. Misstanke: D-ID:s WebRTC-video och 3D-scenen (`how-it-works-scene.tsx`) konkurrerar om GPU-kontexten, vilket kan lämna scenen svart eller frusen. | M#gl1 (prod-konsol 2026-07-24, samma session som D-ID-avataren) | Öppna `/` i prod med avatar-widgeten aktiv och provocera context-loss (flera flikar / GPU-tryck); avgör om scenen återhämtar sig och jämför med avataren av. Utan repro går GPU-tryck inte att skilja från saknad `webglcontextrestored`-hantering. |
| Vercel-toolbaren rapporterar "visible text must be part of accessible name" på `#radix-_r_8_` / `.transition-opacity` / `.group` i prod. Syskonfynd till M#a11y1, men till skillnad från kontrasten går det här inte att peka ut statiskt. | M#a11y2 (Vercel-toolbar a11y-audit mot prod 2026-07-24) | Radix-id:t är autogenererat och byter värde mellan renderingar. Kör auditen igen och plocka elementet via DOM-inspektion för att se vilken `aria-label` som inte innehåller sin synliga text. |
| Gateway-dashboarden visar "Skills 53/53 active" samtidigt som ~40 av dem har saknade beroenden (`1password`, `apple-notes`, `apple-reminders` m.fl. — desktop-/macOS-integrationer i en Linux-container). Oklart om OpenClaw ändå exponerar dem som tool-definitioner mot modellen, vilket i så fall kostar tokens och kan få agenten att resonera om verktyg den inte kan köra. Prompten själv (`infra/openclaw/config/workspace/TOOLS.md`) påstår INTE att den har dem, så det är ingen promptinkonsekvens. | M#ocskill1 (Render-dashboard 2026-07-24) | Inspektera vad gatewayen faktiskt skickar som `tools` i ett `/v1/chat/completions`-anrop, eller OpenClaws skill-registry i containern, och avgör om skills med saknade deps filtreras bort. Filtreras de bort är raden avfärdad. |

## Väntar på ägarbeslut

Systemet gör som tänkt; "fixen" är ett produkt-/arkitekturval. Räknas **inte** som risk i canvasen. Princip: *systemet får misslyckas, men aldrig ljuga* — placeholder/stub/saknad hard-dossier får aldrig signalera success.

Varje rad ska ha en beslutsägare och en deadline eller trigger. Utan det blir beslutet en permanent pseudobugg.

| Prio | Beslut | Beslutsägare | Deadline / trigger |
| --- | --- | --- | --- |
| P2 | Ska en pending dossier **ersätta** modellbyggd serverkod med samma capability, ska användaren tillfrågas, eller ska bygget fortsätta konservativt (två ytor kan samexistera)? Capability surface ownership-kontraktet pekar mot ersätt. Ägare: `src/lib/gen/dossiers/pending-integrations.ts` + `finalize-design`-flödet. `journal R3` | Jake | Nästa gång F3-byggvägen rörs |
| P2 | Ska `stream_ended_without_version` återbetalas när modelltext levererats men ingen version sparats? `journal R11` | Jake | Före MVP-leverans (rör kreditsanning) |
| P2 | Ska `BuildPlanCard` visas i normalvyn, eller ska Plan-läget döljas helt tills approvalflödet exponeras? `journal R17` | Jake | Före MVP-leverans |
| P2 | Vad betyder "alla versioner failed" per anropare — historik visas i UI, men follow-up och deploy får `null`/error? Kontraktet behöver ett svar per caller. `journal R45` | Jake | Nästa gång versionsläsvägen rörs |
| P2 | Rotera Actions-secreten `OPENAI_API_KEY`, kör ny baseline (`--save-baseline`) och besluta om notifiering vid schemalagt evalfel. Nuvarande baseline är från 2026-03-18 och senaste körningen föll på `invalid_api_key`. `journal R54` | Jake (ägaråtgärd) | Före nästa gång evalgaten citeras som kvalitetsbevis |
| P3 | Ska tool-only-förslag + approval prissättas som två modellrundor eller ett användarsteg? `journal R14` | Jake | Före MVP-leverans (prissättning) |
| P3 | Efter nuvarande minnesmitigering: vilken SLO skulle motivera en separat verify-lane från preview-VM:n? `journal R34` | Jake | Bara om preview-VM-minnet blir en incident igen |

### Tidigare beslutskluster

Oförändrade rader från före omstruktureringen. De saknar beslutsägare/deadline i tabellformen — sätt dem när raden rörs.

| Kluster | Källor | Val / blockare |
| --- | --- | --- |
| `THREE.Clock`-deprecation + `Context Lost` i konsolen (tredjepartsbrus) | Vercel-toolbaren 2026-08-01, kodverifierad (M#a11y1, PR med builder-a11y-fixarna) | three r183 deprecatade `Clock` (`three/build/three.core.js:56272`) och `@react-three/fiber` 9.x skapar en `new THREE.Clock()` per Canvas-mount internt (`events-*.esm.js:985`). Vi instansierar den ingenstans själva — enda fiber-ytorna är landing-scenerna (`particle-orb-canvas.tsx`, `how-it-works-scene.tsx`), och varningen följer med in i buildern eftersom konsolen inte rensas vid SPA-navigering. `WebGLRenderer: Context Lost` kommer från samma scener när canvasen avmonteras eller GPU:n släpper kontexten, och är benign. Uppströmsfixen finns bara i **orelease:ad R3F v10** (pmndrs/react-three-fiber#3741 → `state.clock` borttagen till förmån för RAF-timing); 9.7.0 har den inte. **Val:** vänta på v10 och bumpa då — alternativet är ett console-filter som selektivt sväljer exakt den strängen, vilket döljer framtida three-varningar och måste städas bort ändå. Ingen kodändring nu. |
| Browser-fel från F2-preview når DB men inte en fixande LLM | /logg-utredning + kodkartläggning 2026-08-01 (chat `e8bd3ba6`); bridge mergad PR #778; Product Postcheck utökad 2026-08-05 | **Delvis stängt.** Medan builder-previewn är öppen når iframe-fel `engine_version_error_logs` via preview client-error-bryggan (PR #778, Advisory `preview:client-error`). Product Postcheck (`SAJTMASKIN_F2_PRODUCT_POSTCHECK`, default på) fångar nu även console/network/hydration advisory + bounded desktop-crawl utöver fatala `pageerror`/overlay — men matar fortfarande inte RepairGate. Preview-hosten ser bara serverprocessens stdout/stderr. RepairGate äter idag typecheck/build/lint + VM-byggfel. **Kvarstår medvetet:** koppla bekräftade browser-fel in i RepairGate (hydration-plan steg 4) — kräver först mätning av den nya datan; brus-/privacyavvägningar kvarstår. Rotorsaken till just hydration-klassen (layout-provider-fixerns mid-tree-injektion) är fixad deterministiskt 2026-08-01. |
| Okänd innehållsrevision är fail-open i preview-kvittot | Bugbot på #703 2026-08-01 | `revisionMatchesContent` (`generation-telemetry.ts`) godtar `files_revision IS NULL` som träff, även när anroparen sagt vilken revision VM:n bootade. En NULL-rad som är NYARE än raden för den bootade revisionen tar då stämpeln, medan cachen nycklas på den bootade revisionen och hoppar över vidare skrivningar. Beteendet är beslut 1b (okänd revision = fail-open, aldrig spärr — se `schema.ts` och `add-files-revision.sql`) och inte en regression: samma NULL-rad vann även före #703, då mot versionens nuvarande revision. Dubbelt latent — grinden är av bakom `SAJTMASKIN_CONTENT_REVISION_GATE`, och `resolveFilesRevision` sätter alltid en revision när `versionId` finns. **Val:** behåll fail-open, eller kräv exakt match när anroparen skickat `bootedFilesRevision` (då blir en okänd rad ostämplad i stället för fel-stämplad). Avgörs innan flaggan slås på. |
| Stripe-utbetalningsvägen är inte ekonomiskt avstämd (ägaråtgärd) | Extern djupgranskning 2026-08-01 (deep-research-rapporten), kodkartlagd | Kreditköpet är kodmässigt spårbart — Checkout-session med metadata (`stripe/checkout/route.ts`), signaturverifierad idempotent webhook som skriver `transactions`-raden med `stripe_session_id`/`payment_intent` (`stripe/webhook/route.ts`), och deploy debiteras före leverans (`prepareCredits`). Men repot innehåller ingen Stripe Connect-/transfer-/payout-kod: plattformsköp landar direkt på kontot bakom `STRIPE_SECRET_KEY`, och dossierernas Stripe gäller användarsajternas egna nycklar. Att pengarna faktiskt når avsett konto, i rätt mode, mot rätt price-id och slutlig payout kan inte bevisas i kod — det är en ekonomisk avstämning i Stripe-dashboarden (charge → balance transaction → payout → bank). **Åtgärd är ägarens:** följ en verklig lågkostnadstransaktion hela kedjan och dokumentera utfallet; först då är "köp till avstämd intäkt"-resan bevisad. |
| Eval-gaten mäter inte verklig generering (ägaråtgärd) | Extern granskning 2026-08-01 (#670) | Baslinjen är `2026-03-18` (`eval-baseline.json`) och saknar `blockingChecks`; senaste körningen (`30588420364`, 2026-07-30) föll på `invalid_api_key` med `files=0` och ~2,7 s mot baslinjens ~67 s. Koden mitigerar redan det värsta (`baselineTracksBlockingChecks()` hoppar över blocker-diffen mot en legacy-baslinje) och gaten är **inte** kopplad till PR-CI — den kör på veckoschema i `eval-baseline-update.yml`, medvetet utanför merge-grinden per ADR 0001. Det är alltså ingen false-green i CI, men siffrorna i den senaste dokumenterade körningen beskriver inte generering. **Åtgärd är ägarens:** sätt en giltig `OPENAI_API_KEY` som secret och kör om med `--save-baseline`. |
| Degraded/placeholder-policy (placeholder = degraded, aldrig grön) | N#1, G#17, G#22, G#35, G#49, G#51, U#72 | Warning vs blocker per lane = gate-policybeslut; kan flippa version-status röd. |
| F2/F3 runtime-gate (Product Postcheck) | G#10, N#4, N#H3, R#6 | Default-off + fail-open idag; produktifiering till blockerande = pipeline-beslut. |
| Simplified-brief fallback sänker premium/3D | G#13 | Degraded-mode-signal = kvalitetspolicy (påverkar brief-compose + capability-threading). |
| Verifier-scope + recurring findings | G#33, N#5 | Hela filer vs snippets = verifier-arkitektur + token-budget; recurring-findings = nytt prompt-steg. |
| Verify-skip / cold cache | G#31, G#32 | Warm typecheck dokumenterat fail-open (`cache_cold`); cold-blockerande = infra-beslut. |
| Follow-up kvalitet/budget | G#57, N#3 | Jämka follow-up-gate mot init + hård regression-gate i CI = process-beslut. |
| Env-precedence / docs / refaktor | G#16, G#18, G#19 | `process.env`-refaktor (~100 filer), env-docs canonical-källa, `.env.local`-precedence = egna pass. |
| Säkerhet (deprio — eget pass) | G#40 (residual), #196, #197, B07, B-GA | TOCTOU/DNS-rebinding (IP-pinning), inspect spoof (nonce), publik media GET, OAuth-loggning. App-side SSRF redan fixad #196. |
| Quality-gate fail-open | B08, M#pg1 | **Härdat i två steg:** #299 gjorde `/quality-gate`-routen fail-closed-men-retrybar vid guard-/telemetri-läsfel (`onReadError:"indeterminate"` → ingen promotion, ingen terminal `failed`), och 2026-07-02 (branch `fix/bug-swarm-batch-0702`) fick även kanoniska `promoteVersion`/`acceptRepair` samma `onReadError:"indeterminate"` — läs-exception promotar aldrig. **Kvarvarande medvetet val:** no-telemetri-rader (`signal===null`) behåller fail-open överallt (back-compat för template-import/rollback/legacy). |
| Clerk-dossierns middleware fail-open utan riktiga nycklar | M#oc2 (residual, #351), extern coach post-#351-review "P1-B" | #351:s key-gate:ade `clerk-auth`-middleware returnerar `NextResponse.next()` för ALLA routes (även `/dashboard`, `/api/protected` osv.) när nycklar saknas/är placeholder — medvetet val så F2-preview renderar hela sajten i stället för 500/vit sida. Trade-off: deployar användaren utanför den gate:ade F3-vägen med placeholder-nycklar blir "skyddade" sidor tyst publika. Coach-rekommendation: fail-closed (redirect/503) för protected routes när Clerk är okonfigurerad. Beslut uppskjutet — fail-closed skulle blocka:a member-sidor i F2-preview, och F3/deploy-vägen kräver redan riktiga nycklar via tier3-readiness-gaten. Revisit om export/deploy-utanför-F3 blir en riktig väg. Ägare: `data/dossiers/hard/clerk-auth/components/middleware.ts`. |
| Capability-ytans ownership: prompt-kontrakt + Advisory, ingen deterministisk radering | M#dchat1 (prod-incident 2026-07-13, chat `747636c8`, DB/files_json-verifierad; omtriagerad 2026-07-28 efter #639) | **Ägarbeslut 2026-07-28: prompt-prevention + Advisory är slutläget.** #639 namnger dossierns komponent, importväg och route i follow-up-prompten (modellen ska adaptera eller ersätta, aldrig lämna två) och `runProjectSanityChecks` flaggar en komponent som anropar en API-väg utan route-handler. Det som medvetet **inte** byggs är ett `REPLACES:`-utdataprotokoll där modellen deklarerar ersatta paths och `finalize-merge.ts` raderar dem: en felaktig deklaration raderar användarens filer, alltså dataförlust i utbyte mot mindre brus. Kvarvarande trade-off: struntar modellen i prompt-blocket kan två chatt-ytor samexistera som i incidenten (LLM-byggd `chatbot-widget.tsx` + egen `/api/ai-chat` v5, dossiern `openai-chat` tillagd v6, F3-verify v7 föll på widgetens `TS2345 data.reply`) — vi upptäcker det i diagnostiken i stället för att stoppa det. Revisit bara om Advisory-träffarna visar att det händer ofta i prod. Ägare: `src/lib/gen/system-prompt/sections/dossiers.ts` + `src/lib/gen/validation/project-sanity.ts`. |
| Write-on-read i `/files`-GET (arkitektur) | M#files1 (residual) | Bägge persist-grenarna (heal + materialize) är nu fail-fast/best-effort (`lockTimeoutMs:2000` + try/catch) så läsvägen aldrig 429/500:ar på låskonflikt. Kvarvarande **arkitekturval**: flytta persist från GET till skrivvägen helt — eget pass, ingen aktiv defekt. |
| Draft-version utan parent-lease (vanlig generation) | M#qe1 (residual) | Quick-edit tar nu lease på basversionen (`quick_edit`-kind). Den generella `addAssistantMessageAndCreateDraftVersion`-vägen (vanlig generation/follow-up) tar medvetet INGEN parent-lease — den skapar alltid en ny rad från stream-output och muterar inte basen; stale-base-gaten (5-2) täcker klientsidan. |
| Scaffold/variant/font-tuning | G#50, G#52, G#54, G#66 | Scaffold-defaulttext, pre-match vs embedding, Geist-workaround, adaptiv `MAX_PAGES` = heuristik-/produkttuning. |
| Produkt-/UX-gap | U#10, U#23, U#42, U#47, U#49, G#71 | Optimistic conflict, fler transcribe-språk, scraper-prioritet, OpenClaw 180k-context, D-ID session, PDF print-flöde. |
| Logg-/observability-/storage-städ | G#59, G#60, G#63, U#53, U#63, U#66 | Bred städ med sampling-/namespace-policy = sammanhållet pass, inte punktfix. |
| Arkitektur (deploy-topologi/lane) | B3/E2, B1, B4, F4/F5, B7/#140 | Durable event-bus (multi-instans), S3-lane blockerande, canvas-PR-token, bus-emits/manifest-Zod, DB/Blob-gate-PR. |
| A7-2 kod-default | B05 | Kod-fix (scope till valda dossiers) mergad #211; kod-default OFF kvarstår som ditt val (env satt i Vercel). |
| CTA-knapp i init-fast-lane | M#1 (EGEN-05) | `simpleWebsitePath` blockar `CTA-knapp` från fast lane via bredare `cta`-match än follow-up-vocabulary. By-design: namngiven sektions-capability i init ska gå full dossier-pipeline (#242 Alt A, testat `simple-website-path.test.ts:134`). Skillnaden mot follow-up (styling-tweak) är medveten. |
| components/ui canonical-skydd (drop av LLM-emitterade shadcn-stem-filer) | #263 | Avsiktligt: `components/ui/<shadcn-stem>` behandlas som host-ägda canonical runtime-filer; drop sker bara när canonical ersättning finns. App-specifika exports i en canonical shadcn-path stöds ej → revisitas om det dyker upp i riktiga generationer. Codex P2 på PR #263. |
| Preview-retention (last-good, #345) rensas inte i efterhand — flyttad ur Aktiv kö 2026-07-22 | BB#345r7 (bugbot round 7) | Om en follow-up-version retainar föregående tier-2-frame och SENARE hård-failar utan egen `previewUrl` ligger gamla framen kvar (`didChangeVersion` one-shot). Status/chat visar failen sanningsenligt; smal repro (F2 typecheck-fail är advisory → klassen krympte). **Medvetet val** (raden var redan märkt "medvetet loggad i st.f. fixad"): undvik mer stateful komplexitet i retention-pathen. Ev. framtida effekt: rensa retained preview när aktiv version når terminal `failed` utan previewUrl (`useBuilderPageController.ts` preview-sync). |
| Deferred re-verify hoppas över av inflight | BB-265 (MEDIUM) | Stale-base-recovery i `repair/route.ts` schemalägger `triggerServerVerification` via `after()`; om `inflight` redan håller versionId returnerar den tidigt och callbacken no-op:ar medan raden står i `repairing`. Täcks av readiness-watchdogen (`failVersionVerificationIfUnleased`, lease-safe, targetar `repairing` när lease-tabellen finns) + den samtidiga körningen som äger versionen. Ingen ny race införd. Kommentar finns vid `after()`-siten. |
| Repair-loop pass 0 startar trots deadline | #286, M#cr4 | Avsiktligt: `pass > 0`-budgetguarden ger alltid ≥1 fixer-försök (`repair-loop.ts:569`). LLM-fixern har egen `AbortController(llmTimeoutMs)` men bounds inte av `repairDeadlineEpochMs`, så ett sent pass 0 kan löpa mot route-`maxDuration`. Final-gate-verifyn har separat budgetguard (`resolveFinalGateVerifyBudget`). Att låta pass 0 respektera deadline = risk att tappa "alltid ett försök"-garantin. |
| Dependabot auto-merge on-switch saknar sweep + protected-path-filter | Codex P2 pa #453 (2026-07-08, `.github/workflows/dependabot-safe-automerge.yml`) | Innan `DEPENDABOT_AUTOMERGE_ENABLED=true` ar sakert: (a) `--auto` kor ingen bot-fynd-sweep/settling → sena Codex/VADE-fynd kan auto-mergas otriagerade; (b) klassificeraren kollar bara patch + core-namn, sa en `github-actions`-patch (`actions/checkout`) som andrar `.github/workflows/*` passerar trots protected-path-policyn. Val: automatisera sweep-gate + lagg changed-files/protected-path-filter i workflow:et innan on-switchen anvands. Halls AV tills dess. |
| Master-ruleset required status checks — ATGARDAT 2026-07-08 | Repo-granskning 2026-07-08 | `quality`, `backoffice-tests`, `schema-drift` ar nu tillagda som required checks pa "Protect master" (icke-admin-PR kan inte merge:as med dem roda). Admin bypassar fortfarande allt, sa `--admin`-merge med rod CI ar fortsatt agent-disciplin. Kvar (separat val): satt `DEPENDABOT_AUTOMERGE_ENABLED=true` om dependabot ska auto-merga patch-PR:er. |
| CI-grindar som failar hårt men inte blockerar merge (required-status, forts. på raden ovan) | CI-genomgång 2026-07-29 (rulesetet `Protect master` + `gh pr checks` mot #644) | Fyra jobb failar hårt men står **inte** som required: det nya `build`, `preview-host-guards`, `dead-code` (orphan-filgrind) och `db-blob-sync`. Ett rött resultat i dem stoppas alltså bara av agent-/människodisciplin. Val per jobb, inte ett paketbeslut: `build` och `preview-host-guards` är deterministiska och nyckelfria → naturliga required-kandidater; `dead-code` blandar en blockerande orphan-grind med en rådgivande knip-rapport; `db-blob-sync` kör utan credentials på PR och är därför inte meningsfull som merge-grind i nuläget. Kräver rulesetändring, alltså ägaråtgärd — inte något ett kod-PR gör självt. Aktuell tabell: [`docs/testing.md`](docs/testing.md) → "Vilka CI-jobb blockerar faktiskt merge". |
| Warn-only-lanen har fyra tester ingen ser, och en engångsvakt lever kvar | CI-genomgång 2026-07-29 (`vitest.stability.config.ts` + `ci.yml`) | Två småval i samma område. (a) `stability`-jobbet är `continue-on-error` och innehåller sex `*.stability.test.ts` — men bara två (`followup-freeze`, `followup-capabilities`) hard-gate:as separat via `test:followup-contract`. De fyra övriga (`false-green-projection`, `status-resolver-single-writer`, `aao-invariant`, `refuse-dossier-stubs`) kan vara röda utan att någon märker det; "warn-only för alltid" är ingen signal. Val: befordra dem till blockerande (som followup-paret) eller radera dem. (b) `compat:v0-chat-boundary:check` är en engångsvakt för borttagna v0-chat-routes — låg återfallsrisk, permanent CI-kostnad. Val: behåll eller avveckla. Ingen av dem är en defekt idag. |
| Publikt repo exponerar admin/credit-mekanik + infra-id:n i docs — ÄGARVAL: PUBLIC 2026-07-18 | Repo-granskning 2026-07-08 → PUBLIC; omkontroll 2026-07-13 → PRIVATE; anonym GitHub API + `git ls-remote` 2026-07-18 → PUBLIC | Ägaren gjorde repot publikt igen. Det är inte i sig en kodbugg, men gör publik docs-hygien obligatorisk: inga secrets och sparsamt med admin-/credit-recept eller infra-id:n. Verifiera aktuell synlighet innan nya styrbeslut bygger på raden. |
| F2-demo-deploy utan placeholder-env (vit sida-risk) | Codex P1 på #443 (postad efter merge, triagerad 2026-07-09 → beslut, ej defekt) | En F2/design-version vars enda env-täckning är genererad `.env.local` deployas MEDVETET utan de värdena: `.env.local` strippas ur deploy-paketet (`route.ts` + `strip-env-local-for-zip.ts`) och Vercel får bara `projectEnv.configuredMap`; `envWarnings` säger uttryckligen "deployas utan riktiga värden tills du fyller i dem". Risk: en demo som kräver nycklarna i build/SSR kan ge vit sida. Val om beteendet ska ändras: injicera tier3-stub-värden för `placeholderCoveredKeys` i deploy-env-assembly, eller blocka F2-deploy när kod kräver nyckeln vid build. Ägare: `src/app/api/v0/deployments/route.ts` env-assembly. |
| Template-import env/drift (uppskjutet) | M#tmpl1 (audit 2026-07-08, `scripts/v0-templates/audit-template-repos.mjs`) | Importerade v0-mallar previewar verbatim och de flesta funkar; lågprio-val vi medvetet skjuter upp (ingen aktiv defekt): (1) preview-placeholders för de 11 nu **gömda** env-krasch-mallarna (modultopp, `EXCLUDED_TEMPLATE_IDS`) så de kan återinföras (nycklar t.ex. `FAL_KEY`, `NEON_DATABASE_URL`, `X_CLIENT_*`, `SUPABASE_SERVICE_ROLE_KEY`, `AI_GATEWAY_API_KEY`, QStash) i `config/ai_models/41-tier3-stub-placeholders.env.txt`; (2) bredare placeholders för ~19 "feature-döda" mallar (renderar men läser okuvrad server-nyckel lazy); ~~(3) verifiera om next/react-major-drift faktiskt bryter en mall vid första follow-up~~ ✅ ELIMINERAT 2026-07-23 (imported repo mode): follow-ups på `imported_repo`-chattar hoppar över `buildCompleteProject`/force-pins helt — mallens versioner + lockfil bevaras verbatim genom hela follow-up/verify/export-kedjan, och ingen scaffold auto-matchas/pinnas längre; se `docs/architecture/templates.md` §4. ~~(4) full borttagning av deprecerad legacy v0-auto-fetch~~ ✅ KLART 2026-07-08 (PR `chore/ta-bort-legacy-template-sync`): script, workflow, admin-route, `TEMPLATE_SYNC_*`-env och admin-UI raderade — Blob-vägen är enda katalogvägen. Kontext: `docs/architecture/templates.md`. |
| Preview-förvärmning (host-väckning + överlappad install, default OFF) | Codex P1 post-merge #480 + två `/granska`-härdningar (2026-07-11) | **Fixat:** explicit `prewarm`/`prewarmReplacementPending` döljer skelettet via HTTP och nekar alla WS; failed real replacement är stabil 503 utan refresh/auto-requeue, explicit real retry återgår till `starting`; ordinary booting behåller last-good och stale persisted `starting` återköas. Store-lås stoppar sent prewarm (`409`); idempotent recovery restartar bara missing/dead, inte healthy. `429` gör ingen auto-retry men släpper app-process-dedup för senare user-retry. Lease-HMAC kräver preview-host API-key + kanonisk user/trusted-IP-identitet; bootfel behåller cooldown, real claim/update/patch/destroy/cleanup/expiry/reset släpper, hibernate behåller. Fast kod-cap 4096 + admin count/expiry telemetry. **By design:** parallell andra chat får normal cold finalize (generation blockas inte); IP-rotation kan inte elimineras; `ip:unknown` är restriktivt; multi-machine-lock är framtida topologi. Tester: `test-prewarm-guards.mjs` + real HTTP/WS `test-preview-proxy-contract.mjs`. Flaggan förblir OFF; host-first deploy krävs. |
| Prod auto-migration parallellt med Vercel-deploy (race) | /granska-svärm 2026-07-11 (djävulens advokat + regression) | `prod-migrations-apply` väntar på `quality`+`schema-drift` medan Vercel bygger direkt på push → prod-kod kan bli live några minuter före schemat migrerats. **Medvetet val:** migrationerna är additiva (`ADD COLUMN IF NOT EXISTS`) så en parallell deploy är ofarlig (ny kod läser kolumner som strax finns; gammal kod bryr sig inte). Helt race-fritt kräver att Vercel-deployen gate:as bakom jobbet — egen infra-ändring, inte gjord nu. Ägare: `.github/workflows/ci.yml` + Vercel deploy-hook. |
| CI-verifiering `prod-migrations-applied` skippas vid röd `quality`/`schema-drift` | /granska-svärm 2026-07-11 (regression) | `needs: prod-migrations-apply` gör att ledger-checken **skippas** (inte körs) när apply-jobbet skippas pga failade gates. **Medvetet:** en trasig merge ska aldrig mutera prod-schemat, och master är redan röd via `quality`/`schema-drift` då — ingen separat migration-signal behövs. På nästa gröna push körs apply→applied normalt, så ingen bestående drift. |
| Capability-provenance (splittrat ägarskap) — flyttad ur Aktiv kö 2026-07-13 (coach-granskad) | BB#capsrc1 + #491/#492/#497 + extern coach (arkitektur) | **Ägarbeslut 2026-07-12 (etapp 7-upplåsning): accepterad som deferred.** Durable tombstone (#494→#497: merge + `buildFollowUpContract`) räcker; ORCHESTRATE-vägens rund-scopade removal (`orchestrate.ts:~1136/1590/1675/1157`) är mitigerad av strippad brief + tom-floor-auktoritet + #497:s subtraktion. Kanonisk `CapabilityIntentDelta`-refaktor tas som eget initiativ när området rörs — INGA fler capability-regexar/konsumentfall innan dess. |
| DATABASE_URL utan `integrationRegistry`-entry (parity-gap, ej false-green) — flyttad ur Aktiv kö 2026-07-13 | Codex P1→P2 på #445 | `ready:true` är **korrekt by design**: `DATABASE_URL` är feature-runtime + seed-fallback (samma mönster som ably/fal i #430, vars envs inte heller ligger i registryt). Val: ev. smal `postgres`-registry-entry + detektion MEN måste garantera feature-runtime-klassning i `deriveTier3BuildSpec` (naiv entry → falsk BLOCKER mot seed-fallback-beslutet). Ägare: `src/lib/integrations/registry.ts` + `detect-integrations.ts` + `tier3-build-spec.ts`. |
| CSP report-only-spam i console — flyttad ur Aktiv kö 2026-07-13, app-ytan åtgärdad 2026-07-25 | /logg-internet 2026-07-09 (#14) + ägarrapport 2026-07-25 (prod-console) | Report-only är fortsatt avsiktligt CSP-läge. `unsafe-eval`-bruset (×45) spårades 2026-07-25 till **Zod v4:s JIT** — `new Function` per objektschema, plus dess egen `Function("")`-probe — inte till preview-runtimen som raden först antog. Borta sedan `src/instrumentation-client.ts` sätter `z.config({ jitless: true })` i klienten; CSP:n behövde inte luckras upp. Kvar som medvetet val: report-only-läget självt, och nonce/allowlist för preview-dev-runtimen när CSP:n ändå revideras. |
| P11 orphan engine-chat save/restore-policy — flyttad ur Aktiv kö 2026-07-13 | M#p11b (extern post-merge-review R-P11) | `save`/`restore` tillåter `not_found` för olänkade chattar — rimligt idag. Öppen fråga är ett **policyval** (`allowed` vs `forbidden` när orphan saknar `project_id`), inte felbeteende. Lås valet + test när området rörs (`src/lib/tenant.ts` `resolveEngineChatOwnershipForRequest` + `save/route.ts`). |
| Admin-UI klient-gate (`localStorage["admin-auth"]`) vs server-allowlist — **ÅTGÄRDAT 2026-07-24** | PR #355-triage #33 | Klient-gaten och den egna inloggningsformen är borta. `/admin` har nu en server-gate i `src/app/admin/layout.tsx` (`getAdminUserForPage()` → samma `isAdminEmailEdge`-predikat som `requireAdminAccess` och proxy-gaten), så en inloggad admin kommer in direkt och en icke-admin redirectas av `src/proxy.ts`. `NEXT_PUBLIC_ADMIN_EMAILS` används bara för att visa/gömma genvägen i navbaren (`src/lib/auth/is-admin-client.ts`), aldrig som gate. |
| `/api/analytics` GET gate:ad på enbart `TEST_USER_EMAIL` — **ÅTGÄRDAT 2026-07-24** | Hittat vid /admin-renoveringen (manuell körning med admin-konto) | Statistikvyn gav `401` för varje riktigt admin-konto i `ADMIN_EMAILS`/`SUPERADMIN_EMAIL`, och gamla admin-UI:t tolkade 401 som utloggning. Routen använder nu `requireAdminAccess` som övriga admin-routes (predikatet inkluderar fortfarande `TEST_USER_EMAIL` → strikt superset, ingen åtkomst borttagen). |
| Bulkradering av Vercel-projekt i admin-UI kunde radera Sajtmaskins eget prod-projekt — **ÅTGÄRDAT 2026-07-24** | Hittat vid /admin-renoveringen (kodläsning + rutt-test) | `mega-cleanup` och `cleanup-vercel-projects` listade `api.vercel.com/v9/projects` och raderade **alla** projekt token såg, inklusive `VERCEL_PROJECT_ID`, bakom en två-klicks-knapp. `cleanup-vercel-projects` är pensionerad (410), `mega-cleanup` är nu en data-/cache-nollställning utan Vercel-anrop, och per-projekt-radering vägrar appens eget projekt (`src/lib/vercel/self-project-guard.ts`). Låst med tester. |
| Health-endpoints bevisar process-liveness, inte modellkapacitet | M#och1 (prod-felsökning 2026-07-24, Codex-kvoten utmattad) | Både `/api/openclaw/health` och gatewayens `/health` svarade 200/`"ok"` medan varje modell i fallback-kedjan var kvotslut och Sajtagenten inte kunde svara ett enda tecken — en falsk grön signal som kostade en hel felsökningssession. **Medvetet val:** en riktig modell-probe per health-anrop kostar pengar och kan själv slå i kvoten. I stället är fällan dokumenterad (`infra/openclaw/DEPLOY_INFO.txt` §6) och orsaken ytas nu i chatten i st.f. att sväljas (`src/lib/openclaw/gateway-response.ts`, PR #605). Revisit om en billig kapacitets-signal blir tillgänglig. Ägare: `src/lib/openclaw/status.ts`. |

Full detalj + alla `[x]`/avfärdade rader: [`backlog-arkiv-2026-07-02.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-07-02.md) · [`backlog-arkiv-2026-06-27.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-06-27.md) · [`backlog-arkiv-2026-06-24.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-06-24.md).

## Säkerhet, infra och teknisk skuld

Ingen av dessa är en produktbugg. De ska inte inflatera `Aktiv kö` och räknas inte som öppen risk i canvasen — men de försvinner inte heller av sig själva.

| Prio | Klass | Skuld / åtgärd | Källa |
| --- | --- | --- | --- |
| P2 | Säkerhetshardening | Läsande CI-jobb använder samma prod-credentials som skrivande. Ge dem en separat read-only prod-roll/DSN (least privilege). | `journal R22` |
| P3 | Latent modellhardening | `resolvePhaseThinking` strippar bara `reasoningMode`; pekas `SAJTMASKIN_MODEL_PREMIUM` (eller legacy `SAJTMASKIN_MODEL_FAST`) mot en modell utan reasoning-stöd skickas `high` ändå. **Inte nåbar idag** — ingen av variablerna är satt i något Vercel-target eller repo-env. Klampa effort mot den resolvade modellens capability. Ägare: `src/lib/models/phase-routing.ts` + `src/lib/gen/engine.ts`. | `journal R1` |
| P3 | Latent modellhardening | Separera `budgetModelId` från BuildSpec-modellen vid planner-override, så budget och faktisk modell inte kan glida isär. | `journal R32` |
| P3 | Test-/evalskuld | Besluta evaltröskeln och implementera `must`/`mustNot` + embedding-`textHash`. | `journal R9` |
| P3 | Testskuld | Komplettera runtime-guards med kö-samspel och `idle→hibernated→reboot`. PR #799 betalar en del av detta (`test-runtime-guards.mjs`) — omvärdera raden när den mergats. | `journal R38` |
| P3 | Arkitekturskuld | Ta bort det döda SSE-callbackförsöket och gör polling kanonisk i stället för att ha två halva vägar. | `journal R10` |
| P3 | Latent promptkvalitet | Mät hur ofta budgettriggern slår och inför fil-/fence-medveten truncering i stället för blind kapning. | `journal R13` |
| P3 | Legacy-yta | Bestäm kompatibilitetsperiod för `template_cache` och migrera eller droppa. Ser ut som en radering tills man räknar de 8–12 filerna plus migrationen. | `journal R18` |
| P3 | UX-hardening | Disabla save under `verifying`/`repairing` och bevara den lokala draften. | `journal R19` |
| P3 | Observability-skuld | Välj en durable signal för `deterministic_release_exempted` — men bara om det finns en verklig konsument. | `journal R26` |
| P3 | Dokumentationsskuld | Låt `avklarat`-indexet bära status; detaljfiler behåller bara invariants och proveniens. | `journal R23` |
| P3 | Repohygien | Normalisera CRLF separat eller on-touch. Ingen runtimebugg. | `journal R15` |
| P3 | Scaffold-skuld | Dashboard-light-gapet (delen av `SM-021` som inte är metadatakonflikten). | `journal R7` |
| P3 | Modellkälla-skuld | Bredare gap bakom `SM-020`: adminverktygets modellista härleds inte ur workload-manifestet, så retired ID kan dyka upp igen på nya ytor. | `journal R53` |

## Arkiv (avslutat i denna omstrukturering)

Flyttade ur kön med fixreferens. Full historik i [`backlog-omstrukturering-2026-08-05.md`](docs/plans/avklarat/bug-swarm/backlog-omstrukturering-2026-08-05.md).

| Rad | Dom | Bevis / destination |
| --- | --- | --- |
| `journal R37` | Kodfixad | #770 trådar innehållsrevision i både enskild och batchad status. Eventuell flaggaktivering hör till rollout, inte till en buggrad. |
| `journal R39` | Gammal/felaktig premiss | False-green-riktningen var redan avfärdad och stale revision degraderas. Absorberas av revisionsrolloutet. |
| `journal R42` | Kodfixad + dublett | #710 binder previewkvittot till `filesRevision`. Kvarvarande feature-flag-fråga hör till revisionsrolloutet. |
| `journal R47` | Kodfixad | Preview-hostens status skiljer nu `running`, `httpReady` och `readinessState`. |
| `journal R40` (moderrad) | Levererad; residual kvar | Moderraden är levererad; bara F10-residualen står kvar och lever som `SM-001`. |
| `journal R16`, `R7`, `R53` (delar) | Delvis löst | De redan lösta eller skuldklassade delarna är arkiverade; bara den smala aktiva premissen står kvar som `SM-009`, `SM-021` respektive `SM-020`. |

`journal R50` bar `SM-010` i den första omstruktureringen, eftersom fixen då låg i öppen PR #799. #799 mergades innan den här filen nådde master, så raden är arkiverad som `[x] Fixad` i [`backlog-arkiv-2026-07-25.md`](docs/plans/avklarat/bug-swarm/backlog-arkiv-2026-07-25.md) och `SM-010` är **förbrukat** — ID:t återanvänds aldrig. Platsen i kön togs i stället av `SM-022`, som är den enda rad master hann lägga till efter omstruktureringens bas.

## Naming-debt: `v0ChatId`

Inte ett dött fält. Live DB-kolumn (`chats.v0_chat_id`, notNull/unique) + load-bearing konsument (`useBuilderVmPreview.ts` gatar VM-preview-bootstrap för legacy-mappade chattar). Full borttagning = tyst regression + bruten DB/payload-nyckel → kräver **migrationsplan** (byt internt symbolnamn, behåll DB/payload-kompat) per `docs/architecture/code-map.md`. Den säkra delmängden är **borttagen** 2026-06-28 (död `|| data.v0ChatId`-läsning i `useCreateChat.ts` + okonsumerade `v0ChatId`-svarsnycklar i `/api/projects/[id]/chat`); DB-kolumnen, `useBuilderVmPreview.ts`-gaten och interna DB-lookups behålls tills en migrationsplan finns.
