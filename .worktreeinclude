# Default is empty on purpose. `worktree:setup` seeds `.cursor/mcp.json` from
# the tracked `.cursor/mcp.json.example` and copies only paths listed here.
# Add only an explicitly reviewed, non-sensitive path when a task genuinely
# needs it. Never add `.env.local` or live connector credentials.
