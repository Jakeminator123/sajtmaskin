# Track Plan Template

Read `../PROTOCOL.md` and the parent run's `ROADMAP.md` first.

## Track

- Track name:
- Parent run:
- Owner agent:
- Recommended model tier:
- Dependencies from other tracks:

## Scope

Describe the broader workstream this track owns. Keep the scope coherent enough
that one tier-2 agent can manage it without needing the full run context every
time.

## Inputs

- Relevant files:
- Relevant docs:
- Known risks:
- Constraints:

## Micro-workloads

Split this track into `2` to `8` narrow tasks.

1. `01`:
2. `02`:
3. `03`:

## Acceptance criteria

- The track's outputs are verified before the next track depends on them.
- Each micro-workload writes an agent log.
- Cross-track assumptions are recorded in `ORCHESTRATOR_LOG.md`.
