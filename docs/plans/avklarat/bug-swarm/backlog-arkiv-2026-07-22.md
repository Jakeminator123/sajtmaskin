# Bug-backlog — arkiv (2026-07-22)

> Flyttade rader från [`BUG-SWARM-BACKLOG.md`](../../../../BUG-SWARM-BACKLOG.md) efter nattens gated bug-swarm-pass 2026-07-22 (read-only-triage av hela Aktiv kö mot master `bd09e331` + fix-PR **#592**, en commit per rotorsak). Nyare rader överst i huvudfilen; äldre historik: [`backlog-arkiv-2026-07-02.md`](backlog-arkiv-2026-07-02.md) · [`backlog-arkiv-2026-06-27.md`](backlog-arkiv-2026-06-27.md) · [`backlog-arkiv-2026-06-24.md`](backlog-arkiv-2026-06-24.md).

## Fixade i PR #592 (bug-swarm-passet 2026-07-22)

| Klar | Status | Prio | Fynd | Källa | Fix-referens |
| --- | --- | --- | --- | --- | --- |
| [x] | Fixad | P3 | **Flake i `quality`-CI:** timer-läcka i `usePreviewPanelInspectMapPlacement.ts` (`window.setTimeout` fyrade efter jsdom-teardown → `ReferenceError: window is not defined`); fällde quality-jobbet på #500 trots 4359/4359 gröna tester. | CI-run 29202297223 (#500, 2026-07-12) | **PR #592** `fix(preview-panel)`: pending sleep-timer rensas i effect-cleanup; regressionstest verifierat rött utan fixen (0 pending timers efter unmount). |
| [x] | Fixad | P2 | `dashboard-charts` med `promptInstructionMode: compact` släppte client-only/avoid-regler i codegen-prompten → modellen kunde bryta SSR/VChart-kontrakt. | Automat A#6 (#422) | **PR #592** `fix(dossiers)`: manifestet bytt till `selected-sections` (samma läge som 11 andra dossiers) — When to use/How to integrate/Avoid når nu prompten. `dossiers:validate-all` grön. |
| [x] | Fixad | P2 | Dossier promotion i backoffice jämförde inte `manifest.capability` mot plan — fel dossier kunde promotas. | Automat A#14 (#419) | **PR #592** `fix(backoffice)`: `_promote_prospect` kräver normaliserad capability-match mot plan-postens `targetCapability` (äldre planer utan fältet släpps igenom); 3 nya tester i `test_dossiers_page.py`. |
| [x] | Fixad | P3 | CLI `normalize-legacy-prospect` kraschade på trasig `prospects.json` (`undefined.filter`). | Automat A#15 (#419) | **PR #592** `fix(dossier-scripts)`: `parseProspectsPlanFile` ger actionable fel för ogiltig JSON/saknad `prospects`-array; `main()` gate:ad med `isInvokedDirectly` (mönstret från `check-sdk-versions.mjs`); 5 tester. |
| [x] | Fixad | P3 | `inventory-legacy.mjs` saknade npm-script trots planreferens; hårdkodad Windows-default. | Automat A#19–A#20 (#419) | **PR #592** `fix(dossier-scripts)`: `npm run dossiers:inventory-legacy` + plattformsneutral default-root (syskonmapp `../dossiers-legacy-2026-04-20`, override `DOSSIER_LEGACY_ROOT`/`--root=`). |
| [x] | Fixad | P3 | **quality-gate-routens error-log-skrivningar saknade `lockTimeoutMs`**: blockerade till globala `statement_timeout` vid lease-kontention innan `.catch` degraderade. | M#el2 (syskonfynd till /error-log-fixen 2026-07-03) | **PR #592** `fix(quality-gate)`: alla 4 `createEngineVersionErrorLogs`-anrop i routen kör med `lockTimeoutMs: 3000` (samma som /error-log-routen); route-testet låser optionskontraktet. |
| [x] | Fixad | P3 | Auth-hydration-glapp: landing-headern renderade "Logga in"/"Kom igång gratis" i ~1 render trots aktiv session innan auth-state kom ikapp. | /logg-internet 2026-07-09 (fynd #22) | **PR #592** `fix(landing)`: auth-beroende CTA:er (desktop + mobil) renderas först när `isInitialized`; skeleton-placeholder håller platsen. 3 regressionstester. |
| [x] | Fixad | P3 | F2-verify-lanen (`post-checks.ts`) retryade inte retrybara 503 från quality-gate (`lease_unavailable`/`quality_gate_unavailable`) — F3-vägarna retryar generellt, F2-lanen hanterade det som generiskt fel. | Granska-svärm F5 på #504 | **PR #592** `fix(post-checks)`: bounded 503-retry i F2-lanen (2 försök, 2s/4s backoff), därefter ordinarie felhantering. 2 tester (retry-till-grön + utmattning). |
| [x] | Fixad | P3 | Codex P2 ×3 från #353, punkt (2): `tier3-readiness-gate.ts` inspekterade bara senaste 200 error-log-rader för `product_postcheck.summary` — postcheck skriver en rad per produktvarning så summary-raden kunde skjutas utanför fönstret (tyst avblockering). | Codex P2 på mergad #353 | **PR #592** `fix(tier3-gate)`: ny `getLatestEngineVersionErrorLogForCategory` (kategori-scopad `ORDER BY created_at DESC LIMIT 1`) — läsningen kan inte trängas ut. Punkt (1) och (3) triagerade separat, se nedan. |
| [x] | Fixad | P3 | `database`-vokabulärens konkurrent-veto (`prisma\|mongoose\|supabase\|firebase\|…`) tystade hela `database`-capabilityn även när konkurrenten var NEGERAD ("lägg till postgres, inte prisma"). | Codex P2 på #445 | **PR #592** `fix(capability-detection)`: veton hoppar över termer vars samtliga förekomster ligger i negationsfönster (ny cross-cutting `isTermFullyNegated` i `prompt-negation.ts`). Regressionstester för både sv/en. |
| [x] | Fixad | P2 | Spegelfallet: en NEGERAD DB-provider i en annars orelaterad follow-up ("add a contact form, no postgres") matchade ändå `database`-vokabulären positivt → F3-runda kunde injicera default-Postgres-dossiern trots explicit negativ. | Codex P2 på #445 | **PR #592** `fix(capability-detection)` (samma svep): positiva capability-träffar kräver minst en förekomst utanför negationsfönstren; "mongodb, inte postgres"-fallet bevarat (icke-negerad förekomst vinner). 543 builder-tester gröna. |
| [x] | Fixad | P2 | Fresh-entry chat-reset avbröt inte en pågående generation-stream (`cancelActiveGeneration` anropades inte) → en gammal stream kunde fortsätta skriva efter reset. | PR #355-triage #7 (kodverifierad 2026-07-08) | **PR #592** `fix(builder)`: reset-grenen anropar `cancelActiveGeneration()` (idempotent abort) före state-nollställningen. Controllern saknar testharness — verifierad via builder-sviterna + typecheck/lint (dokumenterat i commiten). |
| [x] | Fixad | P2 | MCP generate kastade när preview-start fallerade i st.f. att returnera den sparade versionen/filerna → en färdig generation kastades bort pga preview-fail. | PR #355-triage #40 (kodverifierad 2026-07-08) | **PR #592** `fix(mcp)`: `generateOwnEngineSiteFromPrompt` returnerar partial success med `previewStartFailed: {stage, message}`; MCP-svaret bär samma fält. 2 regressionstester. |
| [x] | Fixad | P3 | `db-blob-sync`-gaten (`pydatabastest.py`): `ACKNOWLEDGED_EXTRA_TABLES`-tillåtelsen (schema_migrations) och okänd extra tabell→FAIL (drift) låg inline i `inspect_db` och var otestade. | Extern coach-review på #495 | **PR #592** `test(db-blob-sync)`: klassificeringen extraherad till ren `classify_table_drift` + 4 tester (acknowledged/okänd/missing-known-extra/rent schema). Ingen beteendeförändring; 12/12 tester gröna. |
| [x] | Fixad | P3 | `/messages`-nätverksfallbacken återanvände inte 409 `stale_base_version`-reload-UX:en som stream-vägen har → stale-base-edit via fallback gav generiskt fel. | PR #355-triage #20 (kodverifierad 2026-07-08) | **PR #592** `fix(chat)`: delad `handleStaleBaseVersion`-hanterare för stream- och fallback-vägen; regressionstest för fallback-409:an. |
| [x] | Fixad | P3 | Doc-sync-gap efter CI auto-migration + `review-window` (2026-07-11): `docs/testing.md`, `scripts/README.md` och `.cursor/rules/workflow.mdc` nämnde inte jobben `prod-migrations-apply`/`prod-migrations-applied` eller required-checken `review-window`. | /granska-svärm 2026-07-11 | **PR #592** `docs`: alla tre docs uppdaterade (CI-jobbtabell, db-raden, required-checks-not). `docs:links` grön. |

## Redan fixade / obsoleta / avfärdade (verifierade 2026-07-22, ej kodändring i #592)

| Klar | Status | Prio | Fynd | Källa | Verdict + bevis |
| --- | --- | --- | --- | --- | --- |
| [x] | Redan fixad | P2 | "Uppdatera preview"-snurran roterade ~konstant (`isLoading` driven av `previewLifecycle === "recovering"` + `previewPending`) → användaren tolkade det som häng. | /logg-internet 2026-07-09 (fynd #15, UI-1) | **ALREADY_FIXED av PR #564** (`83ce3472`, declutter): den manuella "Uppdatera preview"-knappen (RefreshCw med spinn) är borttagen; kvarvarande Loader2 visas bara vid `versionWorkInProgress \|\| previewTruth.tone === "pending"` (`PreviewPanelChrome.tsx:633-635`). Confidence 85. |
| [x] | Redan fixad | P3 | Info-ytor utan åtgärd tog chatt-/preview-plats: F2-env-inforutan, 2 env-panel-modaler och 2 permanenta positiva banners ovanför iframen åt ~2 rader iframe-höjd konstant. | /logg-internet 2026-07-09 (fynd #17, UI-3/4) | **ALREADY_FIXED av PR #564 + #573**: #564 tog bort de informativa bannersen + surfaceDescriptor-remsan (previewTruth renderar inget i vilo-/success-läge); #573 tog bort `ProjectEnvVarsPanel` inkl. modalerna (Byggblock enda env-ytan). Confidence 85. |
| [x] | Obsolet | P3 | Env-panelens blockeringsräkning (`totalIssues` = klient-heuristiken `likelyRequiredEnvKeys`) speglade inte serverns auktoritativa `buildBlockingKeys`. | PR #355-triage #31 | **OBSOLETE**: `ProjectEnvVarsPanel.tsx` är raderad (ägarbeslut 2026-07-22, PR #573 — Byggblock är enda env-ytan). Ytan som räknade fel finns inte längre; kravytan (`F3RequirementsSurface`) drivs av serverns `missingByIntegration`. Confidence 95. |
| [x] | Avfärdad (falskt fynd) | P3 | **`describe.ts` (Fas 1) abandonerar timeoutade registry-fetches utan rejection-absorbering** — påstådd unhandled promise rejection när en abandonerad fetch rejectar efter timeout. | BB på feat/shadcn-resolver-search (2026-07-22) | **POLICY_NOT_BUG — empiriskt motbevisat 2026-07-22**: `withTimeout` racear via `Promise.race`, som fäster rejection-handlers på båda promisen — en sen rejection av förloraren är per definition hanterad. Node-repro körd: ingen `unhandledRejection` triggas. (Recipe-search-"fixen" `pending.catch(() => {})` är harmlös men var onödig.) Confidence 90. |
| [x] | Delvis åtgärdad → triagerad | P3 | Codex P2 ×3 från #353, punkt (1) `mutateVersions`-revalidering före `!res.ok`-grenen i `useResumePendingVerification` och punkt (3) resume-selektorn utan positiv provenance. | Codex P2 på mergad #353 | Punkt (3) **ALREADY_FIXED**: provenance-gate (`editKind == null`) + `RESUME_VERIFY_MAX_AGE_MS` (24 h) finns i `useResumePendingVerification.ts:44-48,161-164` med explicit Codex-#353-referens. Punkt (1) **by-design**: mutate-ordningen är dokumenterad normal-lane-parity (kommentar rad 389-393) och påverkar inte retry-beslutet (som keyar på `res.status`). Punkt (2) fixad i PR #592 (kategori-scopad läsning), se ovan. |

## Flyttade (ej stängda) 2026-07-22

- **Preview-retention rensas inte i efterhand (BB#345r7)** → flyttad till `## Beslut & policy` — raden var redan explicit "Medvetet loggad i st.f. fixad — undvik mer stateful komplexitet i retention-pathen"; det är ett policyval, inte en öppen defekt.
- **Loopia auto-DNS "post finns redan" (Bugbot #395)** → flyttad till `## Behöver repro` — fixen kräver Loopias faktiska dubblett-svarskod, vilket inte kan avgöras statiskt.
