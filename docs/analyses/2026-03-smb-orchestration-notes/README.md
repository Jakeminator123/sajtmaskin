# 2026-03 SMB Orchestration Notes

This folder gathers working analysis notes that previously lived in ad hoc
root-level buckets such as `egen_svart_låda/` and `stora_natten/stortest_repo/`.

These notes are still useful as analysis/reference material, but they are not
the canonical runtime truth. Keep them here while they continue to inform
architecture, SMB prompt understanding, section planning, and design/taste
direction.

Files here should be treated as:

- analysis input
- comparison material
- architecture/reference notes

They should not be treated as the sole source of truth when newer canonical
docs or runtime code say otherwise.
