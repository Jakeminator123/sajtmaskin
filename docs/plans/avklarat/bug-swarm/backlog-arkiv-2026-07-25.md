# Bug-backlog — arkiv (2026-07-25)

> Flyttade rader från [`BUG-SWARM-BACKLOG.md`](../../../../BUG-SWARM-BACKLOG.md) efter riktad tvåradsleverans 2026-07-25 (branch `cursor/warm-cache-sdk-och-sendmessage-outcome-2150`, mot master `1cd8f547`). Båda raderna var P3 och båda var "kniviga" i den mening att de krävde ett vägval snarare än en självklar patch — warm-cache-raden hade tre konkurrerande linjer och `sendMessage`-raden rörde hela caller-kedjan. Filen har sedan fyllts på med en P1-rad som stängdes 2026-07-26 och med de nio defekter som åtgärdsprogrammet efter observationssessionen 2026-07-25 stängde samma dag (egna sektioner nedan). Äldre historik: [`backlog-arkiv-2026-07-24.md`](backlog-arkiv-2026-07-24.md) · [`backlog-arkiv-2026-07-22.md`](backlog-arkiv-2026-07-22.md) · [`backlog-arkiv-2026-07-02.md`](backlog-arkiv-2026-07-02.md) · [`backlog-arkiv-2026-06-27.md`](backlog-arkiv-2026-06-27.md) · [`backlog-arkiv-2026-06-24.md`](backlog-arkiv-2026-06-24.md).

## Fixade 2026-08-05 (kedja-batch, #780)

Tre rader körda som en batch: en steg 0-ram per bugg, en billig runner-subagent per bugg som driver steg 1–6 i egna worktrees, orkestratorn dömer maskinellt och kör bugbot. Två kandidater med **namngiven, olika** ansats per bugg — i två av tre fall vann den mindre diffen och den utslagna kandidaten var lärorik i sig (se sista kolumnen).

| Klar | Status | Prio | Fynd | Källa | Fix-referens |
| --- | --- | --- | --- | --- | --- |
| [x] | Fixad | P2 | **Mall-arkivet fortsätter laddas efter att timeouten löpt ut:** `withTimeout` avbröt bara *väntan* — nedladdning, uppackning och base64-kodning fortsatte i bakgrunden, och avslaget raderade cache-posten så nästa init startade om samma dyra arbete medan den första kopian levde kvar. | Bugbot-subagent på #730 2026-08-01, kodverifierad | **#780:** cache-posten släpps först när laddningen faktiskt avgjorts, så efterföljande anrop återanvänder samma in-flight-load (dedupe) i stället för att stapla en ny; `withTimeout` flyttad till anroparsidan så varje väntare får sin egen tidsgräns utan att döda det delade arbetet. `config/variant-template-addenda.json` regenererad — extractor-fingerprintet hashar ägarfilen. Utslagen kandidat: abort-signal in i arkivladdaren, grön **bara mot test-mocken** eftersom `loadLocalV0TemplateReferenceFiles` bara tar `{ timeoutMs }` och kandidaten `as`-castade förbi typecheck. Den fällan är nu ett eget domkriterium i `kedja.md` (mock-grönt ≠ grönt). |
| [x] | Fixad | P3 | **`contractAuthProvider` tappas ur snapshotten av den breda sensitive-regexen:** `SENSITIVE_KEY_SUBSTR` matchar delsträngen `auth`, så leverantörsnamnet (`clerk`) filtrerades bort vid persistering och snapshotten kunde inte återge auth-kontraktet för en follow-up. | Kodläsning 2026-07-27 (`/auth/i.test("contractAuthProvider") === true`, verifierad) | **#780:** exakt allowlist före filtret; regexen själv orörd. Raden varnade för att en för generös omskrivning är värre än fyndet, och det höll: den utslagna kandidaten skrev om regexen, var grön i domen, men under-filtrerade framtida nycklar (`userAuth` hade sluppit igenom). Motprov låser att `authToken`, `authorization`, `password`, `apiKey`, `clientSecret` och `cookie` fortfarande filtreras. |
| [x] | Fixad | P3 | **`rebuildContent`-fallbacken kan skriva i fel fil:** vid miss på den sökvägsmedvetna fence-regexen föll koden tillbaka på `result.replace(orig.content, fixed.content)` — första förekomsten i hela den serialiserade strängen — så två filer med identiskt innehåll kunde ge fixen i fel fil. | 24-agentsvärm 2026-07-31, kodverifierad (M#gs12) | **#780:** fallbacken borttagen (−2 rader); fence-matchad ersättning oförändrad och nu testlåst i nya `rebuild-content.test.ts` — funktionen saknade testtäckning helt, vilket raden själv påpekade. **Avvägningen som accepterades:** raden föreslog "fail + warn", men `rebuildContent` returnerar bara en sträng och har ingen varningskanal — att införa en hade ändrat signaturen och spridit sig till anroparna. Levererat är alltså "fail" utan warn: vid fence-miss behåller filen sitt ursprungliga innehåll i stället för att fixen landar i fel fil. Tyst, men strikt säkrare än tyst korruption, och testet asserterar numera uttryckligen att inget skrivs. Vill man ha varningen senare är den en egen liten ändring i anroparkedjan. Utslagen kandidat: positionsbaserad splice, också grön men +14/−1. |

## Fixade 2026-08-05 (tre /kedja-körningar i delegerat läge)

Tre rader drivna genom `/kedja` 2026-08-04/05 — de två första i pipelinens ordinarie form, den tredje i det nya delegerade läget (en billig runner-subagent kör steg 1–6, orkestratorn dömer och kör bugbot). Varje fix är testbunden: rött test före, grönt efter, plus motprov.

| Klar | Status | Prio | Fynd | Källa | Fix-referens |
| --- | --- | --- | --- | --- | --- |
| [x] | Fixad | P2 | **`auth-pages` räknas som app-scaffold i retry-pivoten:** att byta den handplockade `APP_SCAFFOLD_IDS`-mängden mot `isAppScaffold` (`scaffold-aware-retry.ts:82-87`) gör att `site-shell-mismatch` nu även träffar `auth-pages`, som är `siteKind: "app"` men ett giltigt val för `website`-intent. På ett website-bygge med färre än tre route-filer och blockerande preflight kan retry därför pivotera mot `landing-page` fast auth var rätt match. | Bugbot-subagent på #684:s diff 2026-07-31 (merge-agentens pass), kodläst utan repro | **#766:** mismatch-villkoret undantar scaffolds vars manifest tillåter aktuellt build-intent (`allowedBuildIntents`) — befintlig manifestinfo, ingen ny hårdkodad lista. Motprov: `dashboard` på website-intent klassas fortfarande som mismatch. |
| [x] | Fixad | P2 | **Variantvalets keyword-väg hashar bort en tydlig vinnare:** `pickScaffoldVariant` tar topp-4 och väljer med seed-hash (`matcher.ts:229-236`) utan dominansmarginal — exakt det som gav `corporate-grid` 0/20 i landing-auditen och som embedding-vägen fick en fix för (`VARIANT_DOMINANT_MARGIN`, `:392-404`). Keyword-vägen är den som kör utan API-nyckel, utan embeddings-cache och vid svag cosine, så feltypen lever kvar där. | 24-agentsvärm 2026-07-31, kodverifierad (M#gs5) | **#767:** keyword-pickern speglar embedding-vägens semantik — leder #1 med minst `VARIANT_DOMINANT_MARGIN` väljs den direkt, rotation bara vid jämnt fält; konstanten hissad och delad. Motprov: rotation kvarstår vid jämna poäng. |
| [x] | Fixad | P2 | **SEO-copypasset kan inte åtgärda en nyckel som saknas helt:** `missing-title` och `missing-description` står i både `FIXABLE` (`audit.ts`) och `COPY_FINDING_IDS` (`llm-copy.ts`), så passet anropar modellen — men `writeMetadataString` (`metadata-literal.ts`) *ersätter* en befintlig toppnivå-literal och returnerar källan orörd när nyckeln inte finns. En layout som exporterar metadata utan `title`-fält får alltså ett modellanrop utan resultat. **Ingen false-green** — rapporten sa korrekt att bristen stod kvar; kostnaden var ett bortkastat modellanrop och en utlovad fix som inte levererades. | Bugbot på #707 2026-08-01, kodverifierad | **#768:** `writeMetadataString` infogar saknad nyckel **först** i metadata-objektet (append efter sista propertyn kunde lägga separator-kommat inne i en trailing `//`-kommentar → oparsbar fil; bugbot-fynd på första kandidaten, regressionstestat). Alla vakter kvar: computed/template-hål/saknat metadata-objekt lämnas orörda. `COPY_FINDING_IDS` orörd — löftet blev sant i stället för borttaget. |

## Fixade 2026-08-05 (armerad autonomi — restraderna efter #762)

Två OpenClaw-rader som blev över när continuation-handskakningen landade: den ena var #762:s egen bugbot-rest, den andra den kvarvarande halvan av BB#oc1.

| Klar | Status | Prio | Fynd | Källa | Fix-referens |
| --- | --- | --- | --- | --- | --- |
| [x] | Fixad | P2 | **Avvisningsstämpeln i armerad autonomi var global, inte bunden till turen:** `sendMessage`-wrappern satte `lastTurnRejectedAt` vid varje `rejected`/`failed`/`aborted` utfall, och `decideArmedContinuation` avväpnade mandatet så snart stämpeln var ≥ watchens `startedAt`. En orelaterad send som misslyckades medan en watch pågick (manuell retry, katalog-insert, planbeslut, avbrott) stoppade därför en auto-sändning som fortfarande kunde lyckas. | BB (bugbot-local på #762, kodverifierad) | Tidsstämpeln ersatt av ett **per-send-id**: buildern numrerar varje send och publicerar `rejectedSendSeq`, och bara en avvisning med exakt watchens id avväpnar mandatet. **Id:t förutsägs inte** — det skrivs av sändningen själv (`bindArmedContinuationSend`) när den når buildern, igenkänd på **texten** OpenClaw fyllde i composern (`isOpenClawPreparedSend`). Fyra bugbot-pass formade lösningen: (1) ett id som lästes före klicket kunde tas av en annan sändare, eftersom composern `await`:ar ett bilagesteg mellan klicket och sändningen — samma felklass, bara flyttad; (2) `openclaw-prepared`-taggen som bindningssignal faller bort vid bilagor eller ett påhängt Figma-/inspect-block, så en armerad sändning kunde bli obunden; (3) att i stället binda på att en prepared fill *fanns* var för brett — ett katalogval i samma fönster kunde vinna first-claim, varpå autonomins egen avvisning inte matchade och mandatet fortsatte som om steget lyckats (den farliga riktningen); (4) en sändning som ändå aldrig hinner namnge sig får inte tappa sin egen avvisning — därför ligger tidsjämförelsen kvar som **fallback enbart för ett obundet id**. Bindningen kräver nu att utgående text börjar med den ifyllda (prefix, eftersom composern hänger på Figma-/inspect-block efteråt), och första bindningen vinner. Tolv enhetstester + ett integrationstest där en främmande send (id 6) failar mitt i bygget utan att mandatet dör, och autonomins egen (id 5) sedan avvisas. |
| [x] | Fixad | P3 | **OC_DEBUG: `debugOwned` krävde BÅDE `chatId` och `activeVersionId`,** så en chatt utan valt versionsid fick ingen filmanifest/fynd-block. Prompt-halvan var redan stängd i #754; kvar var själva kontexten. | BB#oc1 (prompt-halvan fixad i #754) | Ny ägarverifierad uppslagning `getLatestEngineVersionForChatForRequest` i `src/lib/tenant.ts` (preferred → senaste, samma ordning som `getLatestVersionFiles`), och routen löser versionen serverside när klienten bara skickar chatt-id. Ägarskapet är oförändrat — det kommer från chattens app-projekt i båda fallen — men version-id:t härleds nu i stället för att tas från klienten. Debug-prompten säger inte längre att en version måste vara vald. Fyra tester i nya `src/lib/tenant.test.ts` låser även cross-tenant-riktningen: en chatt man inte äger slår aldrig upp någon version. |
## Fixade 2026-08-04

Första körningen av `/kedja` — den stegade buggfix-pipelinen. Raden nedan var testfallet.

| Klar | Status | Prio | Fynd | Källa | Fix-referens |
| --- | --- | --- | --- | --- | --- |
| [x] | Fixad | P2 | **Add-verb-vakten släpper igenom refine-prompter:** de nya `ska … jag … kunna`- och `skulle vilja testa/prova`-mönstren satte `allowDetection` även när prompten innehöll refine-verb som `flytta` (`follow-up-capability-detection.ts:330-335`), så en follow-up som flyttade ett befintligt kontaktformulär klassades som `capability-add` och återinjicerade dossier-skal på en ren layoutändring. | Bugbot-subagent på #683:s diff 2026-07-31 (merge-agentens pass), kodläst utan repro | **PR B (denna leverans):** add-verben delas efter styrka. **Starka** (handlingsverb: `lägg till`, `skapa`, `bygg`, `add`, `create`) överlever ett refine-verb i meddelandet — "lägg till ett kontaktformulär och flytta det högst upp" är en add med placeringsönskan. **Svaga** (önske-/modalformer: `behöver`, `ska jag kunna`, `i want`) förlorar mot refine. Bredare än radens bokstav med avsikt: hade bara de två namngivna mönstren vetats hade samma bugg legat kvar en omformulering bort (`Flytta formuläret, vi **behöver** det högst upp`). Credential-cue och `modifyReference` orörda. Fyra motprov tillagda. |

> **Lärdom, dokumenterad i [`kedja-fix-pipeline`](../../../../.cursor/skills/kedja-fix-pipeline/SKILL.md):** pipelinens två egna kandidater fixade raden genom att göra refine-vetot absolut, vilket slog ut **varje** legitim add — en dyrare bugg än den ursprungliga, eftersom användaren då ber om ett kontaktformulär och tyst inte får något. Alla grindar var gröna: det röda testet passerade, 157 befintliga tester höll, bugbot teg. Ingenting mätte den motsatta riktningen. Steg 2 kräver därför nu ett **motprov** och steg 5 **uttryckligen olika ansats** per kandidat.

## Fixade 2026-08-04 (armerad autonomi — continuation-handshake)

Resten av OpenClaw-svepet (#754, som stängde fyra av fem fynd och gjorde UI-texten ärlig men lämnade själva funktionen).

| Klar | Status | Prio | Fynd | Källa | Fix-referens |
| --- | --- | --- | --- | --- | --- |
| [x] | Fixad | P2 | **Armerad autonomi fullföljde inte mandatet:** ett mandat på N follow-ups gav i praktiken EN auto-sändning — ingen kod väckte Sajtagenten igen när builderturen nått terminalt läge, så resterande steg låg oanvända. `review_next` saknade konsument helt (`canArmedSend` kräver `followups`). | Verifierad mot master `1b2686cc` (2026-08-04); UI-halvan fixad i #754 | Ny ren beslutsmodul `src/lib/openclaw/debug/armed-continuation.ts` + `useOpenClawArmedContinuation`: auto-sändningen registrerar en watch, loopen väntar in terminalt builder-läge (streaming + versionsstatus) och väcker Sajtagenten exakt en gång med färsk kontext. Stoppar vid mandatets tak, failed/blocked version, chattbyte, "stopp", timeout och parallell OpenClaw-tur. Fortsättningsturen kan inte re-armera (`allowArming: false` + inert prompttext, båda testade). `review_next` avbryts tyst. Integrationstest kör två follow-ups genom riktiga kort + loop.<br><br>**Elva bugbot-pass formade grindarna** — nästan varje runda visade ett nytt sätt att läsa builderns läge fel, och de flesta av dem hade lett till ett steg för mycket eller ett steg för tidigt: (1) mandatet överlevde avbrottsbesked, (2) `idle`-status räknades som klar, (3) resume utan builder-kontext, (4) terminal status på en **pinnad äldre** version, (5) stale watch vid omarmering, (6) misslyckad send strandade mandatet, (7) snabba turar avbröts som "startade aldrig", (8) evig väntan på pinnad vy, (9) `send` läste streaming-läget från renderingen i stället för storen, (10) chattillväxt (som uppstår redan när auto-sändningen postar sitt eget meddelande) togs som bevis på klar tur, (11) uppföljningsklockan tickade medan svaret skrevs, (12) en **avvisad** sändning (stale-base/F3-412/kreditgrind) lämnade versionsstatusen orörd och lästes som ett färdigt bygge, (13) en plan som väntade på godkännande kunde förbigås, (14) en terminal status på den version man skickade **från** beskrev förra bygget. Två signaler kopplades in för att stänga de sista: `SendMessageOutcome` (kontraktet fanns redan, ingen sparade svaret) speglas nu som en **tidsstämpel** i builder-kontexten — en boolean kunde batchas bort av React — och `isAwaitingInput` + `getLatestPendingReplyFromTooling` stoppar autonomin när frågan är användarens. Ett tidigare beslut vändes: handskakningen svarar **inte** på builderns motfrågor, eftersom skillnaden mot ett väntande godkännande inte är värd risken. |
## Fixade 2026-08-02 (prodlogg-åtgärderna efter /logg-internet 2026-08-01)

Tio fynd (M#li1–M#li10) från två live prod-körningar 2026-08-01 (fritext-generering chat `7a4d609f` + v0-mallen "AI Landing Page" chat `4d6b5546`). Raderna dokumenterades och fixades i samma leverans — fyra parallella spår i egna worktrees (#735, #736, #737, #739) plus #732 som stängde M#li3 — så de har aldrig legat som `[ ]`-rader i Aktiv kö på master.

| Klar | Status | Prio | Fynd | Källa | Fix-referens |
| --- | --- | --- | --- | --- | --- |
| [x] | Fixad | P1 | **"Byggd — live" härleddes ur env-nycklar, inte ur filbevis:** Byggblocket "AI-assistent med verktyg" visade Byggd — live medan den publicerade sajten 404:ade på `POST /api/chat` — chatten var en klientattrapp med hårdkodade svar. `dossiers/route.ts` satte `built-live` på ren env-presence utan krav på server-route i versionens filer. | /logg-internet 2026-08-01 (prod chat `7a4d609f`), kodverifierad (M#li1) | **#739:** `built-live` kräver serverfilbevis — dossier-post: ALLA manifest-`server`-filer i versionen (partiell injektion aldrig live, faller inte till fallback); modellbyggd post: en API-route som läser `process.env` och nämner nyckeln som fristående identifierare (warn-only/`NEXT_PUBLIC_*` räknas inte). Sex bugbot-pass skärpte bevisregeln iterativt; built-demo-hinten täcker nu båda orsakerna. Testbundet i `dossiers/route.test.ts` (25 fall). |
| [x] | Fixad | P1 | **F2-deployens env-backstopp blockerade feature-runtime-nycklar:** readiness gav `canDeploy:true` medan `POST /api/v0/deployments` 409:ade med `DEPLOY_MISSING_ENV` på `EMAIL_FROM`/`CONTACT_EMAIL_TO` (Resend, `feature-runtime`). F2-grenen blockerade på hela `missingEnvKeys`. | /logg-internet 2026-08-01 (prod chat `7a4d609f`), kodverifierad (M#li2) | **#739:** ny delad resolver-mängd `designDeployBlockingKeys` (= truly-absent `build`-nycklar); deploy-routens F2-gren och readiness blockar på exakt samma mängd. Osparad chatt får `project-context-missing`, inte missing-env. `buildBlockingKeys` medvetet INTE använd i F2 (placeholder-täckta nycklar — Codex #443/bugbot #461 respekterade). Tester i deployments-/readiness-/resolver-sviterna. |
| [x] | Fixad | P1 | **Degeneracy-grinden blockerade previewn terminalt på mallens egna assets:** `texture_earth.jpg` (1327 KB, orörd från mallimporten) bröt per-fil-taket 750 KB → `Readiness: FAIL (preflight_preview_blocked)` utan väg vidare i UI:t. Taket var dimensionerat för källkod men mätte binära assets med samma linjal. | /logg-internet 2026-08-01 (prod chat `4d6b5546`, version `512d7b50`), kodverifierad (M#li3) | **#732:** detekteringen är binär-medveten via `isNonTextContentFile` — binära assets bedöms mot eget tak (`maxBinaryAssetBytes` 2 MB, speglar preview-hostens kontrakt) i stället för källkodstaket. |
| [x] | Fixad | P1 | **`capDegeneratePayload` förstörde mall-assets i den sparade versionen — tyst dataförlust:** när degeneracy detekterades byttes mallens 1,3 MB-textur ut mot en JS-kommentarstub i persisterade `files_json`; bilden var borta ur versionen även om preview-blocket lyfts. | /logg-internet 2026-08-01 (prod chat `4d6b5546`), kodverifierad (M#li4) | **#736:** cappen är binär-medveten (egna tak 2 MB/asset, 12 MB total — preview-hostens kontrakt) och ny `preservePaths` skyddar basversions-identiska filer (ärvt mall-/versionsinnehåll stubbas aldrig). Bugbot-fynd om 3–12 MB-spannet triagerat som medveten avvägning: dataförlust är värre än DB-bloat, preview blockeras ändå. Tester i `degeneracy-guard.cap.test.ts` + preflight-integrationstest. |
| [x] | Fixad | P2 | **"Annat" på F3-förslagsfrågan tappade F3-kontexten tyst:** svaret startade en vanlig designrunda (`lifecycleStage: "design"`) utan att chatten sa att integrationsläget avslutats — användaren trodde den var kvar i F3. | /logg-internet 2026-08-01 (prod chat `7a4d609f`), kodverifierad (M#li5) | **#739:** utfall (b) — när F3-markören konsumeras av ett "unrelated"-svar persisteras en enrads svensk assistentnotis direkt efter användarens svar (bara vid bekräftad atomär konsumtion; race-paritet med reject-vägens neutrala copy). Utfall (a) avvisades: markör-bevarande hade krävt omdesign av pending-walken och riskerat F3-looparna. Notisen strömmas inte live i pågående session (kräver `stream-handlers.ts` som #734 skrev om) — accepterad avvägning. |
| [x] | Fixad | P2 | **Dubblerade AI-Byggblock för samma capability:** follow-up "lägg till AI-chatbot" gav tre block för två funktioner — modellbyggda "AI-assistent med verktyg" (Byggd) och dossiern "AI-chatt — OpenAI" (Planerad) samtidigt. | /logg-internet 2026-08-01 (prod chat `7a4d609f`) (M#li6) | **#739:** ny `isPlannedDossierCoveredByModelBuiltBlock` — planerad post döljs ur overview-svaret när ett modellbyggt block delar capability eller env-nyckelyta (any-overlap = samma vendor, speglar `findMatchingCluster`). Endast visningslager: pending-signalen röjs inte, dossier-injicerade block superseder aldrig planerade syskon (provider-migrations-UX bevarad). Byggvägs-dedupen (F3 bredvid modellmock) är egen öppen rad (M#li12). |
| [x] | Fixad | P2 | **Route-drift i follow-up: dubbla prissidor på två språk + oombedda sidor:** prompten bad om `/priser` och `/om-oss` — modellen skapade även `app/pricing/page.tsx` (engelsk dublett) och `app/support/page.tsx`. Språkdublettregeln renderades bara i den icke-kompakta prompt-grenen som follow-ups normalt hoppar över. | /logg-internet 2026-08-01 (prod chat `4d6b5546`) (M#li7) | **#735:** nytt follow-up-block `## Existing Route Pages (do not duplicate)` — befintliga app-routes härledda ur `previousFilePaths` via kanoniska `extractAppRoutePathsFromFilePaths`, plus explicita regler mot språkdubbletter, oombedda sidor och route-borttagning. Renderas direkt efter Route Plan; init opåverkat; budgetprio 88 ej required (trunkerad lista vore sämre än ingen). Mjuk styrning — post-hoc-verifierare medvetet utanför scope. |
| [x] | Fixad | P2 | **Onboarding-vyn blinkade förbi i template-flödet (~6 s):** "Vad vill du bygga?"-reglagen visades tills chatId hydrerats trots att mallen redan bestämt typ/stil/sidor. | /logg-internet 2026-08-01 (prod chat `4d6b5546`), kodverifierad (M#li8) | **#737:** `pendingTemplateInit` (= `templateId` i URL && `!chatId`) visar laddläge "Läser in mallen" i stället för onboarding; repair-knappen spärrad (ingen chat att reparera). Gate:ar medvetet bara på `templateId`, inte `project` (tomt projekt utan chat är legitimt onboarding-läge). Rest: spinnern ligger kvar vid misslyckad template-init (M#li11). |
| [x] | Fixad | P3 | **Publiceringsdialogen förifyllde sajtnamnet med hela prompten:** en 266-teckens friprompt blev förifyllt "projektnamn". | /logg-internet 2026-08-01 (prod chat `7a4d609f`), kodverifierad (M#li9) | **#737:** ny `suggestProjectNameFromPrompt` — första raden, max 40 tecken kapade på ordgräns (unicode-säker regex utan `\b`/`\w`), trailing-interpunktion strippad. Explicit valt namn trunkeras aldrig; täcker även deploy-fallbacken. |
| [x] | Fixad | P3 | **Builder-a11y: `label-content-name-mismatch` på tre element:** synlig text ingick inte i accessible name (Sajtmaskin-knappen, Mer-knappen, Sajtagenten-FAB:en) — röststyrning kunde inte träffa knapparna. | Vercel-toolbar axe-svep i prod 2026-08-01 (M#li10) | **#737:** aria-labels börjar nu med sin synliga text ("Sajtmaskin — gå till startsidan", "Mer — spara, inställningar, import och export", "Sajtagenten — stäng/öppna chattrutan"). Latenta element lämnade med skäl: container-div:ar namnges inte av innehåll (regeln gäller inte); `Messages` finns inte i repots kod (genererat sajtinnehåll i preview-iframen). |

## Fixade 2026-08-01

| Klar | Status | Prio | Fynd | Källa | Fix-referens |
| --- | --- | --- | --- | --- | --- |
| [x] | Fixad | P2 | **Rematchad scaffold persisteras innan kontraktsgrinden har passerat:** `codegen-turn.ts:336-343` skriver `updateChatScaffoldId` innan `contractClarification`-grenen (`:426`) kan avbryta rundan, och init-vägen pinnar scaffolden redan vid `createChat(..., resolvedScaffold?.id)` (`create-chat-stream-post.ts:807-811`). Om grinden avbryter för att ställa en följdfråga ligger alltså ett matchningsbeslut kvar som fattats på den *ofullständiga* prompten — och nästa tur ser det som `persistedScaffoldId`, vilket stänger av rematchen. Fel scaffold blir därmed permanent för chatten. | 24-agentsvärm 2026-07-31, kodverifierad (M#gs2) | **PR #720 (branch `cursor/scaffold-persist-efter-kontraktsgrind-02a4`):** valde "flytta skrivningen" på båda vägarna i stället för rollback — inget matchningsbeslut hinner då bli synligt för nästa tur, så ingen kompensationsväg kan missa. Follow-up: `updateChatScaffoldId` ligger nu efter `contractClarification`-returen i `codegen-turn.ts`, precis före `finalizeOrchestrationPrompts`. Init: gate-grenens `createChat` får inget scaffold-id längre (`create-chat-stream-post.ts`), medan den genererande vägen pinnar som förut. Gate-strömmens SSE-meta bär fortfarande den provisoriska matchen, så tidslinjen tappar ingen observabilitet. Fyra tester binder ordningen — negativ + positiv kontroll per väg — i `src/app/api/engine/chats/[chatId]/stream/route.test.ts` och `src/app/api/engine/chats/stream/route.test.ts`; båda negativa verifierat röda mot den gamla ordningen. Ägare: `src/lib/api/engine/chats/chat-message-stream/codegen-turn.ts`, `create-chat-stream-post.ts`. |
| [x] | Fixad | P2 | **SEO-tillvalet skriver aldrig om baseline-robots/sitemap, så det riktiga `siteUrl` aldrig når filerna:** `seo-defaults.ts:356-361` hoppar över varje sökväg som redan finns (`if (existingPaths.has(path)) continue`), och export-baseline har redan lagt `app/robots.ts` + `app/sitemap.ts` med `process.env.NEXT_PUBLIC_SITE_URL \|\| "https://example.com"` (`project-scaffold.ts:323-341`). Enda stället pipelinen sätter `NEXT_PUBLIC_SITE_URL` är placeholder-kuvertet, med värdet `https://example.com`. En publicerad sajt får alltså robots och sitemap som pekar på example.com trots att användaren valde SEO. | 24-agentsvärm 2026-07-31, kodverifierad (M#gs3) | **PR: SEO-granskning vid publicering.** Fixad via publiceringspasset i stället för i `seo-defaults.ts`: `applySeoToProjectFiles` behåller sin dokumenterade idempotens (befintliga filer lämnas orörda, `seo-defaults.test.ts` oförändrad), och den nya `src/lib/seo/improve.ts` äger beslutet "den här filen ÄR en placeholder". Den matchar på innehållet — bara filer som faktiskt innehåller `https://example.com` släpps och återinjiceras med riktig `siteUrl`, så en LLM-/användarskriven variant med en riktig adress klubbas inte. Rapporten som visas för användaren härleds ur den faktiska före/efter-diffen, så fixen kan inte påstås utan att ha landat. Verifierat end-to-end: robots/sitemap som gick in med example.com kommer ut med sajtens adress, och `placeholder-site-url` finns i `before` men inte i `after` (`src/lib/seo/publish-pass.test.ts`). **Kvarstår medvetet:** ett direktanrop till `applySeoToProjectFiles` utanför deploy-vägen hoppar fortfarande över befintliga filer — det är kontraktet den funktionen har, och deploy är den enda vägen där buggen manifesterade sig. Ägare: `src/lib/seo/improve.ts`, `src/app/api/v0/deployments/route.ts`. |
| [x] | Fixad | P2 | **En dev-process som avslutar rent i en omstartsloop lämnade readiness på `starting` för alltid.** | Post-deploy prod-smoke 2026-08-01 (integratorsagenten), hostloggar | **PR #710 (`codex/bug-swarm-batch-1`):** preview-host räknar oväntade rena exits per session+version i ett tvåminutersfönster. Tre exits gör tillståndet terminalt `error`/`readinessState: failed`, inkluderar senaste Next-output och stoppar proxytrafiken från att köa fler automatiska omstarter. Ett explicit start/update-försök nollställer budgeten. Guard-test låser gräns och fönster. |
| [x] | Fixad | P2 | **Preview-sessionen bar inte den bootade innehållsrevisionen hela vägen till kvittovägarna.** | Coach-review av #693 2026-08-01, kodverifierad | **PR #710 (`codex/bug-swarm-batch-1`):** `PreviewSessionEntry` lagrar `filesRevision` i minne och Redis; start/update/patch fångar versionens DB-genererade `files_revision` när filerna sänds. `preview-status`, `preview-heartbeat`, `preview-session` och `generation-stream-post-finalize` skickar sedan den bootade revisionen till kvittoskrivaren i stället för att läsa versionens eventuellt nyare DB-rad. |
| [x] | Fixad | P2 | **Tidsberoende tester failade under parallell last, oberoende av diffen.** | Full `test:ci` + riktad `src/lib/gen`-körning 2026-07-31; reproducerad före fix i denna branch | **PR #710 (`codex/bug-swarm-batch-1`):** `llm-usage.test.ts` dränerar promises registrerade med `after()` deterministiskt och ordningstestet använder en explicit insert-gate utan 5 ms-väntan. Mappningstestet laddar sin tunga modul utanför testets timeout och warm-eslint-testet använder en injicerad process-seam i stället för en riktig `npx`-spawn; egen testtid sjönk från 26 s till 39 ms under belastning. Efter fix: 184/184 riktade tester gröna. |
| [x] | Fixad | P2 | **RenderGate saknar completeness-koll: ett partiellt check-svar kan bli "alla passed":** `qualityGateAllPassed` (`preview-quality-gate.ts:181-183`) krävde icke-tom lista och att alla poster var `passed`, men jämförde aldrig *begärda* checkar mot *returnerade* — ett `ok:true`-svar som tappat `build` eller `typecheck` räknades som grönt. Tom lista och otillgänglig gate var redan fail-closed (B08), och ingen känd väg producerade ett ofullständigt svar, så raden var en kodverifierad false-green-risk utan observerad incident. | 24-agentsvärm 2026-07-31, kodverifierad (M#gs8) | **#712** (branch `cursor/rendergate-completeness-6176`). Kollen ligger i ägaren `runQualityGateChecks`, inte i `qualityGateAllPassed`: saknas en begärd kanonisk check i ett svar där inget failat kastas `QualityGateUnavailableError` (ej retrybar) — samma fail-closed-väg som en onåbar verify-lane, som alla tre callsites (`/quality-gate`, `/repair`, `server-verify`) redan hanterar. **Medvetet avgränsat till svar utan failade rader:** verify-lanen avbryter före de kanoniska checkarna när `install` failar, och det svaret måste behålla sitt (ofta repairable) failure-verdikt i stället för att bli ett infra-fel. Kompletta svar är oförändrade. Tester i `preview-quality-gate.unavailable.test.ts` (saknad check + tomt svar ⇒ unavailable; komplett svar + install-fail ⇒ oförändrat). Ägare: `src/lib/gen/verify/preview-quality-gate.ts`. |
| [x] | Fixad | P1 | **`fixMissingImports` hittar på en importsökväg, och stub-skaparen gömmer felet:** `jsx-checker.ts:478-485` lade till `import <Name> from "@/components/<kebab>"` för varje PascalCase-JSX-symbol som saknade import, utan att fråga projektets exportindex — till skillnad från `fixMissingLocalSymbolImports` (`common-import-fixer.ts`) och `runDeterministicImportRepair`, som båda kräver exakt en exportträff. Vid finalize skapade `cross-file-import-checker.ts` sedan en platshållarmodul för den olösta sökvägen, så bygget blev grönt medan komponenten renderade en stub — en false-green i stället för ett ärligt `Module not found`. | 24-agentsvärm på LLM-flödet 2026-07-31, kodverifierad av granskningsagent (M#gs1) | **PR: jsx-checker kräver unik exportträff.** `runJsxChecker` tar nu emot projektets exportindex (namngivna via `buildProjectExportIndex`, default via nya `buildProjectDefaultExportIndex`) från `runAutoFix`, och residyn — PascalCase-taggar som varken är lucide-ikoner eller shadcn-komponenter — löses genom samma `resolveOwnComponentImports` som `runDeterministicImportRepair` använder. Ingen träff eller flera träffar ⇒ ingen import; symbolen lämnas oimporterad med en warning, så `undefined-jsx-symbol`-banan (build-breaking i `should-start-preview`) rapporterar den ärligt. `pascalToKebab` är borta ur filen — det fanns ingen annan konsument. Tester i `jsx-checker.test.ts`: unik default-export, unik namngiven export, okänd symbol (plus att `checkCrossFileImports` då inte har någon sökväg att stubba), tvetydig symbol i två filer, saknat index, och ett `runAutoFix`-fall som binder ihopkopplingen. Ägare: `src/lib/gen/autofix/jsx-checker.ts`, `common-import-fixer.ts`, `pipeline.ts`. |
| [x] | Fixad | P2 | **Dep-pinningen rör `package.json` men lämnar låsfilen orörd:** `completeProjectDependencies` skriver om `package.json` vid imported-repo-finalize (`finalize-preflight.ts:1382-1400`) utan att uppdatera ett befintligt `package-lock.json`. Beroende-fingeravtrycket ändras då så preview-host installerar, men `resolveInstallCommand` väljer `npm ci` för låsfilsbackade projekt utan `alwaysAllowFallback`, och fallbacken är `npm ci --legacy-peer-deps` — ingen install som försonar en gammal låsfil. En follow-up som lägger till ett känt import kan därför fela i install och ändå aldrig få modulen, vilket är precis det felläge ändringen skulle lösa för templates med npm-lås. | Bugbot-subagent på #682:s diff 2026-07-31 (merge-agentens pass, kom in efter merge), kodläst utan repro | **PR #701 (branch `fix/preview-install-readiness`):** stale-lockfile-protokollet. `finalize-preflight`/`/template` sätter en `.sajtmaskin/lockfile-stale.json`-sentinel (`markLockfileStaleInFiles`) när `package.json` muteras med en låsfil närvarande; `resolveInstallCommand` kör då **en** icke-frozen install (pnpm `--no-frozen-lockfile` / `npm install` / yarn utan `--frozen-lockfile`), en PM-säker postcondition (`pnpm/npm/yarn ls`) verifierar direkt-beroendena innan fingeravtrycket stämplas, och den regenererade låsfilen returneras via `/status` och persisteras tillbaka till `engine_versions.files_json` (markören rensas). Ägare: `src/lib/gen/stream/finalize-preflight.ts`, `src/lib/gen/autofix/dep-completer.ts`, `preview-host/src/runtime.js`. |
| [x] | Fixad | P2 | **Nästlade route-sidor rankas under `components/` i prompt-serialiseringen:** `CRITICAL_PATH_PATTERNS` (`serialize.ts:347-475`) ger `app/page.tsx` rang 4 och `components/**` rang 9, men allt *annat* under `app/` faller till 20 (lägre rang väljs först). `app/blog/page.tsx` och andra undersidor är därför de första som stryks när filbudgeten tar slut — trots att route-planen kräver dem, vilket ger en sajt utan de undersidor som planerades. | 24-agentsvärm 2026-07-31, kodverifierad (M#gs6) | **#715 (branch `cursor/nastlade-route-sidor-ranking-6b0b`):** fyra mönster inskjutna före `components/` i `CRITICAL_PATH_PATTERNS` — statisk nästlad `page.tsx`, dynamisk nästlad `page.tsx`, sedan nästlad `layout.tsx` i samma ordning. Rangordningen valdes över ett `routePlan`-tie-break av tre skäl: `scoreRouteRelevance` ger max −4, så 20 − 4 = 16 hade fortfarande förlorat mot komponenternas 9; `routePlan` är optional och saknas i flera anropsvägar; och rangtabellen är redan kanonisk ägare av "vilken fil är viktigast". Motiveringen i koden är kontraktet: route-sidor är `llm-owned` + `mustEmit: true` (`buildFileContract`), så en struken sida blir en sida modellen aldrig skriver, medan en struken `scaffold-owned` komponent behåller sin default. Statisk före dynamisk är med eftersom `[slug]`-detaljmallen annars vann `localeCompare`-tie-breaket och tog platsen från den sida route-planen beställt (mätt på blog-scaffolden). Mätt effekt: `blog`/`dashboard`/`app-shell` byter en komponentsignatur mot sin nästlade sida, `auth-pages`/`ecommerce` får sina nästlade sidor namngivna i omitted-summeringen i stället för utelämnade, enfilsscaffolds oförändrade. Fyra nya fall i `serialize.test.ts` (verifierade att de failar utan fixen). Ägare: `src/lib/gen/scaffolds/serialize.ts`. |
| [x] | Fixad | P2 | **RepairGate klarar inte kolliderande importer — två körningar i rad föll på fel den borde kunna laga:** prod-canaryn 2026-07-29 (chat `85f8db72`, frisörsalong med bokning) gav en innehållsrik 43-filssajt men **ingen** version nådde `passed`. F2 v1: verifieraren flaggade två blockerare korrekt (`app/api/contact/route.ts` använder `Resend` utan import; `components/booking-form.tsx` refererar `sv` som är importerad type-only), server-repair körde **två** försök och gav upp med "code is syntactically valid but typecheck/build still fails". F3 v2 föll sedan på `components/booking-form.tsx(14,85): error TS2440: Import declaration conflicts with local declaration of 'CircleDot'` i både typecheck och build. Alla tre felen är rena import-/symbolkollisioner — deterministiskt lagbara utan LLM. | Prod-canary 2026-07-29 (`/logg-internet`-flöde körd manuellt, prod-DB + Vercel-loggar verifierade) | **#725 (`fix(repair): deterministisk prepass lagar import- och symbolkollisioner`):** de tre klasserna ägs nu deterministiskt före RepairGate:s LLM-varv — saknad värdeimport täcktes redan av known-import-fixern i F3 (`ts2304-known-import-fixer`); `import type` av ett värde som används i runtime växlas till värdeimport inkl. TS1361 inline-specifiers (`value-used-from-type-import-fixer.ts`); TS2440-kollisioner mellan import och lokal deklaration lagas när kompilatorn bekräftar namnet (`duplicate-import-local-type-collision-fixer.ts`). Raden blev kvar som `[ ]` i Aktiv kö när PR:en mergades 2026-08-01 — flyttad hit i backlog-synken 2026-08-05 (avstämningen av saneringssteg 1). |
| [x] | Fixad | P2 | **Plan-lägets follow-up-turer kan sluta i total tystnad — fem user-meddelanden utan ett enda persisterat assistentsvar eller bygge:** prod chat `785c8d7a` (audit-entry, planläge, 2026-07-30): turn 1–2 svarade korrekt, men därefter gav "Hur går det?" ×2 samt reparera-knappens canned prompt ×3 **ingen** assistentrad i `engine_messages`, ingen rad i `engine_generation_logs` och inga Vercel-runtime-fel i fönstret — först den sjätte sändningen byggde v1. Två separata hål: (a) planner-turer persisterade bara svar när utdatan parsades som plan (`persistAssistantSummary` i `plan-mode-turn.ts` — icke-plan-svar var SSE-only och försvann vid reload), (b) tre sändningar producerade varken generation-logg eller fel (kandidater: credit-gate-402, klientens job-kö/stream-lock, planMode-arv via köade jobb). | /logg-utredning 2026-07-31 (prod-DB `engine_messages` + `prompt_logs` + `engine_generation_logs` + Vercel `get_runtime_errors`, read-only) | **#723 (`fix(plan-mode): persistera icke-plan-svar + instrumentera tysta sändningar`):** båda forensikstegen levererade — (a) planner-svar persisteras via `buildPlanModeAssistantMessage` även när utdatan inte parsas som plan, (b) codegen-turens tidiga exits (credit-gate, lock, kö) instrumenterade så en tyst sändning alltid lämnar spår i DB. Restfallet "parsas till substanslöst objekt" stängdes separat i MVP-stabiliseringssvepet (egen rad nedan). Raden blev kvar som `[ ]` i Aktiv kö när PR:en mergades 2026-08-01 — flyttad hit i backlog-synken 2026-08-05. |

| [x] | Fixad | P2 | **Appens primärknapp klarade inte WCAG 2 AA i liten text:** vit text på `--primary` (`219 100% 60%` → `#337aff`) gav **3,91:1** mot kravet 4,5:1 — `Button`-komponentens default-variant (`bg-primary text-primary-foreground`), inte en enskild knapp. | Axe-core-svep av `/builder` 2026-08-01 (M#a11y2), kodverifierad | **MVP-stabiliseringssvepet 2026-08-01 (branch `fix/mvp-svep-0801`):** ägarbeslutet blev alternativ (b) i skärpt form — appen är mörk-endast, så en mörkare `--primary` hade sänkt `text-primary` på mörk bakgrund (4,85:1 i ~67 filer) under AA. I stället byttes `--primary-foreground` till bakgrundstonen `220 16% 7%` → **4,85:1**, brand-blå orörd. Enda fg-scrim-missbruket (`bg-primary-foreground/20`-chipen i `site-audit-section.tsx`) flyttades till `bg-white/20` (6,5:1 — var 2,9:1 redan före bytet). Beroendefritt token-kontrasttest låser paret ≥ 4,5:1 (`src/app/globals-token-contrast.test.ts`). Residual: hover-läget `bg-primary/90` ger 4,14:1 transient — egen ny rad i Aktiv kö. |
| [x] | Fixad | P2 | **En oparsbar planner-plan försvann vid omladdning:** `hasPlanArtifact` sattes av `resolvePlanArtifact(...) !== null`, så utdata som parsades till ett substanslöst objekt (t.ex. `{}`) tog plansummerings-grenen och `review.ts` skrev generiska "Plan skapad och redo för granskning" — den råa `accumulatedContent` sparades aldrig. (Prosa-fallet var redan fixat av #723:s `buildPlanModeAssistantMessage`; kvarvarande hål var parse-till-tomt-fallet.) | Extern granskning 2026-08-01 (#682), kodverifierad | **MVP-stabiliseringssvepet 2026-08-01 (branch `fix/mvp-svep-0801`):** nytt predikat `planArtifactHasSubstance` (normaliserad artefakt med minst ett steg, en blockerare, en sida eller en scope-rad) styr persist-beslutet i `plan-mode-stream.ts`; substanslös JSON faller till `planner-text`-grenen som persisterar råtexten. Awaiting-input-planer bär blockerare och förblir planer (testlåst). `pages`/`scope` räknas som substans eftersom det befintliga låsta testet emitterar en legitim plan utan steg/blockerare. Persistens-only, ingen UI-ändring. Tester i `plan-mode-turn.test.ts`. |
| [x] | Fixad | P2 | **Dossier-env rehydrerades inte vid force-restart eller quick-edit-fallback:** `selectedDossierEnvKeys` trådades från finalize till FÖRSTA preview-booten men persisterade aldrig; `POST /preview-session` och quick-edit-fallbacken byggde om `.env.local` utan F2-mock-seedet och dossier-UI:t tappade sitt demo-läge tyst. | Extern granskning 2026-08-01 (#675), kodverifierad | **MVP-stabiliseringssvepet 2026-08-01 (branch `fix/mvp-svep-0801`):** ägarbeslut — full fix med ny nullable jsonb-kolumn `engine_versions.selected_dossier_env_keys` (migration `add-engine-version-selected-dossier-env-keys.sql`, registrerad i `migration-order.mjs` + `db-init.mjs`, applicerad på dev). Finalize skriver urvalet på versionsraden; preview-session-routen trådar det vid (om)start; quick-edit-minorn ärver basversionens nycklar; F3-forken och restore/rollback kopierar dem (ofarligt — seedet kör bara i design-stage). Repair-lanen rör inte kolumnen. NULL = legacy-rad, samma fail-open som förr. Tester: route + quick-edit + finalize-design + Postgres-kontraktstest (`scripts/db/selected-dossier-env-keys-contract.postgres.test.ts`). |
| [x] | Fixad | P3 | **Strömmat innehåll utan sparad version rapporterades som empty-output i UI och telemetri:** servern skilde fallen i `reason` men progress-fasen blev alltid `empty-output`, och klientens `hasRecoveredArtifact` räknade inte strömmad text — användaren kunde se kod i chatten och samtidigt få en fel-toast. | Extern granskning 2026-08-01 (#684), kodverifierad | **MVP-stabiliseringssvepet 2026-08-01 (branch `fix/mvp-svep-0801`):** ny fas `stream-without-version` när `reason === "stream_ended_without_version"`; klienten hoppar över feltoasten och lägger en ärlig progress-rad ("Innehåll strömmades … kunde inte sparas som version") när strömmad text finns. Medvetet INTE inbakat i `hasRecoveredArtifact` — den signalen styr Byggval-reset och ska betyda riktig artefakt. Genuint tomma rundor oförändrade. Tester i `generation-stream.golden.test.ts` + `stream-handlers.test.ts`. |
| [x] | Fixad | P2 | **Klargörandefrågan kastade användarens ursprungliga follow-up-prompt:** svaret på `resolveFollowUpClarification`-frågan ("Förfina nuvarande design" osv.) blev hela nästa generationsprompt; originalprompten låg i historiken men lästes aldrig. Observerat i prod chat `e8bd3ba6`: en detaljerad hydration-fix-prompt reducerades till fyra ord. | /logg-utredning 2026-08-01 (prod chat `e8bd3ba6`), kodverifierad | **MVP-stabiliseringssvepet 2026-08-01 (branch `fix/mvp-svep-0801`):** `persistFollowUpClarification` bär nu maskinläsbara `followUpClarification: true` + `sourceUserMessage` i awaiting-input-payloaden (medvetet INTE `contractClarification` — samma markördisciplin som F3-continuation); ny `collectFollowUpClarificationAnswer` speglar kontraktsflödets B13-mönster och konsumerar bara exakta alternativ-matchningar (fri-typad text = ny prompt). Handlern orkestrerar originalprompten, klassar intent mot den, wrappar fråga+svar i en egen sektion (`## Follow-up Scope Clarification Answer`) och kan inte ställa en andra scope-fråga på svars-turen. Tester: 7 collector-fall + 3 tvåturs-integrationsfall i stream-routens test. |
| [x] | Fixad | P2 | **Ett persist-fel rapporterades som tom utdata:** `finishWithoutVersion` satte alltid fasen `empty-output` och devlog-typen `site.empty_generation` även när innehåll strömmat men dubbel-finalize fallit — klienttext och telemetri sa "ingen användbar kod" trots att assistentmeddelandet innehöll filerna. | Bugbot-subagent på #684:s diff 2026-07-31 (merge-agentens pass), kodläst utan repro | **MVP-stabiliseringssvepet 2026-08-01 (branch `fix/mvp-svep-0801`):** samma leverans som empty-output-raden ovan — ny devlog-typ `site.stream_without_version` skiljer "utdata fanns men kunde inte sparas" från "ingen utdata", och devlog-läsarna (`summaries.ts`, `fault-fix-index.ts`, `status.ts` → status `error_signal`, persistBlocker-metan) känner den nya typen så fallet inte tappar klassificering. |

## Fixade 2026-07-31

| Klar | Status | Prio | Fynd | Källa | Fix-referens |
| --- | --- | --- | --- | --- | --- |
| [x] | Fixad | P2 | **Liveheadern döljer ett terminalt fel medan turen fortsätter:** `getActiveAgentLogLabel` tittade bara på tool-parts i `input-streaming`, och `AgentLogCardContent` föll tillbaka på "Fortsätter med nästa byggsteg." när `activeLabel` var null men `isActive` fortfarande true. Sedan preview-/build-fel markeras terminalt (`output-error`) försvann de därför ur den synliga raden — feltexten fanns bara som en rad i den hopfällda loggen. Ikonen på den raden blev en varningstriangel redan i #681, så felet ljög inte längre om utfallet, men headern berättade det inte medan det hände. | Bugbot-subagent på #681:s diff 2026-07-31 (merge-agentens pass), kodläst utan repro | **#686:** headern signalerar nu felet i stället för att dölja det. `surfaceFailure` är `hasFailures` (`BuilderMessageTooling.tsx:231`), så kortet får destructive-ram och `AlertTriangle` så snart något steg är `failed` — även medan turen fortsätter. Rubriken säger `Ett byggsteg misslyckades` när det aktiva steget felat, `Arbetar vidare efter fel` när ett tidigare steg felat men arbetet pågår, och `Slutsteg (N) · fel` när turen är klar; underraden blir `Fel upptäcktes — visa detaljer`. Ägarvalet löstes med en tredje väg: felstatusen syns utan att den gamla feltexten återges, vilket undviker risken att en inaktuell etikett står kvar efter att pipelinen återhämtat sig. Tester i `BuilderMessageTooling.test.tsx`. |
| [x] | Fixad | P2 | **70 chart-blocks i galleriet hämtar en miniatyr som 404:ar, och UI:t visar samma pusselbit som för poster utan bild:** mätt 2026-07-31 mot `ui.shadcn.com` med exakt de URL:er `buildPreviewImageUrl()` bygger och med blocknamn ur registerindexet (`/r/styles/new-york-v4/registry.json`, 471 poster). Utfall: **`chart-bar*` 404 på alla 10** — precis de kort ägaren såg — `chart-*` 404 på 20 av 20 testade av 70, medan layout-blocks (`dashboard-01`, `sidebar-01`…`14`) ger **200 `image/png`** på samma style-väg i samma stund. **Det var inte en trasig style-väg:** `resolvePreviewImageStyle`-omvägen till `new-york` är korrekt och load-bearing (`new-york-v4` 404:ar på allt). shadcn publicerar helt enkelt inga PNG:er för chart-kategorin. Defekten hos oss var att `RegistryItemThumb` renderade **samma** pusselbit för "ingen bild-URL finns" (`thumbnailUrl()` → `null` för 239 `registry:example`, 61 `registry:ui`, 52 `registry:font`) och för "URL:en fanns men svarade 404" (`onError`) — ett verkligt trasigt CDN blev därmed osynligt. | Notering N8 från observationssessionen 2026-07-30; mätningen körd 2026-07-31 (planens steg 1). **Planens egen hypotes var fel** — den antog att `chart-bar` gav components utan bild by design, men alla 10 posterna är `registry:block` och begär alltså en bild | **#692:** de två lägena är visuellt åtskilda. Poster utan bild-URL får typ-ikon ur `previewKind`/`iconKey` (fanns redan i `registry-service.ts`, användes inte av komponenten); en URL som 404:ar får `ImageOff` med egen text ("Förhandsbilden kunde inte laddas") i dämpad bärnsten plus `title`. Konsolideringen fångade att detaljvyns `fallbackLabel="Ingen förhandsbild"` annars renderades i **båda** lägena, vilket hade återinfört exakt den sammanblandning raden gäller. Fyra tester i `RegistryItemThumb.test.tsx` låser bild / ingen bild / 404 isär och hindrar att texterna byter plats. `resolvePreviewImageStyle` medvetet orörd — en omskrivning hade brutit de layout-blocks som fungerar. Kvar som **ägarfråga, inte bugg**: shadcn publicerar inga chart-PNG:er, så egna Blob-miniatyrer (planens steg 4) byggs bara på begäran. |
| [x] | Fixad | P3 | **F2-previewen seedas med hela placeholder-katalogen oavsett vad sajten använder:** `resolvePreviewEnvLayers` laddade `loadHarmlessPlaceholderRecord()` + `loadTier3StubPlaceholderRecord()` i sin helhet för varje design-preview — 32 + 24 = 56 nycklar räknat 2026-07-27 — och lade dossier-mock-seeden ovanpå. `selectedDossierEnvKeys` användes bara för att LÄGGA TILL stubbar, aldrig för att begränsa katalogen. En sajt med bara ett kontaktformulär fick alltså Stripe-, Supabase-, Redis- och OpenAI-stubbar i sin preview-env. Ingen läcka mot publicerad sajt (kodverifierat): F3 strippar tier3-lagret, deploy skickar bara `projectEnv.configuredMap` och genererad `.env.local` strippas vid deploy-filhopsättningen. | Ägarfråga 2026-07-27 i #629-handovern, kodverifierad i `env-local.ts` + `deployments/route.ts` | **#675:** katalog-lagren (harmless + tier3-stub) scopas per projekt via `src/lib/gen/preview/relevant-env-keys.ts` — en katalognyckel behålls när namnet förekommer i en projektfil, ett importerat SDK läser den internt (inkl. `package.json`-deps som boot-krashvakt: pg/redis/mongoose/@vercel-paket, samt openai/resend/stripe efter bugbot-fynd — `new OpenAI()`/`new Resend()` kastar vid modul-scope-konstruktion utan nyckel) eller vald dossier deklarerar den. Env-artefakter exkluderas ur scannen; fail-open utan filer; `user`/`generated`/`project-preview` filtreras aldrig. Tester i `relevant-env-keys.test.ts` + `env-local.test.ts`. Verify-envens kuvert är medvetet orört — kvarstår som egen öppen rad. |

## Fixade 2026-07-30

| Klar | Status | Prio | Fynd | Källa | Fix-referens |
| --- | --- | --- | --- | --- | --- |
| [x] | Fixad | P1 | **`files_revision`-kontraktet hade ingen Postgres-backad test — bara en mock som stringifierade ett Drizzle-SQL-objekt:** den bevisade att en subselect *formulerades*, inte att den genererade kolumnen fanns, att den räknades om vid `files_json`-uppdatering, eller att rätt hex-hash lagrades. Kontraktet är DB-genererat runtime-beteende, så en ogiltig eller oapplicerad migration hade inte fällt något. Prod-observationen 141/141 var ett kvitto, inte en grind. | Codex P1 på #642, omklassad P2 → P1 efter Codex på #653 | Ny `scripts/db/files-revision-contract.postgres.test.ts` mot **riktig** Postgres, 7 assertions: kolumnen är `GENERATED ALWAYS` med `md5` i uttrycket, stämplas vid INSERT utan att någon skrivare rör fältet, **räknas om** vid `files_json`-UPDATE, **kan inte skrivas manuellt**, telemetrins subselect returnerar versionens faktiska revision, telemetrins revision **följer inte med** när versionen skrivs om (det är hela poängen — mismatchen ska bli upptäckbar), och indexet finns. **Egen lane** (`npm run test:postgres`, `vitest.postgres.config.ts`, glob `*.postgres.test.ts`) eftersom standard-sviten körs medvetet utan databas och flera tester verifierar just den degraderingen — en `POSTGRES_URL` på jobbnivå hade ändrat förutsättningen för dem tyst. CI kör den mot en **efemär** `postgres:16`-service (inte dev-Supabase: varje PR hade annars skrivit i en delad databas och samtidiga PR:er kunnat kollidera). Testet vägrar allt utom en dev-target via repots egen `check-db-env-target.mjs` — verifierat att en prod-URL ger SKIP, inte skrivningar. False-green är stängd på **två** axlar, båda funna under arbetet: `REQUIRE_POSTGRES_TESTS=1` i CI gör att ett **hopp** räknas som fel (vitest avslutar annars med 0 på skippade tester, så en försvunnen databas hade gett grön grind utan bevis), och lanen kör **utan** `--passWithNoTests` så att en **borttagen eller omdöpt fil** faller på "No test files found" i stället för att bli grön med noll assertions. Den andra axeln fångades av bugbot-passet: filens egen grind kan bara larma om filen laddas, så flaggan hade gjort själva skyddet kringgåbart. Båda verifierade genom att provoceras fram. Bevisvärdet syntes direkt: första körningen mot riktig Postgres föll på `model` NOT NULL i `generation_telemetry`, vilket mocken aldrig kunde ha fångat. Fotavtryck: en `engine_chats`-rad, allt annat `ON DELETE CASCADE`, städas i `afterAll`. |
| [x] | Fixad | P3 | **Prometheus-parsern av-escapeade i fel ordning:** `_parse_labels` kedjade tre `replace`-anrop, så varje anrop läste om föregående anrops utdata. Ett etikettvärde som i exposition-formatet är en escapead backslash följd av bokstaven `n` blev därför först `\n` och sedan en riktig radbrytning — ett litteralt `\n` i datan tolkades som nyrad. | P2-3-testarbetet (#650) — hittad vid kodläsning för Prometheus-parsertesterna | Un-escapningen är nu **ett** `re.sub`-pass som konsumerar varje backslash tillsammans med tecknet den escapear, så inget den producerar kan läsas om som en escape. Odefinierade sekvenser behåller sin backslash — spec:en definierar bara `\\`, `\"` och `\n`, och att bevara indatan slår att uppfinna en avkodning. Verifierat före/efter: `C:\\next` gav `C:` + radbrytning + `ext` med gamla koden och ger `C:\next` med den nya, medan samtliga övriga fall är byte-identiska (inklusive de två som redan hade tester) — ändringen rör bara det trasiga fallet. Tre nya tester, varav ett faller på gamla koden. Ägare: `backoffice/pages/observability.py` → `_unescape_label_value`. |
| [x] | Fixad | P3 | **En osynlig 429-blipp gjorde en awaiting-input-runda gratis:** #661 stängde telemetri-halvan av `providerFault`, men credit-halvan låg kvar: `endedAsDesigned`-grenen returnerade **före** `commitCredits()`, så en awaiting-input-runda som normalt debiteras blev gratis om ett övergående 429 råkat passera strömmen tidigare i samma körning. Två körningar med identiskt användarsynligt utfall prissattes alltså olika utifrån en blipp användaren aldrig såg. | Oberoende granskning av #661 2026-07-30; **ägarbeslut 2026-07-30** | Samma regel som telemetrin fick: **utfallet avgör, inte det värsta ögonblicket.** `endedAsDesigned`-grenen anropar nu `commitCredits()` innan den returnerar, så en awaiting-input-runda kostar lika mycket oavsett blipp. Grinden håller kvar sitt ursprungliga syfte — den äkta felvägen (provider-felet **är** skälet att inget producerades) debiterar fortfarande inte och skriver sin `success=false`-rad. Golden-testet som låste det gamla beteendet vändes till att kräva exakt en debitering. |
| [x] | Fixad | P3 | **En kvarhängande `providerFault`-flagga skrev falska misslyckade generationsrader:** flaggan sattes när ett provider-fel passerade strömmen och nollställdes aldrig. Ett *avsiktligt* awaiting-input-avslut efter en tidigare övergående 429 loggades som `logGeneration(success=false)` med providerns meddelande, fast körningen slutade som den skulle. | Djupgranskning av #641 2026-07-29; ägarbeslut 2026-07-30 | Beslutet var att **en generationsrad ska beskriva utfallet, inte det värsta ögonblicket**. `commitCreditsUnlessProviderFault` tar nu `endedAsDesigned`, som `finishWithoutVersion` sätter från det beräknade `awaitingInput` — en överlevd blipp loggas som warn i stället för att arkiveras som ett misslyckande. Kreditbeteendet lämnades oförändrat i den här PR:en — den halvan avgjordes separat och stängdes av raden ovan. Golden-test låser både den överlevda vägen och att den äkta felvägen fortfarande skriver sin rad. Rotorsaken var att `error`-fallet bara bryter sin egen `switch`, så strömmen läser vidare. |
| [x] | Fixad | P3 | **`npm run worktree:remove` lämnade en tom katalog och kastade ett Node-fel på sista steget:** losskopplingen av junctionen fungerade, men `git worktree remove` föll på `Permission denied` från ett kvarvarande filhandtag och skriptet propagerade `execFileSync`-felet som en ohanterad stacktrace — direkt efter raden `unlinked … (target untouched)`, vilket läser som att junction-fällan slagit till. | Städning efter backoffice-mergen 2026-07-29 (två observationer), reproducerad igen 2026-07-30 | Borttagningen försöker nu **två gånger** med kort backoff (handtaget hinner oftast släppa), och faller den ändå skriver `describeRemovalFailure` ett meddelande som **leder med att den delade `node_modules` är oskadad** innan det säger vad som faktiskt föll. Meddelandet skiljer på "git listar fortfarande worktreen — kör om" och "git har redan släppt den, bara mappen är kvar" och ger då `git worktree prune` + exakt raderingskommando. Fem tester på den rena funktionen. |

## Fixade 2026-07-29

| Klar | Status | Prio | Fynd | Källa | Fix-referens |
| --- | --- | --- | --- | --- | --- |
| [x] | Fixad | P2 | Sign-off/merge-freshness: `review-window` väntar bara in botar utan verdict; #494 mergades 55 s efter Codex-P1 med stale sign-off. | Extern coach-review (#494) | Restlista R9 (#644): `.github/workflows/merge-ready-freshness.yml` tar bort `merge:ready` vid ny commit och bot-review/inline nyare än sign-off `at:`; sign-off kräver `sha:` + `at:` (`pr-merge-review-gate.mdc`). |
| [x] | Fixad | P1 | **R7:s missing-env-observation kunde tappas i prod, och testet var grönt ändå:** alla tre 412-vägarna schemalade skrivningen som naket `void logTier3MissingEnvBlocked(...)` direkt före `return`, så en frysande serverless-invokering kunde döda INSERT:en — medan testerna mockade modulen och bara asserterade att helpern anropades. Prod hade 0 rader med `category='f3-readiness:missing-env'`. | Codex P1 + Vercel Agent Review på #643, båda postade före sign-off men aldrig triagerade; hittad igen vid granskningen 2026-07-29 | #646: `logTier3MissingEnvBlockedDetached` registrerar skrivningen med `after()` (fire-and-forget som fallback utanför request-kontext), alla tre callsites använder den, och testerna kör den registrerade promisen. **Kvarstår att observera i prod:** att en rad faktiskt skrivs vid nästa F3 412. |
| [x] | Fixad | P1 | **R7:s missing-env-rad raderades av nästa rena follow-up — durabiliteten som var hela poängen höll inte:** `pruneStaleVersionErrorLogs` tog bort alla rader för versionen där `COALESCE((meta->>'repairPassIndex')::int, 0)` var lägre än aktuellt pass, och en gate-observation sätter aldrig fältet — den räknades som pass 0. R7 skriver dessutom mot design/parent-versionen, precis den som får follow-ups. #646 gjorde att skrivningen hann köra; raden fick ändå inte ligga kvar. | Djupgranskning av #643 2026-07-29, kodverifierad mot båda ägarfilerna | #651: `PRUNE_EXEMPT_CATEGORIES` undantar gate-observationer från prunet, med `COALESCE(category,'')` så kategorilösa rader prunas precis som förut (`NULL NOT IN (…)` är NULL och hade tyst sparat dem). Test låser både den renderade SQL:en och att listan täcker R7:s kategori. |
| [x] | Fixad | P2 | **`dump-logs --kinds=errors` exporterade `meta` för alla kategorier, inte bara R7:s:** tyngsta passageraren var `quality-gate:*-tooling`, vars meta bär upp till 12 000 tecken rå build-output per rad från ett bygge som körts med användarens riktiga env-värden — och `/logg` hämtar 100 rader per kind. | Djupgranskning av #643 2026-07-29 | #651: långa strängar trunkeras på vägen ut (`scripts/db/dump-logs-meta.mjs`, egen modul med test). Codex-P2 på samma PR visade att subträd *på* djupgränsen släpptes igenom okapade — de ersätts nu av en sentinel, verifierat med reproduktion (12 307 → 109 tecken). R7:s `missingByIntegration` passerar oförändrad. **Volym- och exponeringsminskning, inte redaktion** — en logg-dump ska fortsatt behandlas som känslig. |
| [x] | Fixad | P2 | **`merge-ready-freshness` ignorerade den bot som hittade nattens P1:or, och litade på en författarstyrd tidsstämpel:** `vercel[bot]` var helt exkluderad trots att Vercel Agent Review postar riktiga logikfynd som review-kommentarer; `issue_comment` saknades som trigger; sign-off-matchningen krävde hårdkodad em-dash; och ordningen mot bot-fynd avgjordes av `at:`, som är fritext. Borttagningsvägen hade aldrig exekverats skarpt. | Codex P1 på #644 (otriagerad vid merge) + djupgranskning 2026-07-29 + bugbot/Codex på #651–#652 | #651 + #652: `vercel[bot]` räknas på review-vägarna men inte som issue-kommentar (deploy-brus), `issue_comment` tillagd, skiljetecken toleranta. Framför allt: ordningen avgörs nu av `created_at` på sign-off-**kommentaren** — serverside och oskrivbar — i stället för `at:`, som bara är läsbarhet. Att jämföra `at:` mot runnerns klocka räckte inte: en fördröjd jobbstart hann göra ett framtidsdaterat `at:` förflutet. Beslutslogiken ligger i `scripts/ci/merge-ready-freshness.mjs` med tolv tester. **Två medvetna kvarvarande begränsningar:** ingen `labeled`-trigger (en färsk sign-off är alltid nyast, så den skulle inte upptäcka något — kräver verdict-kunskap workflowen inte har) och ingen `check_run`-väg (de kända botarna postar som review eller kommentar). Båda dokumenterade i [`pr-merge-review-gate.mdc`](../../../../.cursor/rules/pr-merge-review-gate.mdc). |
| [x] | Fixad | P1 | **Repair-passets telemetri stämplade den pre-repair-bas som just föll:** `recordRepairPassedQualityGate` gick via subselecten mot `engine_versions.files_revision` medan kandidaten grinden bedömde låg i `repaired_files_json`, så `preflight_passed` arkiverades under fel innehåll — latent false-green för steg 3 och felaktig mismatch-mätning redan nu. | Cursor Bugbot (medium) + Codex P1 + Vercel Agent Review på #642, alla tre otriagerade; hittad igen vid granskningen 2026-07-29 | #646: `saveRepairedFiles` skickar det promotbara innehållet avkodat ur samma payload den just lagrade (byte-identiskt med vad `acceptRepair` skriver) och revisionen räknas med `md5()` i Postgres. Codex hittade dessutom en följdlucka i själva fixen (ett **ersättande** repair-varv före acceptance dedupades bort) — dedupe jämför nu revision, inte bara resultat. |
| [x] | Fixad | P2 | **Tomt användarfält i projekt-rensningen breddade till `--all-test-users`:** `_build_command` föll till `else: --all-test-users` när scope var specifik email/user_id men fältet tomt. | P2-3-testarbetet (#650) / Codex P1 på #650 | Fail-closed i #650: `_build_command` returnerar `None` vid tom specifik scope; `render()` disablar DRY-RUN/APPLY och vägrar köra utan giltigt kommando. |

## Fixade 2026-07-25

| Klar | Status | Prio | Fynd | Källa | Fix-referens |
| --- | --- | --- | --- | --- | --- |
| [x] | Fixad | P3 | **Warm-cachen saknade de flesta dossier-egna SDK-beroenden (restpost efter #603):** pre-VM-typechecken (`SAJTMASKIN_PRE_VM_TYPECHECK` / forcerad i F3) kör mot repots symlänkade `node_modules`, medan en genererad sajt får sina SDK:er ur dossier-manifestens `dependencies` — 15 paket som repot inte installerar (`ably`, `@supabase/supabase-js`, `@supabase/ssr`, `@sentry/nextjs`, `@stripe/stripe-js`, `@paddle/paddle-node-sdk`, `@neondatabase/serverless`, `mongodb`, `next-sanity`, `maplibre-gl`, `minisearch`, `@visactor/react-vchart`, `@ai-sdk/fal`, `embla-carousel-autoplay`, `server-only`). Giltig genererad kod fick TS2307 och skickades i onödan in i repair-loopen. | Codex P2 på #600 → Clerk-delen i #603 → Codex P1 på #606 (restposten fick inte arkiveras med Clerk-delen) | **Linje (c) valdes:** pre-VM-passet släpper olösbara modul-diagnostiker för dossier-deklarerade paket i st.f. att gissa installationsstatus (`src/lib/gen/preview/generated-only-modules.ts`); setet härleds ur manifesten så en ny dossier kräver inget arbete. Riktigt fel behålls: trasig subpath på ett installerat paket, alla icke-TS2307-koder, och paket ingen dossier deklarerar. Släppta paket loggas som `validate.tsc.undecidable-modules`. **Linje (a)** (installera i cachen) föll på tung cache + versionsdrift; **linje (b)** (auto-genererade stubbar) föll på att den bevisat inte fungerar — se raden nedan. Empiriskt verifierat mot en riktigt provisionerad cache före/efter (`warm-typecheck.test.ts`, 3 e2e-fall som kör riktiga `tsc`). |
| [x] | Fixad (ersätter #603-lösningen) | P3 | **Per-paket-stubben i warm-cachen bytte TS2307 mot TS2305:** `#603` aliasade `@clerk/nextjs` till `tests/stubs/clerk-nextjs.tsx` i cachens tsconfig, men rot-stubben exporterar bara 6 symboler (`ClerkProvider`, `SignedIn/Out`, `SignInButton`, `SignUpButton`, `UserButton`). Genererad kod som använder någon annan Clerk-export fick i stället `TS2305: Module '"@clerk/nextjs"' has no exported member 'useUser'` — samma falska diagnostik i en annan kod, och exakt den risk `clerk-nextjs-server.ts`s egen docstring varnade för. En modul-stub kan inte ha catch-all-export, så linjen är strukturellt oförmögen att undvika detta. | Kodverifierat 2026-07-25 mot provisionerad cache (`useUser()` → TS2305) under arbetet med raden ovan | Stub-aliasen togs bort ur `provision-warm-cache.ts` (TS2307 för `@clerk/nextjs` faller nu under samma släpp-regel som övriga dossier-paket). Provisioneringen raderar `__sdk-stubs/` ur gamla cachar och `check-warm-cache.mjs` rapporterar en kvarlämnad stub-alias som `COLD`. `tests/stubs/clerk-nextjs*` behålls — de används av vitest-aliasen och repots egen tsconfig för dossier-komponenttester. |
| [x] | Fixad | P3 | **Fyra dossier-deklarerade paket kunde inte pinnas av importskannern:** `@sentry/nextjs`, `maplibre-gl`, `minisearch` och `@vercel/speed-insights` saknades i `KNOWN_PACKAGES`. `resolveCapabilityDependencies` nådde dem via manifest-fallbacken (`"latest"`) men bara när capabilityn var efterfrågad — `runDepCompleter` (den enda dep-passet på export-vägen) kunde inte, så genererad kod som importerade dem utan capability-val skeppade en `package.json` utan dem → VM-bygget föll på "Module not found". | Upptäckt under warm-cache-raden ovan: släppet av en TS2307 är bara säkert om export-pipelinen garanterat installerar paketet | De fyra lades till i `KNOWN_PACKAGES` (majors verifierade med `npm view <pkg> version` 2026-07-25) och hela täckningen är nu låst av ett generiskt invariant-test i `dep-completer.test.ts` — det ersätter de per-dossier-pin-assertions som annars måste läggas till manuellt för varje ny dossier. Invariantens första version exempterade manifestens **pinnade** form (`stripe@^14.0.0`) och läckte därför exakt den lucka den skulle stänga: importskannern kunde inte läsa manifest-pinnen, så ett pinnat paket utanför `KNOWN_PACKAGES` blev släppt pre-VM men aldrig pinnat i `package.json` (Codex P1 + Vercel Agent, oberoende av varandra, på #610). Fixat genom att importskannern nu resolvar via `resolveExportableVersion` (allowlist → manifest-pin) och att testet asserterar mot **samma** resolver, så det inte kan påstå täckning pipelinen saknar. |
| [x] | Fixad | P3 | **`sendMessage` saknade outcome-kontrakt för programmatiska sändare:** hooken returnerar (i st.f. att rejecta) på flera hanterade felvägar (409 stale base, 412 tier-3 env, vissa aborts), så en caller kunde inte skilja "generation startad" från "avvisad men hanterad". Insättningskorten (#581) mitigerade sin del med neutral copy ("Skickat till chatten — se status där") — kvar var själva kontraktet. | BB#shadcn-lane1 (bugbot-pass på #581, 2026-07-22) | `SendMessageOutcome` (`started`/`settled`/`rejected`/`aborted`/`failed`) returneras från varje utgång i `useSendMessage`, trådad genom `useChatMessaging`, `useAutoFix`, builder-controllern och insättnings-lanens handler-kedja (`ShadcnInsertHandler`). Två axlar hålls isär (bugbot-fynd på #610, medium): "körde en generation" (`started`, det enda ett insättningskort får kalla "Skickat") och "konsumerades prompten" (bara `rejected` betyder nej, det enda utfall där composern behåller utkastet). `settled` är fallet där de skiljer sig — servern gjorde turen till en deterministisk F3-ReleaseGate-runda, så prompten konsumerades men ingen ny generation kördes. Den nästlade finalize-rundan har fyra utfall och bara release-verdiktet är `settled`: `missing_env` klassas som samma avslag som ett direkt 412, `llm_ready` som `f3_build_required` (användaren startar bygget från previewpanelen) och `error` som `failed`. Avslag bär dessutom `turnRecorded`, som säger om servern skrev ner turen — **en** regel som ger prompten exakt ett hemvist: `false` → spök-user-raden tas bort och composern behåller utkastet; `true` → bubblan står kvar och utkastet rensas. Fältet finns för att granskarna annars pendlade mellan de två felen: att dölja en persisterad rad (kommer tillbaka vid omladdning) och att behålla båda kopiorna (omsänd dubblerar turen). Nu härleds båda besluten ur samma fakta i st.f. att bedömas per callsite. Stale-base 409 och tier-3 412 returnerar bevisligen före serverns `addMessage` → alltid `false`. Den nästlade finalize-409:an persisterar user-raden **bara** på approve-continuation-vägen, inte på "Bygg integrationer"-autokicken, och klienten kan inte se skillnaden — så grinden rapporterar den själv (`userTurnPersisted` i 409-svaret, `false` även om den best-effort-insertet misslyckas). Klienten gissar alltså inte; båda utfallen är testade på både server- och klientsidan. Tre ytor slutade ljuga: insättningskorten markerar bara "Skickat" när ett bygge faktiskt startade (ett hanterat 409/412 satte förut badgen — 8 s-timeouten var en workaround för just det), composern behåller utkastet (text, bilagor, Figma-länk, inspect-punkter) vid ett hanterat avslag, och registry-droppens toast/statusrad lovar ett startat bygge i st.f. den neutrala copyn. Callers som inte behöver utfallet ignorerar det fortfarande: autofix, quick replies, page-block-composern och **dossier-katalogen** — katalograderna har ingen egen "skickat"-status att falsifiera, och sändvägen äger redan avslagsytan, så en extra toast där skulle bara stapla sig (bugbot-fynd 2026-07-25, medium). 11 nya outcome-tester + 3 nya UI-regressionstester. |

## Åtgärdsprogrammet efter observationssessionen 2026-07-25 (levererat 2026-07-26)

> Nio av tio defekter från live-sessionen 2026-07-25 (`chat 84979fd3`), levererade
> som sex spår ur planfamiljen `2026-07-25-builder-atgardsprogram` (arkiverad som
> en rad i [`docs/plans/avklarat/README.md`](../README.md) efter acceptanskörningen
> 2026-07-27; full text i git-historiken).
> Raderna fanns aldrig i Aktiv kö — de kom in via planfamiljen och stängs här i
> samma leverans. **F10 är inte fixad** och ligger kvar som öppen rad i
> `BUG-SWARM-BACKLOG.md`, konsoliderad med innehållsrevisions-primitiven.
>
> **Acceptanskörningen 2026-07-27 fällde två av spåren i efterhand:** F9:s fix
> valde fence-markören före stream-markören och lämnade 20 876 tecken kod i en
> chattbubbla, och spår 01 steg 3 föll av en annan orsak än planen antog —
> snapshot-nyckelbudgeten tappade `mutedCapabilities` innan den nådde databasen,
> så läs-fixen aldrig hade något att läsa. Båda stängda i #629, egen sektion nedan.

| Klar | Status | Prio | Fynd | Källa | Fix-referens |
| --- | --- | --- | --- | --- | --- |
| [x] | Fixad | P1 | **F1 — resend-dossiern användes inte; det genererade kontaktformuläret låtsades skicka:** F2-muten tog bort integrations-dossiern ur prompten men sa aldrig åt modellen att låta bli att bygga integrationen för hand. Resultatet blev ett frihandsformulär som såg ut att fungera. | Observationssession 2026-07-25, fynd F1 | Ägarbeslut B1 = alternativ (b): noll integrationskod i F2 plus en synlig förklaring. `renderF2ContractBlock` namnger de uppskjutna integrationerna och förbjuder `app/api/**`-filer, SDK-import och `process.env`-läsning för dem — men kräver att ytan ritas med `useState` + demo-toast. Spår 01 steg 2. Test: `capability-prompt-filter.test.ts`, "tells the model to render the surface without a route or an SDK import". |
| [x] | Fixad | P1 | **F2 — `dashboard-charts` rapporterades som "kopplad" i varje sajt:** manifestet deklarerade `components/lib/utils.ts`, som mappas till baseline-filen `lib/utils.ts`. Ingen annan dossier deklarerade sökvägen, så presence-resolvern räknade den som distinctive bevisning — och charts-dossiern såg byggd ut i sajter utan en enda graf. | Observationssession 2026-07-25, fynd F2 | Manifestraden borttagen, och — det som faktiskt stoppar återfallet — `version-presence.ts` räknar aldrig en baseline-sökväg som distinctive. Baseline-mängden härleds ur `SCAFFOLD_FILES` plus alla scaffold-manifests filer i nya `src/lib/gen/scaffolds/baseline-paths.ts`, så nästa manifest kan inte återinföra buggen. Spår 01 steg 4. Test: `version-presence.test.ts`, "returns [] for a version containing only the scaffold baseline". |
| [x] | Fixad | P1 | **F3 — reparationen pinnade npm-paketet `crypto` för att blidka en falsk preflight:** `project-sanity` och `dep-completer` var oense om vad en Node-kärnmodul är, så `import { createHash } from "crypto"` rapporterades som ett opinnat tredjepartspaket. Reparationen "löste" det genom att skriva ett paket som inte finns på npm in i `package.json` — vilket gör att installationen failar. | Observationssession 2026-07-25, fynd F3 | Ny kanonisk lista `src/lib/gen/validation/node-core-modules.ts` (både `crypto` och `node:crypto`), läst av båda lagren. En kärnmodul deklarerad som dependency ger nu ett eget preflight-fel i stället för att passera. Listan är frusen men diffas mot `Module.builtinModules` i test, så en Node-uppgradering failar ett test i stället för att glida. Spår 02 steg 1. |
| [x] | Fixad | P1 | **F4 — preview-strippad import läckte in i sparade projektfiler:** samma regeluppsättning körde mot både previewkopian och artefakten, så `// … (stripped for preview compatibility)` hamnade i den sparade sajten. | Observationssession 2026-07-25, fynd F4 | Regeluppsättningen delad i `canonical` och `preview` (`suspense/default-rules.ts`): strippningen av `next/og`, `next/headers` och `server-only` körs bara på kopian som skickas till preview-hosten. Same-origin-shimlanen körde tidigare inga preview-regler alls och kunde krascha på sparade filer med serverimporter — den kör dem nu själv via `preview-only-files.ts`. Spår 02 steg 2. Test: `preview-only-files.test.ts` (originalet orört, previewkopian strippad, shimlanen kör reglerna). |
| [x] | Fixad | P1 | **F5 — autofix la till fel import:** `app/api/personal-auth/route.ts` saknade `NextRequest` och `NextResponse`, men fixaren rapporterade "Added missing next/navigation imports: redirect" — en symbol som inte behövdes, medan de två som faktiskt saknades aldrig lades till. Fixaren täckte `next/navigation` men inte `next/server`-typerna i route handlers. | Observationssession 2026-07-25, fynd F5 | `react-import-consolidated.ts` täcker nu `next/server`-symbolerna och lägger inte till symboler som inte används. Spår 02 steg 3. Test: `react-import-consolidated.test.ts`. |
| [x] | Fixad | P1 | **F6 — mailchimp-dossiern användes inte trots att prompten sa "Mailchimp":** samma rot som F1. Här blev utfallet värre: modellen byggde en frihands `app/api/mailchimp/route.ts` på 198 rader, vars importer F2:s deny-list strippade — och previewen blockerades helt. | Observationssession 2026-07-25, fynd F6 | Samma motinstruktion som F1, plus att uppskjutna integrationer nu ackumuleras durabelt i orchestration-snapshotet och visas för användaren som "Planerad — kopplas in i nästa steg" i både Byggblock-panelen och chattens slutsteg. En capability lämnar listan först när filbevis visar att den levererats eller när användaren tar bort den. Spår 01 steg 2–3. Test: `orchestration-snapshot.test.ts` (fyra tester för durabel uppskjutning). |
| [x] | Fixad | P2 | **F7 — kontraktspanelen lovade NextAuth + SQLite som ingen fil infriade:** slutstegens kontraktsrader beskrev vad kontraktet *föreslog*, inte vad versionen innehöll, så ett designvarv som föreslog "Auth: clerk" och sedan inte skrev en rad autentiseringskod lästes som ett levererat faktum. | Observationssession 2026-07-25, fynd F7 | Kontraktsraderna märks `(planerad — kopplas in i nästa steg)` när rundans filbevis inte backar dem; bevisen räknas server-side med `resolveCapabilitiesPresentInVersion`, samma presence-ägare som Byggblock-panelen. Två skärpningar efter granskning: `Data mode: mocked`/`none` får ingen planerad-märkning (de beskriver vad varvet faktiskt levererar), och env-var-raden kräver filbevis för *varje* capability raderna ovan namngav — "någon dossier finns" räckte tidigare, vilket lät en orelaterad soft-dossier gå i god för en Stripe-nyckel. Spår 01 steg 5. Test: `helpers.test.ts` (5 tester). |
| [x] | Fixad | P2 | **F8 — versionsbannern kallade en äldre version "senaste":** copy- och namngivningsfel, inte ett omöjligt tillstånd. `latestVersionId` är `selectPreferredEngineVersion`, alltså senaste **användbara** versionen — en guard hade tystat en legitim varning. | Observationssession 2026-07-25, fynd F8 (omtriagerad efter Codex-granskning på PR #614) | `latestLabel` → `preferredLabel` med `kind: stale-selection \| rejected-active`. Ingen hide-guard. Spår 06. Tester: "explains a rejected active version without calling it senaste" + regressionstestet för när aktivt versionsnummer är högre än det föredragna. |
| [x] | Fixad | P2 | **F9 — rå kodvägg renderades som chattbubbla:** `parseGenerationContent` strippade kod med regexer som kräver en avslutande fence. Guarden för "kodström utan kompletta fences" fungerade bara när det fanns **noll** kompletta block — det blandade fallet (minst ett komplett plus ett oavslutat) hade ingen guard, så koden överlevde strippningen och renderades som en 5 212 px hög prosabubbla. Filräknaren undertalade av samma orsak. | Observationssession 2026-07-25, fynd F9 | `findUnterminatedBlock` hittar svansen (både fence-form och den backtick-lösa strömformen) och lägger den i den kollapsade "Genererat innehåll"-rutan; den oavslutade filen räknas med. Regeln att ren prosa som *nämner* `file="…"` fortfarande är text är testlåst. Spår 03 steg 1. Test: `GenerationSummary.test.tsx` (6 tester). |

## Fixade 2026-07-26

| Klar | Status | Prio | Fynd | Källa | Fix-referens |
| --- | --- | --- | --- | --- | --- |
| [x] | Fixad | P1 | **Dedupe-bailouten i placement-hooken saknade regressionstest på hook-nivå:** #604 testade bara `isSameInsertionPoint` i isolering, så en revert av bailouten i `usePreviewPanelInspectMapPlacement` (tillbaka till `setHoveredPlacement(insertion)`) hade lämnat alla tester gröna medan `PreviewPanel` åter renderade om vid varje `dragover` under hela draget. Skenbar täckning: hjälpfunktionen var låst, beteendet som använder den var det inte. | Codex P1 på mergad #604 (efterhandsgranskning begärd av merge-agenten 2026-07-25) | Två hook-nivåtester i `usePreviewPanelInspectMapPlacement.test.ts` som läser hookens egen `hoveredPlacement` i st.f. hjälpfunktionen: (1) fem `dragover`-lika händelser som alla löser till samma insättningslinje måste behålla **samma objektreferens**, och renderingsantalet får växa med högst 1 efter första uppdateringen (React kan rendera en gång extra innan den bailar ut på identiskt värde, men inte en gång per händelse); (2) en händelse som korsar till nästa linje måste uppdatera state — annars kunde testet bli grönt av att hooken slutade fungera helt. Icke-vakuöst verifierat: med bailouten borttagen faller (1) på `Object.is`-jämförelsen. Fixturen bygger två zoner ur en mockad element-map och kör `bridge`-engine, så pre-warm-loopen inte lämnar timers efter sig. Branch: `test/placement-dedupe-regression`. |

## Fixade 2026-07-27 (acceptanskörningens uppföljning, #629)

> Livekörningen i prod 2026-07-27 (chat `bcd3b493`, deploy `dpl_41GBecFhEwXCZeWvmmwRbCpEBKqa`)
> körde åtgärdsprogrammets acceptansprompt. Sju av åtta kontroller var gröna. De
> två första raderna nedan är hål i #623:s egen leverans som körningen avslöjade;
> de tre sista är separata fynd från samma session. Ingen av dem låg i Aktiv kö —
> de upptäcktes under körningen och stängs här i samma leverans.

| Klar | Status | Prio | Fynd | Källa | Fix-referens |
| --- | --- | --- | --- | --- | --- |
| [x] | Fixad | P2 | **F9 igen — kodväggen kom tillbaka när fence-parningen hamnade ur fas:** `findUnterminatedBlock` prövade `OPEN_FENCE_RE` före `STREAM_FILE_HEADER_RE` och returnerade på första träffen. Modellen skickade 13 trippel-fences — ojämnt antal, dvs. en saknad stängning — så de sex kompletta paren plockades bort och resten började med ofenced `tsx file="app/layout.tsx"` på index 1, medan en kvarglömd fence låg på index 20 879. Klippet hamnade vid fencen och de 20 876 tecknen däremellan blev prosa. Siffran mättes mot den renderade DOM-noden (`generation-summary-prose`, höjd 16 566 px); andra bubblan klipptes vid 6 018 och renderade 6 015. Båda tidigare F9-fixarna skyddade fallet "inga kodblock alls" — detta innehåll har kodblock **och** en läckt svans. | Acceptanskörning 2026-07-27, kontroll 8 (React-fiberns `content`-prop lästes ur den live-renderade bubblan) | Den tidigaste kandidaten vinner nu i stället för fence-först, och en fence utan kropp avbryter inte längre hela sökningen (den `return null` kortslöt även stream-kontrollen). Svansen räknas dessutom upp per fil, så det kollapsade kortet inte undertalar filräknaren när flera filer läckt. Tre nya tester i `GenerationSummary.test.tsx`, varav ett återger prod-formen exakt: kompletta block, ofenced kod, kvarglömd fence efter. |
| [x] | Fixad | P1 | **Spår 01 steg 3 — capability-signalerna nådde aldrig databasen:** läs- och merge-sidan från #623 var korrekta, men `sanitizeOrchestrationSnapshotForStorage` har en budget på 80 nycklar som delas över **all** nästling och `break`:ar tyst när den tar slut. `mutedCapabilities` ligger som toppnyckel ~33 i stream-metan, efter `buildSpec`, `briefSummary`, `contractIntegrations` och `contractEnvVars`, som var och en äter tiotals nycklar. Med en stream-meta i den verkliga nyckelordningen blev `mutedCapabilities`, `removedCapabilities`, `fileEvidenceCapabilities` och `requestedCapabilities` alla `undefined`. Prod-symptomet var `counts.planned: 0` för en uppskjuten Mailchimp-integration trots att slutstegen visade "Planerad — kopplas in i nästa steg: Nyhetsbrev — Mailchimp" (slutstegen läser den efemära SSE-metan, som inte saniteras). Allvarligare än Byggblock-noteringen: **den durable borttagnings-tombstonen `removedCapabilities` kunde försvinna samma väg**, vilket låter en borttagen integration återuppstå nästa runda. Koden kände redan igen fenomenet — `PROTECTED_TOP_LEVEL_KEYS` finns just för att stora nästlade payloads äter budgeten — men skyddade bara fem skalära id-fält. | Acceptanskörning 2026-07-27 (`GET /api/engine/chats/<chatId>/dossiers` mot prod gav `planned: 0` med #623 deployat) | Capability-signalerna kopieras utanför budgeten som begränsade string-arrayer (`PROTECTED_CAPABILITY_SIGNAL_KEYS`, max 40 poster, samma strängtrunkering). Fem nya tester i `orchestration-snapshot.test.ts` som bygger stream-metan i den verkliga nyckelordningen, inklusive ett som kör hela kedjan skriv → merge → läs. Det sista täcker luckan som gjorde att buggen kunde levereras: rutans enhetstest mockar `readMutedCapabilitiesFromSnapshot`, så skriv-sidan var aldrig testad. |
| [x] | Fixad | P3 | **Thumbnail-racet rapporterades som 502:** navigerar previewen om mitt i skottet ger Playwright "Target page, context or browser has been closed", och `POST /api/projects/<id>/thumbnail` svarade 502 — en i övrigt frisk körning såg ut att ha ett serverfel i observability. Klienten är fire-and-forget och läser inte svaret, så den enda effekten var falskt fellarm. Raden låg tidigare i "Behöver repro" (från /logg-internet 2026-07-09) och är nu reproducerad med Vercel-loggen som pekar ut stage `screenshot`. | Acceptanskörning 2026-07-27 (Vercel runtime-logg korrelerad mot POST-anropet) | `isTransientCaptureAbort` skiljer den klassen från äkta fel: racet loggas på info-nivå och svaras 200, men behåller `success: false` och `skipped: true` — ingen thumbnail sparades och svaret påstår inte annat. Äkta fel (navigeringstimeout, allowlist-avvisning, launch-fel) ger fortfarande 502. Rutans test kör den riktiga klassificeringen i st.f. en mock av den. |
| [x] | Fixad | P2 | **Deep Brief-fallbacken kunde aldrig lyckas mot OpenAI:** fallback-schemat ger varje fält ett `.default()` så en osäker modell kan utelämna det, men OpenAI:s strikta structured outputs kräver att `required` listar varje nyckel i `properties` — och `.default()` gör fältet optional i det genererade JSON-schemat. Fjorton av femton toppnycklar plus `bullets` i sektionsobjektet låg utanför `required`, så OpenAI avvisade schemat med 422 "Missing 'bullets'" innan modellen ens svarade. Säkerhetsnätet fanns bara på pappret: varje gång det fulla schemat föll, föll även fallbacken — av en helt annan orsak än den som fällde originalet. | Live-session 2026-07-26 (422 på `/api/ai/brief` bakom en kvot-429), schemat verifierat med `z.toJSONSchema(..., { io: "input" })` 2026-07-27 | Toleransen är hela poängen med schemat, så inställningen ger sig: båda fallback-anropen skickar `strictJsonSchema: false` (nyckeln ignoreras av andra leverantörer). Testet uttrycker invarianten som ett villkor som överlever båda framtida riktningar — antingen är schemat strict-safe, eller skickas det icke-strikt — plus ett test som fäster det konkreta prod-symptomet (`pages[].sections[].bullets` utanför `required`). |
| [x] | Fixad | P2 | **Plan-läget visade genereringsfel för en lyckad plan:** plan-läget avslutar medvetet utan version och utan preview — planen ÄR resultatet — men `hasRecoveredArtifact` räknade bara `awaitingInput`, `versionId` och `previewUrl`. En plan **utan** blockerare föll därför i empty-output-grenen: toasten "Genereringen avslutades utan version eller preview." visades, meddelandet finaliserades och grenen bröt loopen innan plan-kortet monterades och innan "Plan skapad!" nåddes. Planer med blockerare var opåverkade eftersom `awaitingInput` redan var sant där. | Kodläsning av plan-lägets funktionsduglighet 2026-07-27 (frågan var om läget alls fungerar) | `planArtifact` räknas som återfunnen artefakt. Testet är verifierat icke-vakuöst: med fixen backad faller det på att `toast.success` aldrig anropas. Kvar som öppen rad i Aktiv kö: godkänn-kortet renderas fortfarande bara i `?debug=1`. |

## Fixade 2026-07-27 (dev/prod-målet)

> Två rader ur samma rotorsak: ingenting tvingade en process att säga **vilken**
> databas den pratade med. Den ena hade legat röd i tre månader, den andra fick
> en agent att läsa dev och tro att det var prod under städningen efter #629.

| Klar | Status | Prio | Fynd | Källa | Fix-referens |
| --- | --- | --- | --- | --- | --- |
| [x] | Fixad | P3 | **Den veckovisa eval-baslinjen hade aldrig kört:** `Eval Baseline Update` injicerade bara `OPENAI_API_KEY`, men `runEval` kräver en anslutningssträng (`resolveEvalEnvironment` → `resolveConfiguredDbEnv`) eftersom codegen-vägens scaffold-scoring läser `generation_telemetry`. Utan träff markerades varje prompt `skipped` med den blockerande checken `preflight_env`, gaten exitade 1, och baslinjen skrevs aldrig. Loggen såg därför likadan ut varje vecka: alla scenarier `PASS → FAIL` plus `+preflight_env` — vilket läser som kvalitetskollaps, inte som ett felkonfigurerat jobb. Alla 14 schemalagda körningar i den synliga historiken (2026-04-27 → 2026-07-27) var röda; DB-guarden lades 2026-04-30 i `85867fb0`, så `eval-baseline.json` var fryst i tre månader. En permanent röd veckobadge tränar dessutom bort reaktionen på rött. | Kodläsning + körhistorik 2026-07-27 (uppföljning på #629-städningen); ägarbeslut samma dag | Jobbet får prod-databasen (`POSTGRES_URL_PROD`). Evalet är läsande — det persisterar ingen chat/version, drar inga credits och bootar ingen preview-VM (`src/lib/gen/eval/README.md`), och de enda DB-konsumenterna i vägen (`scaffold-scoring.ts`, `scaffold-aware-retry.ts`) är rena `.select()`. Prod är dessutom det enda meningsfulla målet: scaffold-scoring rankar mot verklig generationshistorik, som dev saknar. `DB_ALLOW_PROD_LIKE_WRITE` lämnas medvetet osatt, så en framtida skrivning skulle falla i st.f. att tyst mutera prod. Samma `check-db-env-target.mjs --expect=prod`-guard som prod-migrationerna kör före körningen, och båda secrets kontrolleras före install så en saknad nyckel failar med sin riktiga orsak. Codex-granskningen på #631 gav två följdfixar i samma PR: credentialen är **stegskopad** i st.f. jobbskopad (ett jobb-`env:` hade gett prod-DSN:en till `npm ci` med dess postinstall-skript och till tredjeparts-actionen `create-pull-request`, som ingen av dem behöver den), och no-op-vägen räddades — `saveBaseline()` skriver om `timestamp`, per-prompt `generationTimeMs` och `summary.avgTimeMs` varje körning, så `git diff --quiet` hade alltid varit smutsig och varje måndag hade öppnat ett draft-PR med bara klockbrus. `scripts/eval/baseline-meaningful-change.mjs` jämför nu utfallet (poäng, pass/fail, blockerande checkar, filantal, modell, icke-timing-summary) och verifierades mot den riktiga 15-prompts-baslinjen: enbart timing → `false`, en ändrad poäng → `true`. Kvar som öppen rad: DSN:en är fortfarande skrivbar, en read-only Supabase-roll är ägaråtgärd. |
| [x] | Fixad | P3 | **DB-skripten kunde inte säga vilken miljö de läste:** `warnIfProdLikeReadTarget` jämför bara mot `.env.vercel.production.pulled` och kan därför bara säga "det här ÄR prod" — läser kommandot dev är den tyst. `db:latest` skrev dessutom sin egen rad rakt ur `url.hostname`, så utskriften blev `db:latest -> aws-1-eu-north-1.pooler.supabase.com/postgres`: identiskt formaterad med en prod-läsning om man inte kan regionerna utantill. Konkret utfall 2026-07-27: `db:latest --env=.env.vercel.production.pulled` (en flagga skriptet inte har — det tar `--prod`) ignorerades tyst, kommandot läste dev, och svaret "senaste sajten är från 22 juli" togs för ett prod-svar. Samma flagga fungerar i syskonskriptet `check-migrations-applied.mjs`, vilket är varför den kändes rimlig. | Egen felläsning under #630-städningen, kodverifierad | `summarizeTarget` slår upp project ref:en mot `config/db-targets.json` och prefixar identiteten med `[DEV]`/`[PROD]`; okända projekt (lokal Postgres) lämnas omärkta. `db:latest` och `db:rows` skriver nu sin målrad genom den nya `summarizeConnectionString` i st.f. att formatera `url.hostname` själva, så etiketten gäller även den rad man faktiskt läser. Uppslaget är lazy och sväljer fel — modulen importeras av varje plain-node-skript, så en trasig targets-fil får inte fälla ett orelaterat läskommando. Tre nya fall i `db-target-guard.test.ts` läser refs ur den kanoniska filen i st.f. att hårdkoda dem. Flaggan i sig är fortfarande tyst ignorerad; etiketten gör bara utfallet synligt. |
