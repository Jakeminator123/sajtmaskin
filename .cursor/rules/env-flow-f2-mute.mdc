---
description: "F2 får inte fråga användaren om env-nycklar. Env-trafik hör till F3."
globs:
  - src/lib/api/engine/chats/**
  - src/lib/providers/own-engine/**
  - src/lib/gen/preview/**
  - src/lib/gen/detect-integrations.ts
  - src/app/api/engine/chats/[chatId]/readiness/**
  - src/app/builder/BuilderShellContent.tsx
  - src/components/builder/F3RequirementsSurface.tsx
  - src/components/builder/ProjectEnvVarsPanel.tsx
  - src/components/builder/preview-panel/**
alwaysApply: false
---

# Env-flöde i F2 är tyst

F2 (`lifecycleStage === "design"`) får inte producera env-frågor i chatten. Env-keys parkeras i `env.example`. Efter ett explicit klick på **Bygg integrationer** visar buildern alla normala F3-resultat i en persistent, icke-modal F3-status-/kravyta — aldrig som toast. Vid `finalize-design` `412` innehåller kravyta exakt serverns `missingByIntegration`-nycklar; användaren fyller riktiga värden där och väljer explicit retry. Byggblock-popovern är katalog/status, aldrig den auto-öppnade F3-nyckelytan.

F2 ska ändå se ut som en riktig sajt: dossierns `mock`-läge + F2-mock-seed (`env-local.ts`) renderar demo-ytan utan riktiga nycklar. Tystnaden gäller env-*trafik i chatten*, inte den visuella ytan.

## Skyddslager

1. Tool-gate: env-tools exponeras inte i F2.
2. SSE-filter: tool-events droppas i F2.
3. Panel-gate: `<ProjectEnvVarsPanel>` bara i F3.
4. Post-finalize-gate: detected integrations droppas i F2.

## Förbjudet

- Visa env-panel i F2.
- Läsa `missingEnvKeys` som blocker i F2 (demo måste förbli publicerbart).
- Auto-promotera till F3 baserat på prompt-keywords.
- Emittera env-tool-events i F2.
- Öppna Byggblock-popovern för att samla F3-nycklar efter `finalize-design` 412.
- Öppna "Svar krävs"-dialogen för `f3-continuation`-markern — F3 auto-fortsätter (live-marker) eller visar inline quick-replies (reload). Se `llm-pipeline.md` § F3-förslagsrunda.
