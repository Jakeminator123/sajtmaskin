export default function SubmittedBooking(props: Record<string, unknown>) {
  return (
    <div
      data-stub="@/components/submitted-booking"
      style={{
        padding: "2rem",
        border: "2px dashed #666",
        borderRadius: "0.5rem",
        color: "#999",
        textAlign: "center",
        fontSize: "0.875rem",
      }}
    >
      [SubmittedBooking]
    </div>
  );
}

export { SubmittedBooking };
