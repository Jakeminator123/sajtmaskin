{
  "name": "sajtmaskin-preview-host",
  "version": "0.1.0",
  "private": true,
  "description": "Isolated preview-host prototype for stateful preview sessions.",
  "engines": {
    "node": ">=22.14.0 <23"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/runtime.js && node --check src/store.js && node --check src/validate.js",
    "smoke": "node scripts/smoke.js",
    "test:patch": "node scripts/test-patch.mjs"
  },
  "dependencies": {
    "acorn": "^8.16.0",
    "http-proxy": "^1.18.1"
  }
}
