---
description: "Hur ändringar utförs: smal scope, beroenden först, verifiering efteråt."
alwaysApply: true
---

# Workflow

## Normal ändringsordning

```text
förstå scope → spåra beroenden → ändra → uppdatera referenser/docs → verifiera → rapportera
```

## Städning/refaktor

Vid städning: gör faktisk cleanup, men håll scope smalt. Radera duplicat/död kod bara när beroenden är kontrollerade.

Checklista:

1. Sök referenser innan flytt/radering.
2. Uppdatera importer, länkar, tester och docs i samma ändring.
3. Undvik bred formattering.
4. Stage bara aktuell uppgift.
5. Kör riktad verifiering.
6. Tar du bort en spårad fil/mapp: radera även dess ospårade syskon (t.ex. `__pycache__`, `*.pyc`, byggcache). `git` raderar bara spårade filer, så ospårade artefakter överlever annars `git pull` och håller mappen kvar på disk. Verifiera att sökvägen är borta med både `git status` och `Test-Path <sökväg>` (eller kör `npm run clean:orphans`).

## Sök-index kan vara kallt

Glob/Grep bygger på ett index som är stale i nyskapade worktrees och för nyss skapade filer. **0 träffar betyder inte att filen saknas.** Verifiera med `Read` på den förväntade sökvägen eller `Test-Path -LiteralPath <sökväg>` innan du drar slutsatsen "finns inte" — särskilt för rot-filer och allt i en färsk worktree.

## Verifiering

| Ändring | Minsta verifiering |
|---|---|
| Docs-only | länk-/diff-koll; ingen runtime-test krävs |
| TS/runtime | `npm run typecheck` + riktade tester |
| Scaffold | `npm run typecheck` + `npm run scaffolds:validate` |
| Dossier | `npm run dossiers:validate-all` (capability-map är genererad vy — regenerera vid behov med `npm run dossiers:capability-map:write`, ej CI-tvingad) |
| Pipeline/status | `npm run typecheck` + relevant vitest + docs-sync |
| Stor filflytt | `npm run typecheck` + grep efter gamla paths |

Required checks på PR mot master: `quality`, `backoffice-tests`, `schema-drift`, `build` (tillagd 2026-07-30 via #660) samt `review-window` (pending tills PR ≥ 7 min + externa botar klara — se `pr-merge.mdc`). Efter merge (push till master) kör CI dessutom `prod-migrations-apply` → `prod-migrations-applied` som applicerar/verifierar prod-migrationer (gate:ade bakom `quality` + `schema-drift`).

## Docs

Docs ska spegla runtime. Ersätt gammal text, lägg inte nya lager ovanpå. Ta bort döda länkar och obsolete tabellrader när du rör området.

## Innan PR eller push till master

Kör ett **Cursor Bugbot-pass på egen diff** (bugbot-subagent, `readonly: true`, `model: <grok-4.5>`) före du skapar PR eller pushar till master. Detta pass **ÄR** författarens obligatoriska bugg-efterkontroll — samma pass, bara kört före push i stället för efter PR-öppning, så för-filter och efterkontroll är ett och samma för aktuell head-SHA. **Kör om bugbot på varje ny commit.** Protected-path-passet, 7-min/Codex-fönstret och sign-off-ordningen ligger i `pr-merge.mdc`.

Kör `/automat` bara för bred repo-audit — inte som diff-granskning. En bugg som ska åtgärdas går genom `/kedja`. (Den tidigare `/granska`-8-agentssvärmen togs bort 2026-08-02 — återinför den inte; de allvarliga fynden kom ändå från Bugbot/Codex.)

## När du ska pausa

Pausa och fråga vid: bred rename, arkitekturbeslut, möjlig dataförlust, oklart canonical ownership, security/cross-tenant-risk eller >40 filer.
