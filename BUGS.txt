BUG LOG - v0 quality and behavior
Date: 2026-01-30

1) Template init quality forced to "standard" (v0-pro)
   Probability: 90%
   Severity: Medium-High
   Impact: Lower initial demoUrl quality for template-based projects.
   Status: Fixed in current change set (UI + API default updated to "max").

2) Registry init default quality "standard" (v0-pro)
   Probability: 85%
   Severity: Medium
   Impact: Lower initial demoUrl quality for registry-based projects.
   Status: Fixed in current change set (API default updated to "max").

3) Deep Brief default OFF
   Probability: 80%
   Severity: Medium
   Impact: Less detailed prompt assist on complex projects; can reduce demoUrl quality.
   Status: Fixed in current change set (default deep now ON).

4) openai-compat default model v0-1.5-md (128K)
   Probability: 75%
   Severity: Low-Medium
   Impact: Smaller context window for prompt assist; can reduce quality on long prompts.
   Status: Fixed in current change set (default now v0-1.5-lg).

5) System prompt trimming
   Probability: 20%
   Severity: Low
   Impact: Could alter intentional leading/trailing whitespace in system prompt formatting.
   Status: Not a bug in practice; keep for safety unless a specific formatting case fails.
