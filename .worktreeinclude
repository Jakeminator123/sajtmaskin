# No ignored machine-local files are copied into Codex-managed worktrees by
# default. Add only an explicitly reviewed, non-sensitive path when a task
# genuinely needs it. Never add `.env.local` or live connector credentials.
